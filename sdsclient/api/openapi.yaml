openapi: 3.0.2
info:
  contact:
    email: support-api@efficientip.com
    name: Support
    url: https://www.efficientip.com/services/support/
  description: OpenAPI 3.0.2 API definition for SOLIDserver service from EfficientIP.<p>Copyright
    © 2000-2021 EfficientIP</p><p><em>All specifications and information regarding
    the products in  this document are subject to change without notice and should
    not be  construed as a commitment by EfficientIP. EfficientIP assumes no  responsibility
    or liability for any mistakes or inaccuracies that may appear  in this document.
    All statements and recommendations in this document are  believed to be accurate
    but are presented without warranty. Users must take  full responsibility for their
    application of any product.</em></p><p>Generated (Monday 14th of June 2021 12:30:34
    PM)</p>
  license:
    name: EfficientIP
  title: SOLIDserver API
  version: "2.0"
servers:
- url: https://{host}:{port}/api/v2.0
  variables:
    host:
      default: SDS-URL
      description: server ip or server fqdn
    port:
      default: "443"
      enum:
      - "443"
security:
- HttpHeaderLoginKey: []
  HttpHeaderPasswordKey: []
- BasicAuth: []
paths:
  /ipam/space/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: ipam_space_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_space_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the spaces
      tags:
      - ipam
  /ipam/space/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: ipam_space_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of spaces
      tags:
      - ipam
  /ipam/space/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: ipam_space_info
      parameters:
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_space_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a space
      tags:
      - ipam
  /ipam/space/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: space_name'
      operationId: ipam_space_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_space_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_space_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_space_add_failed'
          description: error object edition
      summary: Add a space
      tags:
      - ipam
  /ipam/space/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (space_id || space_name)'
      operationId: ipam_space_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_space_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_space_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_space_edit_failed'
          description: error object edition
      summary: Edit a space
      tags:
      - ipam
  /ipam/space/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (space_id || space_name)'
      operationId: ipam_space_delete
      parameters:
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the space.
        explode: true
        in: query
        name: space_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_space_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_space_delete_failed'
          description: error object edition
      summary: Delete a space
      tags:
      - ipam
  /ipam/network/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: ipam_network_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the IPv4 block/subnet-type networks
      tags:
      - ipam
  /ipam/network/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: ipam_network_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of IPv4 block/subnet-type networks
      tags:
      - ipam
  /ipam/network/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: ipam_network_info
      parameters:
      - description: The database identifier (ID) of the IPv4 network, a unique numeric
          key value automatically incremented when you add an IPv4 network. Use the
          ID to specify the IPv4 network of your choice.
        explode: true
        in: query
        name: network_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of an IPv4 block/subnet-type network
      tags:
      - ipam
  /ipam/network/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.If
        an existing identifier is specified, the value of all the parameters specified
        in input edits the corresponding objects.Note that to add a block-type network,
        setting the input parameter subnet_level to 0 is mandatory.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.</br></br><b>Mandatory Parameters</b>: (network_addr && (network_end_addr
        || network_size || network_mask || network_prefix) && (space_id || space_name
        || parent_network_id))'
      operationId: ipam_network_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_network_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network_add_failed'
          description: error object edition
      summary: Add an IPv4 block/subnet-type network
      tags:
      - ipam
  /ipam/network/edit:
    put:
      description: 'This service allows to edit existing ones. A call can only edit
        one object.If an existing identifier is specified, the value of all the parameters
        specified in input edits the corresponding objects.Note that to add a block-type
        network, setting the input parameter subnet_level to 0 is mandatory.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (network_id
        || (network_addr && (network_end_addr || network_size || network_mask || network_prefix)
        && (space_id || space_name || parent_network_id)))'
      operationId: ipam_network_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_network_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network_edit_failed'
          description: error object edition
      summary: Edit an IPv4 block/subnet-type network
      tags:
      - ipam
  /ipam/network/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (network_id || (network_addr && (network_end_addr || network_size || network_mask
        || network_prefix) && (space_id || space_name || parent_network_id)))'
      operationId: ipam_network_delete
      parameters:
      - description: The database identifier (ID) of the parent IPv4 network. Use
          the ID to specify the parent IPv4 network of your choice.
        explode: true
        in: query
        name: parent_network_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the space.
        explode: true
        in: query
        name: space_name
        required: false
        schema:
          type: string
        style: form
      - description: The start IP address of the IPv4 network, its first IP address.
        explode: true
        in: query
        name: network_addr
        required: false
        schema:
          type: string
        style: form
      - description: The end IP address of the IPv4 network, its last IP address.
        explode: true
        in: query
        name: network_end_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv4 network, a unique numeric
          key value automatically incremented when you add an IPv4 network. Use the
          ID to specify the IPv4 network of your choice.
        explode: true
        in: query
        name: network_id
        required: false
        schema:
          type: integer
        style: form
      - description: The netmask of the IPv4 network. It is expressed in dot-decimal
          notation and defines the number of addresses the network contains.
        explode: true
        in: query
        name: network_mask
        required: false
        schema:
          type: string
        style: form
      - description: The prefix of the IPv4 network, an integer that defines the number
          of addresses the network contains.
        explode: true
        in: query
        name: network_prefix
        required: false
        schema:
          type: string
        style: form
      - description: The size of the IPv4 network, the number of IP addresses it contains.
        explode: true
        in: query
        name: network_size
        required: false
        schema:
          type: integer
        style: form
      - description: The position of the network within the hierarchy of networks
          of a VLSM organization. It calculates between <b>0</b> and <b>n</b> all
          the levels of the organization, its behavior depends on the value of the
          parameter <b>use_reversed_relative_position</b>:<ul class=dashed ><li><b>use_reversed_relative_position=0</b>
          where <b>0</b> indicates a block-type network at the highest level possible,
          in a space-based organization, it belongs to the top space. The levels increment
          from <b>0</b> down to <b>n</b>, the lowest level you set up, within networks
          or spaces.<br/>                                            </li><li><b>use_reversed_relative_position=1</b>
          where <b>1</b> indicates a network located at the lowest level of the organization,
          within networks or spaces. The levels increment from <b>0</b> up to <b>n</b>,
          the network at the highest level of the organization.<br/>                                            </li></ul>
        explode: true
        in: query
        name: relative_position
        required: false
        schema:
          type: integer
        style: form
      - description: The level of the network within the space:<ul class=dashed ><li>                                                Set
          it to <b>0</b> for a block-type network.<br/>                                            </li><li>                                                Set
          it to a value between <b>1</b> and <b>n</b> for a subnet-type network.<br/>                                            </li></ul>If
          you set a value between <b>2</b> and <b>n</b>, you are setting a network-based
          VLSM organization where non terminal subnet-type networks can contain other
          subnet-type networks.
        explode: true
        in: query
        name: network_level
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the IPv4 network.
        explode: true
        in: query
        name: network_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to determine if the calculation of the parameter <b>relative_position</b>
          should start from the top (<b>0</b>) or the bottom (<b>1</b>) of the VLSM
          organization.
        explode: true
        in: query
        name: use_reversed_relative_position
        required: false
        schema:
          type: integer
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network_delete_failed'
          description: error object edition
      summary: Delete an IPv4 block/subnet-type network
      tags:
      - ipam
  /ipam/pool/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: ipam_pool_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the IPv4 pools
      tags:
      - ipam
  /ipam/pool/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: ipam_pool_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of IPv4 pools
      tags:
      - ipam
  /ipam/pool/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: ipam_pool_info
      parameters:
      - description: The database identifier (ID) of the IPv4 pool, a unique numeric
          key value automatically incremented when you add an IPv4 pool. Use the ID
          to specify the IPv4 pool of your choice.
        explode: true
        in: query
        name: pool_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of an IPv4 pool
      tags:
      - ipam
  /ipam/pool/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (pool_start_ip_addr
        && (pool_end_ip_addr || pool_size) && (network_id || space_id || space_name))'
      operationId: ipam_pool_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_pool_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool_add_failed'
          description: error object edition
      summary: Add an IPv4 pool
      tags:
      - ipam
  /ipam/pool/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (pool_id || (pool_start_ip_addr && (pool_end_ip_addr || pool_size) && (network_id
        || space_id || space_name)))'
      operationId: ipam_pool_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_pool_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool_edit_failed'
          description: error object edition
      summary: Edit an IPv4 pool
      tags:
      - ipam
  /ipam/pool/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (pool_id || (pool_start_ip_addr && (pool_end_ip_addr || pool_size) && (network_id
        || space_id || space_name)))'
      operationId: ipam_pool_delete
      parameters:
      - description: The last IP address of the pool.
        explode: true
        in: query
        name: pool_end_ip_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv4 pool, a unique numeric
          key value automatically incremented when you add an IPv4 pool. Use the ID
          to specify the IPv4 pool of your choice.
        explode: true
        in: query
        name: pool_id
        required: false
        schema:
          type: integer
        style: form
      - description: The size of the pool, the number of IP addresses it contains.
        explode: true
        in: query
        name: pool_size
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the space.
        explode: true
        in: query
        name: space_name
        required: false
        schema:
          type: string
        style: form
      - description: The first IP address of the pool.
        explode: true
        in: query
        name: pool_start_ip_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv4 network, a unique numeric
          key value automatically incremented when you add an IPv4 network. Use the
          ID to specify the IPv4 network of your choice.
        explode: true
        in: query
        name: network_id
        required: false
        schema:
          type: integer
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool_delete_failed'
          description: error object edition
      summary: Delete an IPv4 pool
      tags:
      - ipam
  /ipam/address/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: ipam_address_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the IPv4 addresses
      tags:
      - ipam
  /ipam/address/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: ipam_address_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of IPv4 addresses
      tags:
      - ipam
  /ipam/address/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: ipam_address_info
      parameters:
      - description: The database identifier (ID) of the IPv4 address, a unique numeric
          key value automatically incremented when you add an IPv4 address. Use the
          ID to specify the IPv4 address of your choice.
        explode: true
        in: query
        name: address_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of an IPv4 address
      tags:
      - ipam
  /ipam/address/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (address_hostaddr
        && (space_id || space_name))'
      operationId: ipam_address_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_address_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address_add_failed'
          description: error object edition
      summary: Add an IPv4 address
      tags:
      - ipam
  /ipam/address/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (address_id || (address_hostaddr && (space_id || space_name)))'
      operationId: ipam_address_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_address_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address_edit_failed'
          description: error object edition
      summary: Edit an IPv4 address
      tags:
      - ipam
  /ipam/address/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (address_id || (address_hostaddr && (space_id || space_name)))'
      operationId: ipam_address_delete
      parameters:
      - description: The IP address.
        explode: true
        in: query
        name: address_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv4 address, a unique numeric
          key value automatically incremented when you add an IPv4 address. Use the
          ID to specify the IPv4 address of your choice.
        explode: true
        in: query
        name: address_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the space.
        explode: true
        in: query
        name: space_name
        required: false
        schema:
          type: string
        style: form
      - description: The name of the IPv4 address.
        explode: true
        in: query
        name: address_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address_delete_failed'
          description: error object edition
      summary: Delete an IPv4 address
      tags:
      - ipam
  /ipam/alias/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: ipam_alias_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the IPv4 address, a unique numeric
          key value automatically incremented when you add an IPv4 address. Use the
          ID to specify the IPv4 address of your choice.
        explode: true
        in: query
        name: address_id
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the aliases of an IPv4 address
      tags:
      - ipam
  /ipam/alias/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: ipam_alias_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv4 address, a unique numeric
          key value automatically incremented when you add an IPv4 address. Use the
          ID to specify the IPv4 address of your choice.
        explode: true
        in: query
        name: address_id
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of aliases of an IPv4 address
      tags:
      - ipam
  /ipam/alias/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (address_name
        && (address_id || (alias_hostaddr && (space_id || space_name))))'
      operationId: ipam_alias_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_alias_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias_add_failed'
          description: error object edition
      summary: Add an IPv4 address alias
      tags:
      - ipam
  /ipam/alias/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (alias_id || (address_name && (address_id || (alias_hostaddr && (space_id
        || space_name)))))'
      operationId: ipam_alias_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_alias_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias_edit_failed'
          description: error object edition
      summary: Edit an IPv4 address alias
      tags:
      - ipam
  /ipam/alias/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (alias_id || (address_name && (address_id || (alias_hostaddr && (space_id
        || space_name)))))'
      operationId: ipam_alias_delete
      parameters:
      - description: 'todo(here) :ipam.alias.delete.input.address_hostaddr. : IPv4
          address'
        explode: true
        in: query
        name: address_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv4 address, a unique numeric
          key value automatically incremented when you add an IPv4 address. Use the
          ID to specify the IPv4 address of your choice.
        explode: true
        in: query
        name: address_id
        required: false
        schema:
          type: integer
        style: form
      - description: 'todo(here) :ipam.alias.delete.input.alias_name. : String'
        explode: true
        in: query
        name: alias_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv4 alias, a unique numeric
          key value automatically incremented when you add an IPv4 alias. Use the
          ID to specify the IPv4 alias to of your choice.
        explode: true
        in: query
        name: alias_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the space.
        explode: true
        in: query
        name: space_name
        required: false
        schema:
          type: string
        style: form
      - description: The type of the alias.
        explode: true
        in: query
        name: alias_type
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias_delete_failed'
          description: error object edition
      summary: Delete an IPv4 address alias
      tags:
      - ipam
  /ipam/network6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: ipam_network6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the IPv6 block/subnet-type networks
      tags:
      - ipam
  /ipam/network6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: ipam_network6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of IPv6 block/subnet-type networks
      tags:
      - ipam
  /ipam/network6/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: ipam_network6_info
      parameters:
      - description: The database identifier (ID) of the IPv6 network, a unique numeric
          key value automatically incremented when you add an IPv6 network. Use the
          ID to specify the IPv6 network of your choice.
        explode: true
        in: query
        name: network6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of an IPv6 block/subnet-type network
      tags:
      - ipam
  /ipam/network6/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (network6_addr
        && (network6_end_addr || network6_prefix) && (space_id || space_name || parent_network6_id))'
      operationId: ipam_network6_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_network6_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network6_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network6_add_failed'
          description: error object edition
      summary: Add an IPv6 block/subnet-type network
      tags:
      - ipam
  /ipam/network6/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (network6_id || (network6_addr && (network6_end_addr || network6_prefix) &&
        (space_id || space_name || parent_network6_id)))'
      operationId: ipam_network6_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_network6_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network6_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network6_edit_failed'
          description: error object edition
      summary: Edit an IPv6 block/subnet-type network
      tags:
      - ipam
  /ipam/network6/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (network6_id || (network6_addr && (network6_end_addr || network6_prefix) &&
        (space_id || space_name || parent_network6_id)))'
      operationId: ipam_network6_delete
      parameters:
      - description: The database identifier (ID) of the parent IPv6 network. Use
          the ID to specify the parent IPv6 network of your choice.
        explode: true
        in: query
        name: parent_network6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the space.
        explode: true
        in: query
        name: space_name
        required: false
        schema:
          type: string
        style: form
      - description: The start IP address of the IPv6 network, its first IP address.
        explode: true
        in: query
        name: network6_addr
        required: false
        schema:
          type: string
        style: form
      - description: The end IP address of the IPv6 network, its last IP address.
        explode: true
        in: query
        name: network6_end_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv6 network, a unique numeric
          key value automatically incremented when you add an IPv6 network. Use the
          ID to specify the IPv6 network of your choice.
        explode: true
        in: query
        name: network6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The prefix of the IPv6 network, an integer that defines the number
          of address the network contains.
        explode: true
        in: query
        name: network6_prefix
        required: false
        schema:
          type: string
        style: form
      - description: The position of the network within the hierarchy of networks
          of a VLSM organization. It calculates between <b>0</b> and <b>n</b> all
          the levels of the organization, its behavior depends on the value of the
          parameter <b>use_reversed_relative_position</b>:<ul class=dashed ><li><b>use_reversed_relative_position=0</b>
          where <b>0</b> indicates a block-type network at the highest level possible,
          in a space-based organization, it belongs to the top space. The levels increment
          from <b>0</b> down to <b>n</b>, the lowest level you set up, within networks
          or spaces.<br/>                                            </li><li><b>use_reversed_relative_position=1</b>
          where <b>1</b> indicates a network located at the lowest level of the organization,
          within networks or spaces. The levels increment from <b>0</b> up to <b>n</b>,
          the network at the highest level of the organization.<br/>                                            </li></ul>
        explode: true
        in: query
        name: relative_position
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the IPv6 network.
        explode: true
        in: query
        name: network6_name
        required: false
        schema:
          type: string
        style: form
      - description: The level of the network within the space:<ul class=dashed ><li>                                                Set
          it to <b>0</b> for a block-type network.<br/>                                            </li><li>                                                Set
          it to a value between <b>1</b> and <b>n</b> for a subnet-type network.<br/>                                            </li></ul>If
          you set a value between <b>2</b> and <b>n</b>, you are setting a network-based
          VLSM organization where non terminal subnet-type networks can contain other
          subnet-type networks.
        explode: true
        in: query
        name: network_level
        required: false
        schema:
          type: integer
        style: form
      - description: A way to determine if the calculation of the parameter <b>relative_position</b>
          should start from the top (<b>0</b>) or the bottom (<b>1</b>) of the VLSM
          organization.
        explode: true
        in: query
        name: use_reversed_relative_position
        required: false
        schema:
          type: integer
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network6_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_network6_delete_failed'
          description: error object edition
      summary: Delete an IPv6 block/subnet-type network
      tags:
      - ipam
  /ipam/pool6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: ipam_pool6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the IPv6 pools
      tags:
      - ipam
  /ipam/pool6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: ipam_pool6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of IPv6 pools
      tags:
      - ipam
  /ipam/pool6/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: ipam_pool6_info
      parameters:
      - description: The database identifier (ID) of the IPv6 pool, a unique numeric
          key value automatically incremented when you add an IPv6 pool. Use the ID
          to specify the IPv6 pool of your choice.
        explode: true
        in: query
        name: pool6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of an IPv6 pool
      tags:
      - ipam
  /ipam/pool6/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (pool6_start_ip_addr
        && pool6_end_ip_addr && (network6_id || space_id || space_name))'
      operationId: ipam_pool6_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_pool6_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool6_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool6_add_failed'
          description: error object edition
      summary: Add an IPv6 pool
      tags:
      - ipam
  /ipam/pool6/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (pool6_id || (pool6_start_ip_addr && pool6_end_ip_addr && (network6_id ||
        space_id || space_name)))'
      operationId: ipam_pool6_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_pool6_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool6_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool6_edit_failed'
          description: error object edition
      summary: Edit an IPv6 pool
      tags:
      - ipam
  /ipam/pool6/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (pool6_id || (pool6_start_ip_addr && pool6_end_ip_addr && (network6_id ||
        space_id || space_name)))'
      operationId: ipam_pool6_delete
      parameters:
      - description: The last IP address of the pool.
        explode: true
        in: query
        name: pool6_end_ip_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv6 pool, a unique numeric
          key value automatically incremented when you add an IPv6 pool. Use the ID
          to specify the IPv6 pool of your choice.
        explode: true
        in: query
        name: pool6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the space.
        explode: true
        in: query
        name: space_name
        required: false
        schema:
          type: string
        style: form
      - description: The first IP address of the pool.
        explode: true
        in: query
        name: pool6_start_ip_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv6 network, a unique numeric
          key value automatically incremented when you add an IPv6 network. Use the
          ID to specify the IPv6 network of your choice.
        explode: true
        in: query
        name: network6_id
        required: false
        schema:
          type: integer
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool6_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_pool6_delete_failed'
          description: error object edition
      summary: Delete an IPv6 pool
      tags:
      - ipam
  /ipam/address6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: ipam_address6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the IPv6 addresses
      tags:
      - ipam
  /ipam/address6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: ipam_address6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of IPv6 addresses
      tags:
      - ipam
  /ipam/address6/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: ipam_address6_info
      parameters:
      - description: The database identifier (ID) of the IPv6 address, a unique numeric
          key value automatically incremented when you add an IPv6 address. Use the
          ID to specify the IPv6 address of your choice.
        explode: true
        in: query
        name: address6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of an IPv6 address
      tags:
      - ipam
  /ipam/address6/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (address6_hostaddr
        && (space_id || space_name))'
      operationId: ipam_address6_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_address6_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address6_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address6_add_failed'
          description: error object edition
      summary: Add an IPv6 address
      tags:
      - ipam
  /ipam/address6/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (address6_id || (address6_hostaddr && (space_id || space_name)))'
      operationId: ipam_address6_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_address6_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address6_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address6_edit_failed'
          description: error object edition
      summary: Edit an IPv6 address
      tags:
      - ipam
  /ipam/address6/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (address6_id || (address6_hostaddr && (space_id || space_name)))'
      operationId: ipam_address6_delete
      parameters:
      - description: The IP address.
        explode: true
        in: query
        name: address6_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv6 address, a unique numeric
          key value automatically incremented when you add an IPv6 address. Use the
          ID to specify the IPv6 address of your choice.
        explode: true
        in: query
        name: address6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the space.
        explode: true
        in: query
        name: space_name
        required: false
        schema:
          type: string
        style: form
      - description: The name of the IPv6 address.
        explode: true
        in: query
        name: address6_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address6_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_address6_delete_failed'
          description: error object edition
      summary: Delete an IPv6 address
      tags:
      - ipam
  /ipam/alias6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: ipam_alias6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the IPv6 address, a unique numeric
          key value automatically incremented when you add an IPv6 address. Use the
          ID to specify the IPv6 address of your choice.
        explode: true
        in: query
        name: address6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the aliases of an IPv6 address
      tags:
      - ipam
  /ipam/alias6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: ipam_alias6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv6 address, a unique numeric
          key value automatically incremented when you add an IPv6 address. Use the
          ID to specify the IPv6 address of your choice.
        explode: true
        in: query
        name: address6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of aliases of an IPv6 address
      tags:
      - ipam
  /ipam/alias6/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (address6_name
        && (address6_id || (alias6_hostaddr && (space_id || space_name))))'
      operationId: ipam_alias6_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_alias6_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias6_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias6_add_failed'
          description: error object edition
      summary: Add an IPv6 address alias
      tags:
      - ipam
  /ipam/alias6/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (alias6_id || (address6_name && (address6_id || (alias6_hostaddr && (space_id
        || space_name)))))'
      operationId: ipam_alias6_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ipam_alias6_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias6_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias6_edit_failed'
          description: error object edition
      summary: Edit an IPv6 address alias
      tags:
      - ipam
  /ipam/alias6/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (alias6_id || (address6_name && (address6_id || (alias6_hostaddr && (space_id
        || space_name)))))'
      operationId: ipam_alias6_delete
      parameters:
      - description: 'todo(here) :ipam.alias6.delete.input.address6_hostaddr. : IPv6
          address'
        explode: true
        in: query
        name: address6_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv6 address, a unique numeric
          key value automatically incremented when you add an IPv6 address. Use the
          ID to specify the IPv6 address of your choice.
        explode: true
        in: query
        name: address6_id
        required: false
        schema:
          type: integer
        style: form
      - description: 'todo(here) :ipam.alias6.delete.input.alias6_name. : String'
        explode: true
        in: query
        name: alias6_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the IPv6 alias, a unique numeric
          key value automatically incremented when you add an IPv6 alias. Use the
          ID to specify the IPv6 alias to of your choice.
        explode: true
        in: query
        name: alias6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the space, a unique numeric key
          value automatically incremented when you add a space. Use the ID to specify
          the space of your choice.
        explode: true
        in: query
        name: space_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the space.
        explode: true
        in: query
        name: space_name
        required: false
        schema:
          type: string
        style: form
      - description: The type of the alias.
        explode: true
        in: query
        name: alias6_type
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias6_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ipam_alias6_delete_failed'
          description: error object edition
      summary: Delete an IPv6 address alias
      tags:
      - ipam
  /dhcp/server/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_server_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_server_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv4 servers
      tags:
      - dhcp
  /dhcp/server/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_server_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv4 servers
      tags:
      - dhcp
  /dhcp/server/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_server_info
      parameters:
      - description: The database identifier (ID) of the DHCPv4 server, a unique numeric
          key value automatically incremented when you add a DHCPv4 server. Use the
          ID to specify the DHCPv4 server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_server_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv4 server
      tags:
      - dhcp
  /dhcp/failover/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_failover_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_failover_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv4 failover channels
      tags:
      - dhcp
  /dhcp/failover/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_failover_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv4 failover channels
      tags:
      - dhcp
  /dhcp/failover/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_failover_info
      parameters:
      - description: The database identifier (ID) of the DHCPv4 failover channel,
          a unique numeric key value automatically incremented when you add a DHCPv4
          failover channel. Use the ID to specify the DHCPv4 failover channel of your
          choice.
        explode: true
        in: query
        name: failover_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_failover_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv4 failover channel
      tags:
      - dhcp
  /dhcp/group/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_group_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_group_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv4 groups
      tags:
      - dhcp
  /dhcp/group/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_group_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv4 groups
      tags:
      - dhcp
  /dhcp/group/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_group_info
      parameters:
      - description: The database identifier (ID) of the DHCPv4 group, a unique numeric
          key value automatically incremented when you add a DHCPv4 group. Use the
          ID to specify the DHCPv4 group of your choice.
        explode: true
        in: query
        name: group_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_group_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv4 group
      tags:
      - dhcp
  /dhcp/group/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (group_name
        && (server_id || server_name || ip_id || server_hostaddr))'
      operationId: dhcp_group_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_group_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_group_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_group_add_failed'
          description: error object edition
      summary: Add a DHCPv4 group
      tags:
      - dhcp
  /dhcp/group/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (group_id || (group_name && (server_id || server_name || ip_id || server_hostaddr)))'
      operationId: dhcp_group_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_group_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_group_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_group_edit_failed'
          description: error object edition
      summary: Add a DHCPv4 group
      tags:
      - dhcp
  /dhcp/group/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (group_id || (group_name && (server_id || server_name || ip_id || server_hostaddr)))'
      operationId: dhcp_group_delete
      parameters:
      - description: The database identifier (ID) of the DHCPv4 server, a unique numeric
          key value automatically incremented when you add a DHCPv4 server. Use the
          ID to specify the DHCPv4 server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv4 server.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv4 group, a unique numeric
          key value automatically incremented when you add a DHCPv4 group. Use the
          ID to specify the DHCPv4 group of your choice.
        explode: true
        in: query
        name: group_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv4 group.
        explode: true
        in: query
        name: group_name
        required: false
        schema:
          type: string
        style: form
      - description: The IP address of the DHCP server.
        explode: true
        in: query
        name: server_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_group_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_group_delete_failed'
          description: error object edition
      summary: Delete a DHCPv4 group
      tags:
      - dhcp
  /dhcp/sharednetwork/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_sharednetwork_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv4 shared networks
      tags:
      - dhcp
  /dhcp/sharednetwork/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_sharednetwork_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv4 shared networks
      tags:
      - dhcp
  /dhcp/sharednetwork/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_sharednetwork_info
      parameters:
      - description: The database identifier (ID) of the DHCPv4 shared network, a
          unique numeric key value automatically incremented when you add a DHCPv4
          shared network. Use the ID to specify the DHCPv4 shared network of your
          choice.
        explode: true
        in: query
        name: sharednetwork_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv4 shared network
      tags:
      - dhcp
  /dhcp/sharednetwork/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (sharednetwork_name
        && (server_id || server_name || ip_id || server_hostaddr))'
      operationId: dhcp_sharednetwork_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_sharednetwork_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork_add_failed'
          description: error object edition
      summary: Add a DHCPv4 shared network
      tags:
      - dhcp
  /dhcp/sharednetwork/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        ((sharednetwork_id || sharednetwork_name) && (server_id || server_name ||
        ip_id || server_hostaddr))'
      operationId: dhcp_sharednetwork_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_sharednetwork_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork_edit_failed'
          description: error object edition
      summary: Edit a DHCPv4 shared network
      tags:
      - dhcp
  /dhcp/sharednetwork/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        ((sharednetwork_id || sharednetwork_name) && (server_id || server_name ||
        ip_id || server_hostaddr))'
      operationId: dhcp_sharednetwork_delete
      parameters:
      - description: The database identifier (ID) of the DHCPv4 server, a unique numeric
          key value automatically incremented when you add a DHCPv4 server. Use the
          ID to specify the DHCPv4 server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv4 server.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv4 shared network, a
          unique numeric key value automatically incremented when you add a DHCPv4
          shared network. Use the ID to specify the DHCPv4 shared network of your
          choice.
        explode: true
        in: query
        name: sharednetwork_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv4 shared network.
        explode: true
        in: query
        name: sharednetwork_name
        required: false
        schema:
          type: string
        style: form
      - description: The IP address of the DHCP server.
        explode: true
        in: query
        name: server_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork_delete_failed'
          description: error object edition
      summary: Delete a DHCPv4 Shared Network
      tags:
      - dhcp
  /dhcp/scope/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_scope_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv4 scopes
      tags:
      - dhcp
  /dhcp/scope/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_scope_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv4 scopes
      tags:
      - dhcp
  /dhcp/scope/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_scope_info
      parameters:
      - description: The database identifier (ID) of the DHCPv4 scope, a unique numeric
          key value automatically incremented when you add a DHCPv4 scope. Use the
          ID to specify the DHCPv4 scope of your choice.
        explode: true
        in: query
        name: scope_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv4 scope
      tags:
      - dhcp
  /dhcp/scope/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (scope_net_addr
        && scope_net_mask && (server_id || server_name || server_hostaddr))'
      operationId: dhcp_scope_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_scope_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope_add_failed'
          description: error object edition
      summary: Add a DHCPv4 scope
      tags:
      - dhcp
  /dhcp/scope/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (scope_id || (scope_net_addr && scope_net_mask && (server_id || server_name
        || server_hostaddr)))'
      operationId: dhcp_scope_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_scope_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope_edit_failed'
          description: error object edition
      summary: Edit a DHCPv4 scope
      tags:
      - dhcp
  /dhcp/scope/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (scope_id || (scope_net_addr && (server_id || server_name || server_hostaddr)))'
      operationId: dhcp_scope_delete
      parameters:
      - description: The database identifier (ID) of the DHCPv4 server, a unique numeric
          key value automatically incremented when you add a DHCPv4 server. Use the
          ID to specify the DHCPv4 server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv4 server.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv4 scope, a unique numeric
          key value automatically incremented when you add a DHCPv4 scope. Use the
          ID to specify the DHCPv4 scope of your choice.
        explode: true
        in: query
        name: scope_id
        required: false
        schema:
          type: integer
        style: form
      - description: The first IP address of the DHCPv4 scope.
        explode: true
        in: query
        name: scope_net_addr
        required: false
        schema:
          type: string
        style: form
      - description: The IP address of the DHCP server.
        explode: true
        in: query
        name: server_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope_delete_failed'
          description: error object edition
      summary: Delete a DHCPv4 scope
      tags:
      - dhcp
  /dhcp/range/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_range_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv4 ranges
      tags:
      - dhcp
  /dhcp/range/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_range_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv4 ranges
      tags:
      - dhcp
  /dhcp/range/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_range_info
      parameters:
      - description: The database identifier (ID) of the DHCPv4 range, a unique numeric
          key value automatically incremented when you add a DHCPv4 range. Use the
          ID to specify the DHCPv4 range of your choice.
        explode: true
        in: query
        name: range_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv4 range
      tags:
      - dhcp
  /dhcp/range/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (range_start_addr
        && range_end_addr && (scope_id || server_id || server_name || server_hostaddr))'
      operationId: dhcp_range_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_range_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range_add_failed'
          description: error object edition
      summary: Add a DHCPv4 range
      tags:
      - dhcp
  /dhcp/range/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (range_id || (range_start_addr && range_end_addr && (scope_id || server_id
        || server_name || server_hostaddr)))'
      operationId: dhcp_range_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_range_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range_edit_failed'
          description: error object edition
      summary: Edit a DHCPv4 range
      tags:
      - dhcp
  /dhcp/range/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (range_id || ((range_name || range_start_addr || range_end_addr) && (server_id
        || server_name || server_hostaddr || scope_id)))'
      operationId: dhcp_range_delete
      parameters:
      - description: The database identifier (ID) of the DHCPv4 server, a unique numeric
          key value automatically incremented when you add a DHCPv4 server. Use the
          ID to specify the DHCPv4 server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv4 server.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The last IP address of the DHCPv4 range.
        explode: true
        in: query
        name: range_end_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv4 range, a unique numeric
          key value automatically incremented when you add a DHCPv4 range. Use the
          ID to specify the DHCPv4 range of your choice.
        explode: true
        in: query
        name: range_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv4 range.
        explode: true
        in: query
        name: range_name
        required: false
        schema:
          type: string
        style: form
      - description: The first IP address of the DHCPv4 range.
        explode: true
        in: query
        name: range_start_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv4 scope, a unique numeric
          key value automatically incremented when you add a DHCPv4 scope. Use the
          ID to specify the DHCPv4 scope of your choice.
        explode: true
        in: query
        name: scope_id
        required: false
        schema:
          type: integer
        style: form
      - description: The IP address of the DHCP server.
        explode: true
        in: query
        name: server_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range_delete_failed'
          description: error object edition
      summary: Delete a DHCPv4 range
      tags:
      - dhcp
  /dhcp/lease/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_lease_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_lease_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv4 leases
      tags:
      - dhcp
  /dhcp/lease/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_lease_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv4 leases
      tags:
      - dhcp
  /dhcp/lease/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_lease_info
      parameters:
      - description: The database identifier (ID) of the DHCPv4 lease, a unique numeric
          key value automatically incremented when you add a DHCPv4 lease. Use the
          ID to specify the DHCPv4 lease of your choice.
        explode: true
        in: query
        name: lease_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_lease_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv4 lease
      tags:
      - dhcp
  /dhcp/static/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_static_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv4 statics
      tags:
      - dhcp
  /dhcp/static/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_static_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv4 statics
      tags:
      - dhcp
  /dhcp/static/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_static_info
      parameters:
      - description: The database identifier (ID) of the DHCP static, a unique numeric
          key value automatically incremented when you add a DHCP sttestatic. Use
          the ID to specify the DHCP static of your choice.
        explode: true
        in: query
        name: static_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv4 static
      tags:
      - dhcp
  /dhcp/static/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (static_mac_addr
        && (server_id || server_name || ip_id || server_hostaddr))'
      operationId: dhcp_static_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_static_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static_add_failed'
          description: error object edition
      summary: Add a DHCPv4 static
      tags:
      - dhcp
  /dhcp/static/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (static_id || (static_mac_addr && (server_id || server_name || ip_id || server_hostaddr)))'
      operationId: dhcp_static_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_static_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static_edit_failed'
          description: error object edition
      summary: Edit a DHCPv4 static
      tags:
      - dhcp
  /dhcp/static/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (static_id || ((static_addr)&& (server_id || server_name || server_hostaddr
        || scope_id)))'
      operationId: dhcp_static_delete
      parameters:
      - description: The database identifier (ID) of the DHCPv4 server, a unique numeric
          key value automatically incremented when you add a DHCPv4 server. Use the
          ID to specify the DHCPv4 server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv4 server.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The IP address associated with the DHCPv4 static.
        explode: true
        in: query
        name: static_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv4 static, a unique numeric
          key value automatically incremented when you add a DHCPv4 static. Use the
          ID to specify the DHCPv4 static of your choice.
        explode: true
        in: query
        name: static_id
        required: false
        schema:
          type: integer
        style: form
      - description: TODO:dhcp_static_delete.input.static_identifier
        explode: true
        in: query
        name: static_identifier
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv4 scope, a unique numeric
          key value automatically incremented when you add a DHCPv4 scope. Use the
          ID to specify the DHCPv4 scope of your choice.
        explode: true
        in: query
        name: scope_id
        required: false
        schema:
          type: integer
        style: form
      - description: The IP address of the DHCP server.
        explode: true
        in: query
        name: server_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: The MAC address you want to associate with the IPv4 static, it
          must include the MAC address type. The address has 7 sections, <b>00:11:22:33:44:55:66</b>
          , where <b>00</b> indicates the type. For Ethernet, specify <b>01</b>.
        explode: true
        in: query
        name: static_mac_addr
        required: false
        schema:
          type: string
        style: form
      - description: The name of the DHCPv4 static.
        explode: true
        in: query
        name: static_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static_delete_failed'
          description: error object edition
      summary: Delete a DHCPv4 static
      tags:
      - dhcp
  /dhcp/acl/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_acl_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_acl_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCP ACLs
      tags:
      - dhcp
  /dhcp/acl/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_acl_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCP ACLs
      tags:
      - dhcp
  /dhcp/acl/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_acl_info
      parameters:
      - description: The database identifier (ID) of the DHCPv4 ACL, a unique numeric
          key value automatically incremented when you add a DHCPv4 ACL. Use the ID
          to specify the DHCPv4 ACL of your choice.
        explode: true
        in: query
        name: acl_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_acl_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCP ACL
      tags:
      - dhcp
  /dhcp/aclentry/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_aclentry_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_aclentry_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCP ACL entries
      tags:
      - dhcp
  /dhcp/aclentry/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_aclentry_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCP ACL entries
      tags:
      - dhcp
  /dhcp/aclentry/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_aclentry_info
      parameters:
      - description: The database identifier (ID) of the DHCPv4 ACL entry, a unique
          numeric key value automatically incremented when you add a DHCPv4 ACL entry.
          Use the ID to specify the DHCPv4 ACL entry of your choice.
        explode: true
        in: query
        name: aclentry_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_aclentry_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCP ACL entry
      tags:
      - dhcp
  /dhcp/server6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_server6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_server6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv6 servers
      tags:
      - dhcp
  /dhcp/server6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_server6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv6 servers
      tags:
      - dhcp
  /dhcp/server6/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_server6_info
      parameters:
      - description: The database identifier (ID) of the DHCPv6 server, a unique numeric
          key value automatically incremented when you add a DHCPv6 server. Use the
          ID to specify the DHCPv6 server of your choice.
        explode: true
        in: query
        name: server6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_server6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv6 server
      tags:
      - dhcp
  /dhcp/group6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_group6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_group6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv6 groups
      tags:
      - dhcp
  /dhcp/sharednetwork6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_sharednetwork6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv6 shared networks
      tags:
      - dhcp
  /dhcp/sharednetwork6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_sharednetwork6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv6 shared networks
      tags:
      - dhcp
  /dhcp/sharednetwork6/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_sharednetwork6_info
      parameters:
      - description: The database identifier (ID) of the DHCPv6 shared network, a
          unique numeric key value automatically incremented when you add a DHCPv6
          shared network. Use the ID to specify the DHCPv6 shared network of your
          choice.
        explode: true
        in: query
        name: sharednetwork6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv6 shared network
      tags:
      - dhcp
  /dhcp/sharednetwork6/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (sharednetwork6_name
        && (server6_id || server6_name || ip_id || server6_hostaddr))'
      operationId: dhcp_sharednetwork6_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_sharednetwork6_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork6_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork6_add_failed'
          description: error object edition
      summary: Add a DHCPv6 shared network
      tags:
      - dhcp
  /dhcp/sharednetwork6/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        ((sharednetwork6_id || sharednetwork6_name) && (server6_id || server6_name
        || ip_id || server6_hostaddr))'
      operationId: dhcp_sharednetwork6_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_sharednetwork6_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork6_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork6_edit_failed'
          description: error object edition
      summary: Edit a DHCPv6 shared network
      tags:
      - dhcp
  /dhcp/sharednetwork6/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        ((sharednetwork6_id || sharednetwork6_name) && (server6_id || server6_name
        || ip_id || server6_hostaddr))'
      operationId: dhcp_sharednetwork6_delete
      parameters:
      - description: The database identifier (ID) of the DHCPv6 server, a unique numeric
          key value automatically incremented when you add a DHCPv6 server. Use the
          ID to specify the DHCPv6 server of your choice.
        explode: true
        in: query
        name: server6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv6 server.
        explode: true
        in: query
        name: server6_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv6 shared network, a
          unique numeric key value automatically incremented when you add a DHCPv6
          shared network. Use the ID to specify the DHCPv6 shared network of your
          choice.
        explode: true
        in: query
        name: sharednetwork6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv6 shared network.
        explode: true
        in: query
        name: sharednetwork6_name
        required: false
        schema:
          type: string
        style: form
      - description: The IP address of the DHCP server.
        explode: true
        in: query
        name: server6_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork6_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_sharednetwork6_delete_failed'
          description: error object edition
      summary: Delete a DHCPv6 Shared Network
      tags:
      - dhcp
  /dhcp/scope6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_scope6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv6 scopes
      tags:
      - dhcp
  /dhcp/scope6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_scope6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv6 scopes
      tags:
      - dhcp
  /dhcp/scope6/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_scope6_info
      parameters:
      - description: The database identifier (ID) of the DHCPv6 scope, a unique numeric
          key value automatically incremented when you add a DHCPv6 scope. Use the
          ID to specify the DHCPv6 scope of your choice.
        explode: true
        in: query
        name: scope6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv6 scope
      tags:
      - dhcp
  /dhcp/scope6/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (scope6_start_addr
        && (scope6_end_addr || scope6_prefix) && (server6_id || server6_name || server6_hostaddr))'
      operationId: dhcp_scope6_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_scope6_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope6_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope6_add_failed'
          description: error object edition
      summary: Add a DHCPv6 scope
      tags:
      - dhcp
  /dhcp/scope6/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (scope6_id || (scope6_start_addr && (scope6_end_addr || scope6_prefix) &&
        (server6_id || server6_name || server6_hostaddr)))'
      operationId: dhcp_scope6_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_scope6_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope6_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope6_edit_failed'
          description: error object edition
      summary: Edit a DHCPv6 scope
      tags:
      - dhcp
  /dhcp/scope6/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (scope6_id || (scope6_start_addr && (server6_id || server6_name || server6_hostaddr)))'
      operationId: dhcp_scope6_delete
      parameters:
      - description: The database identifier (ID) of the DHCPv6 server, a unique numeric
          key value automatically incremented when you add a DHCPv6 server. Use the
          ID to specify the DHCPv6 server of your choice.
        explode: true
        in: query
        name: server6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv6 server.
        explode: true
        in: query
        name: server6_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv6 scope, a unique numeric
          key value automatically incremented when you add a DHCPv6 scope. Use the
          ID to specify the DHCPv6 scope of your choice.
        explode: true
        in: query
        name: scope6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The first IP address of the DHCPv6 scope.
        explode: true
        in: query
        name: scope6_start_addr
        required: false
        schema:
          type: string
        style: form
      - description: The IP address of the DHCP server.
        explode: true
        in: query
        name: server6_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope6_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_scope6_delete_failed'
          description: error object edition
      summary: Delete a DHCPv6 scope
      tags:
      - dhcp
  /dhcp/range6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_range6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv6 ranges
      tags:
      - dhcp
  /dhcp/range6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_range6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv6 ranges
      tags:
      - dhcp
  /dhcp/range6/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_range6_info
      parameters:
      - description: The database identifier (ID) of the DHCPv6 range, a unique numeric
          key value automatically incremented when you add a DHCPv6 range. Use the
          ID to specify the DHCPv6 of your choice.
        explode: true
        in: query
        name: range6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv6 range
      tags:
      - dhcp
  /dhcp/range6/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (range6_start_addr
        && range6_end_addr && (scope6_id || server6_id || server6_name || server6_hostaddr))'
      operationId: dhcp_range6_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_range6_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range6_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range6_add_failed'
          description: error object edition
      summary: Add a DHCPv6 range
      tags:
      - dhcp
  /dhcp/range6/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (range6_id || (range6_start_addr && range6_end_addr && (scope6_id || server6_id
        || server6_name || server6_hostaddr)))'
      operationId: dhcp_range6_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_range6_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range6_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range6_edit_failed'
          description: error object edition
      summary: Edit a DHCPv6 range
      tags:
      - dhcp
  /dhcp/range6/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (range6_id || ((range6_start_addr || range6_end_addr) && (server6_id || server6_name
        || server6_hostaddr || scope6_id)))'
      operationId: dhcp_range6_delete
      parameters:
      - description: The database identifier (ID) of the DHCPv6 server, a unique numeric
          key value automatically incremented when you add a DHCPv6 server. Use the
          ID to specify the DHCPv6 server of your choice.
        explode: true
        in: query
        name: server6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv6 server.
        explode: true
        in: query
        name: server6_name
        required: false
        schema:
          type: string
        style: form
      - description: The last IP address of the DHCPv6 range.
        explode: true
        in: query
        name: range6_end_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv6 range, a unique numeric
          key value automatically incremented when you add a DHCPv6 range. Use the
          ID to specify the DHCPv6 of your choice.
        explode: true
        in: query
        name: range6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The first IP address of the DHCPv6 range.
        explode: true
        in: query
        name: range6_start_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv6 scope, a unique numeric
          key value automatically incremented when you add a DHCPv6 scope. Use the
          ID to specify the DHCPv6 scope of your choice.
        explode: true
        in: query
        name: scope6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The IP address of the DHCP server.
        explode: true
        in: query
        name: server6_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range6_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_range6_delete_failed'
          description: error object edition
      summary: Delete a DHCPv6 range
      tags:
      - dhcp
  /dhcp/lease6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_lease6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_lease6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv6 leases
      tags:
      - dhcp
  /dhcp/lease6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_lease6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv6 leases
      tags:
      - dhcp
  /dhcp/static6/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dhcp_static6_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DHCPv6 statics
      tags:
      - dhcp
  /dhcp/static6/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dhcp_static6_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DHCPv6 statics
      tags:
      - dhcp
  /dhcp/static6/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dhcp_static6_info
      parameters:
      - description: The database identifier (ID) of the DHCP static, a unique numeric
          key value automatically incremented when you add a DHCP static. Use the
          ID to specify the DHCP static of your choice.
        explode: true
        in: query
        name: static6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DHCPv6 static
      tags:
      - dhcp
  /dhcp/static6/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (static6_name
        && (static6_mac_addr || static6_client_duid) && (server6_id || server6_name
        || ip_id || server6_hostaddr))'
      operationId: dhcp_static6_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_static6_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static6_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static6_add_failed'
          description: error object edition
      summary: Add a DHCPv6 static
      tags:
      - dhcp
  /dhcp/static6/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (static6_id || (static6_name && (static6_mac_addr || static6_client_duid)
        && (server6_id || server6_name || ip_id || server6_hostaddr)))'
      operationId: dhcp_static6_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dhcp_static6_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static6_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static6_edit_failed'
          description: error object edition
      summary: Edit a DHCPv6 static
      tags:
      - dhcp
  /dhcp/static6/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (static6_id || ((static6_name || static6_addr) && (server6_id || server6_name
        || server6_hostaddr || scope6_id)))'
      operationId: dhcp_static6_delete
      parameters:
      - description: The database identifier (ID) of the DHCPv6 server, a unique numeric
          key value automatically incremented when you add a DHCPv6 server. Use the
          ID to specify the DHCPv6 server of your choice.
        explode: true
        in: query
        name: server6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv6 server.
        explode: true
        in: query
        name: server6_name
        required: false
        schema:
          type: string
        style: form
      - description: The IP address associated with the DHCPv6 static.
        explode: true
        in: query
        name: static6_addr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv6 static, a unique numeric
          key value automatically incremented when you add a DHCPv6 static. Use the
          ID to specify the DHCPv6 static of your choice.
        explode: true
        in: query
        name: static6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DHCPv6 static.
        explode: true
        in: query
        name: static6_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DHCPv6 scope, a unique numeric
          key value automatically incremented when you add a DHCPv6 scope. Use the
          ID to specify the DHCPv6 scope of your choice.
        explode: true
        in: query
        name: scope6_id
        required: false
        schema:
          type: integer
        style: form
      - description: The IP address of the DHCP server.
        explode: true
        in: query
        name: server6_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static6_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_static6_delete_failed'
          description: error object edition
      summary: Delete a DHCPv6 static
      tags:
      - dhcp
  /dhcp/acl6/list:
    get:
      description: 'todo(here) :dhcp.acl6.list. : Service dhcp6_class6_list'
      operationId: dhcp_acl6_list
      parameters:
      - description: 'todo(here) :dhcp.acl6.list.input.orderby. : '
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'todo(here) :dhcp.acl6.list.input.where. : '
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: 'todo(here) :dhcp.acl6.list.input.limit. : '
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: 'todo(here) :dhcp.acl6.list.input.offset. : '
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_acl6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: 'todo(here) :dhcp.acl6.list.purpose : Service dhcp6_class6_list'
      tags:
      - dhcp
  /dhcp/aclentry6/list:
    get:
      description: 'todo(here) :dhcp.aclentry6.list. : Service dhcp6_subclass6_list'
      operationId: dhcp_aclentry6_list
      parameters:
      - description: 'todo(here) :dhcp.aclentry6.list.input.orderby. : '
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'todo(here) :dhcp.aclentry6.list.input.where. : '
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: 'todo(here) :dhcp.aclentry6.list.input.limit. : '
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: 'todo(here) :dhcp.aclentry6.list.input.offset. : '
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_aclentry6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: 'todo(here) :dhcp.aclentry6.list.purpose : Service dhcp6_subclass6_list'
      tags:
      - dhcp
  /dhcp/acl6/count:
    get:
      description: 'todo(here) :dhcp.acl6.count. : Service dhcp6_class6_count'
      operationId: dhcp_acl6_count
      parameters:
      - description: 'todo(here) :dhcp.acl6.count.input.where. : '
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: 'todo(here) :dhcp.acl6.count.purpose : Service dhcp6_class6_count'
      tags:
      - dhcp
  /dhcp/aclentry6/count:
    get:
      description: 'todo(here) :dhcp.aclentry6.count. : Service dhcp6_subclass6_count'
      operationId: dhcp_aclentry6_count
      parameters:
      - description: 'todo(here) :dhcp.aclentry6.count.input.where. : '
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: 'todo(here) :dhcp.aclentry6.count.purpose : Service dhcp6_subclass6_count'
      tags:
      - dhcp
  /dhcp/acl6/info:
    get:
      description: 'todo(here) :dhcp.acl6.info. : Service dhcp6_class6_info'
      operationId: dhcp_acl6_info
      parameters:
      - description: 'todo(here) :dhcp.acl6.info.input.acl6_id. : '
        explode: true
        in: query
        name: acl6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_acl6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: 'todo(here) :dhcp.acl6.info.purpose : Service dhcp6_class6_info'
      tags:
      - dhcp
  /dhcp/aclentry6/info:
    get:
      description: 'todo(here) :dhcp.aclentry6.info. : Service dhcp6_subclass6_info'
      operationId: dhcp_aclentry6_info
      parameters:
      - description: 'todo(here) :dhcp.aclentry6.info.input.aclentry6_id. : '
        explode: true
        in: query
        name: aclentry6_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dhcp_aclentry6_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: 'todo(here) :dhcp.aclentry6.info.purpose : Service dhcp6_subclass6_info'
      tags:
      - dhcp
  /dns/server/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dns_server_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_server_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DNS servers
      tags:
      - dns
  /dns/server/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dns_server_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DNS servers
      tags:
      - dns
  /dns/server/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dns_server_info
      parameters:
      - description: The database identifier (ID) of the DNS server, a unique numeric
          key value automatically incremented when you add a DNS server. Use the ID
          to specify the DNS server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_server_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DNS server
      tags:
      - dns
  /dns/view/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dns_view_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_view_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the views
      tags:
      - dns
  /dns/view/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dns_view_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of views
      tags:
      - dns
  /dns/view/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dns_view_info
      parameters:
      - description: The database identifier (ID) of the DNS view. Use the ID to specify
          the DNS view of your choice.
        explode: true
        in: query
        name: view_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_view_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a view
      tags:
      - dns
  /dns/view/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (view_name
        && (server_id || server_name || server_hostaddr))'
      operationId: dns_view_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_view_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_view_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_view_add_failed'
          description: error object edition
      summary: Add a view
      tags:
      - dns
  /dns/view/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (view_id || (view_name && (server_id || server_name || server_hostaddr)))'
      operationId: dns_view_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_view_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_view_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_view_edit_failed'
          description: error object edition
      summary: Edit a view
      tags:
      - dns
  /dns/view/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (view_id || (view_name  && (server_id || server_name || server_hostaddr)))'
      operationId: dns_view_delete
      parameters:
      - description: The database identifier (ID) of the DNS server, a unique numeric
          key value automatically incremented when you add a DNS server. Use the ID
          to specify the DNS server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS server.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DNS view. Use the ID to specify
          the DNS view of your choice.
        explode: true
        in: query
        name: view_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS view.
        explode: true
        in: query
        name: view_name
        required: false
        schema:
          type: string
        style: form
      - description: The IP address of the DNS server.
        explode: true
        in: query
        name: server_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_view_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_view_delete_failed'
          description: error object edition
      summary: Delete a view
      tags:
      - dns
  /dns/zone/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dns_zone_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zone_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DNS options of a zone
      tags:
      - dns
  /dns/zone/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dns_zone_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of zones
      tags:
      - dns
  /dns/zone/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dns_zone_info
      parameters:
      - description: The database identifier (ID) of the DNS zone, a unique numeric
          key value automatically incremented when you add a DNS zone. Use the ID
          to specify the DNS zone of your choice.
        explode: true
        in: query
        name: zone_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zone_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a zone
      tags:
      - dns
  /dns/zone/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (zone_name
        && zone_type && (view_id || view_name) && (server_id || server_name || server_hostaddr))'
      operationId: dns_zone_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_zone_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zone_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zone_add_failed'
          description: error object edition
      summary: Add a zone
      tags:
      - dns
  /dns/zone/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (zone_id || (zone_name && (view_id || view_name) && (server_id || server_name
        || server_hostaddr)))'
      operationId: dns_zone_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_zone_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zone_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zone_edit_failed'
          description: error object edition
      summary: Edit a zone
      tags:
      - dns
  /dns/zone/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (zone_id || (zone_name && (view_id || (view_name && (server_id || server_name
        || server_hostaddr)))))'
      operationId: dns_zone_delete
      parameters:
      - description: The database identifier (ID) of the DNS server, a unique numeric
          key value automatically incremented when you add a DNS server. Use the ID
          to specify the DNS server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS server.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DNS view the object belongs
          to.
        explode: true
        in: query
        name: view_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS view the object belongs to.
        explode: true
        in: query
        name: view_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DNS zone, a unique numeric
          key value automatically incremented when you add a DNS zone. Use the ID
          to specify the DNS zone of your choice.
        explode: true
        in: query
        name: zone_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS zone the object belongs to.
        explode: true
        in: query
        name: zone_name
        required: false
        schema:
          type: string
        style: form
      - description: The IP address of the DNS server.
        explode: true
        in: query
        name: server_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zone_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zone_delete_failed'
          description: error object edition
      summary: Delete a zone
      tags:
      - dns
  /dns/rr/list:
    get:
      description: 'This service allows to list the resource records.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.<br/>Adding some resource records requires to specify one or
        more values:<table><caption>Expected values for the DNS records</caption><br/><thead><tr><th>Type</th><th>Value
        number</th><th>Related field(s)</th><th>Syntax</th></tr><br/></thead><br/><tbody><tr><td
        rowspan=''7''>SOA</td><td >1</td><td >Name server</td><td >Type in the FQDN
        of the primary Master name server for the zone the record belongs to. Has
        a special meaning when used with Dynamic DNS (DDNS): called MNAME, it allows
        the DNS client to know on which DNS server it has to update itself with DDNS.</td></tr><tr><td
        >2</td><td >Responsible</td><td >Type in the administrator email address for
        the zone the record belongs to.</td></tr><tr><td >3</td><td >Serial number</td><td
        >Type in the serial number for the zone the record belongs to. The serial
        number is automatically incremented for each zone change.</td></tr><tr><td
        >4</td><td >Refresh</td><td >Type in the refresh delay for the zone the record
        belongs to, in seconds. When reached, it forces the slave name server(s) to
        read the SOA record. If this record is higher than the slave''s one, a zone
        transfer will be triggered by the slave to get the latest version of the zone.
        Typical values are 3 to 24 hours.</td></tr><tr><td >5</td><td >Retry</td><td
        >Type in the retry delay for the zone the record belongs to, in seconds. When
        reached, it forces the slave server to retry the request if it fails to reach
        the master server during a refresh cycle. Typical values are from 10 to 60
        minutes.</td></tr><tr><td >6</td><td >Expiration</td><td >Type in the expiration
        time for the zone the record belongs to, in seconds. When reached, the zone
        records are considered to be no longer valid/authoritative. The DNS server
        then stops responding to queries for the zone. To avoid a major outage, the
        typical value is pretty high, between 1 to 3 weeks.</td></tr><tr><td >7</td><td
        >Minimum</td><td >Type in the minimum time for the zone the record belongs
        to, in seconds. It defines the period of time that negative responses can
        be cached from the slave. For instance, if a request cannot be resolved, the
        server will answer with a NXDOMAIN result (No such domain). The server will
        continue returning this value until the Minimum value expires, then it will
        retry the resolution. This value has to be between 0 and 3 hours.</td></tr><tr><td
        >NS</td><td >1</td><td >DNS server</td><td >Type in the DNS server hostname.</td></tr><tr><td
        rowspan=''2''>MX</td><td >1</td><td >Preference</td><td >Type a number, between
        0 and 65535, to define which server has priority if there are several RRs
        in the zone. The lowest the value has the priority over the other server(s).</td></tr><tr><td
        >2</td><td >Mail server</td><td >Type in the SMTP (mail) server hostname.</td></tr><tr><td
        >A</td><td >1</td><td >IP address</td><td >Type in the IPv4 Address of the
        host.</td></tr><tr><td >AAAA</td><td >1</td><td >IPv6 address</td><td >Type
        in the IPv6 Address of the host.</td></tr><tr><td >PTR</td><td >1</td><td
        >Localization</td><td >Type in the hostname that should be returned when the
        IP address is queried.</td></tr><tr><td >CNAME</td><td >1</td><td >Hostname</td><td
        >Type in the hostname.</td></tr><tr><td >TXT</td><td >1</td><td >Text</td><td
        >Type in the description of your choice (max. 255 characters including spaces).</td></tr><tr><td
        rowspan=''4''>SRV</td><td >1</td><td >Priority</td><td >Type a number, between
        0 and 65535, to define which server has priority if there are several SRV
        RRs in the zone. The lowest the value has the priority over the other server(s).</td></tr><tr><td
        >2</td><td >Weight</td><td >Type a number, between 0 and 65535, that defines
        the server weight. If two SRV RRs have the same priority, the weight defines
        which server is more used. The field gives priority to the SRV RR with the
        greatest weight value: the greater the value is, the more the server is solicited.
        If you type in 0, there is no weighting.</td></tr><tr><td >3</td><td >Ports</td><td
        >Type in the port number that delivers the service to the target.</td></tr><tr><td
        >4</td><td >Target</td><td >Type in the hostname of the server delivering
        the service.</td></tr><tr><td rowspan=''2''>HINFO</td><td >1</td><td >CPU</td><td
        >Type in the name of the CPU, either , , , , , , ,  or .</td></tr><tr><td
        >2</td><td >OS</td><td >The name of the operating system, either , , , , ,
        , , , , , , or .</td></tr><tr><td rowspan=''2''>MINFO</td><td >1</td><td >Responsible
        email</td><td >Type in the email address of the administrator of the mail
        list.</td></tr><tr><td >2</td><td >Error email</td><td >Type in the email
        address that should receive the error messages regarding the mail list.</td></tr><tr><td
        >DNAME</td><td >1</td><td >Domain</td><td >Type in the domain name of a subdomain
        of the zone.</td></tr><tr><td rowspan=''2''>AFSDB</td><td >1</td><td >Preference</td><td
        >Type the version of AFS service used:  (AFS version 3.0) or  (OSF DCE/NCA
        version).</td></tr><tr><td >2</td><td >AFS server</td><td >Type in the AFS
        hostname.</td></tr><tr><td rowspan=''6''>NAPTR</td><td >1</td><td >Order</td><td
        >Type a number, between 0 and 65535, to define which RR has priority if there
        are several NAPTR RRs in the zone. The lowest the value has the priority over
        the other record(s).</td></tr><tr><td >2</td><td >Preference</td><td >Type
        a number, between 0 and 65535, to define which RR has priority if there are
        several NAPTR RRs have the same order in the zone. The lowest the value has
        the priority over the other record(s).</td></tr><tr><td >2</td><td >Flags</td><td
        >Type in the string that corresponds to the action you want your client application
        to perform. The flag specified impacts the data expected in the field ,  and/or
        .</td></tr><tr><td >3</td><td >Services</td><td >Type in the services parameters
        to which applies the action specified in the field . You must respect your
        client application syntax.</td></tr><tr><td >4</td><td >Regex</td><td >Type
        in the string that contains a substitution expression matching the format  to
        which applies the action specified the field .</td></tr><tr><td >5</td><td
        >Replace</td><td >Type in an FQDN domain name to which applies the action
        specified the field . You can specify no domain name if you type in  (dot)
        in the field.</td></tr><tr><td >NSAP</td><td >1</td><td >Name</td><td >Type
        in the NSAP address of the end system. It should start with  and not exceed
        255 hexadecimal characters separated by dots.</td></tr><tr><td rowspan=''4''>DS</td><td
        >1</td><td >Key Tag</td><td >Type in the parent zone DS key tag.</td></tr><tr><td
        >2</td><td >Key Algorithm</td><td >Type in the parent zone DS algorithm key.</td></tr><tr><td
        >3</td><td >Digest Type</td><td >Type in the parent zone DS digest type.</td></tr><tr><td
        >4</td><td >Digest</td><td >Type in the parent zone DS digest.</td></tr><tr><td
        rowspan=''4''>DNSKEY</td><td >1</td><td >Flags</td><td >Type in or paste the
        zone key flag.</td></tr><tr><td >2</td><td >Protocol</td><td >Type in or paste
        the protocol value.</td></tr><tr><td >3</td><td >Algorithm</td><td >Type in
        or paste the public key''s cryptographic algorithm.</td></tr><tr><td >4</td><td
        >Key</td><td >Type in or paste the public key material.</td></tr><tr><td rowspan=''3''>WKS</td><td
        >1</td><td >IP address</td><td >Type in the IPv4 Address of the host that
        contains the services listed in the Services field.</td></tr><tr><td >2</td><td
        >Protocol</td><td >Type in  or .</td></tr><tr><td >3</td><td >Services</td><td
        >Type in the list of needed services.</td></tr></tbody></table></p><br/>'
      operationId: dns_rr_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_rr_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the resource records
      tags:
      - dns
  /dns/rr/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dns_rr_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of resource records
      tags:
      - dns
  /dns/rr/info:
    get:
      description: 'This service allows to display the properties of a resource record.To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.Adding some resource records requires to specify
        one or more values:<br/>                                Expected values for
        the DNS records<br/>                                <br/>                                    <br/>                                    <br/>                                    <br/>                                    <br/>                                    <br/>                                        <br/>                                            Type<br/>                                            Value
        number<br/>                                            Related field(s)<br/>                                            Syntax<br/>                                        <br/>                                    <br/>                                    <br/>                                        <br/>                                            SOA<br/>                                            1<br/>                                            Name
        server<br/>                                            Type in the FQDN of
        the primary Master name server for the zone the record belongs to. Has a special
        meaning when used with Dynamic DNS (DDNS): called MNAME, it allows the DNS
        client to know on which DNS server it has to update itself with DDNS.<br/>                                        <br/>                                        <br/>                                            2<br/>                                            Responsible<br/>                                            Type
        in the administrator email address for the zone the record belongs to.<br/>                                        <br/>                                        <br/>                                            3<br/>                                            Serial
        number<br/>                                            Type in the serial
        number for the zone the record belongs to. The serial number is automatically
        incremented for each zone change.<br/>                                        <br/>                                        <br/>                                            4<br/>                                            Refresh<br/>                                            Type
        in the refresh delay for the zone the record belongs to, in seconds. When
        reached, it forces the slave name server(s) to read the SOA record. If this
        record is higher than the slave''s one, a zone transfer will be triggered
        by the slave to get the latest version of the zone. Typical values are 3 to
        24 hours.<br/>                                        <br/>                                        <br/>                                            5<br/>                                            Retry<br/>                                            Type
        in the retry delay for the zone the record belongs to, in seconds. When reached,
        it forces the slave server to retry the request if it fails to reach the master
        server during a refresh cycle. Typical values are from 10 to 60 minutes.<br/>                                        <br/>                                        <br/>                                            6<br/>                                            Expiration<br/>                                            Type
        in the expiration time for the zone the record belongs to, in seconds. When
        reached, the zone records are considered to be no longer valid/authoritative.
        The DNS server then stops responding to queries for the zone. To avoid a major
        outage, the typical value is pretty high, between 1 to 3 weeks.<br/>                                        <br/>                                        <br/>                                            7<br/>                                            Minimum<br/>                                            Type
        in the minimum time for the zone the record belongs to, in seconds. It defines
        the period of time that negative responses can be cached from the slave. For
        instance, if a request cannot be resolved, the server will answer with a NXDOMAIN
        result (No such domain). The server will continue returning this value until
        the Minimum value expires, then it will retry the resolution. This value has
        to be between 0 and 3 hours.<br/>                                        <br/>                                        <br/>                                            NS<br/>                                            1<br/>                                            DNS
        server<br/>                                            Type in the DNS server
        hostname.<br/>                                        <br/>                                        <br/>                                            MX<br/>                                            1<br/>                                            Preference<br/>                                            Type
        a number, between 0 and 65535, to define which server has priority if there
        are several RRs in the zone. The lowest the value has the priority over the
        other server(s).<br/>                                        <br/>                                        <br/>                                            2<br/>                                            Mail
        server<br/>                                            Type in the SMTP (mail)
        server hostname.<br/>                                        <br/>                                        <br/>                                            A<br/>                                            1<br/>                                            IP
        address<br/>                                            Type in the IPv4 Address
        of the host.<br/>                                        <br/>                                        <br/>                                            AAAA<br/>                                            1<br/>                                            IPv6
        address<br/>                                            Type in the IPv6 Address
        of the host.<br/>                                        <br/>                                        <br/>                                            PTR<br/>                                            1<br/>                                            Localization<br/>                                            Type
        in the hostname that should be returned when the IP address is queried.<br/>                                        <br/>                                        <br/>                                            CNAME<br/>                                            1<br/>                                            Hostname<br/>                                            Type
        in the hostname.<br/>                                        <br/>                                        <br/>                                            TXT<br/>                                            1<br/>                                            Text<br/>                                            Type
        in the description of your choice (max. 255 characters including spaces).<br/>                                        <br/>                                        <br/>                                            SRV<br/>                                            1<br/>                                            Priority<br/>                                            Type
        a number, between 0 and 65535, to define which server has priority if there
        are several SRV RRs in the zone. The lowest the value has the priority over
        the other server(s).<br/>                                        <br/>                                        <br/>                                            2<br/>                                            Weight<br/>                                            Type
        a number, between 0 and 65535, that defines the server weight. If two SRV
        RRs have the same priority, the weight defines which server is more used.
        The field gives priority to the SRV RR with the greatest weight value: the
        greater the value is, the more the server is solicited. If you type in 0,
        there is no weighting.<br/>                                        <br/>                                        <br/>                                            3<br/>                                            Ports<br/>                                            Type
        in the port number that delivers the service to the target.<br/>                                        <br/>                                        <br/>                                            4<br/>                                            Target<br/>                                            Type
        in the hostname of the server delivering the service.<br/>                                        <br/>                                        <br/>                                            HINFO<br/>                                            1<br/>                                            CPU<br/>                                            Type
        in the name of the CPU, either INTEL, AMD, SPARC, ALPHA, HPPA, POWERPC, MIPS,
        MOTOROLA or Other.<br/>                                        <br/>                                        <br/>                                            2<br/>                                            OS<br/>                                            The
        name of the operating system, either AIX, FREEBSD, HPUX, IRIX, LINUX, OSF,
        OS/2, SOLARIS, SUNOS, VMS, WINDOWS, or Other.<br/>                                        <br/>                                        <br/>                                            MINFO<br/>                                            1<br/>                                            Responsible
        email<br/>                                            Type in the email address
        of the administrator of the mail list.<br/>                                        <br/>                                        <br/>                                            2<br/>                                            Error
        email<br/>                                            Type in the email address
        that should receive the error messages regarding the mail list.<br/>                                        <br/>                                        <br/>                                            DNAME<br/>                                            1<br/>                                            Domain<br/>                                            Type
        in the domain name of a subdomain of the zone.<br/>                                        <br/>                                        <br/>                                            AFSDB<br/>                                            1<br/>                                            Preference<br/>                                            Type
        the version of AFS service used: 1 (AFS version 3.0) or 2 (OSF DCE/NCA version).<br/>                                        <br/>                                        <br/>                                            2<br/>                                            AFS
        server<br/>                                            Type in the AFS hostname.<br/>                                        <br/>                                        <br/>                                            NAPTR<br/>                                                    The
        record NAPTR is described in the RFC 3403, available on IETF website: http://tools.ietf.org/html/rfc3403.<br/>                                                <br/>                                            1<br/>                                            Order<br/>                                            Type
        a number, between 0 and 65535, to define which RR has priority if there are
        several NAPTR RRs in the zone. The lowest the value has the priority over
        the other record(s).<br/>                                        <br/>                                        <br/>                                            2<br/>                                            Preference<br/>                                            Type
        a number, between 0 and 65535, to define which RR has priority if there are
        several NAPTR RRs have the same order in the zone. The lowest the value has
        the priority over the other record(s).<br/>                                        <br/>                                        <br/>                                            2<br/>                                            Flags<br/>                                            Type
        in the string that corresponds to the action you want your client application
        to perform. The flag specified impacts the data expected in the field Services,
        Regex and/or Replace.<br/>                                        <br/>                                        <br/>                                            3<br/>                                            Services<br/>                                            Type
        in the services parameters to which applies the action specified in the field
        Flags. You must respect your client application syntax.<br/>                                        <br/>                                        <br/>                                            4<br/>                                            Regex<br/>                                            Type
        in the string that contains a substitution expression matching the format
        <delimit ereg delimit substitution delimit flag> to which applies the action
        specified the field Flags.<br/>                                        <br/>                                        <br/>                                            5<br/>                                            Replace<br/>                                            Type
        in an FQDN domain name to which applies the action specified the field Flags.
        You can specify no domain name if you type in . (dot) in the field.<br/>                                        <br/>                                        <br/>                                            NSAP<br/>                                            1<br/>                                            Name<br/>                                            Type
        in the NSAP address of the end system. It should start with 0x and not exceed
        255 hexadecimal characters separated by dots.<br/>                                        <br/>                                        <br/>                                            DS<br/>                                            1<br/>                                            Key
        Tag<br/>                                            Type in the parent zone
        DS key tag.<br/>                                        <br/>                                        <br/>                                            2<br/>                                            Key
        Algorithm<br/>                                            Type in the parent
        zone DS algorithm key.<br/>                                        <br/>                                        <br/>                                            3<br/>                                            Digest
        Type<br/>                                            Type in the parent zone
        DS digest type.<br/>                                        <br/>                                        <br/>                                            4<br/>                                            Digest<br/>                                            Type
        in the parent zone DS digest.<br/>                                        <br/>                                        <br/>                                            DNSKEY<br/>                                            1<br/>                                            Flags<br/>                                            Type
        in or paste the zone key flag.<br/>                                        <br/>                                        <br/>                                            2<br/>                                            Protocol<br/>                                            Type
        in or paste the protocol value.<br/>                                        <br/>                                        <br/>                                            3<br/>                                            Algorithm<br/>                                            Type
        in or paste the public key''s cryptographic algorithm.<br/>                                        <br/>                                        <br/>                                            4<br/>                                            Key<br/>                                            Type
        in or paste the public key material.<br/>                                        <br/>                                        <br/>                                            WKS<br/>                                            1<br/>                                            IP
        address<br/>                                            Type in the IPv4 Address
        of the host that contains the services listed in the Services field.<br/>                                        <br/>                                        <br/>                                            2<br/>                                            Protocol<br/>                                            Type
        in TCP or UDP.<br/>                                        <br/>                                        <br/>                                            3<br/>                                            Services<br/>                                            Type
        in the list of needed services.<br/>                                        <br/>                                    <br/>                                <br/>                            '
      operationId: dns_rr_info
      parameters:
      - description: The database identifier (ID) of the DNS resource record, a unique
          numeric key value automatically incremented when you add a DNS RR. Use the
          ID to specify the DNS RR of your choice.
        explode: true
        in: query
        name: rr_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_rr_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a resource record
      tags:
      - dns
  /dns/rr/add:
    post:
      description: 'This service allows to add a resource record or edit an existing
        one.<br/>To execute this service, users must be granted the permission to
        use it. The rows returned to the user running the service depend on the resources
        granted to the group they belong to.<br/>Adding some resource records requires
        to specify one or more values:<table><caption>Expected values for the DNS
        records</caption><br/><thead><tr><th>Type</th><th>Value number</th><th>Related
        field(s)</th><th>Syntax</th></tr><br/></thead><br/><tbody><tr><td rowspan=''7''>SOA</td><td
        >1</td><td >Name server</td><td >Type in the FQDN of the primary Master name
        server for the zone the record belongs to. Has a special meaning when used
        with Dynamic DNS (DDNS): called MNAME, it allows the DNS client to know on
        which DNS server it has to update itself with DDNS.</td></tr><tr><td >2</td><td
        >Responsible</td><td >Type in the administrator email address for the zone
        the record belongs to.</td></tr><tr><td >3</td><td >Serial number</td><td
        >Type in the serial number for the zone the record belongs to. The serial
        number is automatically incremented for each zone change.</td></tr><tr><td
        >4</td><td >Refresh</td><td >Type in the refresh delay for the zone the record
        belongs to, in seconds. When reached, it forces the slave name server(s) to
        read the SOA record. If this record is higher than the slave''s one, a zone
        transfer will be triggered by the slave to get the latest version of the zone.
        Typical values are 3 to 24 hours.</td></tr><tr><td >5</td><td >Retry</td><td
        >Type in the retry delay for the zone the record belongs to, in seconds. When
        reached, it forces the slave server to retry the request if it fails to reach
        the master server during a refresh cycle. Typical values are from 10 to 60
        minutes.</td></tr><tr><td >6</td><td >Expiration</td><td >Type in the expiration
        time for the zone the record belongs to, in seconds. When reached, the zone
        records are considered to be no longer valid/authoritative. The DNS server
        then stops responding to queries for the zone. To avoid a major outage, the
        typical value is pretty high, between 1 to 3 weeks.</td></tr><tr><td >7</td><td
        >Minimum</td><td >Type in the minimum time for the zone the record belongs
        to, in seconds. It defines the period of time that negative responses can
        be cached from the slave. For instance, if a request cannot be resolved, the
        server will answer with a NXDOMAIN result (No such domain). The server will
        continue returning this value until the Minimum value expires, then it will
        retry the resolution. This value has to be between 0 and 3 hours.</td></tr><tr><td
        >NS</td><td >1</td><td >DNS server</td><td >Type in the DNS server hostname.</td></tr><tr><td
        rowspan=''2''>MX</td><td >1</td><td >Preference</td><td >Type a number, between
        0 and 65535, to define which server has priority if there are several RRs
        in the zone. The lowest the value has the priority over the other server(s).</td></tr><tr><td
        >2</td><td >Mail server</td><td >Type in the SMTP (mail) server hostname.</td></tr><tr><td
        >A</td><td >1</td><td >IP address</td><td >Type in the IPv4 Address of the
        host.</td></tr><tr><td >AAAA</td><td >1</td><td >IPv6 address</td><td >Type
        in the IPv6 Address of the host.</td></tr><tr><td >PTR</td><td >1</td><td
        >Localization</td><td >Type in the hostname that should be returned when the
        IP address is queried.</td></tr><tr><td >CNAME</td><td >1</td><td >Hostname</td><td
        >Type in the hostname.</td></tr><tr><td >TXT</td><td >1</td><td >Text</td><td
        >Type in the description of your choice (max. 255 characters including spaces).</td></tr><tr><td
        rowspan=''4''>SRV</td><td >1</td><td >Priority</td><td >Type a number, between
        0 and 65535, to define which server has priority if there are several SRV
        RRs in the zone. The lowest the value has the priority over the other server(s).</td></tr><tr><td
        >2</td><td >Weight</td><td >Type a number, between 0 and 65535, that defines
        the server weight. If two SRV RRs have the same priority, the weight defines
        which server is more used. The field gives priority to the SRV RR with the
        greatest weight value: the greater the value is, the more the server is solicited.
        If you type in 0, there is no weighting.</td></tr><tr><td >3</td><td >Ports</td><td
        >Type in the port number that delivers the service to the target.</td></tr><tr><td
        >4</td><td >Target</td><td >Type in the hostname of the server delivering
        the service.</td></tr><tr><td rowspan=''2''>HINFO</td><td >1</td><td >CPU</td><td
        >Type in the name of the CPU, either , , , , , , ,  or .</td></tr><tr><td
        >2</td><td >OS</td><td >The name of the operating system, either , , , , ,
        , , , , , , or .</td></tr><tr><td rowspan=''2''>MINFO</td><td >1</td><td >Responsible
        email</td><td >Type in the email address of the administrator of the mail
        list.</td></tr><tr><td >2</td><td >Error email</td><td >Type in the email
        address that should receive the error messages regarding the mail list.</td></tr><tr><td
        >DNAME</td><td >1</td><td >Domain</td><td >Type in the domain name of a subdomain
        of the zone.</td></tr><tr><td rowspan=''2''>AFSDB</td><td >1</td><td >Preference</td><td
        >Type the version of AFS service used:  (AFS version 3.0) or  (OSF DCE/NCA
        version).</td></tr><tr><td >2</td><td >AFS server</td><td >Type in the AFS
        hostname.</td></tr><tr><td rowspan=''6''>NAPTR</td><td >1</td><td >Order</td><td
        >Type a number, between 0 and 65535, to define which RR has priority if there
        are several NAPTR RRs in the zone. The lowest the value has the priority over
        the other record(s).</td></tr><tr><td >2</td><td >Preference</td><td >Type
        a number, between 0 and 65535, to define which RR has priority if there are
        several NAPTR RRs have the same order in the zone. The lowest the value has
        the priority over the other record(s).</td></tr><tr><td >2</td><td >Flags</td><td
        >Type in the string that corresponds to the action you want your client application
        to perform. The flag specified impacts the data expected in the field ,  and/or
        .</td></tr><tr><td >3</td><td >Services</td><td >Type in the services parameters
        to which applies the action specified in the field . You must respect your
        client application syntax.</td></tr><tr><td >4</td><td >Regex</td><td >Type
        in the string that contains a substitution expression matching the format  to
        which applies the action specified the field .</td></tr><tr><td >5</td><td
        >Replace</td><td >Type in an FQDN domain name to which applies the action
        specified the field . You can specify no domain name if you type in  (dot)
        in the field.</td></tr><tr><td >NSAP</td><td >1</td><td >Name</td><td >Type
        in the NSAP address of the end system. It should start with  and not exceed
        255 hexadecimal characters separated by dots.</td></tr><tr><td rowspan=''4''>DS</td><td
        >1</td><td >Key Tag</td><td >Type in the parent zone DS key tag.</td></tr><tr><td
        >2</td><td >Key Algorithm</td><td >Type in the parent zone DS algorithm key.</td></tr><tr><td
        >3</td><td >Digest Type</td><td >Type in the parent zone DS digest type.</td></tr><tr><td
        >4</td><td >Digest</td><td >Type in the parent zone DS digest.</td></tr><tr><td
        rowspan=''4''>DNSKEY</td><td >1</td><td >Flags</td><td >Type in or paste the
        zone key flag.</td></tr><tr><td >2</td><td >Protocol</td><td >Type in or paste
        the protocol value.</td></tr><tr><td >3</td><td >Algorithm</td><td >Type in
        or paste the public key''s cryptographic algorithm.</td></tr><tr><td >4</td><td
        >Key</td><td >Type in or paste the public key material.</td></tr><tr><td rowspan=''3''>WKS</td><td
        >1</td><td >IP address</td><td >Type in the IPv4 Address of the host that
        contains the services listed in the Services field.</td></tr><tr><td >2</td><td
        >Protocol</td><td >Type in  or .</td></tr><tr><td >3</td><td >Services</td><td
        >Type in the list of needed services.</td></tr></tbody></table></p><br/></br></br><b>Mandatory
        Parameters</b>: (rr_name && rr_type && rr_value1 && (server_id || server_name
        || server_hostaddr))'
      operationId: dns_rr_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_rr_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_rr_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_rr_add_failed'
          description: error object edition
      summary: Add a resource record
      tags:
      - dns
  /dns/rr/edit:
    put:
      description: 'This service allows to add a resource record or edit an existing
        one.<br/>To execute this service, users must be granted the permission to
        use it. The rows returned to the user running the service depend on the resources
        granted to the group they belong to.<br/>Adding some resource records requires
        to specify one or more values:<table><caption>Expected values for the DNS
        records</caption><br/><thead><tr><th>Type</th><th>Value number</th><th>Related
        field(s)</th><th>Syntax</th></tr><br/></thead><br/><tbody><tr><td rowspan=''7''>SOA</td><td
        >1</td><td >Name server</td><td >Type in the FQDN of the primary Master name
        server for the zone the record belongs to. Has a special meaning when used
        with Dynamic DNS (DDNS): called MNAME, it allows the DNS client to know on
        which DNS server it has to update itself with DDNS.</td></tr><tr><td >2</td><td
        >Responsible</td><td >Type in the administrator email address for the zone
        the record belongs to.</td></tr><tr><td >3</td><td >Serial number</td><td
        >Type in the serial number for the zone the record belongs to. The serial
        number is automatically incremented for each zone change.</td></tr><tr><td
        >4</td><td >Refresh</td><td >Type in the refresh delay for the zone the record
        belongs to, in seconds. When reached, it forces the slave name server(s) to
        read the SOA record. If this record is higher than the slave''s one, a zone
        transfer will be triggered by the slave to get the latest version of the zone.
        Typical values are 3 to 24 hours.</td></tr><tr><td >5</td><td >Retry</td><td
        >Type in the retry delay for the zone the record belongs to, in seconds. When
        reached, it forces the slave server to retry the request if it fails to reach
        the master server during a refresh cycle. Typical values are from 10 to 60
        minutes.</td></tr><tr><td >6</td><td >Expiration</td><td >Type in the expiration
        time for the zone the record belongs to, in seconds. When reached, the zone
        records are considered to be no longer valid/authoritative. The DNS server
        then stops responding to queries for the zone. To avoid a major outage, the
        typical value is pretty high, between 1 to 3 weeks.</td></tr><tr><td >7</td><td
        >Minimum</td><td >Type in the minimum time for the zone the record belongs
        to, in seconds. It defines the period of time that negative responses can
        be cached from the slave. For instance, if a request cannot be resolved, the
        server will answer with a NXDOMAIN result (No such domain). The server will
        continue returning this value until the Minimum value expires, then it will
        retry the resolution. This value has to be between 0 and 3 hours.</td></tr><tr><td
        >NS</td><td >1</td><td >DNS server</td><td >Type in the DNS server hostname.</td></tr><tr><td
        rowspan=''2''>MX</td><td >1</td><td >Preference</td><td >Type a number, between
        0 and 65535, to define which server has priority if there are several RRs
        in the zone. The lowest the value has the priority over the other server(s).</td></tr><tr><td
        >2</td><td >Mail server</td><td >Type in the SMTP (mail) server hostname.</td></tr><tr><td
        >A</td><td >1</td><td >IP address</td><td >Type in the IPv4 Address of the
        host.</td></tr><tr><td >AAAA</td><td >1</td><td >IPv6 address</td><td >Type
        in the IPv6 Address of the host.</td></tr><tr><td >PTR</td><td >1</td><td
        >Localization</td><td >Type in the hostname that should be returned when the
        IP address is queried.</td></tr><tr><td >CNAME</td><td >1</td><td >Hostname</td><td
        >Type in the hostname.</td></tr><tr><td >TXT</td><td >1</td><td >Text</td><td
        >Type in the description of your choice (max. 255 characters including spaces).</td></tr><tr><td
        rowspan=''4''>SRV</td><td >1</td><td >Priority</td><td >Type a number, between
        0 and 65535, to define which server has priority if there are several SRV
        RRs in the zone. The lowest the value has the priority over the other server(s).</td></tr><tr><td
        >2</td><td >Weight</td><td >Type a number, between 0 and 65535, that defines
        the server weight. If two SRV RRs have the same priority, the weight defines
        which server is more used. The field gives priority to the SRV RR with the
        greatest weight value: the greater the value is, the more the server is solicited.
        If you type in 0, there is no weighting.</td></tr><tr><td >3</td><td >Ports</td><td
        >Type in the port number that delivers the service to the target.</td></tr><tr><td
        >4</td><td >Target</td><td >Type in the hostname of the server delivering
        the service.</td></tr><tr><td rowspan=''2''>HINFO</td><td >1</td><td >CPU</td><td
        >Type in the name of the CPU, either , , , , , , ,  or .</td></tr><tr><td
        >2</td><td >OS</td><td >The name of the operating system, either , , , , ,
        , , , , , , or .</td></tr><tr><td rowspan=''2''>MINFO</td><td >1</td><td >Responsible
        email</td><td >Type in the email address of the administrator of the mail
        list.</td></tr><tr><td >2</td><td >Error email</td><td >Type in the email
        address that should receive the error messages regarding the mail list.</td></tr><tr><td
        >DNAME</td><td >1</td><td >Domain</td><td >Type in the domain name of a subdomain
        of the zone.</td></tr><tr><td rowspan=''2''>AFSDB</td><td >1</td><td >Preference</td><td
        >Type the version of AFS service used:  (AFS version 3.0) or  (OSF DCE/NCA
        version).</td></tr><tr><td >2</td><td >AFS server</td><td >Type in the AFS
        hostname.</td></tr><tr><td rowspan=''6''>NAPTR</td><td >1</td><td >Order</td><td
        >Type a number, between 0 and 65535, to define which RR has priority if there
        are several NAPTR RRs in the zone. The lowest the value has the priority over
        the other record(s).</td></tr><tr><td >2</td><td >Preference</td><td >Type
        a number, between 0 and 65535, to define which RR has priority if there are
        several NAPTR RRs have the same order in the zone. The lowest the value has
        the priority over the other record(s).</td></tr><tr><td >2</td><td >Flags</td><td
        >Type in the string that corresponds to the action you want your client application
        to perform. The flag specified impacts the data expected in the field ,  and/or
        .</td></tr><tr><td >3</td><td >Services</td><td >Type in the services parameters
        to which applies the action specified in the field . You must respect your
        client application syntax.</td></tr><tr><td >4</td><td >Regex</td><td >Type
        in the string that contains a substitution expression matching the format  to
        which applies the action specified the field .</td></tr><tr><td >5</td><td
        >Replace</td><td >Type in an FQDN domain name to which applies the action
        specified the field . You can specify no domain name if you type in  (dot)
        in the field.</td></tr><tr><td >NSAP</td><td >1</td><td >Name</td><td >Type
        in the NSAP address of the end system. It should start with  and not exceed
        255 hexadecimal characters separated by dots.</td></tr><tr><td rowspan=''4''>DS</td><td
        >1</td><td >Key Tag</td><td >Type in the parent zone DS key tag.</td></tr><tr><td
        >2</td><td >Key Algorithm</td><td >Type in the parent zone DS algorithm key.</td></tr><tr><td
        >3</td><td >Digest Type</td><td >Type in the parent zone DS digest type.</td></tr><tr><td
        >4</td><td >Digest</td><td >Type in the parent zone DS digest.</td></tr><tr><td
        rowspan=''4''>DNSKEY</td><td >1</td><td >Flags</td><td >Type in or paste the
        zone key flag.</td></tr><tr><td >2</td><td >Protocol</td><td >Type in or paste
        the protocol value.</td></tr><tr><td >3</td><td >Algorithm</td><td >Type in
        or paste the public key''s cryptographic algorithm.</td></tr><tr><td >4</td><td
        >Key</td><td >Type in or paste the public key material.</td></tr><tr><td rowspan=''3''>WKS</td><td
        >1</td><td >IP address</td><td >Type in the IPv4 Address of the host that
        contains the services listed in the Services field.</td></tr><tr><td >2</td><td
        >Protocol</td><td >Type in  or .</td></tr><tr><td >3</td><td >Services</td><td
        >Type in the list of needed services.</td></tr></tbody></table></p><br/></br></br><b>Mandatory
        Parameters</b>: (rr_id || (rr_name && rr_type && rr_value1 && (server_id ||
        server_name || server_hostaddr)))'
      operationId: dns_rr_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_rr_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_rr_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_rr_edit_failed'
          description: error object edition
      summary: Edit a resource record
      tags:
      - dns
  /dns/rr/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (rr_id || (rr_name && (zone_id || (zone_name && (server_id || server_name
        || server_hostaddr)))))'
      operationId: dns_rr_delete
      parameters:
      - description: The database identifier (ID) of the DNS server, a unique numeric
          key value automatically incremented when you add a DNS server. Use the ID
          to specify the DNS server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS server.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DNS zone, a unique numeric
          key value automatically incremented when you add a DNS zone. Use the ID
          to specify the DNS zone of your choice.
        explode: true
        in: query
        name: zone_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS zone the object belongs to.
        explode: true
        in: query
        name: zone_name
        required: false
        schema:
          type: string
        style: form
      - description: The IP address of the DNS server.
        explode: true
        in: query
        name: server_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DNS resource record, a unique
          numeric key value automatically incremented when you add a DNS RR. Use the
          ID to specify the DNS RR of your choice.
        explode: true
        in: query
        name: rr_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS resource record.
        explode: true
        in: query
        name: rr_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DNS view, a unique numeric
          key value automatically incremented when you add a DNS view. Use the ID
          to specify the DNS view of your choice.
        explode: true
        in: query
        name: view_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS view.
        explode: true
        in: query
        name: view_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the space associated with the
          DNS zone the record belongs to.
        explode: true
        in: query
        name: zone_space_id
        required: false
        schema:
          type: integer
        style: form
      - description: 'The type of the DNS resource record.<table><caption>rr_type
          possible values</caption><br/><thead><tr><th>Value</th><th>Record type description</th></tr><br/></thead><br/><tbody><tr><td
          >SOA</td><td >Start of Authority. Defines the zone name, an email contact
          and various time and refresh values applicable to the zone. It is automatically
          generated upon creation of a zone and cannot be added manually.</td></tr><tr><td
          >NS</td><td >Name Server. Defines the authoritative name server(s) for the
          domain (defined by the SOA record) or the subdomain. The NS record that
          indicates which server has authority over a zone is automatically generated
          upon the creation of a zone, once the server has been synchronized.</td></tr><tr><td
          >A</td><td >IPv4 Address. An IPv4 address for a host.</td></tr><tr><td >PTR</td><td
          >Pointer Record. Address Resolution, from an IP address (IPv4 or IPv6) to
          a host. Used in reverse mapping.</td></tr><tr><td >AAAA</td><td >IPv6 Address.
          An IPv6 address for a host.</td></tr><tr><td >CNAME</td><td >Canonical Name.
          An alias name for a host.</td></tr><tr><td >MX</td><td >Mail Exchange. The
          mail server/exchanger that services this zone.</td></tr><tr><td >SRV</td><td
          >Services record. Defines services available in the zone, for example, LDAP,
          HTTP, etc...</td></tr><tr><td >DNAME</td><td >Delegation of Reverse Names.
          Delegation of reverse addresses primarily in IPv6. (Deprecated, use the
          CNAME RR instead)</td></tr><tr><td >TXT</td><td >Text. Information associated
          with a name.</td></tr><tr><td >DS</td><td >Delegation Signer, a DNSSEC related
          RR used to verify the validity of the ZSK of a subdomain.</td></tr><tr><td
          >DNSKEY</td><td >DNS Key. It contains the public cryptographic key used
          to sign the zone with DNSSEC.</td></tr><tr><td >65534</td><td >A private
          type record automatically added to the zone once it is signed with DNSSEC.</td></tr><tr><td
          >HINFO</td><td >System Information. Information about a host: hardware type
          and operating system description.</td></tr><tr><td >MINFO</td><td >Mailbox
          mail list Information. Defines the mail administrator for a mail list and
          optionally a mailbox to receive error messages relating to the mail list.</td></tr><tr><td
          >AFSDB</td><td >AFS Database. Location of the AFS servers.</td></tr><tr><td
          >WKS</td><td >Well-Known Service. Defines the services and protocols supported
          by a host. (Deprecated, use the SRV RR instead)</td></tr><tr><td >NAPTR</td><td
          >Naming Authority Pointer Record. General purpose definition of rule set
          to be used by applications e.g. VoIP.</td></tr><tr><td >NSAP</td><td >Network
          Service Access Point. Defines record (equivalent of an A record) maps a
          host name to an endpoint address.</td></tr></tbody></table></p><br/>Note
          that the parameter is not case sensitive, you could specify <b>A</b> or
          <b>a</b>.'
        explode: true
        in: query
        name: rr_type
        required: false
        schema:
          enum:
          - A
          - NS
          - MD
          - MF
          - CNAME
          - SOA
          - MB
          - MG
          - MR
          - "NULL"
          - WKS
          - PTR
          - HINFO
          - MINFO
          - MX
          - TXT
          - SPF
          - RP
          - AFSDB
          - X25
          - ISDN
          - RT
          - NSAP
          - NSAP_PTR
          - SIG
          - KEY
          - PX
          - GPOS
          - AAAA
          - LOC
          - NXT
          - EID
          - NIMLOC
          - SRV
          - ATMA
          - NAPTR
          - KX
          - CERT
          - A6
          - DNAME
          - OPT
          - DS
          - DNSSIG
          - NSEC
          - DNSKEY
          - NSEC3
          - NSEC3PARAM
          - CDS
          - CDNSKEY
          - CAA
          - TLSA
          - SSHFP
          - OPENPGPKEY
          - URI
          - AVC
          - NINFO
          - DLV
          - DHCID
          - EUI48
          - EUI64
          - NID
          - L32
          - L64
          type: string
        style: form
      - description: The first or only value required for the DNS resource record,
          as detailed in the service description
        explode: true
        in: query
        name: rr_value1
        required: false
        schema:
          type: string
        style: form
      - description: The second value of the DNS resource record, depending on its
          type, as detailed in the service description
        explode: true
        in: query
        name: rr_value2
        required: false
        schema:
          type: string
        style: form
      - description: The third value of the DNS resource record, depending on its
          type, as detailed in the service description
        explode: true
        in: query
        name: rr_value3
        required: false
        schema:
          type: string
        style: form
      - description: The fourth value of the DNS resource record, depending on its
          type, as detailed in the service description
        explode: true
        in: query
        name: rr_value4
        required: false
        schema:
          type: string
        style: form
      - description: The fifth value of the DNS resource record, depending on its
          type, as detailed in the service description
        explode: true
        in: query
        name: rr_value5
        required: false
        schema:
          type: string
        style: form
      - description: The sixth value of the DNS resource record, depending on its
          type, as detailed in the service description
        explode: true
        in: query
        name: rr_value6
        required: false
        schema:
          type: string
        style: form
      - description: The seventh value of the DNS resource record, depending on its
          type, as detailed in the service description
        explode: true
        in: query
        name: rr_value7
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_rr_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_rr_delete_failed'
          description: error object edition
      summary: Delete a resource record
      tags:
      - dns
  /dns/acl/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dns_acl_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_acl_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DNS ACLs
      tags:
      - dns
  /dns/acl/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dns_acl_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DNS ACLs
      tags:
      - dns
  /dns/acl/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dns_acl_info
      parameters:
      - description: The database identifier (ID) of the DNS ACL, a unique numeric
          key value automatically incremented when you add a DNS ACL. Use the ID to
          specify the DNS ACL of your choice.
        explode: true
        in: query
        name: acl_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_acl_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DNS ACL
      tags:
      - dns
  /dns/acl/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: ((acl_name  &&
        (server_id || server_name || server_hostaddr)) && acl_value)'
      operationId: dns_acl_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_acl_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_acl_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_acl_add_failed'
          description: error object edition
      summary: Add a DNS ACL
      tags:
      - dns
  /dns/acl/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        ((acl_id || (acl_name && (server_id || server_name || server_hostaddr))) &&
        acl_value)'
      operationId: dns_acl_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_acl_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_acl_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_acl_edit_failed'
          description: error object edition
      summary: Edit a DNS ACL
      tags:
      - dns
  /dns/acl/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        acl_id'
      operationId: dns_acl_delete
      parameters:
      - description: The database identifier (ID) of the DNS ACL, a unique numeric
          key value automatically incremented when you add a DNS ACL. Use the ID to
          specify the DNS ACL of your choice.
        explode: true
        in: query
        name: acl_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the DNS server, a unique numeric
          key value automatically incremented when you add a DNS server. Use the ID
          to specify the DNS server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DNS server.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The name of the DNS ACL.
        explode: true
        in: query
        name: acl_name
        required: false
        schema:
          type: string
        style: form
      - description: The IP address of the DNS server.
        explode: true
        in: query
        name: server_hostaddr
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_acl_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_acl_delete_failed'
          description: error object edition
      summary: Delete a DNS ACL
      tags:
      - dns
  /dns/viewparam/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dns_viewparam_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_viewparam_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DNS options of a view
      tags:
      - dns
  /dns/viewparam/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dns_viewparam_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DNS options of a view
      tags:
      - dns
  /dns/viewparam/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dns_viewparam_info
      parameters:
      - description: The database identifier (ID) of the DNS view. Use the ID to specify
          the DNS view of your choice.
        explode: true
        in: query
        name: view_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_viewparam_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DNS option set on a view
      tags:
      - dns
  /dns/viewparam/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (view_id
        && viewparam_key)'
      operationId: dns_viewparam_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_viewparam_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_viewparam_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_viewparam_add_failed'
          description: error object edition
      summary: Add a DNS option on a view
      tags:
      - dns
  /dns/viewparam/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (view_id && viewparam_key)'
      operationId: dns_viewparam_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_viewparam_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_viewparam_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_viewparam_edit_failed'
          description: error object edition
      summary: Edit a DNS option on a view
      tags:
      - dns
  /dns/viewparam/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (view_id && viewparam_key)'
      operationId: dns_viewparam_delete
      parameters:
      - description: The database identifier (ID) of the DNS view. Use the ID to specify
          the DNS view of your choice.
        explode: true
        in: query
        name: view_id
        required: false
        schema:
          type: integer
        style: form
      - description: 'The name of the DNS option that you want to remove from the
          view: <b>param_key=&lt;option-name&gt;</b>.'
        explode: true
        in: query
        name: viewparam_key
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_viewparam_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_viewparam_delete_failed'
          description: error object edition
      summary: Delete a DNS option from a view
      tags:
      - dns
  /dns/zoneparam/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: dns_zoneparam_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zoneparam_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the DNS options of a zone
      tags:
      - dns
  /dns/zoneparam/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: dns_zoneparam_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of DNS options of a zone
      tags:
      - dns
  /dns/zoneparam/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: dns_zoneparam_info
      parameters:
      - description: The database identifier (ID) of the DNS zone, a unique numeric
          key value automatically incremented when you add a DNS zone. Use the ID
          to specify the DNS zone of your choice.
        explode: true
        in: query
        name: zone_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zoneparam_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a DNS option set on a zone
      tags:
      - dns
  /dns/zoneparam/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (zone_id
        && zoneparam_key)'
      operationId: dns_zoneparam_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_zoneparam_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zoneparam_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zoneparam_add_failed'
          description: error object edition
      summary: Add a DNS option on a zone
      tags:
      - dns
  /dns/zoneparam/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (zone_id && zoneparam_key)'
      operationId: dns_zoneparam_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/dns_zoneparam_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zoneparam_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zoneparam_edit_failed'
          description: error object edition
      summary: Edit a DNS option on a zone
      tags:
      - dns
  /dns/zoneparam/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (zone_id && zoneparam_key)'
      operationId: dns_zoneparam_delete
      parameters:
      - description: The database identifier (ID) of the DNS zone, a unique numeric
          key value automatically incremented when you add a DNS zone. Use the ID
          to specify the DNS zone of your choice.
        explode: true
        in: query
        name: zone_id
        required: false
        schema:
          type: integer
        style: form
      - description: 'The name of the DNS option that you want to remove from the
          zone: <b>param_key=&lt;option-name&gt;</b>.'
        explode: true
        in: query
        name: zoneparam_key
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zoneparam_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dns_zoneparam_delete_failed'
          description: error object edition
      summary: Delete a DNS option from a zone
      tags:
      - dns
  /app/application/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: app_application_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_application_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the applications
      tags:
      - app
  /app/application/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: app_application_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of applications
      tags:
      - app
  /app/application/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: app_application_info
      parameters:
      - description: The database identifier (ID) of the application, a unique numeric
          key value automatically incremented when you add an application. Use the
          ID to specify the application of your choice.
        explode: true
        in: query
        name: application_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_application_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of an application
      tags:
      - app
  /app/application/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (application_name
        && application_fqdn)'
      operationId: app_application_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/app_application_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_application_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_application_add_failed'
          description: error object edition
      summary: Add an application
      tags:
      - app
  /app/application/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (application_id || (application_name && application_fqdn))'
      operationId: app_application_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/app_application_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_application_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_application_edit_failed'
          description: error object edition
      summary: Edit an application
      tags:
      - app
  /app/application/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (application_id || (application_name && application_fqdn))'
      operationId: app_application_delete
      parameters:
      - description: The database identifier (ID) of the application, a unique numeric
          key value automatically incremented when you add an application. Use the
          ID to specify the application of your choice.
        explode: true
        in: query
        name: application_id
        required: false
        schema:
          type: integer
        style: form
      - description: The Fully Qualified Domain Name (FQDN) of the application.
        explode: true
        in: query
        name: application_fqdn
        required: false
        schema:
          type: string
        style: form
      - description: The name of the application.
        explode: true
        in: query
        name: application_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the GSLB server associated with
          the application, a unique identifier automatically incremented when you
          add the server. Use it to identify the GSLB server of your choice.
        explode: true
        in: query
        name: gslbserver_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the GSLB server associated with the application.
        explode: true
        in: query
        name: gslbserver_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_application_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_application_delete_failed'
          description: error object edition
      summary: Delete an application
      tags:
      - app
  /app/pool/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: app_pool_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_pool_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the pools
      tags:
      - app
  /app/pool/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: app_pool_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of pools
      tags:
      - app
  /app/pool/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: app_pool_info
      parameters:
      - description: The database identifier (ID) of the pool, a unique numeric key
          value automatically incremented when you add a pool. Use the ID to specify
          the pool of your choice.
        explode: true
        in: query
        name: pool_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_pool_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a pool
      tags:
      - app
  /app/pool/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (pool_name
        && pool_type && pool_lb_mode && (application_id || (appapplication_pool_name
        && application_fqdn)))'
      operationId: app_pool_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/app_pool_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_pool_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_pool_add_failed'
          description: error object edition
      summary: Add a pool
      tags:
      - app
  /app/pool/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (pool_id || (pool_name && (application_id || (appapplication_pool_name &&
        application_fqdn))))'
      operationId: app_pool_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/app_pool_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_pool_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_pool_edit_failed'
          description: error object edition
      summary: Edit a pool
      tags:
      - app
  /app/pool/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (pool_id || (pool_name && (application_id || (appapplication_pool_name &&
        application_fqdn))))'
      operationId: app_pool_delete
      parameters:
      - description: The Fully Qualified Domain Name (FQDN) of the application the
          object belongs to.
        explode: true
        in: query
        name: application_fqdn
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the application the object belongs
          to.
        explode: true
        in: query
        name: application_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the application the object belongs to.
        explode: true
        in: query
        name: application_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the pool, a unique numeric key
          value automatically incremented when you add a pool. Use the ID to specify
          the pool of your choice.
        explode: true
        in: query
        name: pool_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the pool.
        explode: true
        in: query
        name: pool_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the GSLB server associated with
          the application, a unique identifier automatically incremented when you
          add the server. Use it to identify the GSLB server of your choice.
        explode: true
        in: query
        name: gslbserver_id
        required: false
        schema:
          type: integer
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_pool_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_pool_delete_failed'
          description: error object edition
      summary: Delete a pool
      tags:
      - app
  /app/node/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: app_node_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_node_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the nodes
      tags:
      - app
  /app/node/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: app_node_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of nodes
      tags:
      - app
  /app/node/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: app_node_info
      parameters:
      - description: The database identifier (ID) of the node, a unique numeric key
          value automatically incremented when you add a node. Use the ID to specify
          the node of your choice.
        explode: true
        in: query
        name: node_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_node_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a node
      tags:
      - app
  /app/node/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (node_name
        && node_hostaddr && (pool_id || (apppool_node_name && (application_id || (appapplication_node_name
        && application_fqdn)))))'
      operationId: app_node_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/app_node_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_node_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_node_add_failed'
          description: error object edition
      summary: Add a node
      tags:
      - app
  /app/node/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (node_id || (node_name && (pool_id || (apppool_node_name && (application_id
        || (appapplication_node_name && application_fqdn))))))'
      operationId: app_node_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/app_node_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_node_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_node_edit_failed'
          description: error object edition
      summary: Edit a node
      tags:
      - app
  /app/node/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (node_id || (node_name && (pool_id || (apppool_node_name && (application_id
        || (appapplication_node_name && application_fqdn))))))'
      operationId: app_node_delete
      parameters:
      - description: The Fully Qualified Domain Name (FQDN) of the application the
          object belongs to.
        explode: true
        in: query
        name: application_fqdn
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the application the object belongs
          to.
        explode: true
        in: query
        name: application_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the application the object belongs to.
        explode: true
        in: query
        name: application_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the node, a unique numeric key
          value automatically incremented when you add a node. Use the ID to specify
          the node of your choice.
        explode: true
        in: query
        name: node_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the pool, a unique numeric key
          value automatically incremented when you add a pool. Use the ID to specify
          the pool of your choice.
        explode: true
        in: query
        name: pool_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the pool.
        explode: true
        in: query
        name: pool_name
        required: false
        schema:
          type: string
        style: form
      - description: The name of the node.
        explode: true
        in: query
        name: node_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the GSLB server associated with
          the application, a unique identifier automatically incremented when you
          add the server. Use it to identify the GSLB server of your choice.
        explode: true
        in: query
        name: gslbserver_id
        required: false
        schema:
          type: integer
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_node_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/app_node_delete_failed'
          description: error object edition
      summary: Delete a node
      tags:
      - app
  /guardian/policy/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: guardian_policy_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/guardian_policy_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the policies
      tags:
      - guardian
  /guardian/policy/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: guardian_policy_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of policies
      tags:
      - guardian
  /guardian/policy/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: guardian_policy_info
      parameters:
      - description: The database identifier (ID) of the policy, a unique identifier
          automatically incremented when you add the policy. Use it to identify the
          policy of your choice.
        explode: true
        in: query
        name: policy_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/guardian_policy_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a policy
      tags:
      - guardian
  /guardian/policy/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (policy_name)'
      operationId: guardian_policy_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/guardian_policy_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/guardian_policy_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/guardian_policy_add_failed'
          description: error object edition
      summary: Add a policy
      tags:
      - guardian
  /guardian/policy/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (policy_id || (policy_name))'
      operationId: guardian_policy_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/guardian_policy_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/guardian_policy_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/guardian_policy_edit_failed'
          description: error object edition
      summary: Edit a policy
      tags:
      - guardian
  /guardian/policy/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (policy_id || (policy_name && (dns_policy_name || server_id))'
      operationId: guardian_policy_delete
      parameters:
      - description: The database identifier (ID) of the Guardian server associated
          with the policy, a unique identifier automatically incremented when you
          add the server. Use it to identify the Guardian server of your choice.
        explode: true
        in: query
        name: server_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the Guardian server associated with the policy.
        explode: true
        in: query
        name: server_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the policy, a unique identifier
          automatically incremented when you add the policy. Use it to identify the
          policy of your choice.
        explode: true
        in: query
        name: policy_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the policy.
        explode: true
        in: query
        name: policy_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/guardian_policy_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/guardian_policy_delete_failed'
          description: error object edition
      summary: Delete a policy
      tags:
      - guardian
  /device/device/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: device_device_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_device_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the Device Manager devices
      tags:
      - device
  /device/device/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: device_device_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of Device Manager devices
      tags:
      - device
  /device/device/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: device_device_info
      parameters:
      - description: The database identifier (ID) of the Device Manager device, a
          unique numeric key value automatically incremented when you add a device.
          Use the ID to specify the device of your choice.
        explode: true
        in: query
        name: device_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_device_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a Device Manager device
      tags:
      - device
  /device/device/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (device_name)'
      operationId: device_device_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/device_device_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_device_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_device_add_failed'
          description: error object edition
      summary: Add a Device Manager device
      tags:
      - device
  /device/device/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (device_id || device_name)'
      operationId: device_device_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/device_device_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_device_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_device_edit_failed'
          description: error object edition
      summary: Add a Device Manager device
      tags:
      - device
  /device/device/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (device_id || device_name)'
      operationId: device_device_delete
      parameters:
      - description: The database identifier (ID) of the Device Manager device, a
          unique numeric key value automatically incremented when you add a device.
          Use the ID to specify the device of your choice.
        explode: true
        in: query
        name: device_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the Device Manager device.
        explode: true
        in: query
        name: device_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_device_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_device_delete_failed'
          description: error object edition
      summary: Delete a Device Manager device
      tags:
      - device
  /device/interface/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: device_interface_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_interface_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the Device Manager ports &amp; interfaces
      tags:
      - device
  /device/interface/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: device_interface_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number Device Manager ports &amp; interfaces
      tags:
      - device
  /device/interface/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: device_interface_info
      parameters:
      - description: The database identifier (ID) of the Device Manager port or interface,
          a unique numeric key value automatically incremented when you add a port
          or interface. Use the ID to specify the port or interface of your choice.
        explode: true
        in: query
        name: interface_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_interface_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a Device Manager port or interface
      tags:
      - device
  /device/interface/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (interface_name
        && (device_id || device_name)'
      operationId: device_interface_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/device_interface_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_interface_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_interface_add_failed'
          description: error object edition
      summary: Add a Device Manager port or interface
      tags:
      - device
  /device/interface/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (interface_id || (interface_name && (device_id || device_name))'
      operationId: device_interface_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/device_interface_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_interface_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_interface_edit_failed'
          description: error object edition
      summary: Add a Device Manager port or interface
      tags:
      - device
  /device/interface/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (interface_id || (interface_name && (device_id || device_name))'
      operationId: device_interface_delete
      parameters:
      - description: The database identifier (ID) of the Device Manager device, a
          unique numeric key value automatically incremented when you add a device.
          Use the ID to specify the device of your choice.
        explode: true
        in: query
        name: device_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the Device Manager device.
        explode: true
        in: query
        name: device_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the Device Manager port or interface,
          a unique numeric key value automatically incremented when you add a port
          or interface. Use the ID to specify the port or interface of your choice.
        explode: true
        in: query
        name: interface_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the Device Manager port or interface.
        explode: true
        in: query
        name: interface_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_interface_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_interface_delete_failed'
          description: error object edition
      summary: Delete a Device Manager port or interface
      tags:
      - device
  /device/link/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: device_link_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the Device Manager port or interface,
          a unique numeric key value automatically incremented when you add a port
          or interface. Use the ID to specify the port or interface of your choice.
        explode: true
        in: query
        name: interface_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_link_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List Device Manager ports &amp; interfaces
      tags:
      - device
  /device/link/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: device_link_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the Device Manager port or interface,
          a unique numeric key value automatically incremented when you add a port
          or interface. Use the ID to specify the port or interface of your choice.
        explode: true
        in: query
        name: interface_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of links between Device Manager devices
      tags:
      - device
  /device/link/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: ( (interface1_id
        || (interface1_name && device1_name)) && (interface2_id || (interface2_name
        && device2_name)) )'
      operationId: device_link_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/device_link_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_link_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_link_add_failed'
          description: error object edition
      summary: Link two Device Manager devices using their ports and/or interfaces
      tags:
      - device
  /device/link/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (link_id || ( (interface1_id || (interface1_name && device1_name)) && (interface2_id
        || (interface2_name && device2_name)) ))'
      operationId: device_link_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/device_link_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_link_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_link_edit_failed'
          description: error object edition
      summary: Link two Device Manager devices using their ports and/or interfaces
      tags:
      - device
  /device/link/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (link_id || ( (interface1_id || (interface1_name && device1_name)) && (interface2_id
        || (interface2_name && device2_name)) ))'
      operationId: device_link_delete
      parameters:
      - description: The name of the device to which belongs the DM port or interface
          you want to unlink from <b>hostiface2_id</b>.
        explode: true
        in: query
        name: device1_name
        required: false
        schema:
          type: string
        style: form
      - description: The name of the device to which belongs the DM port or interface
          you want to unlink from <b>hostiface1_id</b>.
        explode: true
        in: query
        name: device2_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DM port or interface you
          want to unlink from <b>hostiface2_id</b>.
        explode: true
        in: query
        name: interface1_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DM port or interface you want to unlink from
          <b>hostiface2_id</b>.
        explode: true
        in: query
        name: interface1_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the DM port or interface you
          want to unlink from <b>hostiface1_id</b>.
        explode: true
        in: query
        name: interface2_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the DM port or interface you want to unlink from
          <b>hostiface1_id</b>.
        explode: true
        in: query
        name: interface2_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the Device Manager port or interface
          link, a unique numeric key value automatically incremented when you add
          a link between a device and a port or interface. Use the ID to specify the
          port or interface link of your choice.
        explode: true
        in: query
        name: link_id
        required: false
        schema:
          type: integer
        style: form
      - description: A way to determine if the link between two Device Manager devices
          is set automatically (<b>1</b>) or manually (<b>0</b>).
        explode: true
        in: query
        name: link_auto_link
        required: false
        schema:
          type: integer
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_link_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/device_link_delete_failed'
          description: error object edition
      summary: Delete a link between two Device Manager devices
      tags:
      - device
  /vlan/domain/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: vlan_domain_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_domain_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the VLAN domains
      tags:
      - vlan
  /vlan/domain/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: vlan_domain_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of VLAN domains
      tags:
      - vlan
  /vlan/domain/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: vlan_domain_info
      parameters:
      - description: The database identifier (ID) of the VLAN domain, a unique numeric
          key value automatically incremented when you add a VLAN domain. Use the
          ID to specify the VLAN domain of your choice.
        explode: true
        in: query
        name: domain_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_domain_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a VLAN domain
      tags:
      - vlan
  /vlan/domain/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: domain_name'
      operationId: vlan_domain_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/vlan_domain_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_domain_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_domain_add_failed'
          description: error object edition
      summary: Add a VLAN domain
      tags:
      - vlan
  /vlan/domain/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (domain_id || domain_name)'
      operationId: vlan_domain_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/vlan_domain_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_domain_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_domain_edit_failed'
          description: error object edition
      summary: Edit a VLAN domain
      tags:
      - vlan
  /vlan/domain/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (domain_id || domain_name)'
      operationId: vlan_domain_delete
      parameters:
      - description: The database identifier (ID) of the VLAN domain, a unique numeric
          key value automatically incremented when you add a VLAN domain. Use the
          ID to specify the VLAN domain of your choice.
        explode: true
        in: query
        name: domain_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the VLAN domain.
        explode: true
        in: query
        name: domain_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_domain_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_domain_delete_failed'
          description: error object edition
      summary: Delete a VLAN domain
      tags:
      - vlan
  /vlan/range/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: vlan_range_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_range_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the VLAN ranges
      tags:
      - vlan
  /vlan/range/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: vlan_range_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of VLAN ranges
      tags:
      - vlan
  /vlan/range/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: vlan_range_info
      parameters:
      - description: The database identifier (ID) of the VLAN range, a unique numeric
          key value automatically incremented when you add a VLAN range. Use the ID
          to specify the VLAN range of your choice.
        explode: true
        in: query
        name: range_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_range_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a VLAN range
      tags:
      - vlan
  /vlan/range/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (range_name
        && (domain_id || domain_name))'
      operationId: vlan_range_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/vlan_range_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_range_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_range_add_failed'
          description: error object edition
      summary: Add a VLAN range
      tags:
      - vlan
  /vlan/range/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (range_id || (range_name && (domain_id || domain_name)))'
      operationId: vlan_range_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/vlan_range_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_range_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_range_edit_failed'
          description: error object edition
      summary: Edit a VLAN range
      tags:
      - vlan
  /vlan/range/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (range_id || (range_name && (domain_id || domain_name)))'
      operationId: vlan_range_delete
      parameters:
      - description: The database identifier (ID) of the VLAN domain, a unique numeric
          key value automatically incremented when you add a VLAN domain. Use the
          ID to specify the VLAN domain of your choice.
        explode: true
        in: query
        name: domain_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the VLAN domain.
        explode: true
        in: query
        name: domain_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the VLAN range, a unique numeric
          key value automatically incremented when you add a VLAN range. Use the ID
          to specify the VLAN range of your choice.
        explode: true
        in: query
        name: range_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the VLAN range.
        explode: true
        in: query
        name: range_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_range_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_range_delete_failed'
          description: error object edition
      summary: Delete a VLAN range
      tags:
      - vlan
  /vlan/vlan/list:
    get:
      description: This service allows to list the objects.<br/>To execute this service,
        users must be granted the permission to use it. The rows returned to the user
        running the service depend on the resources granted to the group they belong
        to.
      operationId: vlan_vlan_list
      parameters:
      - description: 'A clause that allows to sort the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          sort the result using class parameters, you must tag them first. For more
          details, refer to the section .The parameters and their value must be specified
          following the operators and syntax of the SQL standard, as follows: <b>&lt;parameter&gt;=''&lt;value&gt;''</b>.
          The clause must be encoded in URL format.You can add the optional keyword
          <b>ASC</b> (ascending) or <b>DESC</b> (descending) after each parameter.
          If not specified, <b>ASC</b> is used by default. The order of the parameters
          specified is set using their value''s name or ordinal number. Each parameter
          value is compared from one row to the next. If all the parameters of the
          rows are equal, they are returned in an implementation-dependent order.'
        explode: true
        in: query
        name: orderby
        required: false
        schema:
          type: string
        style: form
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service in this clause, except class parameters.To
          filter the result using class parameters, you must tag them first<i>                                            It
          is not possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The maximum number of results to be returned. Depending on the
          user resources and the database content, it can return less results than
          the value you have specified.The input parameter <b>limit</b> must be specified
          in <b>lowercase</b>.
        explode: true
        in: query
        name: limit
        required: false
        schema:
          type: integer
        style: form
      - description: The number of rows to skip in the service output.The input parameter
          <b>offset</b> must be specified in <b>lowercase</b>.
        explode: true
        in: query
        name: offset
        required: false
        schema:
          type: integer
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_vlan_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: List the VLANs
      tags:
      - vlan
  /vlan/vlan/count:
    get:
      description: This service allows to return the number of objects.<br/>To execute
        this service, users must be granted the permission to use it. The rows returned
        to the user running the service depend on the resources granted to the group
        they belong to.
      operationId: vlan_vlan_count
      parameters:
      - description: 'A clause that allows to filter the result. You can include any
          output parameter of the service <b>*_list</b> of the object in this clause,
          except class parameters.To filter the result using class parameters, you
          must tag them first<i>                                            It is
          no longer possible to use the structure <b>&lt;object-name&gt;_class_parameters
          like &lt;value&gt;</b> directly in the clause <b>WHERE</b>.<br/>                                        </i>.
          For more details, refer to the section .The parameters and their value must
          be specified following the operators and syntax of the SQL standard, as
          in the following examples : <b>&lt;parameter&gt;=''&lt;value&gt;''</b> or
          <b>&lt;parameter&gt; IS NOT NULL</b>. The clause must be encoded in URL
          format.'
        explode: true
        in: query
        name: where
        required: false
        schema:
          type: string
        style: form
      - description: The list of class parameters to tag in the call, separated by
          a comma. Each parameter must be specified following the format <object-type>.<parameter>,
          e.g. site.decription .
        explode: true
        in: query
        name: tags
        required: false
        schema:
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiCountResponseSuccess'
          description: result operation count
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Count the number of VLANs
      tags:
      - vlan
  /vlan/vlan/info:
    get:
      description: This service allows to display the properties of an object.<br/>To
        execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.
      operationId: vlan_vlan_info
      parameters:
      - description: The database identifier (ID) of the VLAN, a unique numeric key
          value automatically incremented when you add a VLAN. Use the ID to specify
          the VLAN of your choice.
        explode: true
        in: query
        name: vlan_id
        required: false
        schema:
          type: integer
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_vlan_data'
          description: successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
          description: error executing the action
      summary: Display the properties of a VLAN
      tags:
      - vlan
  /vlan/vlan/add:
    post:
      description: 'This service allows to add objects. A call can only add one object.
        To execute this service, users must be granted the permission to use it. The
        rows returned to the user running the service depend on the resources granted
        to the group they belong to.</br></br><b>Mandatory Parameters</b>: (vlan_vlan_id
        && (domain_id || domain_name))'
      operationId: vlan_vlan_add
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/vlan_vlan_add_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_vlan_add_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_vlan_add_failed'
          description: error object edition
      summary: Add a VLAN
      tags:
      - vlan
  /vlan/vlan/edit:
    put:
      description: 'This service allows to edit existing objects using their identifier.
        All the input parameters and their value specified in input edit the identified
        object, any parameter not specified is left unchanged. A call can only edit
        one object.  To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (vlan_id || (vlan_vlan_id && (domain_id || domain_name)))'
      operationId: vlan_vlan_edit
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/vlan_vlan_edit_input'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_vlan_edit_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_vlan_edit_failed'
          description: error object edition
      summary: Edit a VLAN
      tags:
      - vlan
  /vlan/vlan/delete:
    delete:
      description: 'This service allows to delete an object. A call can only delete
        one object.<br/>To execute this service, users must be granted the permission
        to use it. The rows returned to the user running the service depend on the
        resources granted to the group they belong to.</br></br><b>Mandatory Parameters</b>:
        (vlan_id || (vlan_vlan_id && (domain_id || domain_name)))'
      operationId: vlan_vlan_delete
      parameters:
      - description: The database identifier (ID) of the VLAN domain, a unique numeric
          key value automatically incremented when you add a VLAN domain. Use the
          ID to specify the VLAN domain of your choice.
        explode: true
        in: query
        name: domain_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the VLAN domain.
        explode: true
        in: query
        name: domain_name
        required: false
        schema:
          type: string
        style: form
      - description: The database identifier (ID) of the VLAN, a unique numeric key
          value automatically incremented when you add a VLAN. Use the ID to specify
          the VLAN of your choice.
        explode: true
        in: query
        name: vlan_id
        required: false
        schema:
          type: integer
        style: form
      - description: The VLAN identifier (ID) of the VLAN, a unique numeric key value
          within a VLAN domain. Use the ID to specify the VLAN of your choice.
        explode: true
        in: query
        name: vlan_vlan_id
        required: false
        schema:
          type: integer
        style: form
      - description: The database identifier (ID) of the VLAN range, a unique numeric
          key value automatically incremented when you add a VLAN range. Use the ID
          to specify the VLAN range of your choice.
        explode: true
        in: query
        name: range_id
        required: false
        schema:
          type: integer
        style: form
      - description: The name of the VLAN range.
        explode: true
        in: query
        name: range_name
        required: false
        schema:
          type: string
        style: form
      - description: The name of the VLAN.
        explode: true
        in: query
        name: vlan_name
        required: false
        schema:
          type: string
        style: form
      - description: A way to bypass <b>(accept) </b>any enabled rule that would return
          warning messages. If the service returns an error message, you cannot bypass
          the enabled rules.
        explode: true
        in: query
        name: warnings
        required: false
        schema:
          enum:
          - accept
          type: string
        style: form
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_vlan_delete_success'
          description: successful object edition
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/vlan_vlan_delete_failed'
          description: error object edition
      summary: Delete a VLAN
      tags:
      - vlan
components:
  responses:
    SuccessObjectChanges:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
      description: operation succeed
    ErrorObjectChanges:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiResponse'
      description: The specified resource was not found
    EmptyObjectChanges:
      description: no result
    SuccessObjectCount:
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ApiCountResponseSuccess'
      description: result operation count
  schemas:
    ApiResponse:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ApiCountResponseSuccess:
      example:
        data:
        - count: 0
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ApiCountResponseSuccess_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ApiMessageEntry:
      example:
        msg: msg
        code: 0
        extras:
          key: extras
        type: error
      properties:
        code:
          format: int32
          type: integer
        msg:
          type: string
        type:
          enum:
          - error
          - warning
          - notice
          type: string
        extras:
          additionalProperties:
            type: string
          type: object
      type: object
    ApiClassParameterOutputEntry:
      example:
        propagation: propagate
        name: name
        inheritance: set
        source: source
        value: value
      properties:
        name:
          type: string
        value:
          type: string
        inheritance:
          enum:
          - set
          - inherited
          type: string
        propagation:
          enum:
          - propagate
          - restrict
          type: string
        source:
          type: string
      type: object
    ApiClassParameterInputEntry:
      properties:
        name:
          type: string
        value:
          type: string
        inheritance:
          enum:
          - set
          - inherited
          type: string
        propagation:
          enum:
          - propagate
          - restrict
          type: string
      type: object
    app_application_data:
      example:
        data:
        - application_fqdn: application_fqdn
          application_inactive_nodes: application_inactive_nodes
          application_delayed_time: application_delayed_time
          application_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          application_total_nodes: application_total_nodes
          application_class_name: application_class_name
          parent_application_id: parent_application_id
          application_multistatus: application_multistatus
          gslbserver_id: gslbserver_id
          application_id: application_id
          gslbserver_list: gslbserver_list
          gslbserver_type: gslbserver_type
          gslbserver_name: gslbserver_name
          gslbserver_status: gslbserver_status
          application_name: application_name
          parent_application_name: parent_application_name
        - application_fqdn: application_fqdn
          application_inactive_nodes: application_inactive_nodes
          application_delayed_time: application_delayed_time
          application_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          application_total_nodes: application_total_nodes
          application_class_name: application_class_name
          parent_application_id: parent_application_id
          application_multistatus: application_multistatus
          gslbserver_id: gslbserver_id
          application_id: application_id
          gslbserver_list: gslbserver_list
          gslbserver_type: gslbserver_type
          gslbserver_name: gslbserver_name
          gslbserver_status: gslbserver_status
          application_name: application_name
          parent_application_name: parent_application_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_application_data_data'
          type: array
      type: object
    app_application_add_input:
      properties:
        application_fqdn:
          description: The Fully Qualified Domain Name (FQDN) of the application.
          type: string
        application_name:
          description: The name of the application.
          type: string
        gslbserver_list:
          description: The name of all the GSLB servers associated with the application.
            You can specify one or more names.
          type: string
        application_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        application_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    app_application_edit_input:
      properties:
        application_id:
          description: The database identifier (ID) of the application, a unique numeric
            key value automatically incremented when you add an application. Use the
            ID to specify the application of your choice.
          type: integer
        application_fqdn:
          description: The Fully Qualified Domain Name (FQDN) of the application.
          type: string
        application_name:
          description: The name of the application.
          type: string
        gslbserver_list:
          description: The name of all the GSLB servers associated with the application.
            You can specify one or more names.
          type: string
        application_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        application_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    app_application_add_success:
      example:
        data:
        - application_id: application_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_application_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    app_application_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    app_application_edit_success:
      example:
        data:
        - application_id: application_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_application_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    app_application_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    app_application_delete_success:
      example:
        data:
        - application_id: application_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_application_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    app_application_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    app_node_data:
      example:
        data:
        - healthcheck_timeout: healthcheck_timeout
          healthcheck_id: healthcheck_id
          gslbserver_name: gslbserver_name
          application_name: application_name
          node_delayed_time: node_delayed_time
          admin_status: admin_status
          pool_lb_mode: pool_lb_mode
          healthcheck_name: healthcheck_name
          translated_healthcheck_name: translated_healthcheck_name
          application_fqdn: application_fqdn
          node_status: node_status
          healthcheck_params: healthcheck_params
          node_weight: node_weight
          translated_pool_lb_mode: translated_pool_lb_mode
          node_name: node_name
          parent_application_id: parent_application_id
          healthcheck_failover: healthcheck_failover
          pool_id: pool_id
          gslbserver_id: gslbserver_id
          application_id: application_id
          pool_name: pool_name
          gslbserver_type: gslbserver_type
          node_address_addr: node_address_addr
          healthcheck_freq: healthcheck_freq
          node_address6_addr: node_address6_addr
          healthcheck_failback: healthcheck_failback
          node_id: node_id
        - healthcheck_timeout: healthcheck_timeout
          healthcheck_id: healthcheck_id
          gslbserver_name: gslbserver_name
          application_name: application_name
          node_delayed_time: node_delayed_time
          admin_status: admin_status
          pool_lb_mode: pool_lb_mode
          healthcheck_name: healthcheck_name
          translated_healthcheck_name: translated_healthcheck_name
          application_fqdn: application_fqdn
          node_status: node_status
          healthcheck_params: healthcheck_params
          node_weight: node_weight
          translated_pool_lb_mode: translated_pool_lb_mode
          node_name: node_name
          parent_application_id: parent_application_id
          healthcheck_failover: healthcheck_failover
          pool_id: pool_id
          gslbserver_id: gslbserver_id
          application_id: application_id
          pool_name: pool_name
          gslbserver_type: gslbserver_type
          node_address_addr: node_address_addr
          healthcheck_freq: healthcheck_freq
          node_address6_addr: node_address6_addr
          healthcheck_failback: healthcheck_failback
          node_id: node_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_node_data_data'
          type: array
      type: object
    app_node_add_input:
      properties:
        application_fqdn:
          description: The Fully Qualified Domain Name (FQDN) of the application the
            object belongs to.
          type: string
        application_id:
          description: The database identifier (ID) of the application the object
            belongs to.
          type: integer
        application_name:
          description: The name of the application the object belongs to.
          type: string
        pool_id:
          description: The database identifier (ID) of the pool, a unique numeric
            key value automatically incremented when you add a pool. Use the ID to
            specify the pool of your choice.
          type: integer
        pool_name:
          description: The name of the pool.
          type: string
        node_hostaddr:
          description: The IPv4 or IPv6 address of the node.
          type: string
        node_name:
          description: The name of the node.
          type: string
        admin_status:
          description: The administrative status of the node, managed (1) or unmanaged
            (0).
          type: integer
        gslbserver_id:
          description: The database identifier (ID) of the GSLB server associated
            with the application, a unique identifier automatically incremented when
            you add the server. Use it to identify the GSLB server of your choice.
          type: integer
        healthcheck_name:
          description: The type of health check of the node.
          enum:
          - ok
          - ping
          - tcp
          - http
          - custom
          type: string
        node_status:
          description: Internal use. Not documented.
          enum:
          - "0"
          - "1"
          - "2"
          type: string
        node_weight:
          description: The weight of the node, it sets which node is used first within
            the pool. It must be an integer between 0 and 255, where 0 sets a node
            as backup.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    app_node_edit_input:
      properties:
        application_fqdn:
          description: The Fully Qualified Domain Name (FQDN) of the application the
            object belongs to.
          type: string
        application_id:
          description: The database identifier (ID) of the application the object
            belongs to.
          type: integer
        application_name:
          description: The name of the application the object belongs to.
          type: string
        node_id:
          description: The database identifier (ID) of the node, a unique numeric
            key value automatically incremented when you add a node. Use the ID to
            specify the node of your choice.
          type: integer
        pool_id:
          description: The database identifier (ID) of the pool, a unique numeric
            key value automatically incremented when you add a pool. Use the ID to
            specify the pool of your choice.
          type: integer
        pool_name:
          description: The name of the pool.
          type: string
        node_hostaddr:
          description: The IPv4 or IPv6 address of the node.
          type: string
        node_name:
          description: The name of the node.
          type: string
        admin_status:
          description: The administrative status of the node, managed (1) or unmanaged
            (0).
          type: integer
        gslbserver_id:
          description: The database identifier (ID) of the GSLB server associated
            with the application, a unique identifier automatically incremented when
            you add the server. Use it to identify the GSLB server of your choice.
          type: integer
        healthcheck_name:
          description: The type of health check of the node.
          enum:
          - ok
          - ping
          - tcp
          - http
          - custom
          type: string
        node_status:
          description: Internal use. Not documented.
          enum:
          - "0"
          - "1"
          - "2"
          type: string
        node_weight:
          description: The weight of the node, it sets which node is used first within
            the pool. It must be an integer between 0 and 255, where 0 sets a node
            as backup.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    app_node_add_success:
      example:
        data:
        - node_id: node_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_node_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    app_node_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    app_node_edit_success:
      example:
        data:
        - node_id: node_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_node_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    app_node_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    app_node_delete_success:
      example:
        data:
        - node_id: node_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_node_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    app_node_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    app_pool_data:
      example:
        data:
        - pool_affinity_state: pool_affinity_state
          application_fqdn: application_fqdn
          pool_inactive_nodes: pool_inactive_nodes
          translated_pool_lb_mode: translated_pool_lb_mode
          pool_weight: pool_weight
          parent_application_id: parent_application_id
          pool_delayed_time: pool_delayed_time
          pool_id: pool_id
          gslbserver_id: gslbserver_id
          application_id: application_id
          pool_total_nodes: pool_total_nodes
          pool_multistatus: pool_multistatus
          pool_name: pool_name
          gslbserver_type: gslbserver_type
          pool_best_active_nodes: pool_best_active_nodes
          gslbserver_name: gslbserver_name
          application_name: application_name
          pool_affinity_session_time: pool_affinity_session_time
          pool_lb_mode: pool_lb_mode
          pool_type: pool_type
        - pool_affinity_state: pool_affinity_state
          application_fqdn: application_fqdn
          pool_inactive_nodes: pool_inactive_nodes
          translated_pool_lb_mode: translated_pool_lb_mode
          pool_weight: pool_weight
          parent_application_id: parent_application_id
          pool_delayed_time: pool_delayed_time
          pool_id: pool_id
          gslbserver_id: gslbserver_id
          application_id: application_id
          pool_total_nodes: pool_total_nodes
          pool_multistatus: pool_multistatus
          pool_name: pool_name
          gslbserver_type: gslbserver_type
          pool_best_active_nodes: pool_best_active_nodes
          gslbserver_name: gslbserver_name
          application_name: application_name
          pool_affinity_session_time: pool_affinity_session_time
          pool_lb_mode: pool_lb_mode
          pool_type: pool_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_pool_data_data'
          type: array
      type: object
    app_pool_add_input:
      properties:
        application_fqdn:
          description: The Fully Qualified Domain Name (FQDN) of the application the
            object belongs to.
          type: string
        application_id:
          description: The database identifier (ID) of the application the object
            belongs to.
          type: integer
        application_name:
          description: The name of the application the object belongs to.
          type: string
        pool_lb_mode:
          description: The load-balancing mode of the pool.
          enum:
          - weighted
          - round-robin
          - latency
          type: string
        pool_name:
          description: The name of the pool.
          type: string
        pool_type:
          description: The type of the pool.
          enum:
          - ipv4
          - ipv6
          type: string
        pool_affinity_session_time:
          description: The session duration, in seconds. You only need to set it if
            the parameter <b>affinity_state</b> is set to <b>1</b>.
          type: integer
        pool_affinity_state:
          description: The session affinity activation status.
          type: integer
        gslbserver_id:
          description: The database identifier (ID) of the GSLB server associated
            with the application, a unique identifier automatically incremented when
            you add the server. Use it to identify the GSLB server of your choice.
          type: integer
        pool_best_active_nodes:
          description: The maximum number of active nodes with the lowest latency
            that must answer the queries made to the application FQDN. You only need
            to set it if you set the <b>lb_mode</b> to <b>latency</b>.
          type: integer
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    app_pool_edit_input:
      properties:
        application_fqdn:
          description: The Fully Qualified Domain Name (FQDN) of the application the
            object belongs to.
          type: string
        application_id:
          description: The database identifier (ID) of the application the object
            belongs to.
          type: integer
        application_name:
          description: The name of the application the object belongs to.
          type: string
        pool_id:
          description: The database identifier (ID) of the pool, a unique numeric
            key value automatically incremented when you add a pool. Use the ID to
            specify the pool of your choice.
          type: integer
        pool_lb_mode:
          description: The load-balancing mode of the pool.
          enum:
          - weighted
          - round-robin
          - latency
          type: string
        pool_name:
          description: The name of the pool.
          type: string
        pool_type:
          description: The type of the pool.
          enum:
          - ipv4
          - ipv6
          type: string
        pool_affinity_session_time:
          description: The session duration, in seconds. You only need to set it if
            the parameter <b>affinity_state</b> is set to <b>1</b>.
          type: integer
        pool_affinity_state:
          description: The session affinity activation status.
          type: integer
        gslbserver_id:
          description: The database identifier (ID) of the GSLB server associated
            with the application, a unique identifier automatically incremented when
            you add the server. Use it to identify the GSLB server of your choice.
          type: integer
        pool_best_active_nodes:
          description: The maximum number of active nodes with the lowest latency
            that must answer the queries made to the application FQDN. You only need
            to set it if you set the <b>lb_mode</b> to <b>latency</b>.
          type: integer
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    app_pool_add_success:
      example:
        data:
        - pool_id: pool_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_pool_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    app_pool_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    app_pool_edit_success:
      example:
        data:
        - pool_id: pool_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_pool_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    app_pool_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    app_pool_delete_success:
      example:
        data:
        - pool_id: pool_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_pool_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    app_pool_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    device_device_data:
      example:
        data:
        - dev_name: dev_name
          device_id: device_id
          row_state: row_state
          device_port_used_percent: device_port_used_percent
          device_address_formated: device_address_formated
          interface_free: interface_free
          device_port_free: device_port_free
          device_class_name: device_class_name
          device_address_addr: device_address_addr
          device_space_id: device_space_id
          dev_id: dev_id
          device_name: device_name
          device_port_total: device_port_total
          interface_used_percent: interface_used_percent
          device_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          interface_total: interface_total
          device_port_used: device_port_used
          device_space_name: device_space_name
          interface_used: interface_used
        - dev_name: dev_name
          device_id: device_id
          row_state: row_state
          device_port_used_percent: device_port_used_percent
          device_address_formated: device_address_formated
          interface_free: interface_free
          device_port_free: device_port_free
          device_class_name: device_class_name
          device_address_addr: device_address_addr
          device_space_id: device_space_id
          dev_id: dev_id
          device_name: device_name
          device_port_total: device_port_total
          interface_used_percent: interface_used_percent
          device_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          interface_total: interface_total
          device_port_used: device_port_used
          device_space_name: device_space_name
          interface_used: interface_used
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_device_data_data'
          type: array
      type: object
    device_device_add_input:
      properties:
        device_name:
          description: The name of the Device Manager device, each device must have
            a unique name.
          type: string
        device_addr:
          description: The IP address you want to associate with the Device Manager
            device, in decimal format.
          type: string
        device_address_addr:
          description: The IP address you want to associate with the Device Manager
            device, in hexadecimal format.
          type: string
        device_space_id:
          description: The database identifier (ID) of a space you want to associate
            with the Device Manager device.
          type: integer
        dev_id:
          description: The database identifier (ID) of a NetChange network device
            you want to associate with the Device Manager device.
          type: integer
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          enum:
          - "1"
          - "2"
          - "3"
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        device_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        device_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    device_device_edit_input:
      properties:
        device_id:
          description: The database identifier (ID) of the Device Manager device,
            a unique numeric key value automatically incremented when you add a device.
            Use the ID to specify which device to edit.
          type: integer
        device_name:
          description: The name of the Device Manager device, each device must have
            a unique name.
          type: string
        device_addr:
          description: The IP address you want to associate with the Device Manager
            device, in decimal format.
          type: string
        device_address_addr:
          description: The IP address you want to associate with the Device Manager
            device, in hexadecimal format.
          type: string
        device_space_id:
          description: The database identifier (ID) of a space you want to associate
            with the Device Manager device.
          type: integer
        dev_id:
          description: The database identifier (ID) of a NetChange network device
            you want to associate with the Device Manager device.
          type: integer
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          enum:
          - "1"
          - "2"
          - "3"
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        device_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        device_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    device_device_add_success:
      example:
        data:
        - device_id: device_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_device_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    device_device_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    device_device_edit_success:
      example:
        data:
        - device_id: device_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_device_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    device_device_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    device_device_delete_success:
      example:
        data:
        - device_id: device_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_device_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    device_device_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    device_interface_data:
      example:
        data:
        - interface_manual_link: interface_manual_link
          pear_iface_id: pear_iface_id
          interface_vendor_key: interface_vendor_key
          interface_nb_ip: interface_nb_ip
          interface_class_name: interface_class_name
          interface_name: interface_name
          interface_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          interface_mac: interface_mac
          device_name: device_name
          data_id: data_id
          interface_add_time: interface_add_time
          interface_type: interface_type
          interface_address_addr: interface_address_addr
          dev_name: dev_name
          interface_address_formated: interface_address_formated
          device_id: device_id
          interface_space_id: interface_space_id
          interface_modify_time: interface_modify_time
          row_state: row_state
          pear_ipl_iface_id: pear_ipl_iface_id
          device_class_name: device_class_name
          dev_enabled: dev_enabled
          dev_id: dev_id
          port_name: port_name
          interface_id: interface_id
          port_enabled: port_enabled
          interface_vendor_mac: interface_vendor_mac
          device_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          port_id: port_id
          interface_auto_link: interface_auto_link
          interface_space_name: interface_space_name
        - interface_manual_link: interface_manual_link
          pear_iface_id: pear_iface_id
          interface_vendor_key: interface_vendor_key
          interface_nb_ip: interface_nb_ip
          interface_class_name: interface_class_name
          interface_name: interface_name
          interface_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          interface_mac: interface_mac
          device_name: device_name
          data_id: data_id
          interface_add_time: interface_add_time
          interface_type: interface_type
          interface_address_addr: interface_address_addr
          dev_name: dev_name
          interface_address_formated: interface_address_formated
          device_id: device_id
          interface_space_id: interface_space_id
          interface_modify_time: interface_modify_time
          row_state: row_state
          pear_ipl_iface_id: pear_ipl_iface_id
          device_class_name: device_class_name
          dev_enabled: dev_enabled
          dev_id: dev_id
          port_name: port_name
          interface_id: interface_id
          port_enabled: port_enabled
          interface_vendor_mac: interface_vendor_mac
          device_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          port_id: port_id
          interface_auto_link: interface_auto_link
          interface_space_name: interface_space_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_interface_data_data'
          type: array
      type: object
    device_interface_add_input:
      properties:
        device_id:
          description: The database identifier (ID) of the Device Manager device,
            a unique numeric key value automatically incremented when you add a device.
            Use the ID to specify the device of your choice.
          type: integer
        device_name:
          description: The name of the Device Manager device.
          type: string
        interface_addr:
          description: The IP addresses you want to associate with the Device Manager
            port or interface, as follows <b>&lt;ip4_list&gt;,&lt;ip6_list&gt;</b>.
          type: string
        interface_mac:
          description: The MAC address you want to associate with the Device Manager
            port or interface.
          type: string
        interface_name:
          description: The name of the Device Manager port or interface, each port
            or interface must have a unique name.
          type: string
        interface_type:
          description: A way to indicate if the object is either a <b>port</b> or
            an <b>interface</b>.
          enum:
          - interface
          - port
          type: string
        interface_ip4_list:
          description: 'The list of IPv4 addresses you want to associate to the Device
            Manager port or interface, in hexadecimal format, as follows: <b>&lt;ip_address&gt;,
            &lt;ip_address&gt;,...</b>'
          type: string
        interface_address6_list:
          description: 'todo(here) :device.interface.add.input.interface_address6_list.
            : List of strings'
          type: string
        port_id:
          description: The database identifier (ID) of a NetChange port you want to
            associate with the Device Manager port or interface.
          type: integer
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          type: integer
        space_id:
          description: The database identifier (ID) of a space you want to associate
            with the Device Manager port or interface.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        interface_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        interface_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    device_interface_edit_input:
      properties:
        device_id:
          description: The database identifier (ID) of the Device Manager device,
            a unique numeric key value automatically incremented when you add a device.
            Use the ID to specify the device of your choice.
          type: integer
        device_name:
          description: The name of the Device Manager device.
          type: string
        interface_addr:
          description: The IP addresses you want to associate with the Device Manager
            port or interface, as follows <b>&lt;ip4_list&gt;,&lt;ip6_list&gt;</b>.
          type: string
        interface_id:
          description: The database identifier (ID) of the Device Manager port or
            interface, a unique numeric key value automatically incremented when you
            add a port or interface. Use the ID to specify which port or interface
            to edit.
          type: integer
        interface_mac:
          description: The MAC address you want to associate with the Device Manager
            port or interface.
          type: string
        interface_name:
          description: The name of the Device Manager port or interface, each port
            or interface must have a unique name.
          type: string
        interface_type:
          description: A way to indicate if the object is either a <b>port</b> or
            an <b>interface</b>.
          enum:
          - interface
          - port
          type: string
        interface_ip4_list:
          description: 'The list of IPv4 addresses you want to associate to the Device
            Manager port or interface, in hexadecimal format, as follows: <b>&lt;ip_address&gt;,
            &lt;ip_address&gt;,...</b>'
          type: string
        interface_address6_list:
          description: 'todo(here) :device.interface.edit.input.interface_address6_list.
            : List of strings'
          type: string
        port_id:
          description: The database identifier (ID) of a NetChange port you want to
            associate with the Device Manager port or interface.
          type: integer
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          type: integer
        space_id:
          description: The database identifier (ID) of a space you want to associate
            with the Device Manager port or interface.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        interface_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        interface_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    device_interface_add_success:
      example:
        data:
        - interface_id: interface_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_interface_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    device_interface_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    device_interface_edit_success:
      example:
        data:
        - interface_id: interface_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_interface_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    device_interface_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    device_interface_delete_success:
      example:
        data:
        - interface_id: interface_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_interface_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    device_interface_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    device_link_data:
      example:
        data:
        - device_name: device_name
          interface_id: interface_id
          device_id: device_id
          link_add_time: link_add_time
          link_auto_link: link_auto_link
          interface_name: interface_name
          interface_type: interface_type
          link_id: link_id
          interface_mac: interface_mac
        - device_name: device_name
          interface_id: interface_id
          device_id: device_id
          link_add_time: link_add_time
          link_auto_link: link_auto_link
          interface_name: interface_name
          interface_type: interface_type
          link_id: link_id
          interface_mac: interface_mac
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_link_data_data'
          type: array
      type: object
    device_link_add_input:
      properties:
        device1_name:
          description: The name of the device to which belongs the DM port or interface
            you want to link with <b>hostiface2_id</b>.
          type: string
        device2_name:
          description: The name of the device to which belongs the DM port or interface
            you want to link with <b>hostiface1_id</b>.
          type: string
        interface1_id:
          description: The database identifier (ID) of the DM port or interface you
            want to link with <b>hostiface2_id</b>.
          type: integer
        interface1_name:
          description: The name of the DM port or interface you want to link with
            <b>hostiface2_id</b>.
          type: string
        interface2_id:
          description: The database identifier (ID) of the DM port or interface you
            want to link with <b>hostiface1_id</b>.
          type: integer
        interface2_name:
          description: The name of the DM port or interface you want to link with
            <b>hostiface1_id</b>.
          type: string
        link_id:
          description: The database identifier (ID) of the Device Manager port or
            interface link, a unique numeric key value automatically incremented when
            you add a link between a device and a port or interface. Use the ID to
            specify which port or interface link to edit.
          type: integer
        link_auto_link:
          description: A way to determine if the link between two Device Manager devices
            is set automatically (<b>1</b>) or manually (<b>0</b>).
          type: integer
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    device_link_edit_input:
      properties:
        device1_name:
          description: The name of the device to which belongs the DM port or interface
            you want to link with <b>hostiface2_id</b>.
          type: string
        device2_name:
          description: The name of the device to which belongs the DM port or interface
            you want to link with <b>hostiface1_id</b>.
          type: string
        interface1_id:
          description: The database identifier (ID) of the DM port or interface you
            want to link with <b>hostiface2_id</b>.
          type: integer
        interface1_name:
          description: The name of the DM port or interface you want to link with
            <b>hostiface2_id</b>.
          type: string
        interface2_id:
          description: The database identifier (ID) of the DM port or interface you
            want to link with <b>hostiface1_id</b>.
          type: integer
        interface2_name:
          description: The name of the DM port or interface you want to link with
            <b>hostiface1_id</b>.
          type: string
        link_id:
          description: The database identifier (ID) of the Device Manager port or
            interface link, a unique numeric key value automatically incremented when
            you add a link between a device and a port or interface. Use the ID to
            specify which port or interface link to edit.
          type: integer
        link_auto_link:
          description: A way to determine if the link between two Device Manager devices
            is set automatically (<b>1</b>) or manually (<b>0</b>).
          type: integer
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    device_link_add_success:
      example:
        data:
        - link_id: link_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_link_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    device_link_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    device_link_edit_success:
      example:
        data:
        - link_id: link_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_link_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    device_link_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    device_link_delete_success:
      example:
        data:
        - link_id: link_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/device_link_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    device_link_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_acl_data:
      example:
        data:
        - server_name: server_name
          acl_statement: acl_statement
          smart_parent_id: smart_parent_id
          acl_name: acl_name
          acl_spawnwith: acl_spawnwith
          acl_delayed_create_time: acl_delayed_create_time
          acl_delayed_delete_time: acl_delayed_delete_time
          acl_id: acl_id
          server_id: server_id
          acl_match: acl_match
          server_type: server_type
        - server_name: server_name
          acl_statement: acl_statement
          smart_parent_id: smart_parent_id
          acl_name: acl_name
          acl_spawnwith: acl_spawnwith
          acl_delayed_create_time: acl_delayed_create_time
          acl_delayed_delete_time: acl_delayed_delete_time
          acl_id: acl_id
          server_id: server_id
          acl_match: acl_match
          server_type: server_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_acl_data_data'
          type: array
      type: object
    dhcp_acl6_data:
      example:
        data:
        - acl6_statement: acl6_statement
          smart_parent_id: smart_parent_id
          acl6_id: acl6_id
          acl6_match: acl6_match
          acl6_spawnwith: acl6_spawnwith
          acl6_name: acl6_name
          server6_id: server6_id
          acl6_delayed_time: acl6_delayed_time
          server6_type: server6_type
          server6_name: server6_name
        - acl6_statement: acl6_statement
          smart_parent_id: smart_parent_id
          acl6_id: acl6_id
          acl6_match: acl6_match
          acl6_spawnwith: acl6_spawnwith
          acl6_name: acl6_name
          server6_id: server6_id
          acl6_delayed_time: acl6_delayed_time
          server6_type: server6_type
          server6_name: server6_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_acl6_data_data'
          type: array
      type: object
    dhcp_aclentry_data:
      example:
        data:
        - aclentry_id: aclentry_id
          server_name: server_name
          smart_parent_id: smart_parent_id
          acl_name: acl_name
          aclentry_delayed_delete_time: aclentry_delayed_delete_time
          aclentry_value: aclentry_value
          aclentry_delayed_create_time: aclentry_delayed_create_time
          acl_id: acl_id
          server_id: server_id
          server_type: server_type
        - aclentry_id: aclentry_id
          server_name: server_name
          smart_parent_id: smart_parent_id
          acl_name: acl_name
          aclentry_delayed_delete_time: aclentry_delayed_delete_time
          aclentry_value: aclentry_value
          aclentry_delayed_create_time: aclentry_delayed_create_time
          acl_id: acl_id
          server_id: server_id
          server_type: server_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_aclentry_data_data'
          type: array
      type: object
    dhcp_aclentry6_data:
      example:
        data:
        - aclentry6_value: aclentry6_value
          smart_parent_id: smart_parent_id
          aclentry6_delayed_time: aclentry6_delayed_time
          acl6_id: acl6_id
          aclentry6_id: aclentry6_id
          acl6_name: acl6_name
          server6_id: server6_id
          server6_type: server6_type
          server6_name: server6_name
        - aclentry6_value: aclentry6_value
          smart_parent_id: smart_parent_id
          aclentry6_delayed_time: aclentry6_delayed_time
          acl6_id: acl6_id
          aclentry6_id: aclentry6_id
          acl6_name: acl6_name
          server6_id: server6_id
          server6_type: server6_type
          server6_name: server6_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_aclentry6_data_data'
          type: array
      type: object
    dhcp_failover_data:
      example:
        data:
        - server_name: server_name
          failover_peer_port: failover_peer_port
          failover_state: failover_state
          failover_name: failover_name
          failover_delayed_create_time: failover_delayed_create_time
          server_id: server_id
          failover_peer_addr: failover_peer_addr
          failover_split: failover_split
          failover_delayed_delete_time: failover_delayed_delete_time
          server_addr: server_addr
          smart_parent_id: smart_parent_id
          failover_addr: failover_addr
          failover_type: failover_type
          failover_auto_partner_down: failover_auto_partner_down
          peer_server_id: peer_server_id
          failover_multistatus: failover_multistatus
          server_state: server_state
          server_type: server_type
          failover_id: failover_id
          failover_port: failover_port
        - server_name: server_name
          failover_peer_port: failover_peer_port
          failover_state: failover_state
          failover_name: failover_name
          failover_delayed_create_time: failover_delayed_create_time
          server_id: server_id
          failover_peer_addr: failover_peer_addr
          failover_split: failover_split
          failover_delayed_delete_time: failover_delayed_delete_time
          server_addr: server_addr
          smart_parent_id: smart_parent_id
          failover_addr: failover_addr
          failover_type: failover_type
          failover_auto_partner_down: failover_auto_partner_down
          peer_server_id: peer_server_id
          failover_multistatus: failover_multistatus
          server_state: server_state
          server_type: server_type
          failover_id: failover_id
          failover_port: failover_port
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_failover_data_data'
          type: array
      type: object
    dhcp_group_data:
      example:
        data:
        - server_name: server_name
          group_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          group_name: group_name
          server_class_name: server_class_name
          group_class_name: group_class_name
          group_multistatus: group_multistatus
          server_id: server_id
          server_version: server_version
          server_addr: server_addr
          smart_parent_id: smart_parent_id
          group_id: group_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          smart_parent_name: smart_parent_name
          group_delayed_create_time: group_delayed_create_time
          group_delayed_delete_time: group_delayed_delete_time
          server_type: server_type
        - server_name: server_name
          group_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          group_name: group_name
          server_class_name: server_class_name
          group_class_name: group_class_name
          group_multistatus: group_multistatus
          server_id: server_id
          server_version: server_version
          server_addr: server_addr
          smart_parent_id: smart_parent_id
          group_id: group_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          smart_parent_name: smart_parent_name
          group_delayed_create_time: group_delayed_create_time
          group_delayed_delete_time: group_delayed_delete_time
          server_type: server_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_group_data_data'
          type: array
      type: object
    dhcp_group_add_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        group_name:
          description: The name of the DHCPv4 group, each DHCPv4 group must have a
            unique name.
          type: string
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        group_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        group_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_group_edit_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        group_id:
          description: The database identifier (ID) of the DHCPv4 group, a unique
            numeric key value automatically incremented when you add a DHCPv4 group.
            Use the ID to specify the DHCPv4 group of your choice.
          type: integer
        group_name:
          description: The name of the DHCPv4 group, each DHCPv4 group must have a
            unique name.
          type: string
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        group_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        group_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_group_add_success:
      example:
        data:
        - group_id: group_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_group_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_group_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_group_edit_success:
      example:
        data:
        - group_id: group_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_group_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_group_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_group_delete_success:
      example:
        data:
        - group_id: group_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_group_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_group_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_group6_data:
      example:
        data:
        - server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server6_version: server6_version
          group6_name: group6_name
          group6_multistatus: group6_multistatus
          group6_delayed_time: group6_delayed_time
          group6_id: group6_id
          server6_name: server6_name
          server6_addr: server6_addr
          smart_parent_id: smart_parent_id
          smart_parent_name: smart_parent_name
          server6_class_name: server6_class_name
          server6_id: server6_id
          server6_type: server6_type
          group6_class_name: group6_class_name
          group6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
        - server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server6_version: server6_version
          group6_name: group6_name
          group6_multistatus: group6_multistatus
          group6_delayed_time: group6_delayed_time
          group6_id: group6_id
          server6_name: server6_name
          server6_addr: server6_addr
          smart_parent_id: smart_parent_id
          smart_parent_name: smart_parent_name
          server6_class_name: server6_class_name
          server6_id: server6_id
          server6_type: server6_type
          group6_class_name: group6_class_name
          group6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_group6_data_data'
          type: array
      type: object
    dhcp_lease_data:
      example:
        data:
        - range_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_name: range_name
          server_name: server_name
          lease_mac_addr: lease_mac_addr
          sharednetwork_id: sharednetwork_id
          server_class_name: server_class_name
          lease_vendor_id: lease_vendor_id
          server_version: server_version
          lease_addr: lease_addr
          percent: percent
          scope_name: scope_name
          range_id: range_id
          lease_name: lease_name
          smart_parent_id: smart_parent_id
          parameter_request_list: parameter_request_list
          scope_id: scope_id
          lease_circuit_id: lease_circuit_id
          lease_time: lease_time
          smart_parent_name: smart_parent_name
          scope_size: scope_size
          lease_fingerbank_os: lease_fingerbank_os
          lease_address_addr: lease_address_addr
          lease_remote_id: lease_remote_id
          lease_first_time: lease_first_time
          range_end_addr: range_end_addr
          time_to_expire: time_to_expire
          sharednetwork_name: sharednetwork_name
          lease_multistatus: lease_multistatus
          server_id: server_id
          server_addr: server_addr
          lease_clientname: lease_clientname
          lease_period: lease_period
          scope_net_addr: scope_net_addr
          lease_domain: lease_domain
          lease_end_time: lease_end_time
          range_start_addr: range_start_addr
          range_class_name: range_class_name
          range_failover_name: range_failover_name
          lease_mac_vendor: lease_mac_vendor
          lease_client_ident: lease_client_ident
          lease_id: lease_id
          server_type: server_type
          scope_class_name: scope_class_name
        - range_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_name: range_name
          server_name: server_name
          lease_mac_addr: lease_mac_addr
          sharednetwork_id: sharednetwork_id
          server_class_name: server_class_name
          lease_vendor_id: lease_vendor_id
          server_version: server_version
          lease_addr: lease_addr
          percent: percent
          scope_name: scope_name
          range_id: range_id
          lease_name: lease_name
          smart_parent_id: smart_parent_id
          parameter_request_list: parameter_request_list
          scope_id: scope_id
          lease_circuit_id: lease_circuit_id
          lease_time: lease_time
          smart_parent_name: smart_parent_name
          scope_size: scope_size
          lease_fingerbank_os: lease_fingerbank_os
          lease_address_addr: lease_address_addr
          lease_remote_id: lease_remote_id
          lease_first_time: lease_first_time
          range_end_addr: range_end_addr
          time_to_expire: time_to_expire
          sharednetwork_name: sharednetwork_name
          lease_multistatus: lease_multistatus
          server_id: server_id
          server_addr: server_addr
          lease_clientname: lease_clientname
          lease_period: lease_period
          scope_net_addr: scope_net_addr
          lease_domain: lease_domain
          lease_end_time: lease_end_time
          range_start_addr: range_start_addr
          range_class_name: range_class_name
          range_failover_name: range_failover_name
          lease_mac_vendor: lease_mac_vendor
          lease_client_ident: lease_client_ident
          lease_id: lease_id
          server_type: server_type
          scope_class_name: scope_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_lease_data_data'
          type: array
      type: object
    dhcp_lease6_data:
      example:
        data:
        - scope6_size: scope6_size
          lease6_multistatus: lease6_multistatus
          server6_version: server6_version
          lease6_id: lease6_id
          lease6_address6_addr: lease6_address6_addr
          range6_failover_name: range6_failover_name
          server6_name: server6_name
          lease6_end_time: lease6_end_time
          lease6_first_time: lease6_first_time
          lease6_period: lease6_period
          range6_end_address6_addr: range6_end_address6_addr
          scope6_prefix: scope6_prefix
          smart_parent_id: smart_parent_id
          smart_parent_name: smart_parent_name
          server6_class_name: server6_class_name
          server6_id: server6_id
          lease6_clientname: lease6_clientname
          server6_type: server6_type
          lease6_mac_vendor: lease6_mac_vendor
          lease6_domain: lease6_domain
          range6_id: range6_id
          range6_start_address6_addr: range6_start_address6_addr
          lease6_time: lease6_time
          sharednetwork6_name: sharednetwork6_name
          lease6_client_duid: lease6_client_duid
          lease6_percent: lease6_percent
          server6_addr: server6_addr
          lease6_mac_addr: lease6_mac_addr
          scope6_class_name: scope6_class_name
          scope6_name: scope6_name
          range6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_id: scope6_id
          sharednetwork6_id: sharednetwork6_id
          lease6_time_to_expire: lease6_time_to_expire
          lease6_name: lease6_name
          scope6_start_address6_addr: scope6_start_address6_addr
          range6_class_name: range6_class_name
        - scope6_size: scope6_size
          lease6_multistatus: lease6_multistatus
          server6_version: server6_version
          lease6_id: lease6_id
          lease6_address6_addr: lease6_address6_addr
          range6_failover_name: range6_failover_name
          server6_name: server6_name
          lease6_end_time: lease6_end_time
          lease6_first_time: lease6_first_time
          lease6_period: lease6_period
          range6_end_address6_addr: range6_end_address6_addr
          scope6_prefix: scope6_prefix
          smart_parent_id: smart_parent_id
          smart_parent_name: smart_parent_name
          server6_class_name: server6_class_name
          server6_id: server6_id
          lease6_clientname: lease6_clientname
          server6_type: server6_type
          lease6_mac_vendor: lease6_mac_vendor
          lease6_domain: lease6_domain
          range6_id: range6_id
          range6_start_address6_addr: range6_start_address6_addr
          lease6_time: lease6_time
          sharednetwork6_name: sharednetwork6_name
          lease6_client_duid: lease6_client_duid
          lease6_percent: lease6_percent
          server6_addr: server6_addr
          lease6_mac_addr: lease6_mac_addr
          scope6_class_name: scope6_class_name
          scope6_name: scope6_name
          range6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_id: scope6_id
          sharednetwork6_id: sharednetwork6_id
          lease6_time_to_expire: lease6_time_to_expire
          lease6_name: lease6_name
          scope6_start_address6_addr: scope6_start_address6_addr
          range6_class_name: range6_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_lease6_data_data'
          type: array
      type: object
    dhcp_range_data:
      example:
        data:
        - range_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_name: range_name
          server_name: server_name
          range_lease_count: range_lease_count
          sharednetwork_id: sharednetwork_id
          server_class_name: server_class_name
          scope_start_address_addr: scope_start_address_addr
          server_version: server_version
          range_size: range_size
          scope_name: scope_name
          range_id: range_id
          range_lease_percent: range_lease_percent
          smart_parent_id: smart_parent_id
          scope_id: scope_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope_if_name: scope_if_name
          smart_parent_name: smart_parent_name
          scope_size: scope_size
          range_delayed_create_time: range_delayed_create_time
          range_delayed_delete_time: range_delayed_delete_time
          server_comment: server_comment
          range_end_addr: range_end_addr
          scope_if_addr: scope_if_addr
          range_state: range_state
          sharednetwork_name: sharednetwork_name
          server_id: server_id
          server_addr: server_addr
          scope_net_mask: scope_net_mask
          scope_net_addr: scope_net_addr
          range_start_addr: range_start_addr
          range_class_name: range_class_name
          range_failover_name: range_failover_name
          scope_space_id: scope_space_id
          scope_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_multistatus: range_multistatus
          range_start_address_addr: range_start_address_addr
          range_end_address_addr: range_end_address_addr
          server_type: server_type
          range_acl: range_acl
          scope_class_name: scope_class_name
        - range_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_name: range_name
          server_name: server_name
          range_lease_count: range_lease_count
          sharednetwork_id: sharednetwork_id
          server_class_name: server_class_name
          scope_start_address_addr: scope_start_address_addr
          server_version: server_version
          range_size: range_size
          scope_name: scope_name
          range_id: range_id
          range_lease_percent: range_lease_percent
          smart_parent_id: smart_parent_id
          scope_id: scope_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope_if_name: scope_if_name
          smart_parent_name: smart_parent_name
          scope_size: scope_size
          range_delayed_create_time: range_delayed_create_time
          range_delayed_delete_time: range_delayed_delete_time
          server_comment: server_comment
          range_end_addr: range_end_addr
          scope_if_addr: scope_if_addr
          range_state: range_state
          sharednetwork_name: sharednetwork_name
          server_id: server_id
          server_addr: server_addr
          scope_net_mask: scope_net_mask
          scope_net_addr: scope_net_addr
          range_start_addr: range_start_addr
          range_class_name: range_class_name
          range_failover_name: range_failover_name
          scope_space_id: scope_space_id
          scope_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_multistatus: range_multistatus
          range_start_address_addr: range_start_address_addr
          range_end_address_addr: range_end_address_addr
          server_type: server_type
          range_acl: range_acl
          scope_class_name: scope_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range_data_data'
          type: array
      type: object
    dhcp_range_add_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        range_end_addr:
          description: The last IP address of the DHCPv4 range.
          type: string
        range_start_addr:
          description: The first IP address of the DHCPv4 range.
          type: string
        scope_id:
          description: The database identifier (ID) of the DHCPv4 scope, a unique
            numeric key value automatically incremented when you add a DHCPv4 scope.
            Use the ID to specify the DHCPv4 scope of your choice.
          type: integer
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        range_acl:
          description: 'The list of ACLs associated with the DHCPv4 range, as follows:
            <b>&lt;ACL_name&gt;;&lt;ACL_name&gt;;</b>... .'
          type: string
        range_name:
          description: The name of the DHCPv4 range, each DHCPv4 range must have a
            unique name.
          type: string
        scope_name:
          description: The name of the DHCPv4 scope.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        range_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        range_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_range_edit_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        range_end_addr:
          description: The last IP address of the DHCPv4 range.
          type: string
        range_id:
          description: The database identifier (ID) of the DHCPv4 range, a unique
            numeric key value automatically incremented when you add a DHCPv4 range.
            Use the ID to specify which DHCPv4 range to edit.
          type: integer
        range_start_addr:
          description: The first IP address of the DHCPv4 range.
          type: string
        scope_id:
          description: The database identifier (ID) of the DHCPv4 scope, a unique
            numeric key value automatically incremented when you add a DHCPv4 scope.
            Use the ID to specify the DHCPv4 scope of your choice.
          type: integer
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        range_acl:
          description: 'The list of ACLs associated with the DHCPv4 range, as follows:
            <b>&lt;ACL_name&gt;;&lt;ACL_name&gt;;</b>... .'
          type: string
        range_name:
          description: The name of the DHCPv4 range, each DHCPv4 range must have a
            unique name.
          type: string
        scope_name:
          description: The name of the DHCPv4 scope.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        range_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        range_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_range_add_success:
      example:
        data:
        - range_id: range_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_range_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_range_edit_success:
      example:
        data:
        - range_id: range_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_range_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_range_delete_success:
      example:
        data:
        - range_id: range_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_range_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_range6_data:
      example:
        data:
        - server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_size: scope6_size
          server6_version: server6_version
          range6_state: range6_state
          scope6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range6_failover_name: range6_failover_name
          server6_name: server6_name
          range6_end_address6_addr: range6_end_address6_addr
          scope6_prefix: scope6_prefix
          server6_comment: server6_comment
          smart_parent_id: smart_parent_id
          smart_parent_name: smart_parent_name
          server6_class_name: server6_class_name
          server6_id: server6_id
          server6_type: server6_type
          range6_delayed_time: range6_delayed_time
          range6_acl: range6_acl
          range6_id: range6_id
          range6_start_address6_addr: range6_start_address6_addr
          sharednetwork6_name: sharednetwork6_name
          range6_multistatus: range6_multistatus
          scope6_end_address6_addr: scope6_end_address6_addr
          server6_addr: server6_addr
          range6_size: range6_size
          scope6_class_name: scope6_class_name
          scope6_name: scope6_name
          scope6_space_id: scope6_space_id
          range6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_id: scope6_id
          range6_lease_count: range6_lease_count
          sharednetwork6_id: sharednetwork6_id
          scope6_start_address6_addr: scope6_start_address6_addr
          range6_class_name: range6_class_name
        - server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_size: scope6_size
          server6_version: server6_version
          range6_state: range6_state
          scope6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range6_failover_name: range6_failover_name
          server6_name: server6_name
          range6_end_address6_addr: range6_end_address6_addr
          scope6_prefix: scope6_prefix
          server6_comment: server6_comment
          smart_parent_id: smart_parent_id
          smart_parent_name: smart_parent_name
          server6_class_name: server6_class_name
          server6_id: server6_id
          server6_type: server6_type
          range6_delayed_time: range6_delayed_time
          range6_acl: range6_acl
          range6_id: range6_id
          range6_start_address6_addr: range6_start_address6_addr
          sharednetwork6_name: sharednetwork6_name
          range6_multistatus: range6_multistatus
          scope6_end_address6_addr: scope6_end_address6_addr
          server6_addr: server6_addr
          range6_size: range6_size
          scope6_class_name: scope6_class_name
          scope6_name: scope6_name
          scope6_space_id: scope6_space_id
          range6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_id: scope6_id
          range6_lease_count: range6_lease_count
          sharednetwork6_id: sharednetwork6_id
          scope6_start_address6_addr: scope6_start_address6_addr
          range6_class_name: range6_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range6_data_data'
          type: array
      type: object
    dhcp_range6_add_input:
      properties:
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server, a unique
            numeric key value automatically incremented when you add a DHCPv6 server.
            Use the ID to specify the DHCPv6 server of your choice.
          type: integer
        server6_name:
          description: The name of the DHCPv6 server.
          type: string
        range6_end_addr:
          description: The last IP address of the DHCPv6 range.
          type: string
        range6_start_addr:
          description: The first IP address of the DHCPv6 range.
          type: string
        scope6_id:
          description: The database identifier (ID) of the DHCPv6 scope, a unique
            numeric key value automatically incremented when you add a DHCPv6 scope.
            Use the ID to specify the DHCPv6 scope of your choice.
          type: integer
        server6_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        range6_acl:
          description: 'The list of ACLs associated with the DHCPv6 range, as follows:
            <b>&lt;ACL_name&gt;;&lt;ACL_name&gt;;...</b> .'
          type: string
        scope6_name:
          description: The name of the DHCPv6 scope.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        range6_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        range6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_range6_edit_input:
      properties:
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server, a unique
            numeric key value automatically incremented when you add a DHCPv6 server.
            Use the ID to specify the DHCPv6 server of your choice.
          type: integer
        server6_name:
          description: The name of the DHCPv6 server.
          type: string
        range6_end_addr:
          description: The last IP address of the DHCPv6 range.
          type: string
        range6_id:
          description: The database identifier (ID) of the DHCPv6 range, a unique
            numeric key value automatically incremented when you add a DHCPv6 range.
            Use the ID to specify which DHCPv6 range to edit.
          type: integer
        range6_start_addr:
          description: The first IP address of the DHCPv6 range.
          type: string
        scope6_id:
          description: The database identifier (ID) of the DHCPv6 scope, a unique
            numeric key value automatically incremented when you add a DHCPv6 scope.
            Use the ID to specify the DHCPv6 scope of your choice.
          type: integer
        server6_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        range6_acl:
          description: 'The list of ACLs associated with the DHCPv6 range, as follows:
            <b>&lt;ACL_name&gt;;&lt;ACL_name&gt;;...</b> .'
          type: string
        scope6_name:
          description: The name of the DHCPv6 scope.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        range6_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        range6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_range6_add_success:
      example:
        data:
        - range6_id: range6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range6_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_range6_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_range6_edit_success:
      example:
        data:
        - range6_id: range6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range6_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_range6_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_range6_delete_success:
      example:
        data:
        - range6_id: range6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range6_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_range6_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_scope_data:
      example:
        data:
        - server_name: server_name
          sharednetwork_id: sharednetwork_id
          server_class_name: server_class_name
          scope_start_address_addr: scope_start_address_addr
          server_version: server_version
          scope_space_name: scope_space_name
          scope_name: scope_name
          scope_delayed_create_time: scope_delayed_create_time
          smart_parent_id: smart_parent_id
          scope_multistatus: scope_multistatus
          scope_id: scope_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          smart_parent_name: smart_parent_name
          scope_size: scope_size
          smart_arch: smart_arch
          scope_end_address_addr: scope_end_address_addr
          failover_name: failover_name
          sharednetwork_name: sharednetwork_name
          scope_delayed_delete_time: scope_delayed_delete_time
          server_id: server_id
          server_addr: server_addr
          scope_net_mask: scope_net_mask
          scope_net_addr: scope_net_addr
          scope_space_id: scope_space_id
          scope_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_type: server_type
          failover_id: failover_id
          scope_class_name: scope_class_name
        - server_name: server_name
          sharednetwork_id: sharednetwork_id
          server_class_name: server_class_name
          scope_start_address_addr: scope_start_address_addr
          server_version: server_version
          scope_space_name: scope_space_name
          scope_name: scope_name
          scope_delayed_create_time: scope_delayed_create_time
          smart_parent_id: smart_parent_id
          scope_multistatus: scope_multistatus
          scope_id: scope_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          smart_parent_name: smart_parent_name
          scope_size: scope_size
          smart_arch: smart_arch
          scope_end_address_addr: scope_end_address_addr
          failover_name: failover_name
          sharednetwork_name: sharednetwork_name
          scope_delayed_delete_time: scope_delayed_delete_time
          server_id: server_id
          server_addr: server_addr
          scope_net_mask: scope_net_mask
          scope_net_addr: scope_net_addr
          scope_space_id: scope_space_id
          scope_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_type: server_type
          failover_id: failover_id
          scope_class_name: scope_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_scope_data_data'
          type: array
      type: object
    dhcp_scope_add_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        scope_net_addr:
          description: The first IP address of the DHCPv4 scope.
          type: string
        scope_net_mask:
          description: The netmask of the DHCPv4 scope. It is expressed in dot-decimal
            notation and defines the number of addresses the scope contains.
          type: string
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        failover_id:
          description: The database identifier (ID) of the DHCPv4 failover channel,
            a unique numeric key value automatically incremented when you add a DHCPv4
            failover channel. Use the ID to specify the DHCPv4 failover channel of
            your choice.
          type: integer
        failover_name:
          description: The name of the DHCPv4 failover channel.
          type: string
        scope_name:
          description: The name of the DHCPv4 scope, each DHCPv4 scope must have a
            unique name.
          type: string
        scope_space_id:
          description: The database identifier (ID) of an existing space you want
            to associate with the DHCPv4 scope.
          type: integer
        scope_space_name:
          description: The name of an existing space you want to associate with the
            DHCPv4 scope.
          type: string
        sharednetwork_id:
          description: The database identifier (ID) of the DHCPv4 shared network,
            a unique numeric key value automatically incremented when you add a DHCPv4
            shared network. Use the ID to specify the DHCPv4 shared network of your
            choice.
          type: integer
        sharednetwork_name:
          description: The name of the DHCPv4 shared network.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        scope_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        scope_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_scope_edit_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        scope_id:
          description: The database identifier (ID) of the DHCPv4 scope, a unique
            numeric key value automatically incremented when you add a DHCPv4 scope.
            Use the ID to specify which DHCPv4 scope to edit.
          type: integer
        scope_net_addr:
          description: The first IP address of the DHCPv4 scope.
          type: string
        scope_net_mask:
          description: The netmask of the DHCPv4 scope. It is expressed in dot-decimal
            notation and defines the number of addresses the scope contains.
          type: string
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        failover_id:
          description: The database identifier (ID) of the DHCPv4 failover channel,
            a unique numeric key value automatically incremented when you add a DHCPv4
            failover channel. Use the ID to specify the DHCPv4 failover channel of
            your choice.
          type: integer
        failover_name:
          description: The name of the DHCPv4 failover channel.
          type: string
        scope_name:
          description: The name of the DHCPv4 scope, each DHCPv4 scope must have a
            unique name.
          type: string
        scope_space_id:
          description: The database identifier (ID) of an existing space you want
            to associate with the DHCPv4 scope.
          type: integer
        scope_space_name:
          description: The name of an existing space you want to associate with the
            DHCPv4 scope.
          type: string
        sharednetwork_id:
          description: The database identifier (ID) of the DHCPv4 shared network,
            a unique numeric key value automatically incremented when you add a DHCPv4
            shared network. Use the ID to specify the DHCPv4 shared network of your
            choice.
          type: integer
        sharednetwork_name:
          description: The name of the DHCPv4 shared network.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        scope_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        scope_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_scope_add_success:
      example:
        data:
        - scope_id: scope_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_scope_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_scope_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_scope_edit_success:
      example:
        data:
        - scope_id: scope_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_scope_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_scope_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_scope_delete_success:
      example:
        data:
        - scope_id: scope_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_scope_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_scope_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_scope6_data:
      example:
        data:
        - scope6_delayed_time: scope6_delayed_time
          server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_size: scope6_size
          server6_version: server6_version
          scope6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server6_name: server6_name
          scope6_prefix: scope6_prefix
          smart_parent_id: smart_parent_id
          scope6_sort_name: scope6_sort_name
          scope6_multistatus: scope6_multistatus
          failover6_name: failover6_name
          smart_parent_name: smart_parent_name
          smart_arch: smart_arch
          server6_class_name: server6_class_name
          server6_id: server6_id
          server6_type: server6_type
          scope6_space_name: scope6_space_name
          failover6_id: failover6_id
          sharednetwork6_name: sharednetwork6_name
          scope6_end_address6_addr: scope6_end_address6_addr
          server6_addr: server6_addr
          scope6_class_name: scope6_class_name
          scope6_name: scope6_name
          scope6_space_id: scope6_space_id
          scope6_id: scope6_id
          sharednetwork6_id: sharednetwork6_id
          scope6_start_address6_addr: scope6_start_address6_addr
        - scope6_delayed_time: scope6_delayed_time
          server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_size: scope6_size
          server6_version: server6_version
          scope6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server6_name: server6_name
          scope6_prefix: scope6_prefix
          smart_parent_id: smart_parent_id
          scope6_sort_name: scope6_sort_name
          scope6_multistatus: scope6_multistatus
          failover6_name: failover6_name
          smart_parent_name: smart_parent_name
          smart_arch: smart_arch
          server6_class_name: server6_class_name
          server6_id: server6_id
          server6_type: server6_type
          scope6_space_name: scope6_space_name
          failover6_id: failover6_id
          sharednetwork6_name: sharednetwork6_name
          scope6_end_address6_addr: scope6_end_address6_addr
          server6_addr: server6_addr
          scope6_class_name: scope6_class_name
          scope6_name: scope6_name
          scope6_space_id: scope6_space_id
          scope6_id: scope6_id
          sharednetwork6_id: sharednetwork6_id
          scope6_start_address6_addr: scope6_start_address6_addr
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_scope6_data_data'
          type: array
      type: object
    dhcp_scope6_add_input:
      properties:
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server, a unique
            numeric key value automatically incremented when you add a DHCPv6 server.
            Use the ID to specify the DHCPv6 server of your choice.
          type: integer
        server6_name:
          description: The name of the DHCPv6 server.
          type: string
        scope6_end_addr:
          description: The last IP address of the DHCPv6 scope.
          type: string
        scope6_prefix:
          description: The prefix of the DHCPv6 scope, an integer that defines the
            number of address the scope contains.
          type: string
        scope6_start_addr:
          description: The first IP address of the DHCPv6 scope.
          type: string
        server6_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        failover6_id:
          description: The database identifier (ID) of the DHCPv6 failover channel,
            a unique numeric key value automatically incremented when you add a DHCPv6
            failover channel. Use the ID to specify the DHCPv6 failover channel of
            your choice.
          type: integer
        failover6_name:
          description: The name of the DHCPv6 failover channel.
          type: string
        scope6_name:
          description: The name of the DHCPv6 scope, each DHCPv6 scope must have a
            unique name.
          type: string
        scope6_space_id:
          description: The database identifier (ID) of an existing space you want
            to associate with the DHCPv6 scope.
          type: integer
        scope6_space_name:
          description: The name of an existing space you want to associate with the
            DHCPv6 scope.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        scope6_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        scope6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_scope6_edit_input:
      properties:
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server, a unique
            numeric key value automatically incremented when you add a DHCPv6 server.
            Use the ID to specify the DHCPv6 server of your choice.
          type: integer
        server6_name:
          description: The name of the DHCPv6 server.
          type: string
        scope6_end_addr:
          description: The last IP address of the DHCPv6 scope.
          type: string
        scope6_id:
          description: The database identifier (ID) of the DHCPv6 scope, a unique
            numeric key value automatically incremented when you add a DHCPv6 scope.
            Use the ID to specify which DHCPv6 scope to edit.
          type: integer
        scope6_prefix:
          description: The prefix of the DHCPv6 scope, an integer that defines the
            number of address the scope contains.
          type: string
        scope6_start_addr:
          description: The first IP address of the DHCPv6 scope.
          type: string
        server6_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        failover6_id:
          description: The database identifier (ID) of the DHCPv6 failover channel,
            a unique numeric key value automatically incremented when you add a DHCPv6
            failover channel. Use the ID to specify the DHCPv6 failover channel of
            your choice.
          type: integer
        failover6_name:
          description: The name of the DHCPv6 failover channel.
          type: string
        scope6_name:
          description: The name of the DHCPv6 scope, each DHCPv6 scope must have a
            unique name.
          type: string
        scope6_space_id:
          description: The database identifier (ID) of an existing space you want
            to associate with the DHCPv6 scope.
          type: integer
        scope6_space_name:
          description: The name of an existing space you want to associate with the
            DHCPv6 scope.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        scope6_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        scope6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_scope6_add_success:
      example:
        data:
        - scope6_id: scope6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_scope6_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_scope6_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_scope6_edit_success:
      example:
        data:
        - scope6_id: scope6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_scope6_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_scope6_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_scope6_delete_success:
      example:
        data:
        - scope6_id: scope6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_scope6_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_scope6_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_server_data:
      example:
        data:
        - server_stat_enabled: server_stat_enabled
          server_cisco_password: server_cisco_password
          server_uboottime: server_uboottime
          server_stat_time: server_stat_time
          server_ms_use_ssl: server_ms_use_ssl
          server_version: server_version
          server_stat_niceness: server_stat_niceness
          server_multistatus: server_multistatus
          smart_parent_name: smart_parent_name
          server_localtime: server_localtime
          smart_ref2_server_id: smart_ref2_server_id
          server_tcp_port: server_tcp_port
          server_snmp_timeout: server_snmp_timeout
          server_stat_period: server_stat_period
          server_windhcp_protocol: server_windhcp_protocol
          server_ipmdhcp_protocol: server_ipmdhcp_protocol
          server_id: server_id
          server_addr: server_addr
          reverse_proxy_conf: reverse_proxy_conf
          server_msrpc_login: server_msrpc_login
          server_name: server_name
          smart_parent_arch: smart_parent_arch
          server_cisco_login: server_cisco_login
          server_class_name: server_class_name
          server_snmp_profile_id: server_snmp_profile_id
          smart_ref2_server_name: smart_ref2_server_name
          smart_ref1_server_name: smart_ref1_server_name
          server_snmp_use_tcp: server_snmp_use_tcp
          smart_parent_id: smart_parent_id
          server_cisco_use_ssh: server_cisco_use_ssh
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_https_login: server_https_login
          smart_arch: smart_arch
          server_cisco_root_password: server_cisco_root_password
          server_state: server_state
          server_comment: server_comment
          server_synching: server_synching
          smart_ref1_server_id: smart_ref1_server_id
          server_is_package: server_is_package
          server_snmp_port: server_snmp_port
          total_smart_members: total_smart_members
          server_isolated: server_isolated
          smart_param1: smart_param1
          server_msrpc_domain: server_msrpc_domain
          ref2_server_name: ref2_server_name
          server_snmp_retry: server_snmp_retry
          ref1_server_name: ref1_server_name
          server_snmp_id: server_snmp_id
          smart_members_name: smart_members_name
          server_type: server_type
        - server_stat_enabled: server_stat_enabled
          server_cisco_password: server_cisco_password
          server_uboottime: server_uboottime
          server_stat_time: server_stat_time
          server_ms_use_ssl: server_ms_use_ssl
          server_version: server_version
          server_stat_niceness: server_stat_niceness
          server_multistatus: server_multistatus
          smart_parent_name: smart_parent_name
          server_localtime: server_localtime
          smart_ref2_server_id: smart_ref2_server_id
          server_tcp_port: server_tcp_port
          server_snmp_timeout: server_snmp_timeout
          server_stat_period: server_stat_period
          server_windhcp_protocol: server_windhcp_protocol
          server_ipmdhcp_protocol: server_ipmdhcp_protocol
          server_id: server_id
          server_addr: server_addr
          reverse_proxy_conf: reverse_proxy_conf
          server_msrpc_login: server_msrpc_login
          server_name: server_name
          smart_parent_arch: smart_parent_arch
          server_cisco_login: server_cisco_login
          server_class_name: server_class_name
          server_snmp_profile_id: server_snmp_profile_id
          smart_ref2_server_name: smart_ref2_server_name
          smart_ref1_server_name: smart_ref1_server_name
          server_snmp_use_tcp: server_snmp_use_tcp
          smart_parent_id: smart_parent_id
          server_cisco_use_ssh: server_cisco_use_ssh
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_https_login: server_https_login
          smart_arch: smart_arch
          server_cisco_root_password: server_cisco_root_password
          server_state: server_state
          server_comment: server_comment
          server_synching: server_synching
          smart_ref1_server_id: smart_ref1_server_id
          server_is_package: server_is_package
          server_snmp_port: server_snmp_port
          total_smart_members: total_smart_members
          server_isolated: server_isolated
          smart_param1: smart_param1
          server_msrpc_domain: server_msrpc_domain
          ref2_server_name: ref2_server_name
          server_snmp_retry: server_snmp_retry
          ref1_server_name: ref1_server_name
          server_snmp_id: server_snmp_id
          smart_members_name: smart_members_name
          server_type: server_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_server_data_data'
          type: array
      type: object
    dhcp_server6_data:
      example:
        data:
        - server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server6_snmp_id: server6_snmp_id
          server6_version: server6_version
          smart_parent_arch: smart_parent_arch
          server6_state: server6_state
          smart_ref2_server6_id: smart_ref2_server6_id
          server6_stat_period: server6_stat_period
          server6_snmp_profile_id: server6_snmp_profile_id
          server6_multistatus: server6_multistatus
          server6_name: server6_name
          server6_stat_time: server6_stat_time
          server6_comment: server6_comment
          server6_snmp_timeout: server6_snmp_timeout
          smart_parent_id: smart_parent_id
          smart_parent_name: smart_parent_name
          smart_ref1_server6_id: smart_ref1_server6_id
          smart_arch: smart_arch
          server6_class_name: server6_class_name
          server6_id: server6_id
          server6_type: server6_type
          server6_snmp_port: server6_snmp_port
          server6_stat_niceness: server6_stat_niceness
          smart_ref1_server6_name: smart_ref1_server6_name
          smart_ref2_server6_name: smart_ref2_server6_name
          server6_synching: server6_synching
          total_smart_members: total_smart_members
          server6_isolated: server6_isolated
          smart_param1: smart_param1
          server6_last_refresh_time: server6_last_refresh_time
          server6_https_login: server6_https_login
          server6_uboottime: server6_uboottime
          server6_addr: server6_addr
          server6_snmp_retry: server6_snmp_retry
          smart_members_name: smart_members_name
          server6_snmp_use_tcp: server6_snmp_use_tcp
          server6_is_package: server6_is_package
          reverse_proxy_conf: reverse_proxy_conf
          server6_stat_enabled: server6_stat_enabled
        - server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server6_snmp_id: server6_snmp_id
          server6_version: server6_version
          smart_parent_arch: smart_parent_arch
          server6_state: server6_state
          smart_ref2_server6_id: smart_ref2_server6_id
          server6_stat_period: server6_stat_period
          server6_snmp_profile_id: server6_snmp_profile_id
          server6_multistatus: server6_multistatus
          server6_name: server6_name
          server6_stat_time: server6_stat_time
          server6_comment: server6_comment
          server6_snmp_timeout: server6_snmp_timeout
          smart_parent_id: smart_parent_id
          smart_parent_name: smart_parent_name
          smart_ref1_server6_id: smart_ref1_server6_id
          smart_arch: smart_arch
          server6_class_name: server6_class_name
          server6_id: server6_id
          server6_type: server6_type
          server6_snmp_port: server6_snmp_port
          server6_stat_niceness: server6_stat_niceness
          smart_ref1_server6_name: smart_ref1_server6_name
          smart_ref2_server6_name: smart_ref2_server6_name
          server6_synching: server6_synching
          total_smart_members: total_smart_members
          server6_isolated: server6_isolated
          smart_param1: smart_param1
          server6_last_refresh_time: server6_last_refresh_time
          server6_https_login: server6_https_login
          server6_uboottime: server6_uboottime
          server6_addr: server6_addr
          server6_snmp_retry: server6_snmp_retry
          smart_members_name: smart_members_name
          server6_snmp_use_tcp: server6_snmp_use_tcp
          server6_is_package: server6_is_package
          reverse_proxy_conf: reverse_proxy_conf
          server6_stat_enabled: server6_stat_enabled
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_server6_data_data'
          type: array
      type: object
    dhcp_sharednetwork_data:
      example:
        data:
        - server_name: server_name
          smart_parent_id: smart_parent_id
          sharednetwork_multistatus: sharednetwork_multistatus
          sharednetwork_id: sharednetwork_id
          sharednetwork_name: sharednetwork_name
          server_id: server_id
          sharednetwork_delayed_create_time: sharednetwork_delayed_create_time
          sharednetwork_delayed_delete_time: sharednetwork_delayed_delete_time
          server_type: server_type
        - server_name: server_name
          smart_parent_id: smart_parent_id
          sharednetwork_multistatus: sharednetwork_multistatus
          sharednetwork_id: sharednetwork_id
          sharednetwork_name: sharednetwork_name
          server_id: server_id
          sharednetwork_delayed_create_time: sharednetwork_delayed_create_time
          sharednetwork_delayed_delete_time: sharednetwork_delayed_delete_time
          server_type: server_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_sharednetwork_data_data'
          type: array
      type: object
    dhcp_sharednetwork_add_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        sharednetwork_name:
          description: The name of the DHCPv4 shared network, each DHCPv4 shared network
            must have a unique name.
          type: string
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_sharednetwork_edit_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        sharednetwork_id:
          description: The database identifier (ID) of the DHCPv4 shared network,
            a unique numeric key value automatically incremented when you add a DHCPv4
            shared network. Use the ID to specify which DHCPv4 shared network to edit.
          type: integer
        sharednetwork_name:
          description: The name of the DHCPv4 shared network, each DHCPv4 shared network
            must have a unique name.
          type: string
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_sharednetwork_add_success:
      example:
        data:
        - sharednetwork_id: sharednetwork_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_sharednetwork_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_sharednetwork_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_sharednetwork_edit_success:
      example:
        data:
        - sharednetwork_id: sharednetwork_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_sharednetwork_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_sharednetwork_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_sharednetwork_delete_success:
      example:
        data:
        - sharednetwork_id: sharednetwork_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_sharednetwork_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_sharednetwork_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_sharednetwork6_data:
      example:
        data:
        - smart_parent_id: smart_parent_id
          row_state: row_state
          sharednetwork6_name: sharednetwork6_name
          sharednetwork6_multistatus: sharednetwork6_multistatus
          sharednetwork6_delayed_time: sharednetwork6_delayed_time
          smart_arch: smart_arch
          sharednetwork6_id: sharednetwork6_id
          server6_id: server6_id
          server6_type: server6_type
          server6_name: server6_name
        - smart_parent_id: smart_parent_id
          row_state: row_state
          sharednetwork6_name: sharednetwork6_name
          sharednetwork6_multistatus: sharednetwork6_multistatus
          sharednetwork6_delayed_time: sharednetwork6_delayed_time
          smart_arch: smart_arch
          sharednetwork6_id: sharednetwork6_id
          server6_id: server6_id
          server6_type: server6_type
          server6_name: server6_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_sharednetwork6_data_data'
          type: array
      type: object
    dhcp_sharednetwork6_add_input:
      properties:
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server, a unique
            numeric key value automatically incremented when you add a DHCPv6 server.
            Use the ID to specify the DHCPv6 server of your choice.
          type: integer
        server6_name:
          description: The name of the DHCPv6 server.
          type: string
        sharednetwork6_name:
          description: The name of the DHCPv6 shared network.
          type: string
        server6_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_sharednetwork6_edit_input:
      properties:
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server, a unique
            numeric key value automatically incremented when you add a DHCPv6 server.
            Use the ID to specify the DHCPv6 server of your choice.
          type: integer
        server6_name:
          description: The name of the DHCPv6 server.
          type: string
        sharednetwork6_id:
          description: The database identifier (ID) of the DHCPv6 shared network,
            a unique numeric key value automatically incremented when you add a DHCPv6
            shared network. Use the ID to specify the DHCPv6 shared network of your
            choice.
          type: integer
        sharednetwork6_name:
          description: The name of the DHCPv6 shared network.
          type: string
        server6_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_sharednetwork6_add_success:
      example:
        data:
        - sharednetwork6_id: sharednetwork6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_sharednetwork6_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_sharednetwork6_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_sharednetwork6_edit_success:
      example:
        data:
        - sharednetwork6_id: sharednetwork6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_sharednetwork6_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_sharednetwork6_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_sharednetwork6_delete_success:
      example:
        data:
        - sharednetwork6_id: sharednetwork6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_sharednetwork6_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_sharednetwork6_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_static_data:
      example:
        data:
        - server_name: server_name
          static_mac_vendor: static_mac_vendor
          sharednetwork_id: sharednetwork_id
          server_class_name: server_class_name
          group_class_name: group_class_name
          scope_start_address_addr: scope_start_address_addr
          server_version: server_version
          static_multistatus: static_multistatus
          scope_name: scope_name
          static_mac_addr: static_mac_addr
          static_delayed_create_time: static_delayed_create_time
          static_delayed_delete_time: static_delayed_delete_time
          smart_parent_id: smart_parent_id
          scope_id: scope_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          smart_parent_name: smart_parent_name
          scope_size: scope_size
          static_last_seen: static_last_seen
          static_expire_time: static_expire_time
          scope_end_address_addr: scope_end_address_addr
          static_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          static_addr: static_addr
          group_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          group_name: group_name
          static_domain: static_domain
          static_id: static_id
          sharednetwork_name: sharednetwork_name
          static_class_name: static_class_name
          server_id: server_id
          scope_net_mask: scope_net_mask
          scope_net_addr: scope_net_addr
          group_id: group_id
          scope_space_id: scope_space_id
          scope_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          static_address_addr: static_address_addr
          static_name: static_name
          static_identifier: static_identifier
          server_type: server_type
          scope_class_name: scope_class_name
        - server_name: server_name
          static_mac_vendor: static_mac_vendor
          sharednetwork_id: sharednetwork_id
          server_class_name: server_class_name
          group_class_name: group_class_name
          scope_start_address_addr: scope_start_address_addr
          server_version: server_version
          static_multistatus: static_multistatus
          scope_name: scope_name
          static_mac_addr: static_mac_addr
          static_delayed_create_time: static_delayed_create_time
          static_delayed_delete_time: static_delayed_delete_time
          smart_parent_id: smart_parent_id
          scope_id: scope_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          smart_parent_name: smart_parent_name
          scope_size: scope_size
          static_last_seen: static_last_seen
          static_expire_time: static_expire_time
          scope_end_address_addr: scope_end_address_addr
          static_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          static_addr: static_addr
          group_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          group_name: group_name
          static_domain: static_domain
          static_id: static_id
          sharednetwork_name: sharednetwork_name
          static_class_name: static_class_name
          server_id: server_id
          scope_net_mask: scope_net_mask
          scope_net_addr: scope_net_addr
          group_id: group_id
          scope_space_id: scope_space_id
          scope_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          static_address_addr: static_address_addr
          static_name: static_name
          static_identifier: static_identifier
          server_type: server_type
          scope_class_name: scope_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_static_data_data'
          type: array
      type: object
    dhcp_static_add_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        static_identifier:
          description: TODO:dhcp_static_add.input.static_identifier
          type: string
        static_mac_addr:
          description: The MAC address you want to associate with the IPv4 static,
            it must include the MAC address type. The address has 7 sections, <b>00:11:22:33:44:55:66</b>
            , where <b>00</b> indicates the type. For Ethernet, specify <b>01</b>.
          type: string
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        group_id:
          description: The database identifier (ID) of the DHCPv4 group, a unique
            numeric key value automatically incremented when you add a DHCPv4 group.
            Use the ID to specify the DHCPv4 group of your choice.
          type: integer
        group_name:
          description: The name of the DHCPv4 group.
          type: string
        static_addr:
          description: The IP address associated with the DHCPv4 static.
          type: string
        static_name:
          description: The name of the DHCPv4 static, each DHCPv4 static must have
            a unique name.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        static_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        static_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_static_edit_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
            Use the ID to specify the DHCPv4 server of your choice.
          type: integer
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        static_id:
          description: The database identifier (ID) of the DHCPv4 static, a unique
            numeric key value automatically incremented when you add a DHCPv4 static.
            Use the ID to specify which DHCPv4 static to edit.
          type: integer
        static_identifier:
          description: TODO:dhcp_static_add.input.static_identifier
          type: string
        static_mac_addr:
          description: The MAC address you want to associate with the IPv4 static,
            it must include the MAC address type. The address has 7 sections, <b>00:11:22:33:44:55:66</b>
            , where <b>00</b> indicates the type. For Ethernet, specify <b>01</b>.
          type: string
        server_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        group_id:
          description: The database identifier (ID) of the DHCPv4 group, a unique
            numeric key value automatically incremented when you add a DHCPv4 group.
            Use the ID to specify the DHCPv4 group of your choice.
          type: integer
        group_name:
          description: The name of the DHCPv4 group.
          type: string
        static_addr:
          description: The IP address associated with the DHCPv4 static.
          type: string
        static_name:
          description: The name of the DHCPv4 static, each DHCPv4 static must have
            a unique name.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        static_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        static_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_static_add_success:
      example:
        data:
        - static_id: static_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_static_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_static_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_static_edit_success:
      example:
        data:
        - static_id: static_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_static_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_static_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_static_delete_success:
      example:
        data:
        - static_id: static_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_static_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_static_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_static6_data:
      example:
        data:
        - server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_size: scope6_size
          static6_address6_addr: static6_address6_addr
          server6_version: server6_version
          scope6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          static6_mac_vendor: static6_mac_vendor
          group6_id: group6_id
          server6_name: server6_name
          smart_parent_id: smart_parent_id
          static6_class_name: static6_class_name
          smart_parent_name: smart_parent_name
          server6_class_name: server6_class_name
          server6_id: server6_id
          static6_time: static6_time
          server6_type: server6_type
          static6_name: static6_name
          static6_client_duid: static6_client_duid
          sharednetwork6_name: sharednetwork6_name
          group6_name: group6_name
          static6_id: static6_id
          static6_end_time: static6_end_time
          scope6_end_address6_addr: scope6_end_address6_addr
          static6_domain: static6_domain
          scope6_class_name: scope6_class_name
          scope6_name: scope6_name
          scope6_space_id: scope6_space_id
          static6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_id: scope6_id
          static6_multistatus: static6_multistatus
          static6_mac_addr: static6_mac_addr
          sharednetwork6_id: sharednetwork6_id
          group6_class_name: group6_class_name
          static6_state: static6_state
          scope6_start_address6_addr: scope6_start_address6_addr
          static6_delayed_time: static6_delayed_time
          group6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
        - server6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_size: scope6_size
          static6_address6_addr: static6_address6_addr
          server6_version: server6_version
          scope6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          static6_mac_vendor: static6_mac_vendor
          group6_id: group6_id
          server6_name: server6_name
          smart_parent_id: smart_parent_id
          static6_class_name: static6_class_name
          smart_parent_name: smart_parent_name
          server6_class_name: server6_class_name
          server6_id: server6_id
          static6_time: static6_time
          server6_type: server6_type
          static6_name: static6_name
          static6_client_duid: static6_client_duid
          sharednetwork6_name: sharednetwork6_name
          group6_name: group6_name
          static6_id: static6_id
          static6_end_time: static6_end_time
          scope6_end_address6_addr: scope6_end_address6_addr
          static6_domain: static6_domain
          scope6_class_name: scope6_class_name
          scope6_name: scope6_name
          scope6_space_id: scope6_space_id
          static6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          scope6_id: scope6_id
          static6_multistatus: static6_multistatus
          static6_mac_addr: static6_mac_addr
          sharednetwork6_id: sharednetwork6_id
          group6_class_name: group6_class_name
          static6_state: static6_state
          scope6_start_address6_addr: scope6_start_address6_addr
          static6_delayed_time: static6_delayed_time
          group6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_static6_data_data'
          type: array
      type: object
    dhcp_static6_add_input:
      properties:
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server, a unique
            numeric key value automatically incremented when you add a DHCPv6 server.
            Use the ID to specify the DHCPv6 server of your choice.
          type: integer
        server6_name:
          description: The name of the DHCPv6 server.
          type: string
        static6_client_duid:
          description: The client DHCP Unique Identifier (DUID) associated with the
            DHCPv6 static.
          type: string
        static6_mac_addr:
          description: The MAC address you want to associate with the IPv6 static.
          type: string
        static6_name:
          description: The name of the DHCPv6 static, each DHCPv6 static must have
            a unique name.
          type: string
        server6_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        group6_id:
          description: The database identifier (ID) of the DHCPv6 group, a unique
            numeric key value automatically incremented when you add a DHCPv6 group.
            Use the ID to specify the DHCPv6 group of your choice.
          type: integer
        group6_name:
          description: The name of the DHCPv6 group.
          type: string
        static6_addr:
          description: The IP address associated with the DHCPv6 static.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        static6_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        static6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_static6_edit_input:
      properties:
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server, a unique
            numeric key value automatically incremented when you add a DHCPv6 server.
            Use the ID to specify the DHCPv6 server of your choice.
          type: integer
        server6_name:
          description: The name of the DHCPv6 server.
          type: string
        static6_client_duid:
          description: The client DHCP Unique Identifier (DUID) associated with the
            DHCPv6 static.
          type: string
        static6_id:
          description: The database identifier (ID) of the DHCPv6 static, a unique
            numeric key value automatically incremented when you add a DHCPv6 static.
            Use the ID to specify which DHCPv6 static to edit.
          type: integer
        static6_mac_addr:
          description: The MAC address you want to associate with the IPv6 static.
          type: string
        static6_name:
          description: The name of the DHCPv6 static, each DHCPv6 static must have
            a unique name.
          type: string
        server6_hostaddr:
          description: The IP address of the DHCP server.
          type: string
        group6_id:
          description: The database identifier (ID) of the DHCPv6 group, a unique
            numeric key value automatically incremented when you add a DHCPv6 group.
            Use the ID to specify the DHCPv6 group of your choice.
          type: integer
        group6_name:
          description: The name of the DHCPv6 group.
          type: string
        static6_addr:
          description: The IP address associated with the DHCPv6 static.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        static6_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        static6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dhcp_static6_add_success:
      example:
        data:
        - static6_id: static6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_static6_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_static6_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_static6_edit_success:
      example:
        data:
        - static6_id: static6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_static6_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_static6_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dhcp_static6_delete_success:
      example:
        data:
        - static6_id: static6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_static6_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dhcp_static6_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_acl_data:
      example:
        data:
        - acl_value: acl_value
          acl_name: acl_name
          acl_id: acl_id
          server_id: server_id
        - acl_value: acl_value
          acl_name: acl_name
          acl_id: acl_id
          server_id: server_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_acl_data_data'
          type: array
      type: object
    dns_acl_add_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DNS server, a unique numeric
            key value automatically incremented when you add a DNS server. Use the
            ID to specify the DNS server of your choice.
          type: integer
        server_name:
          description: The name of the DNS server.
          type: string
        acl_name:
          description: The name of the DNS ACL, each DNS ACL must have a unique name.
          type: string
        acl_value:
          description: 'The values of the DNS ACL in order of priority, as follows:
            &lt;value_1&gt;;&lt;value_2&gt;... .'
          type: string
        server_hostaddr:
          description: The IP address of the DNS server.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dns_acl_edit_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DNS server, a unique numeric
            key value automatically incremented when you add a DNS server. Use the
            ID to specify the DNS server of your choice.
          type: integer
        server_name:
          description: The name of the DNS server.
          type: string
        acl_id:
          description: The database identifier (ID) of the DNS ACL, a unique numeric
            key value automatically incremented when you add a DNS ACL. Use the ID
            to specify which DNS ACL to edit.
          type: integer
        acl_name:
          description: The name of the DNS ACL, each DNS ACL must have a unique name.
          type: string
        acl_value:
          description: 'The values of the DNS ACL in order of priority, as follows:
            &lt;value_1&gt;;&lt;value_2&gt;... .'
          type: string
        server_hostaddr:
          description: The IP address of the DNS server.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dns_acl_add_success:
      example:
        data:
        - acl_id: acl_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_acl_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_acl_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_acl_edit_success:
      example:
        data:
        - acl_id: acl_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_acl_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_acl_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_acl_delete_success:
      example:
        data:
        - acl_id: acl_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_acl_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_acl_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_rr_data:
      example:
        data:
        - rr_id: rr_id
          rr_value_ip4_addr: rr_value_ip4_addr
          server_version: server_version
          rr_ttl: rr_ttl
          smart_parent_name: smart_parent_name
          rr_full_name_utf: rr_full_name_utf
          zone_space_name: zone_space_name
          zone_type: zone_type
          rr_all_value: rr_all_value
          view_class_name: view_class_name
          rr_value1: rr_value1
          server_id: server_id
          rr_glue: rr_glue
          rr_delayed_delete_time: rr_delayed_delete_time
          rr_last_update_time: rr_last_update_time
          zone_masters: zone_masters
          zone_is_reverse: zone_is_reverse
          view_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          rr_full_name: rr_full_name
          server_name: server_name
          zone_sort_zone: zone_sort_zone
          zone_forwarders: zone_forwarders
          server_class_name: server_class_name
          zone_class_name: zone_class_name
          zone_name: zone_name
          rr_name_address_addr: rr_name_address_addr
          rr_value7: rr_value7
          zone_id: zone_id
          rr_value6: rr_value6
          rr_delayed_time: rr_delayed_time
          rr_class_name: rr_class_name
          smart_parent_id: smart_parent_id
          rr_value3: rr_value3
          rr_value2: rr_value2
          rr_value5: rr_value5
          rr_value4: rr_value4
          rr_type: rr_type
          server_cloud: server_cloud
          view_name: view_name
          server_comment: server_comment
          rr_value_address_addr: rr_value_address_addr
          view_id: view_id
          zone_is_rpz: zone_is_rpz
          rr_multistatus: rr_multistatus
          rr_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          zone_name_utf: zone_name_utf
          rr_name_ip4_addr: rr_name_ip4_addr
          rr_delayed_create_time: rr_delayed_create_time
          server_type: server_type
        - rr_id: rr_id
          rr_value_ip4_addr: rr_value_ip4_addr
          server_version: server_version
          rr_ttl: rr_ttl
          smart_parent_name: smart_parent_name
          rr_full_name_utf: rr_full_name_utf
          zone_space_name: zone_space_name
          zone_type: zone_type
          rr_all_value: rr_all_value
          view_class_name: view_class_name
          rr_value1: rr_value1
          server_id: server_id
          rr_glue: rr_glue
          rr_delayed_delete_time: rr_delayed_delete_time
          rr_last_update_time: rr_last_update_time
          zone_masters: zone_masters
          zone_is_reverse: zone_is_reverse
          view_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          rr_full_name: rr_full_name
          server_name: server_name
          zone_sort_zone: zone_sort_zone
          zone_forwarders: zone_forwarders
          server_class_name: server_class_name
          zone_class_name: zone_class_name
          zone_name: zone_name
          rr_name_address_addr: rr_name_address_addr
          rr_value7: rr_value7
          zone_id: zone_id
          rr_value6: rr_value6
          rr_delayed_time: rr_delayed_time
          rr_class_name: rr_class_name
          smart_parent_id: smart_parent_id
          rr_value3: rr_value3
          rr_value2: rr_value2
          rr_value5: rr_value5
          rr_value4: rr_value4
          rr_type: rr_type
          server_cloud: server_cloud
          view_name: view_name
          server_comment: server_comment
          rr_value_address_addr: rr_value_address_addr
          view_id: view_id
          zone_is_rpz: zone_is_rpz
          rr_multistatus: rr_multistatus
          rr_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          zone_name_utf: zone_name_utf
          rr_name_ip4_addr: rr_name_ip4_addr
          rr_delayed_create_time: rr_delayed_create_time
          server_type: server_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_rr_data_data'
          type: array
      type: object
    dns_rr_add_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DNS server, a unique numeric
            key value automatically incremented when you add a DNS server. Use the
            ID to specify the DNS server of your choice.
          type: integer
        server_name:
          description: The name of the DNS server.
          type: string
        server_hostaddr:
          description: The IP address of the DNS server.
          type: string
        rr_name:
          description: The full name of the DNS resource record. Specify it as value,
            it can either follow the format <b>&lt;rr-name&gt;.&lt;existing-zone-name&gt;.&lt;extension&gt;</b>
            or be <b>.</b> (a dot).
          type: string
        rr_type:
          description: 'The type of the DNS resource record.<table><caption>rr_type
            possible values</caption><br/><thead><tr><th>Value</th><th>Record type
            description</th></tr><br/></thead><br/><tbody><tr><td >SOA</td><td >Start
            of Authority. Defines the zone name, an email contact and various time
            and refresh values applicable to the zone. It is automatically generated
            upon creation of a zone and cannot be added manually.</td></tr><tr><td
            >NS</td><td >Name Server. Defines the authoritative name server(s) for
            the domain (defined by the SOA record) or the subdomain. The NS record
            that indicates which server has authority over a zone is automatically
            generated upon the creation of a zone, once the server has been synchronized.</td></tr><tr><td
            >A</td><td >IPv4 Address. An IPv4 address for a host.</td></tr><tr><td
            >PTR</td><td >Pointer Record. Address Resolution, from an IP address (IPv4
            or IPv6) to a host. Used in reverse mapping.</td></tr><tr><td >AAAA</td><td
            >IPv6 Address. An IPv6 address for a host.</td></tr><tr><td >CNAME</td><td
            >Canonical Name. An alias name for a host.</td></tr><tr><td >MX</td><td
            >Mail Exchange. The mail server/exchanger that services this zone.</td></tr><tr><td
            >SRV</td><td >Services record. Defines services available in the zone,
            for example, LDAP, HTTP, etc...</td></tr><tr><td >DNAME</td><td >Delegation
            of Reverse Names. Delegation of reverse addresses primarily in IPv6. (Deprecated,
            use the CNAME RR instead)</td></tr><tr><td >TXT</td><td >Text. Information
            associated with a name.</td></tr><tr><td >DS</td><td >Delegation Signer,
            a DNSSEC related RR used to verify the validity of the ZSK of a subdomain.</td></tr><tr><td
            >DNSKEY</td><td >DNS Key. It contains the public cryptographic key used
            to sign the zone with DNSSEC.</td></tr><tr><td >65534</td><td >A private
            type record automatically added to the zone once it is signed with DNSSEC.</td></tr><tr><td
            >HINFO</td><td >System Information. Information about a host: hardware
            type and operating system description.</td></tr><tr><td >MINFO</td><td
            >Mailbox mail list Information. Defines the mail administrator for a mail
            list and optionally a mailbox to receive error messages relating to the
            mail list.</td></tr><tr><td >AFSDB</td><td >AFS Database. Location of
            the AFS servers.</td></tr><tr><td >WKS</td><td >Well-Known Service. Defines
            the services and protocols supported by a host. (Deprecated, use the SRV
            RR instead)</td></tr><tr><td >NAPTR</td><td >Naming Authority Pointer
            Record. General purpose definition of rule set to be used by applications
            e.g. VoIP.</td></tr><tr><td >NSAP</td><td >Network Service Access Point.
            Defines record (equivalent of an A record) maps a host name to an endpoint
            address.</td></tr></tbody></table></p><br/>Note that the parameter is
            not case sensitive, you could specify <b>A</b> or <b>a</b>.'
          enum:
          - A
          - NS
          - MD
          - MF
          - CNAME
          - SOA
          - MB
          - MG
          - MR
          - "NULL"
          - WKS
          - PTR
          - HINFO
          - MINFO
          - MX
          - TXT
          - SPF
          - RP
          - AFSDB
          - X25
          - ISDN
          - RT
          - NSAP
          - NSAP_PTR
          - SIG
          - KEY
          - PX
          - GPOS
          - AAAA
          - LOC
          - NXT
          - EID
          - NIMLOC
          - SRV
          - ATMA
          - NAPTR
          - KX
          - CERT
          - A6
          - DNAME
          - OPT
          - DS
          - DNSSIG
          - NSEC
          - DNSKEY
          - NSEC3
          - NSEC3PARAM
          - CDS
          - CDNSKEY
          - CAA
          - TLSA
          - SSHFP
          - OPENPGPKEY
          - URI
          - AVC
          - NINFO
          - DLV
          - DHCID
          - EUI48
          - EUI64
          - NID
          - L32
          - L64
          type: string
        rr_value1:
          description: The first or only value required for the DNS resource record,
            as detailed in the service description.
          type: string
        check_value:
          description: A way to check the values of the DNS resource record before
            upon addition (<b>1)</b> in order to create a record with the same name
            but with different values.
          enum:
          - "yes"
          - "no"
          type: string
        view_id:
          description: The database identifier (ID) of the DNS view, a unique numeric
            key value automatically incremented when you add a DNS view. Use the ID
            to specify the DNS view of your choice.
          type: integer
        view_name:
          description: The name of the DNS view.
          type: string
        zone_id:
          description: The database identifier (ID) of the DNS zone, a unique numeric
            key value automatically incremented when you add a DNS zone. Use the ID
            to specify the DNS zone of your choice.
          type: integer
        zone_name:
          description: The name of the DNS zone the object belongs to.
          type: string
        zone_space_id:
          description: The database identifier (ID) of the space associated with the
            DNS zone the record belongs to.
          type: integer
        rr_glue:
          description: The shortname of the DNS resource record.
          type: string
        rr_ttl:
          description: The time to live of the DNS resource record, in seconds.
          type: integer
        rr_value2:
          description: The second value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value3:
          description: The third value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value4:
          description: The fourth value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value5:
          description: The fifth value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value6:
          description: The sixth value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value7:
          description: The seventh value of the DNS resource record, depending on
            its type, as detailed in the service description.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        rr_class_name:
          description: TODO:dns_rr_add.input.rr_class_name
          type: string
        rr_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dns_rr_edit_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DNS server, a unique numeric
            key value automatically incremented when you add a DNS server. Use the
            ID to specify the DNS server of your choice.
          type: integer
        server_name:
          description: The name of the DNS server.
          type: string
        server_hostaddr:
          description: The IP address of the DNS server.
          type: string
        rr_id:
          description: The database identifier (ID) of the DNS resource record, a
            unique numeric key value automatically incremented when you add a DNS
            RR. Use the ID to specify which record to edit.
          type: integer
        rr_name:
          description: The full name of the DNS resource record. Specify it as value,
            it can either follow the format <b>&lt;rr-name&gt;.&lt;existing-zone-name&gt;.&lt;extension&gt;</b>
            or be <b>.</b> (a dot).
          type: string
        rr_type:
          description: 'The type of the DNS resource record.<table><caption>rr_type
            possible values</caption><br/><thead><tr><th>Value</th><th>Record type
            description</th></tr><br/></thead><br/><tbody><tr><td >SOA</td><td >Start
            of Authority. Defines the zone name, an email contact and various time
            and refresh values applicable to the zone. It is automatically generated
            upon creation of a zone and cannot be added manually.</td></tr><tr><td
            >NS</td><td >Name Server. Defines the authoritative name server(s) for
            the domain (defined by the SOA record) or the subdomain. The NS record
            that indicates which server has authority over a zone is automatically
            generated upon the creation of a zone, once the server has been synchronized.</td></tr><tr><td
            >A</td><td >IPv4 Address. An IPv4 address for a host.</td></tr><tr><td
            >PTR</td><td >Pointer Record. Address Resolution, from an IP address (IPv4
            or IPv6) to a host. Used in reverse mapping.</td></tr><tr><td >AAAA</td><td
            >IPv6 Address. An IPv6 address for a host.</td></tr><tr><td >CNAME</td><td
            >Canonical Name. An alias name for a host.</td></tr><tr><td >MX</td><td
            >Mail Exchange. The mail server/exchanger that services this zone.</td></tr><tr><td
            >SRV</td><td >Services record. Defines services available in the zone,
            for example, LDAP, HTTP, etc...</td></tr><tr><td >DNAME</td><td >Delegation
            of Reverse Names. Delegation of reverse addresses primarily in IPv6. (Deprecated,
            use the CNAME RR instead)</td></tr><tr><td >TXT</td><td >Text. Information
            associated with a name.</td></tr><tr><td >DS</td><td >Delegation Signer,
            a DNSSEC related RR used to verify the validity of the ZSK of a subdomain.</td></tr><tr><td
            >DNSKEY</td><td >DNS Key. It contains the public cryptographic key used
            to sign the zone with DNSSEC.</td></tr><tr><td >65534</td><td >A private
            type record automatically added to the zone once it is signed with DNSSEC.</td></tr><tr><td
            >HINFO</td><td >System Information. Information about a host: hardware
            type and operating system description.</td></tr><tr><td >MINFO</td><td
            >Mailbox mail list Information. Defines the mail administrator for a mail
            list and optionally a mailbox to receive error messages relating to the
            mail list.</td></tr><tr><td >AFSDB</td><td >AFS Database. Location of
            the AFS servers.</td></tr><tr><td >WKS</td><td >Well-Known Service. Defines
            the services and protocols supported by a host. (Deprecated, use the SRV
            RR instead)</td></tr><tr><td >NAPTR</td><td >Naming Authority Pointer
            Record. General purpose definition of rule set to be used by applications
            e.g. VoIP.</td></tr><tr><td >NSAP</td><td >Network Service Access Point.
            Defines record (equivalent of an A record) maps a host name to an endpoint
            address.</td></tr></tbody></table></p><br/>Note that the parameter is
            not case sensitive, you could specify <b>A</b> or <b>a</b>.'
          enum:
          - A
          - NS
          - MD
          - MF
          - CNAME
          - SOA
          - MB
          - MG
          - MR
          - "NULL"
          - WKS
          - PTR
          - HINFO
          - MINFO
          - MX
          - TXT
          - SPF
          - RP
          - AFSDB
          - X25
          - ISDN
          - RT
          - NSAP
          - NSAP_PTR
          - SIG
          - KEY
          - PX
          - GPOS
          - AAAA
          - LOC
          - NXT
          - EID
          - NIMLOC
          - SRV
          - ATMA
          - NAPTR
          - KX
          - CERT
          - A6
          - DNAME
          - OPT
          - DS
          - DNSSIG
          - NSEC
          - DNSKEY
          - NSEC3
          - NSEC3PARAM
          - CDS
          - CDNSKEY
          - CAA
          - TLSA
          - SSHFP
          - OPENPGPKEY
          - URI
          - AVC
          - NINFO
          - DLV
          - DHCID
          - EUI48
          - EUI64
          - NID
          - L32
          - L64
          type: string
        rr_value1:
          description: The first or only value required for the DNS resource record,
            as detailed in the service description.
          type: string
        check_value:
          description: A way to check the values of the DNS resource record before
            upon addition (<b>1)</b> in order to create a record with the same name
            but with different values.
          enum:
          - "yes"
          - "no"
          type: string
        view_id:
          description: The database identifier (ID) of the DNS view, a unique numeric
            key value automatically incremented when you add a DNS view. Use the ID
            to specify the DNS view of your choice.
          type: integer
        view_name:
          description: The name of the DNS view.
          type: string
        zone_id:
          description: The database identifier (ID) of the DNS zone, a unique numeric
            key value automatically incremented when you add a DNS zone. Use the ID
            to specify the DNS zone of your choice.
          type: integer
        zone_name:
          description: The name of the DNS zone the object belongs to.
          type: string
        zone_space_id:
          description: The database identifier (ID) of the space associated with the
            DNS zone the record belongs to.
          type: integer
        rr_glue:
          description: The shortname of the DNS resource record.
          type: string
        rr_ttl:
          description: The time to live of the DNS resource record, in seconds.
          type: integer
        rr_value2:
          description: The second value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value3:
          description: The third value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value4:
          description: The fourth value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value5:
          description: The fifth value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value6:
          description: The sixth value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value7:
          description: The seventh value of the DNS resource record, depending on
            its type, as detailed in the service description.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        rr_class_name:
          description: TODO:dns_rr_add.input.rr_class_name
          type: string
        rr_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dns_rr_add_success:
      example:
        data:
        - rr_id: rr_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_rr_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_rr_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_rr_edit_success:
      example:
        data:
        - rr_id: rr_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_rr_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_rr_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_rr_delete_success:
      example:
        data:
        - rr_id: rr_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_rr_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_rr_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_server_data:
      example:
        data:
        - server_ipm_protocol: server_ipm_protocol
          server_stat_enabled: server_stat_enabled
          server_dnssec_validation: server_dnssec_validation
          server_ipm_type: server_ipm_type
          server_stat_time: server_stat_time
          server_gss_keytab_id: server_gss_keytab_id
          server_ldap_domain: server_ldap_domain
          server_version: server_version
          server_stat_niceness: server_stat_niceness
          server_recursion: server_recursion
          az_keyid: az_keyid
          server_multistatus: server_multistatus
          smart_parent_name: smart_parent_name
          server_allow_recursion: server_allow_recursion
          server_key_name: server_key_name
          az_tenantid: az_tenantid
          server_key_proto: server_key_proto
          server_also_notify: server_also_notify
          server_allow_query_cache: server_allow_query_cache
          server_notify: server_notify
          ad_domain: ad_domain
          server_stat_period: server_stat_period
          server_id: server_id
          server_addr: server_addr
          server_gslb_supported: server_gslb_supported
          ad_user: ad_user
          server_forward: server_forward
          server_role: server_role
          reverse_proxy_conf: reverse_proxy_conf
          server_querylog_state: server_querylog_state
          server_name: server_name
          smart_parent_arch: smart_parent_arch
          server_gss_enabled: server_gss_enabled
          server_class_name: server_class_name
          az_subscriptionid: az_subscriptionid
          server_forwarders: server_forwarders
          server_hybrid: server_hybrid
          server_guardian_gui_management_supported: server_guardian_gui_management_supported
          smart_parent_id: smart_parent_id
          server_blast_status: server_blast_status
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_ipm_is_package: server_ipm_is_package
          smart_arch: smart_arch
          server_aws_keyid: server_aws_keyid
          server_ipm_login: server_ipm_login
          server_state: server_state
          server_cloud: server_cloud
          server_blast_enabled: server_blast_enabled
          server_comment: server_comment
          server_synching: server_synching
          total_smart_members: total_smart_members
          server_isolated: server_isolated
          server_windns_port: server_windns_port
          server_force_hybrid: server_force_hybrid
          smart_public_ns_list: smart_public_ns_list
          server_key_value: server_key_value
          server_snmp_id: server_snmp_id
          smart_members_name: smart_members_name
          server_allow_query: server_allow_query
          server_ldap_user: server_ldap_user
          server_guardian_supported: server_guardian_supported
          server_windns_protocol: server_windns_protocol
          server_allow_transfer: server_allow_transfer
          server_windns_use_ssl: server_windns_use_ssl
          az_group: az_group
          server_type: server_type
        - server_ipm_protocol: server_ipm_protocol
          server_stat_enabled: server_stat_enabled
          server_dnssec_validation: server_dnssec_validation
          server_ipm_type: server_ipm_type
          server_stat_time: server_stat_time
          server_gss_keytab_id: server_gss_keytab_id
          server_ldap_domain: server_ldap_domain
          server_version: server_version
          server_stat_niceness: server_stat_niceness
          server_recursion: server_recursion
          az_keyid: az_keyid
          server_multistatus: server_multistatus
          smart_parent_name: smart_parent_name
          server_allow_recursion: server_allow_recursion
          server_key_name: server_key_name
          az_tenantid: az_tenantid
          server_key_proto: server_key_proto
          server_also_notify: server_also_notify
          server_allow_query_cache: server_allow_query_cache
          server_notify: server_notify
          ad_domain: ad_domain
          server_stat_period: server_stat_period
          server_id: server_id
          server_addr: server_addr
          server_gslb_supported: server_gslb_supported
          ad_user: ad_user
          server_forward: server_forward
          server_role: server_role
          reverse_proxy_conf: reverse_proxy_conf
          server_querylog_state: server_querylog_state
          server_name: server_name
          smart_parent_arch: smart_parent_arch
          server_gss_enabled: server_gss_enabled
          server_class_name: server_class_name
          az_subscriptionid: az_subscriptionid
          server_forwarders: server_forwarders
          server_hybrid: server_hybrid
          server_guardian_gui_management_supported: server_guardian_gui_management_supported
          smart_parent_id: smart_parent_id
          server_blast_status: server_blast_status
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_ipm_is_package: server_ipm_is_package
          smart_arch: smart_arch
          server_aws_keyid: server_aws_keyid
          server_ipm_login: server_ipm_login
          server_state: server_state
          server_cloud: server_cloud
          server_blast_enabled: server_blast_enabled
          server_comment: server_comment
          server_synching: server_synching
          total_smart_members: total_smart_members
          server_isolated: server_isolated
          server_windns_port: server_windns_port
          server_force_hybrid: server_force_hybrid
          smart_public_ns_list: smart_public_ns_list
          server_key_value: server_key_value
          server_snmp_id: server_snmp_id
          smart_members_name: smart_members_name
          server_allow_query: server_allow_query
          server_ldap_user: server_ldap_user
          server_guardian_supported: server_guardian_supported
          server_windns_protocol: server_windns_protocol
          server_allow_transfer: server_allow_transfer
          server_windns_use_ssl: server_windns_use_ssl
          az_group: az_group
          server_type: server_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_server_data_data'
          type: array
      type: object
    dns_view_data:
      example:
        data:
        - server_name: server_name
          view_order: view_order
          server_class_name: server_class_name
          server_gss_keytab_id: server_gss_keytab_id
          server_version: server_version
          view_allow_transfer: view_allow_transfer
          view_delayed_create_time: view_delayed_create_time
          view_delayed_delete_time: view_delayed_delete_time
          view_recursion: view_recursion
          smart_parent_id: smart_parent_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          smart_parent_name: smart_parent_name
          server_cloud: server_cloud
          view_name: view_name
          server_comment: server_comment
          view_class_name: view_class_name
          view_key_name: view_key_name
          view_id: view_id
          server_id: server_id
          server_addr: server_addr
          view_multistatus: view_multistatus
          view_match_clients: view_match_clients
          view_match_to: view_match_to
          view_allow_recursion: view_allow_recursion
          view_allow_query: view_allow_query
          view_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_type: server_type
        - server_name: server_name
          view_order: view_order
          server_class_name: server_class_name
          server_gss_keytab_id: server_gss_keytab_id
          server_version: server_version
          view_allow_transfer: view_allow_transfer
          view_delayed_create_time: view_delayed_create_time
          view_delayed_delete_time: view_delayed_delete_time
          view_recursion: view_recursion
          smart_parent_id: smart_parent_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          smart_parent_name: smart_parent_name
          server_cloud: server_cloud
          view_name: view_name
          server_comment: server_comment
          view_class_name: view_class_name
          view_key_name: view_key_name
          view_id: view_id
          server_id: server_id
          server_addr: server_addr
          view_multistatus: view_multistatus
          view_match_clients: view_match_clients
          view_match_to: view_match_to
          view_allow_recursion: view_allow_recursion
          view_allow_query: view_allow_query
          view_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_type: server_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_view_data_data'
          type: array
      type: object
    dns_view_add_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DNS server, a unique numeric
            key value automatically incremented when you add a DNS server. Use the
            ID to specify the DNS server of your choice.
          type: integer
        server_name:
          description: The name of the DNS server.
          type: string
        view_name:
          description: The name of the DNS view, each DNS view must have a unique
            name.
          type: string
        server_hostaddr:
          description: The IP address of the DNS server.
          type: string
        view_allow_query:
          description: 'The ACL values associated with the allow-query configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_allow_recursion:
          description: 'The ACL values associated with the allow-recursion configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_allow_transfer:
          description: 'The ACL values associated with the allow-transfer configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_match_clients:
          description: 'The ACL values associated with the match clients configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;...</b>
            . Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_match_to:
          description: 'The ACL values associated with the match destination configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;...</b>
            . Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_order:
          description: The level of the DNS view, where <b>0</b> represents the highest
            level in the views hierarchy. The parameters <b>dnsview_match_client</b>
            and <b>dnsview_match_to</b> of each view in a server are reviewed following
            this order.
          type: integer
        view_recursion:
          description: The recursion status of the DNS view:<table><caption>dnsview_recursion
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >no</td><td >The view only provides iterative query behavior - normally
            resulting in a referral. If the answer to the query already exists in
            the cache it will be returned whatever the value of this statement.</td></tr><tr><td
            >yes</td><td >The view always provides recursive query behavior if requested
            by the client.</td></tr></tbody></table></p><br/>
          enum:
          - "yes"
          - "no"
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        view_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        view_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dns_view_edit_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DNS server, a unique numeric
            key value automatically incremented when you add a DNS server. Use the
            ID to specify the DNS server of your choice.
          type: integer
        server_name:
          description: The name of the DNS server.
          type: string
        view_id:
          description: The database identifier (ID) of the DNS view, a unique numeric
            key value automatically incremented when you add a DNS view. Use the ID
            to specify which DNS view to edit.
          type: integer
        view_name:
          description: The name of the DNS view, each DNS view must have a unique
            name.
          type: string
        server_hostaddr:
          description: The IP address of the DNS server.
          type: string
        view_allow_query:
          description: 'The ACL values associated with the allow-query configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_allow_recursion:
          description: 'The ACL values associated with the allow-recursion configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_allow_transfer:
          description: 'The ACL values associated with the allow-transfer configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_match_clients:
          description: 'The ACL values associated with the match clients configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;...</b>
            . Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_match_to:
          description: 'The ACL values associated with the match destination configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;...</b>
            . Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_order:
          description: The level of the DNS view, where <b>0</b> represents the highest
            level in the views hierarchy. The parameters <b>dnsview_match_client</b>
            and <b>dnsview_match_to</b> of each view in a server are reviewed following
            this order.
          type: integer
        view_recursion:
          description: The recursion status of the DNS view:<table><caption>dnsview_recursion
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >no</td><td >The view only provides iterative query behavior - normally
            resulting in a referral. If the answer to the query already exists in
            the cache it will be returned whatever the value of this statement.</td></tr><tr><td
            >yes</td><td >The view always provides recursive query behavior if requested
            by the client.</td></tr></tbody></table></p><br/>
          enum:
          - "yes"
          - "no"
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        view_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        view_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dns_view_add_success:
      example:
        data:
        - view_id: view_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_view_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_view_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_view_edit_success:
      example:
        data:
        - view_id: view_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_view_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_view_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_view_delete_success:
      example:
        data:
        - view_id: view_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_view_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_view_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_viewparam_data:
      example:
        data:
        - server_name: server_name
          viewparam_key: viewparam_key
          viewparam_delayed_delete_time: viewparam_delayed_delete_time
          viewparam_delayed_create_time: viewparam_delayed_create_time
          viewparam_value: viewparam_value
          view_id: view_id
          oid: oid
          server_id: server_id
          view_name: view_name
        - server_name: server_name
          viewparam_key: viewparam_key
          viewparam_delayed_delete_time: viewparam_delayed_delete_time
          viewparam_delayed_create_time: viewparam_delayed_create_time
          viewparam_value: viewparam_value
          view_id: view_id
          oid: oid
          server_id: server_id
          view_name: view_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_viewparam_data_data'
          type: array
      type: object
    dns_viewparam_add_input:
      properties:
        view_id:
          description: The database identifier (ID) of the DNS view. Use the ID to
            specify the DNS view of your choice.
          type: integer
        viewparam_key:
          description: 'The name of the DNS option you want to add, edit or remove
            from the view. You can only set one option at a time. <ul class=dashed
            ><li>                                                To add or edit an
            option: specify its name in the parameter <b>param_key</b>, as follows
            <b>param_key=&lt;option-name&gt;</b>, and then specify its value in the
            parameter <b>param_value</b>.<br/>                                            </li><li>                                                To
            remove an option, specify its name in the parameter <b>param_key</b> and
            leave the parameter <b>param_value</b> empty.<br/>                                            </li></ul>To
            set several options, specify as many parameters (<b>param_key</b> and
            <b>param_value</b>) as you need.'
          type: string
        viewparam_is_array:
          description: A way to determine is the DNS view option is an array (<b>1</b>).
          type: integer
        viewparam_value:
          description: 'The value of the DNS option specified in the input <b>param_key</b>.<ul
            class=dashed ><li>                                                To add
            or edit an option value, specify its name in the parameter <b>param_key</b>
            and set its value as follows: <b>param_value=&lt;option-value&gt;</b>
            .<br/>                                            </li><li>                                                To
            remove an option value, specify its name in the parameter <b>param_key</b>
            and leave <b>param_value</b> empty: <b>param_value=</b> .<br/>                                            </li></ul>'
          type: string
      type: object
    dns_viewparam_edit_input:
      properties:
        view_id:
          description: The database identifier (ID) of the DNS view. Use the ID to
            specify the DNS view of your choice.
          type: integer
        viewparam_key:
          description: 'The name of the DNS option you want to add, edit or remove
            from the view. You can only set one option at a time. <ul class=dashed
            ><li>                                                To add or edit an
            option: specify its name in the parameter <b>param_key</b>, as follows
            <b>param_key=&lt;option-name&gt;</b>, and then specify its value in the
            parameter <b>param_value</b>.<br/>                                            </li><li>                                                To
            remove an option, specify its name in the parameter <b>param_key</b> and
            leave the parameter <b>param_value</b> empty.<br/>                                            </li></ul>To
            set several options, specify as many parameters (<b>param_key</b> and
            <b>param_value</b>) as you need.'
          type: string
        viewparam_is_array:
          description: A way to determine is the DNS view option is an array (<b>1</b>).
          type: integer
        viewparam_value:
          description: 'The value of the DNS option specified in the input <b>param_key</b>.<ul
            class=dashed ><li>                                                To add
            or edit an option value, specify its name in the parameter <b>param_key</b>
            and set its value as follows: <b>param_value=&lt;option-value&gt;</b>
            .<br/>                                            </li><li>                                                To
            remove an option value, specify its name in the parameter <b>param_key</b>
            and leave <b>param_value</b> empty: <b>param_value=</b> .<br/>                                            </li></ul>'
          type: string
      type: object
    dns_viewparam_add_success:
      example:
        data:
        - viewparam_id: viewparam_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_viewparam_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_viewparam_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_viewparam_edit_success:
      example:
        data:
        - viewparam_id: viewparam_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_viewparam_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_viewparam_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_viewparam_delete_success:
      example:
        data:
        - viewparam_id: viewparam_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_viewparam_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_viewparam_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_zone_data:
      example:
        data:
        - server_ipm_protocol: server_ipm_protocol
          zone_space_id: zone_space_id
          zone_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_ipm_type: server_ipm_type
          server_gss_keytab_id: server_gss_keytab_id
          server_version: server_version
          smart_parent_name: smart_parent_name
          zone_rpz_log: zone_rpz_log
          zone_space_name: zone_space_name
          zone_type: zone_type
          zone_allow_update: zone_allow_update
          zone_rev_sort_zone: zone_rev_sort_zone
          zone_ad_integrated: zone_ad_integrated
          zone_forward: zone_forward
          view_class_name: view_class_name
          zone_allow_transfer: zone_allow_transfer
          zone_also_notify: zone_also_notify
          zone_synching: zone_synching
          zone_use_update_policy: zone_use_update_policy
          server_id: server_id
          server_addr: server_addr
          zone_xfer_done: zone_xfer_done
          zone_masters: zone_masters
          zone_is_reverse: zone_is_reverse
          zone_notify: zone_notify
          view_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_name: server_name
          zone_num_keys: zone_num_keys
          zone_sort_zone: zone_sort_zone
          zone_forwarders: zone_forwarders
          server_class_name: server_class_name
          zone_class_name: zone_class_name
          zone_name: zone_name
          zone_order: zone_order
          zone_delayed_create_time: zone_delayed_create_time
          zone_id: zone_id
          smart_parent_id: smart_parent_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          zone_ds: zone_ds
          server_state: server_state
          server_cloud: server_cloud
          view_name: view_name
          server_comment: server_comment
          view_id: view_id
          zone_delayed_delete_time: zone_delayed_delete_time
          zone_is_rpz: zone_is_rpz
          server_force_hybrid: server_force_hybrid
          zone_name_utf: zone_name_utf
          zone_allow_query: zone_allow_query
          zone_multistatus: zone_multistatus
          server_type: server_type
        - server_ipm_protocol: server_ipm_protocol
          zone_space_id: zone_space_id
          zone_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_ipm_type: server_ipm_type
          server_gss_keytab_id: server_gss_keytab_id
          server_version: server_version
          smart_parent_name: smart_parent_name
          zone_rpz_log: zone_rpz_log
          zone_space_name: zone_space_name
          zone_type: zone_type
          zone_allow_update: zone_allow_update
          zone_rev_sort_zone: zone_rev_sort_zone
          zone_ad_integrated: zone_ad_integrated
          zone_forward: zone_forward
          view_class_name: view_class_name
          zone_allow_transfer: zone_allow_transfer
          zone_also_notify: zone_also_notify
          zone_synching: zone_synching
          zone_use_update_policy: zone_use_update_policy
          server_id: server_id
          server_addr: server_addr
          zone_xfer_done: zone_xfer_done
          zone_masters: zone_masters
          zone_is_reverse: zone_is_reverse
          zone_notify: zone_notify
          view_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          server_name: server_name
          zone_num_keys: zone_num_keys
          zone_sort_zone: zone_sort_zone
          zone_forwarders: zone_forwarders
          server_class_name: server_class_name
          zone_class_name: zone_class_name
          zone_name: zone_name
          zone_order: zone_order
          zone_delayed_create_time: zone_delayed_create_time
          zone_id: zone_id
          smart_parent_id: smart_parent_id
          server_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          zone_ds: zone_ds
          server_state: server_state
          server_cloud: server_cloud
          view_name: view_name
          server_comment: server_comment
          view_id: view_id
          zone_delayed_delete_time: zone_delayed_delete_time
          zone_is_rpz: zone_is_rpz
          server_force_hybrid: server_force_hybrid
          zone_name_utf: zone_name_utf
          zone_allow_query: zone_allow_query
          zone_multistatus: zone_multistatus
          server_type: server_type
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_zone_data_data'
          type: array
      type: object
    dns_zone_add_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DNS server, a unique numeric
            key value automatically incremented when you add a DNS server. Use the
            ID to specify the DNS server of your choice.
          type: integer
        server_name:
          description: The name of the DNS server.
          type: string
        view_id:
          description: The database identifier (ID) of the DNS view the object belongs
            to.
          type: integer
        view_name:
          description: The name of the DNS view the object belongs to.
          type: string
        zone_name:
          description: 'The name of the DNS zone, each DNS zone must have a unique
            name. For hint zones (<b>dnszone_type</b>: <b>hint</b>), you must specify
            <b>.</b> (dot) as <b>dnszone_name</b>.'
          type: string
        zone_type:
          description: The type of the DNS zone, either <b>master</b>, <b>slave</b>,
            <b>forward</b>, <b>stub</b>, <b>hint</b> or <b>delegation-only</b>.
          enum:
          - master
          - slave
          - hint
          - stub
          - forward
          - delegation-only
          type: string
        server_hostaddr:
          description: The IP address of the DNS server.
          type: string
        zone_ad_integrated:
          description: The AD integrated status of the DNS zone. Set it to <b>1</b>
            to indicate that the DNS zone belongs to an Active Directory integrated
            Microsoft DNS server.
          type: integer
        zone_allow_query:
          description: 'The ACL values associated with the allow-query configuration
            of the DNS zone, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        zone_allow_transfer:
          description: 'The ACL values associated with the allow-transfer configuration
            of the DNS zone, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        zone_allow_update:
          description: 'The ACL values associated with the allow-update configuration
            of the DNS zone, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        zone_also_notify:
          description: The IP address and port of the DNS server managing the smart
            architecture the DNS zone belongs to. If the parameter <b>dnszone_notify</b>
            is set to <b>yes</b> or <b>explicit</b>, the server specified is instantly
            notified of any slave zones updates.
          type: string
        zone_forward:
          description: The forwarding mode of the DNS zone.<table><caption>dnszone_forward
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >first</td><td >The zone sends the queries to the forwarder(s). If no
            answer is returned, it attempts to answer the queries on its own.</td></tr><tr><td
            >only</td><td >The zone only forwards the queries to the forwarder(s).
            Required by some reverse forward zones (e.g., in the case of private addresses).</td></tr></tbody></table></p><br/>
            If the parameter has no value, it indicates that the forwarding is disabled.
          enum:
          - none
          - first
          - only
          - default
          type: string
        zone_forwarders:
          description: 'The IP address(es) of the forwarder(s) associated with the
            DNS zone. It lists the DNS servers to which any unknown query on this
            zone should be sent, as follows: <b>&lt;ip_address1&gt;;&lt;ip_address2&gt;;...</b>
            .'
          type: string
        zone_is_rpz:
          description: The RPZ status of the DNS zone. Set it to <b>1</b> to indicate
            that the DNS zone is a Response Policy Zone.
          type: integer
        zone_masters:
          description: 'For slave DNS zones, the IP address of the DNS server and,
            if relevant, the name of the DNS view that contain the master DNS zone,
            as follows: <b>&lt;ip_addr&gt;;</b> or <b>&lt;ip_addr&gt; key &lt;dnsview_name&gt;;</b>
            .'
          type: string
        zone_notify:
          description: The notify status of the DNS zone.<table><caption>dnszone_notify
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >no</td><td >No notify message is sent.</td></tr><tr><td >yes</td><td
            >A notify message is sent to the name servers defined in the NS records
            of the zone and to the IP address(es) specified in the parameter .</td></tr><tr><td
            >explicit</td><td >A notify message is sent only to the IP address(es)
            specified in the parameter .</td></tr></tbody></table></p><br/>The notify
            message is not sent to the server itself or to the primary server defined
            in the SOA record of the zone.
          enum:
          - "yes"
          - "no"
          - explicit
          type: string
        zone_order:
          description: The level of the DNS RPZ zone, where 0 represents the highest
            level in the views hierarchy. The RPZ rules of each zone are reviewed
            following this order. For non-RPZ zones, that have their parameter zone_is_rpz
            set to <b>0</b>, you do not need to set this parameter.
          type: integer
        zone_response_policy:
          description: The response policy of the DNS zone.<table><caption>dnszone_response_policy
            possible values</caption><br/><thead><tr><th>Policy</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >given</td><td >All the rules specified in the RPZ zone are applied normally.</td></tr><tr><td
            >disabled</td><td >The RPZ zone rules configuration is not applied. All
            the rules it contains are ignored.</td></tr><tr><td >passthru</td><td
            >The rules specified in the RPZ matching the listed RR names are ignored,
            no matter the RPZ zone they belong to.</td></tr><tr><td >nxdomain</td><td
            >The rules specified in the RPZ return an NXDOMAIN response.</td></tr><tr><td
            >nodata</td><td >The rules specified in the RPZ return a NODATA response.</td></tr><tr><td
            >cname <domain-name></td><td >All the rules specified in the RPZ are redirected
            toward the specified domain name.</td></tr></tbody></table></p><br/>You
            can only add RPZ zones on EfficientIP or BIND DNS servers.
          type: string
        zone_rpz_log:
          description: TODO:dns_zone_add.input.zone_rpz_log
          type: integer
        zone_space_id:
          description: The database identifier (ID) of the space associated with the
            DNS zone the record belongs to.
          type: integer
        zone_space_name:
          description: The name of the space associated with the DNS zone the record
            belongs to.
          type: string
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          type: integer
        zone_use_update_policy:
          description: The update policy status of the DNS zone. Set it to <b>1</b>
            to indicate that the DNS zone uses a specific GSS-TSIG/update-policy.
            You can only configure the zone update policy if the parameter <b>gss_enabled</b>
            of the server it belongs to is set to <b>1</b>.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        zone_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        zone_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dns_zone_edit_input:
      properties:
        server_id:
          description: The database identifier (ID) of the DNS server, a unique numeric
            key value automatically incremented when you add a DNS server. Use the
            ID to specify the DNS server of your choice.
          type: integer
        server_name:
          description: The name of the DNS server.
          type: string
        view_id:
          description: The database identifier (ID) of the DNS view the object belongs
            to.
          type: integer
        view_name:
          description: The name of the DNS view the object belongs to.
          type: string
        zone_id:
          description: The database identifier (ID) of the DNS zone, a unique numeric
            key value automatically incremented when you add a DNS zone. Use the ID
            to specify which DNS zone to edit.
          type: integer
        zone_name:
          description: 'The name of the DNS zone, each DNS zone must have a unique
            name. For hint zones (<b>dnszone_type</b>: <b>hint</b>), you must specify
            <b>.</b> (dot) as <b>dnszone_name</b>.'
          type: string
        zone_type:
          description: The type of the DNS zone, either <b>master</b>, <b>slave</b>,
            <b>forward</b>, <b>stub</b>, <b>hint</b> or <b>delegation-only</b>.
          enum:
          - master
          - slave
          - hint
          - stub
          - forward
          - delegation-only
          type: string
        server_hostaddr:
          description: The IP address of the DNS server.
          type: string
        zone_ad_integrated:
          description: The AD integrated status of the DNS zone. Set it to <b>1</b>
            to indicate that the DNS zone belongs to an Active Directory integrated
            Microsoft DNS server.
          type: integer
        zone_allow_query:
          description: 'The ACL values associated with the allow-query configuration
            of the DNS zone, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        zone_allow_transfer:
          description: 'The ACL values associated with the allow-transfer configuration
            of the DNS zone, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        zone_allow_update:
          description: 'The ACL values associated with the allow-update configuration
            of the DNS zone, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        zone_also_notify:
          description: The IP address and port of the DNS server managing the smart
            architecture the DNS zone belongs to. If the parameter <b>dnszone_notify</b>
            is set to <b>yes</b> or <b>explicit</b>, the server specified is instantly
            notified of any slave zones updates.
          type: string
        zone_forward:
          description: The forwarding mode of the DNS zone.<table><caption>dnszone_forward
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >first</td><td >The zone sends the queries to the forwarder(s). If no
            answer is returned, it attempts to answer the queries on its own.</td></tr><tr><td
            >only</td><td >The zone only forwards the queries to the forwarder(s).
            Required by some reverse forward zones (e.g., in the case of private addresses).</td></tr></tbody></table></p><br/>
            If the parameter has no value, it indicates that the forwarding is disabled.
          enum:
          - none
          - first
          - only
          - default
          type: string
        zone_forwarders:
          description: 'The IP address(es) of the forwarder(s) associated with the
            DNS zone. It lists the DNS servers to which any unknown query on this
            zone should be sent, as follows: <b>&lt;ip_address1&gt;;&lt;ip_address2&gt;;...</b>
            .'
          type: string
        zone_is_rpz:
          description: The RPZ status of the DNS zone. Set it to <b>1</b> to indicate
            that the DNS zone is a Response Policy Zone.
          type: integer
        zone_masters:
          description: 'For slave DNS zones, the IP address of the DNS server and,
            if relevant, the name of the DNS view that contain the master DNS zone,
            as follows: <b>&lt;ip_addr&gt;;</b> or <b>&lt;ip_addr&gt; key &lt;dnsview_name&gt;;</b>
            .'
          type: string
        zone_notify:
          description: The notify status of the DNS zone.<table><caption>dnszone_notify
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >no</td><td >No notify message is sent.</td></tr><tr><td >yes</td><td
            >A notify message is sent to the name servers defined in the NS records
            of the zone and to the IP address(es) specified in the parameter .</td></tr><tr><td
            >explicit</td><td >A notify message is sent only to the IP address(es)
            specified in the parameter .</td></tr></tbody></table></p><br/>The notify
            message is not sent to the server itself or to the primary server defined
            in the SOA record of the zone.
          enum:
          - "yes"
          - "no"
          - explicit
          type: string
        zone_order:
          description: The level of the DNS RPZ zone, where 0 represents the highest
            level in the views hierarchy. The RPZ rules of each zone are reviewed
            following this order. For non-RPZ zones, that have their parameter zone_is_rpz
            set to <b>0</b>, you do not need to set this parameter.
          type: integer
        zone_response_policy:
          description: The response policy of the DNS zone.<table><caption>dnszone_response_policy
            possible values</caption><br/><thead><tr><th>Policy</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >given</td><td >All the rules specified in the RPZ zone are applied normally.</td></tr><tr><td
            >disabled</td><td >The RPZ zone rules configuration is not applied. All
            the rules it contains are ignored.</td></tr><tr><td >passthru</td><td
            >The rules specified in the RPZ matching the listed RR names are ignored,
            no matter the RPZ zone they belong to.</td></tr><tr><td >nxdomain</td><td
            >The rules specified in the RPZ return an NXDOMAIN response.</td></tr><tr><td
            >nodata</td><td >The rules specified in the RPZ return a NODATA response.</td></tr><tr><td
            >cname <domain-name></td><td >All the rules specified in the RPZ are redirected
            toward the specified domain name.</td></tr></tbody></table></p><br/>You
            can only add RPZ zones on EfficientIP or BIND DNS servers.
          type: string
        zone_rpz_log:
          description: TODO:dns_zone_add.input.zone_rpz_log
          type: integer
        zone_space_id:
          description: The database identifier (ID) of the space associated with the
            DNS zone the record belongs to.
          type: integer
        zone_space_name:
          description: The name of the space associated with the DNS zone the record
            belongs to.
          type: string
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          type: integer
        zone_use_update_policy:
          description: The update policy status of the DNS zone. Set it to <b>1</b>
            to indicate that the DNS zone uses a specific GSS-TSIG/update-policy.
            You can only configure the zone update policy if the parameter <b>gss_enabled</b>
            of the server it belongs to is set to <b>1</b>.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        zone_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        zone_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    dns_zone_add_success:
      example:
        data:
        - zone_id: zone_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_zone_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_zone_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_zone_edit_success:
      example:
        data:
        - zone_id: zone_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_zone_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_zone_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_zone_delete_success:
      example:
        data:
        - zone_id: zone_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_zone_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_zone_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_zoneparam_data:
      example:
        data:
        - server_name: server_name
          zone_id: zone_id
          zoneparam_key: zoneparam_key
          zoneparam_delayed_delete_time: zoneparam_delayed_delete_time
          zoneparam_delayed_create_time: zoneparam_delayed_create_time
          zoneparam_value: zoneparam_value
          zone_name: zone_name
          oid: oid
          server_id: server_id
        - server_name: server_name
          zone_id: zone_id
          zoneparam_key: zoneparam_key
          zoneparam_delayed_delete_time: zoneparam_delayed_delete_time
          zoneparam_delayed_create_time: zoneparam_delayed_create_time
          zoneparam_value: zoneparam_value
          zone_name: zone_name
          oid: oid
          server_id: server_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_zoneparam_data_data'
          type: array
      type: object
    dns_zoneparam_add_input:
      properties:
        zone_id:
          description: The database identifier (ID) of the DNS zone, a unique numeric
            key value automatically incremented when you add a DNS zone. Use the ID
            to specify the DNS zone of your choice.
          type: integer
        zoneparam_key:
          description: 'The name of the DNS option you want to add, edit or remove
            from the zone. You can only set one option at a time. <ul class=dashed
            ><li>                                                To add or edit an
            option: specify its name in the parameter <b>param_key</b>, as follows
            <b>param_key=&lt;option-name&gt;</b>, and then specify its value in the
            parameter <b>param_value</b>.<br/>                                            </li><li>                                                To
            remove an option, specify its name in the parameter <b>param_key</b> and
            leave the parameter <b>param_value</b> empty.<br/>                                            </li></ul>To
            set several options, specify as many parameters (<b>param_key</b> and
            <b>param_value</b>) as you need.'
          type: string
        zoneparam_is_array:
          description: A way to determine is the DNS zone option is an array (<b>1</b>).
          type: integer
        zoneparam_value:
          description: 'The value of the DNS option specified in the input <b>param_key</b>.<ul
            class=dashed ><li>                                                To add
            or edit an option value, specify its name in the parameter <b>param_key</b>
            and set its value as follows: <b>param_value=&lt;option-value&gt;</b>
            .<br/>                                            </li><li>                                                To
            remove an option value, specify its name in the parameter <b>param_key</b>
            and leave <b>param_value</b> empty: <b>param_value=</b> .<br/>                                            </li></ul>'
          type: string
      type: object
    dns_zoneparam_edit_input:
      properties:
        zone_id:
          description: The database identifier (ID) of the DNS zone, a unique numeric
            key value automatically incremented when you add a DNS zone. Use the ID
            to specify the DNS zone of your choice.
          type: integer
        zoneparam_key:
          description: 'The name of the DNS option you want to add, edit or remove
            from the zone. You can only set one option at a time. <ul class=dashed
            ><li>                                                To add or edit an
            option: specify its name in the parameter <b>param_key</b>, as follows
            <b>param_key=&lt;option-name&gt;</b>, and then specify its value in the
            parameter <b>param_value</b>.<br/>                                            </li><li>                                                To
            remove an option, specify its name in the parameter <b>param_key</b> and
            leave the parameter <b>param_value</b> empty.<br/>                                            </li></ul>To
            set several options, specify as many parameters (<b>param_key</b> and
            <b>param_value</b>) as you need.'
          type: string
        zoneparam_is_array:
          description: A way to determine is the DNS zone option is an array (<b>1</b>).
          type: integer
        zoneparam_value:
          description: 'The value of the DNS option specified in the input <b>param_key</b>.<ul
            class=dashed ><li>                                                To add
            or edit an option value, specify its name in the parameter <b>param_key</b>
            and set its value as follows: <b>param_value=&lt;option-value&gt;</b>
            .<br/>                                            </li><li>                                                To
            remove an option value, specify its name in the parameter <b>param_key</b>
            and leave <b>param_value</b> empty: <b>param_value=</b> .<br/>                                            </li></ul>'
          type: string
      type: object
    dns_zoneparam_add_success:
      example:
        data:
        - zoneparam_id: zoneparam_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_zoneparam_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_zoneparam_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_zoneparam_edit_success:
      example:
        data:
        - zoneparam_id: zoneparam_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_zoneparam_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_zoneparam_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    dns_zoneparam_delete_success:
      example:
        data:
        - zoneparam_id: zoneparam_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dns_zoneparam_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    dns_zoneparam_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    guardian_policy_data:
      example:
        data:
        - server_name: server_name
          policy_id: policy_id
          policy_description: policy_description
          policy_name: policy_name
          server_id: server_id
          policy_delayed_time: policy_delayed_time
          parent_policy_id: parent_policy_id
          policy_readonly: policy_readonly
        - server_name: server_name
          policy_id: policy_id
          policy_description: policy_description
          policy_name: policy_name
          server_id: server_id
          policy_delayed_time: policy_delayed_time
          parent_policy_id: parent_policy_id
          policy_readonly: policy_readonly
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/guardian_policy_data_data'
          type: array
      type: object
    guardian_policy_add_input:
      properties:
        policy_name:
          description: The name of the policy.
          type: string
        description:
          description: The description of the policy.
          type: string
        server_list:
          description: The name of all the Guardian servers associated with the policy.
            You can specify one or more names.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    guardian_policy_edit_input:
      properties:
        policy_id:
          description: The database identifier (ID) of the policy, a unique identifier
            automatically incremented when you add the policy. Use it to identify
            the policy of your choice.
          type: integer
        policy_name:
          description: The name of the policy.
          type: string
        description:
          description: The description of the policy.
          type: string
        server_list:
          description: The name of all the Guardian servers associated with the policy.
            You can specify one or more names.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    guardian_policy_add_success:
      example:
        data:
        - policy_id: policy_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/guardian_policy_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    guardian_policy_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    guardian_policy_edit_success:
      example:
        data:
        - policy_id: policy_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/guardian_policy_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    guardian_policy_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    guardian_policy_delete_success:
      example:
        data:
        - policy_id: policy_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/guardian_policy_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    guardian_policy_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_address_data:
      example:
        data:
        - space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          address_alias: address_alias
          network_is_terminal: network_is_terminal
          pool_class_name: pool_class_name
          free_end_address_addr: free_end_address_addr
          address_class_name: address_class_name
          space_class_name: space_class_name
          port_portnumber: port_portnumber
          space_description: space_description
          parent_network_end_hostaddr: parent_network_end_hostaddr
          device_id: device_id
          network_size: network_size
          tag_container_dhcpstatic: tag_container_dhcpstatic
          port_ifvlan: port_ifvlan
          space_is_template: space_is_template
          address_id: address_id
          network_name: network_name
          parent_network_class_name: parent_network_class_name
          network_id: network_id
          space_name: space_name
          network_start_address_addr: network_start_address_addr
          pool_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network_class_name: network_class_name
          free_start_address_addr: free_start_address_addr
          address_hostaddr: address_hostaddr
          interface_name: interface_name
          pool_start_address_addr: pool_start_address_addr
          address_multistatus: address_multistatus
          address_name: address_name
          port_slotnumber: port_slotnumber
          parent_network_name: parent_network_name
          free_scope_size: free_scope_size
          tag_pool_dhcprange: tag_pool_dhcprange
          device_name: device_name
          parent_network_start_hostaddr: parent_network_start_hostaddr
          address_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          parent_network_size: parent_network_size
          pool_end_address_addr: pool_end_address_addr
          parent_vlsm_network_id: parent_vlsm_network_id
          dev_name: dev_name
          network_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          address_addr: address_addr
          network_lock_network_broadcast: network_lock_network_broadcast
          address_type: address_type
          static_id: static_id
          pool_size: pool_size
          pool_read_only: pool_read_only
          address_mac_addr: address_mac_addr
          pool_id: pool_id
          address_mac_last_seen: address_mac_last_seen
          parent_network_start_address_addr: parent_network_start_address_addr
          pool_name: pool_name
          dev_id: dev_id
          port_name: port_name
          lease_end_time: lease_end_time
          interface_id: interface_id
          parent_network_id: parent_network_id
          space_id: space_id
          network_end_address_addr: network_end_address_addr
          lease_id: lease_id
          parent_network_end_address_addr: parent_network_end_address_addr
        - space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          address_alias: address_alias
          network_is_terminal: network_is_terminal
          pool_class_name: pool_class_name
          free_end_address_addr: free_end_address_addr
          address_class_name: address_class_name
          space_class_name: space_class_name
          port_portnumber: port_portnumber
          space_description: space_description
          parent_network_end_hostaddr: parent_network_end_hostaddr
          device_id: device_id
          network_size: network_size
          tag_container_dhcpstatic: tag_container_dhcpstatic
          port_ifvlan: port_ifvlan
          space_is_template: space_is_template
          address_id: address_id
          network_name: network_name
          parent_network_class_name: parent_network_class_name
          network_id: network_id
          space_name: space_name
          network_start_address_addr: network_start_address_addr
          pool_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network_class_name: network_class_name
          free_start_address_addr: free_start_address_addr
          address_hostaddr: address_hostaddr
          interface_name: interface_name
          pool_start_address_addr: pool_start_address_addr
          address_multistatus: address_multistatus
          address_name: address_name
          port_slotnumber: port_slotnumber
          parent_network_name: parent_network_name
          free_scope_size: free_scope_size
          tag_pool_dhcprange: tag_pool_dhcprange
          device_name: device_name
          parent_network_start_hostaddr: parent_network_start_hostaddr
          address_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          parent_network_size: parent_network_size
          pool_end_address_addr: pool_end_address_addr
          parent_vlsm_network_id: parent_vlsm_network_id
          dev_name: dev_name
          network_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          address_addr: address_addr
          network_lock_network_broadcast: network_lock_network_broadcast
          address_type: address_type
          static_id: static_id
          pool_size: pool_size
          pool_read_only: pool_read_only
          address_mac_addr: address_mac_addr
          pool_id: pool_id
          address_mac_last_seen: address_mac_last_seen
          parent_network_start_address_addr: parent_network_start_address_addr
          pool_name: pool_name
          dev_id: dev_id
          port_name: port_name
          lease_end_time: lease_end_time
          interface_id: interface_id
          parent_network_id: parent_network_id
          space_id: space_id
          network_end_address_addr: network_end_address_addr
          lease_id: lease_id
          parent_network_end_address_addr: parent_network_end_address_addr
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_address_data_data'
          type: array
      type: object
    ipam_address_add_input:
      properties:
        address_hostaddr:
          description: The IP address.
          type: string
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        check_is_server_ip:
          description: A way to force a validity check, if you configured the IPAM
            to DHCP replication. If the check is enabled (<b>1</b>), the configuration
            of the IP address you are adding must be valid as well for the DHCP.
          type: integer
        static_id:
          description: The database identifier (ID) of the DHCP static you want to
            associate with the IP address.
          type: integer
        lease_id:
          description: The database identifier (ID) of the DHCP lease you want to
            associate with the IP address.
          type: integer
        device_id:
          description: The database identifier (ID) of the Device Manager device you
            want to associate with the IP address.
          type: integer
        interface_id:
          description: The database identifier (ID) of the Device Manager interface
            you want to associate with the IP address.
          type: integer
        port_id:
          description: The database identifier (ID) of the NetChange port you want
            to associate with the IP address.
          type: integer
        address_mac_addr:
          description: The MAC address you want to associate with the IPv4 address.
          type: string
        address_name:
          description: The name of the IPv4 address, each IPv4 address must have a
            unique name.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        address_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        address_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_address_edit_input:
      properties:
        address_hostaddr:
          description: The IP address.
          type: string
        address_id:
          description: The database identifier (ID) of the IPv4 address, a unique
            numeric key value automatically incremented when you add an IPv4 address.
            Use the ID to specify which IPv4 address to edit.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        check_is_server_ip:
          description: A way to force a validity check, if you configured the IPAM
            to DHCP replication. If the check is enabled (<b>1</b>), the configuration
            of the IP address you are adding must be valid as well for the DHCP.
          type: integer
        static_id:
          description: The database identifier (ID) of the DHCP static you want to
            associate with the IP address.
          type: integer
        lease_id:
          description: The database identifier (ID) of the DHCP lease you want to
            associate with the IP address.
          type: integer
        device_id:
          description: The database identifier (ID) of the Device Manager device you
            want to associate with the IP address.
          type: integer
        interface_id:
          description: The database identifier (ID) of the Device Manager interface
            you want to associate with the IP address.
          type: integer
        port_id:
          description: The database identifier (ID) of the NetChange port you want
            to associate with the IP address.
          type: integer
        address_mac_addr:
          description: The MAC address you want to associate with the IPv4 address.
          type: string
        address_name:
          description: The name of the IPv4 address, each IPv4 address must have a
            unique name.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        address_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        address_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_address_add_success:
      example:
        data:
        - address_id: address_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_address_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_address_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_address_edit_success:
      example:
        data:
        - address_id: address_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_address_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_address_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_address_delete_success:
      example:
        data:
        - address_id: address_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_address_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_address_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_address6_data:
      example:
        data:
        - parent_network6_class_name: parent_network6_class_name
          pool6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          address6_multistatus: address6_multistatus
          parent_network6_prefix: parent_network6_prefix
          space_class_name: space_class_name
          space_description: space_description
          address6_alias: address6_alias
          free_start_address6_addr: free_start_address6_addr
          parent_network6_end_hostaddr: parent_network6_end_hostaddr
          network6_start_address6_addr: network6_start_address6_addr
          pool6_size: pool6_size
          parent_vlsm_network6_id: parent_vlsm_network6_id
          device_id: device_id
          network_size: network_size
          network6_is_terminal: network6_is_terminal
          vlsm_network6_id: vlsm_network6_id
          address6_type: address6_type
          pool6_id: pool6_id
          pool6_name: pool6_name
          space_name: space_name
          network6_id: network6_id
          pool6_end_address6_addr: pool6_end_address6_addr
          interface_name: interface_name
          parent_network6_id: parent_network6_id
          network6_name: network6_name
          free_scope_size: free_scope_size
          free_end_address6_addr: free_end_address6_addr
          device_name: device_name
          address6_name: address6_name
          parent_network6_start_address6_addr: parent_network6_start_address6_addr
          parent_network6_start_hostaddr: parent_network6_start_hostaddr
          pool6_read_only: pool6_read_only
          pool6_class_name: pool6_class_name
          network6_end_address6_addr: network6_end_address6_addr
          address6_mac_addr: address6_mac_addr
          parent_space_name: parent_space_name
          network6_prefix: network6_prefix
          address6_id: address6_id
          network6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          parent_network6_size: parent_network6_size
          parent_network6_name: parent_network6_name
          parent_network6_end_address6_addr: parent_network6_end_address6_addr
          network6_lock_network_broadcast: network6_lock_network_broadcast
          network6_size: network6_size
          interface_id: interface_id
          address6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network6_class_name: network6_class_name
          address6_addr: address6_addr
          address6_hostaddr: address6_hostaddr
          space_id: space_id
          address6_class_name: address6_class_name
          pool6_start_address6_addr: pool6_start_address6_addr
        - parent_network6_class_name: parent_network6_class_name
          pool6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          address6_multistatus: address6_multistatus
          parent_network6_prefix: parent_network6_prefix
          space_class_name: space_class_name
          space_description: space_description
          address6_alias: address6_alias
          free_start_address6_addr: free_start_address6_addr
          parent_network6_end_hostaddr: parent_network6_end_hostaddr
          network6_start_address6_addr: network6_start_address6_addr
          pool6_size: pool6_size
          parent_vlsm_network6_id: parent_vlsm_network6_id
          device_id: device_id
          network_size: network_size
          network6_is_terminal: network6_is_terminal
          vlsm_network6_id: vlsm_network6_id
          address6_type: address6_type
          pool6_id: pool6_id
          pool6_name: pool6_name
          space_name: space_name
          network6_id: network6_id
          pool6_end_address6_addr: pool6_end_address6_addr
          interface_name: interface_name
          parent_network6_id: parent_network6_id
          network6_name: network6_name
          free_scope_size: free_scope_size
          free_end_address6_addr: free_end_address6_addr
          device_name: device_name
          address6_name: address6_name
          parent_network6_start_address6_addr: parent_network6_start_address6_addr
          parent_network6_start_hostaddr: parent_network6_start_hostaddr
          pool6_read_only: pool6_read_only
          pool6_class_name: pool6_class_name
          network6_end_address6_addr: network6_end_address6_addr
          address6_mac_addr: address6_mac_addr
          parent_space_name: parent_space_name
          network6_prefix: network6_prefix
          address6_id: address6_id
          network6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          parent_network6_size: parent_network6_size
          parent_network6_name: parent_network6_name
          parent_network6_end_address6_addr: parent_network6_end_address6_addr
          network6_lock_network_broadcast: network6_lock_network_broadcast
          network6_size: network6_size
          interface_id: interface_id
          address6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network6_class_name: network6_class_name
          address6_addr: address6_addr
          address6_hostaddr: address6_hostaddr
          space_id: space_id
          address6_class_name: address6_class_name
          pool6_start_address6_addr: pool6_start_address6_addr
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_address6_data_data'
          type: array
      type: object
    ipam_address6_add_input:
      properties:
        address6_hostaddr:
          description: The IP address.
          type: string
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        device_id:
          description: The database identifier (ID) of the Device Manager device you
            want to associate with the IP address.
          type: integer
        interface_id:
          description: The database identifier (ID) of the Device Manager interface
            you want to associate with the IP address.
          type: integer
        address6_mac_addr:
          description: The MAC address you want to associate with the IPv6 address.
          type: string
        address6_name:
          description: The name of the IPv6 address, each IPv6 address must have a
            unique name.
          type: string
        port_id:
          description: The database identifier (ID) of the NetChange port you want
            to associate with the IP address.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        address6_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        address6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_address6_edit_input:
      properties:
        address6_hostaddr:
          description: The IP address.
          type: string
        address6_id:
          description: The database identifier (ID) of the IPv6 address, a unique
            numeric key value automatically incremented when you add an IPv6 address.
            Use the ID to specify which IPv6 address to edit.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        device_id:
          description: The database identifier (ID) of the Device Manager device you
            want to associate with the IP address.
          type: integer
        interface_id:
          description: The database identifier (ID) of the Device Manager interface
            you want to associate with the IP address.
          type: integer
        address6_mac_addr:
          description: The MAC address you want to associate with the IPv6 address.
          type: string
        address6_name:
          description: The name of the IPv6 address, each IPv6 address must have a
            unique name.
          type: string
        port_id:
          description: The database identifier (ID) of the NetChange port you want
            to associate with the IP address.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        address6_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        address6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_address6_add_success:
      example:
        data:
        - address6_id: address6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_address6_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_address6_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_address6_edit_success:
      example:
        data:
        - address6_id: address6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_address6_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_address6_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_address6_delete_success:
      example:
        data:
        - address6_id: address6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_address6_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_address6_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_alias_data:
      example:
        data:
        - device_id: device_id
          address_addr: address_addr
          address_type: address_type
          static_id: static_id
          alias_id: alias_id
          address_id: address_id
          address_name: address_name
          alias_name: alias_name
          address_mac_addr: address_mac_addr
          pool_id: pool_id
          alias_type: alias_type
          network_id: network_id
          interface_id: interface_id
          address_class_name: address_class_name
          address_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_name: space_name
          space_class_name: space_class_name
          port_id: port_id
          space_id: space_id
          lease_id: lease_id
        - device_id: device_id
          address_addr: address_addr
          address_type: address_type
          static_id: static_id
          alias_id: alias_id
          address_id: address_id
          address_name: address_name
          alias_name: alias_name
          address_mac_addr: address_mac_addr
          pool_id: pool_id
          alias_type: alias_type
          network_id: network_id
          interface_id: interface_id
          address_class_name: address_class_name
          address_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_name: space_name
          space_class_name: space_class_name
          port_id: port_id
          space_id: space_id
          lease_id: lease_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_alias_data_data'
          type: array
      type: object
    ipam_alias_add_input:
      properties:
        address_hostaddr:
          description: 'todo(here) :ipam.alias.add.input.address_hostaddr. : IPv4
            address'
          type: string
        address_id:
          description: The database identifier (ID) of the IPv4 address, a unique
            numeric key value automatically incremented when you add an IPv4 address.
            Use the ID to specify the IPv4 address of your choice.
          type: integer
        alias_name:
          description: 'todo(here) :ipam.alias.add.input.alias_name. : String'
          type: string
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        alias_type:
          description: The type of the alias.
          enum:
          - A
          - a
          - CNAME
          - cname
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_alias_edit_input:
      properties:
        address_hostaddr:
          description: 'todo(here) :ipam.alias.edit.input.address_hostaddr. : IPv4
            address'
          type: string
        address_id:
          description: The database identifier (ID) of the IPv4 address, a unique
            numeric key value automatically incremented when you add an IPv4 address.
            Use the ID to specify the IPv4 address of your choice.
          type: integer
        alias_name:
          description: 'todo(here) :ipam.alias.edit.input.alias_name. : String'
          type: string
        alias_id:
          description: The database identifier (ID) of the IPv4 alias, a unique numeric
            key value automatically incremented when you add an IPv4 alias. Use the
            ID to specify which IPv4 alias to edit.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        alias_type:
          description: The type of the alias.
          enum:
          - A
          - a
          - CNAME
          - cname
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_alias_add_success:
      example:
        data:
        - alias_id: alias_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_alias_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_alias_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_alias_edit_success:
      example:
        data:
        - alias_id: alias_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_alias_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_alias_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_alias_delete_success:
      example:
        data:
        - alias_id: alias_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_alias_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_alias_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_alias6_data:
      example:
        data:
        - address6_id: address6_id
          device_id: device_id
          alias_name: alias_name
          pool6_id: pool6_id
          interface_id: interface_id
          address6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          address6_name: address6_name
          space_name: space_name
          network6_id: network6_id
          space_class_name: space_class_name
          alias6_type: alias6_type
          port_id: port_id
          address6_addr: address6_addr
          alias6_id: alias6_id
          address6_mac_addr: address6_mac_addr
          space_id: space_id
          address6_class_name: address6_class_name
        - address6_id: address6_id
          device_id: device_id
          alias_name: alias_name
          pool6_id: pool6_id
          interface_id: interface_id
          address6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          address6_name: address6_name
          space_name: space_name
          network6_id: network6_id
          space_class_name: space_class_name
          alias6_type: alias6_type
          port_id: port_id
          address6_addr: address6_addr
          alias6_id: alias6_id
          address6_mac_addr: address6_mac_addr
          space_id: space_id
          address6_class_name: address6_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_alias6_data_data'
          type: array
      type: object
    ipam_alias6_add_input:
      properties:
        address6_hostaddr:
          description: 'todo(here) :ipam.alias6.add.input.address6_hostaddr. : IPv6
            address'
          type: string
        address6_id:
          description: The database identifier (ID) of the IPv6 address, a unique
            numeric key value automatically incremented when you add an IPv6 address.
            Use the ID to specify the IPv6 address of your choice.
          type: integer
        alias6_name:
          description: 'todo(here) :ipam.alias6.add.input.alias6_name. : String'
          type: string
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        alias6_type:
          description: The type of the alias.
          enum:
          - AAAA
          - aaaa
          - CNAME
          - cname
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_alias6_edit_input:
      properties:
        address6_hostaddr:
          description: 'todo(here) :ipam.alias6.edit.input.address6_hostaddr. : IPv6
            address'
          type: string
        address6_id:
          description: The database identifier (ID) of the IPv6 address, a unique
            numeric key value automatically incremented when you add an IPv6 address.
            Use the ID to specify the IPv6 address of your choice.
          type: integer
        alias6_name:
          description: 'todo(here) :ipam.alias6.edit.input.alias6_name. : String'
          type: string
        alias6_id:
          description: The database identifier (ID) of the IPv6 alias, a unique numeric
            key value automatically incremented when you add an IPv6 alias. Use the
            ID to specify which IPv6 alias to edit.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        alias6_type:
          description: The type of the alias.
          enum:
          - AAAA
          - aaaa
          - CNAME
          - cname
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_alias6_add_success:
      example:
        data:
        - alias6_id: alias6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_alias6_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_alias6_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_alias6_edit_success:
      example:
        data:
        - alias6_id: alias6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_alias6_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_alias6_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_alias6_delete_success:
      example:
        data:
        - alias6_id: alias6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_alias6_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_alias6_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_network_data:
      example:
        data:
        - range_name: range_name
          network_address_used_size: network_address_used_size
          vlan_id: vlan_id
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          vlsm_space_id: vlsm_space_id
          vlan_vlan_id: vlan_vlan_id
          network_is_terminal: network_is_terminal
          vlan_name: vlan_name
          parent_network_level: parent_network_level
          domain_id: domain_id
          vlsm_space_name: vlsm_space_name
          domain_name: domain_name
          network_ripe_waiting_status: network_ripe_waiting_status
          space_class_name: space_class_name
          network_start_hostaddr: network_start_hostaddr
          parent_space_id: parent_space_id
          space_description: space_description
          parent_network_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network_address_used_percent: network_address_used_percent
          network_level: network_level
          network_size: network_size
          space_parent_space_id: space_parent_space_id
          space_is_template: space_is_template
          network_name: network_name
          parent_network_is_terminal: parent_network_is_terminal
          parent_network_class_name: parent_network_class_name
          network_address_free_size: network_address_free_size
          network_id: network_id
          space_name: space_name
          network_is_valid: network_is_valid
          parent_network_path: parent_network_path
          network_allocated_percent: network_allocated_percent
          network_start_address_addr: network_start_address_addr
          network_class_name: network_class_name
          network_end_hostaddr: network_end_hostaddr
          vlsm_network_id: vlsm_network_id
          parent_network_name: parent_network_name
          range_id: range_id
          parent_network_size: parent_network_size
          network_used_size: network_used_size
          network_ripe_waiting_state: network_ripe_waiting_state
          parent_vlsm_network_id: parent_vlsm_network_id
          parent_space_name: parent_space_name
          network_used_percent: network_used_percent
          network_is_in_orphan: network_is_in_orphan
          network_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network_lock_network_broadcast: network_lock_network_broadcast
          parent_network_start_address_addr: parent_network_start_address_addr
          network_multistatus: network_multistatus
          parent_network_id: parent_network_id
          vlsm_block_id: vlsm_block_id
          network_end_address_addr: network_end_address_addr
          space_id: space_id
          parent_network_end_address_addr: parent_network_end_address_addr
          network_allocated_size: network_allocated_size
        - range_name: range_name
          network_address_used_size: network_address_used_size
          vlan_id: vlan_id
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          vlsm_space_id: vlsm_space_id
          vlan_vlan_id: vlan_vlan_id
          network_is_terminal: network_is_terminal
          vlan_name: vlan_name
          parent_network_level: parent_network_level
          domain_id: domain_id
          vlsm_space_name: vlsm_space_name
          domain_name: domain_name
          network_ripe_waiting_status: network_ripe_waiting_status
          space_class_name: space_class_name
          network_start_hostaddr: network_start_hostaddr
          parent_space_id: parent_space_id
          space_description: space_description
          parent_network_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network_address_used_percent: network_address_used_percent
          network_level: network_level
          network_size: network_size
          space_parent_space_id: space_parent_space_id
          space_is_template: space_is_template
          network_name: network_name
          parent_network_is_terminal: parent_network_is_terminal
          parent_network_class_name: parent_network_class_name
          network_address_free_size: network_address_free_size
          network_id: network_id
          space_name: space_name
          network_is_valid: network_is_valid
          parent_network_path: parent_network_path
          network_allocated_percent: network_allocated_percent
          network_start_address_addr: network_start_address_addr
          network_class_name: network_class_name
          network_end_hostaddr: network_end_hostaddr
          vlsm_network_id: vlsm_network_id
          parent_network_name: parent_network_name
          range_id: range_id
          parent_network_size: parent_network_size
          network_used_size: network_used_size
          network_ripe_waiting_state: network_ripe_waiting_state
          parent_vlsm_network_id: parent_vlsm_network_id
          parent_space_name: parent_space_name
          network_used_percent: network_used_percent
          network_is_in_orphan: network_is_in_orphan
          network_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network_lock_network_broadcast: network_lock_network_broadcast
          parent_network_start_address_addr: parent_network_start_address_addr
          network_multistatus: network_multistatus
          parent_network_id: parent_network_id
          vlsm_block_id: vlsm_block_id
          network_end_address_addr: network_end_address_addr
          space_id: space_id
          parent_network_end_address_addr: parent_network_end_address_addr
          network_allocated_size: network_allocated_size
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_network_data_data'
          type: array
      type: object
    ipam_network_add_input:
      properties:
        parent_network_id:
          description: The database identifier (ID) of an existing IPv4 network you
            want to set as the parent of the IPv4 network you are adding. You can
            specify a subnet-type network to set up a network-based VLSM organization.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        network_addr:
          description: The start IP address of the IPv4 network, its first IP address.
          type: string
        network_end_addr:
          description: The end IP address of the IPv4 network, its last IP address.
          type: string
        network_mask:
          description: The netmask of the IPv4 network. It is expressed in dot-decimal
            notation and defines the number of addresses the network contains.
          type: string
        network_prefix:
          description: The prefix of the IPv4 network, an integer that defines the
            number of addresses the network contains.
          type: string
        network_size:
          description: The size of the IPv4 network, the number of IP addresses it
            contains.
          type: integer
        allow_tree_reparenting:
          description: A way to allow (1) or prevent (0) changing the parent of the
            network you are adding. Upon edition of the network, this parameter decides
            if you can associate it with a different parent network.
          type: integer
        network_is_terminal:
          description: A way to determine if a network can contain other networks.
            If set to 1, the network is terminal and cannot contain other subnet-type
            networks. By essence, block-type networks are non-terminal and are always
            set to 0.
          type: integer
        network_lock_network_broadcast:
          description: A way to prevent <b>(1)</b> users from assigning the broadcast
            IP address and network IP address of the network.
          type: integer
        permit_invalid:
          description: A way to authorize (<b>1</b>) IPv4 networks overlapping within
            a space.
          type: integer
        permit_no_block:
          description: A way to force the creation of an IPv4 subnet-type network.
            If set to <b>1</b>, you can create a subnet-type network even if no block-type
            network matching the start address exists.
          type: integer
        relative_position:
          description: The position of the network within the hierarchy of networks
            of a VLSM organization. It calculates between <b>0</b> and <b>n</b> all
            the levels of the organization, its behavior depends on the value of the
            parameter <b>use_reversed_relative_position</b>:<ul class=dashed ><li><b>use_reversed_relative_position=0</b>
            where <b>0</b> indicates a block-type network at the highest level possible,
            in a space-based organization, it belongs to the top space. The levels
            increment from <b>0</b> down to <b>n</b>, the lowest level you set up,
            within networks or spaces.<br/>                                            </li><li><b>use_reversed_relative_position=1</b>
            where <b>1</b> indicates a network located at the lowest level of the
            organization, within networks or spaces. The levels increment from <b>0</b>
            up to <b>n</b>, the network at the highest level of the organization.<br/>                                            </li></ul>
          type: integer
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          enum:
          - "1"
          - "2"
          type: string
        network_level:
          description: The level of the network within the space:<ul class=dashed
            ><li>                                                Set it to <b>0</b>
            for a block-type network.<br/>                                            </li><li>                                                Set
            it to a value between <b>1</b> and <b>n</b> for a subnet-type network.<br/>                                            </li></ul>If
            you set a value between <b>2</b> and <b>n</b>, you are setting a network-based
            VLSM organization where non terminal subnet-type networks can contain
            other subnet-type networks.
          type: integer
        network_name:
          description: The name of the IPv4 network, each IPv4 network must have a
            unique name.
          type: string
        use_reversed_relative_position:
          description: A way to determine if the calculation of the parameter <b>relative_position</b>
            should start from the top (<b>0</b>) or the bottom (<b>1</b>) of the VLSM
            organization.
          type: integer
        vlan_id:
          description: The database identifier (ID) of the VLAN you want to associate
            with the network.
          type: integer
        vlsm_space_id:
          description: The database identifier (ID) of a VLSM child space of the space
            specified in <b>site_id</b>. If you specify an ID, the subnet-type network
            you are adding is duplicated as a VLSM block-type network in the child
            space, with the same name but a different ID. This parameter serves the
            same purpose as <b>vlsm_site_name</b>.
          type: integer
        vlsm_space_name:
          description: The name of a VLSM child space of the space specified in <b>site_id</b>.
            If you specify a name, the subnet-type network you are adding is duplicated
            as a VLSM block-type network in the child space, with the same name but
            a different ID. This parameter serves the same purpose as <b>vlsm_site_id</b>.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        network_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        network_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_network_edit_input:
      properties:
        parent_network_id:
          description: The database identifier (ID) of an existing IPv4 network you
            want to set as the parent of the IPv4 network you are editing. You can
            specify a subnet-type network to set up a network-based VLSM organization.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        network_addr:
          description: The start IP address of the IPv4 network, its first IP address.
          type: string
        network_end_addr:
          description: The end IP address of the IPv4 network, its last IP address.
          type: string
        network_id:
          description: The database identifier (ID) of the IPv4 network, a unique
            numeric key value automatically incremented when you add an IPv4 network.
            Use the ID to specify which IPv4 network to edit.
          type: integer
        network_mask:
          description: The netmask of the IPv4 network. It is expressed in dot-decimal
            notation and defines the number of addresses the network contains.
          type: string
        network_prefix:
          description: The prefix of the IPv4 network, an integer that defines the
            number of addresses the network contains.
          type: string
        network_size:
          description: The size of the IPv4 network, the number of IP addresses it
            contains.
          type: integer
        allow_tree_reparenting:
          description: A way to allow (1) or prevent (0) changing the parent of the
            network you are adding. Upon edition of the network, this parameter decides
            if you can associate it with a different parent network.
          type: integer
        network_is_terminal:
          description: A way to determine if a network can contain other networks.
            If set to 1, the network is terminal and cannot contain other subnet-type
            networks. By essence, block-type networks are non-terminal and are always
            set to 0.
          type: integer
        network_lock_network_broadcast:
          description: A way to prevent <b>(1)</b> users from assigning the broadcast
            IP address and network IP address of the network.
          type: integer
        permit_invalid:
          description: A way to authorize (<b>1</b>) IPv4 networks overlapping within
            a space.
          type: integer
        permit_no_block:
          description: A way to force the creation of an IPv4 subnet-type network.
            If set to <b>1</b>, you can create a subnet-type network even if no block-type
            network matching the start address exists.
          type: integer
        relative_position:
          description: The position of the network within the hierarchy of networks
            of a VLSM organization. It calculates between <b>0</b> and <b>n</b> all
            the levels of the organization, its behavior depends on the value of the
            parameter <b>use_reversed_relative_position</b>:<ul class=dashed ><li><b>use_reversed_relative_position=0</b>
            where <b>0</b> indicates a block-type network at the highest level possible,
            in a space-based organization, it belongs to the top space. The levels
            increment from <b>0</b> down to <b>n</b>, the lowest level you set up,
            within networks or spaces.<br/>                                            </li><li><b>use_reversed_relative_position=1</b>
            where <b>1</b> indicates a network located at the lowest level of the
            organization, within networks or spaces. The levels increment from <b>0</b>
            up to <b>n</b>, the network at the highest level of the organization.<br/>                                            </li></ul>
          type: integer
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          enum:
          - "1"
          - "2"
          type: string
        network_level:
          description: The level of the network within the space:<ul class=dashed
            ><li>                                                Set it to <b>0</b>
            for a block-type network.<br/>                                            </li><li>                                                Set
            it to a value between <b>1</b> and <b>n</b> for a subnet-type network.<br/>                                            </li></ul>If
            you set a value between <b>2</b> and <b>n</b>, you are setting a network-based
            VLSM organization where non terminal subnet-type networks can contain
            other subnet-type networks.
          type: integer
        network_name:
          description: The name of the IPv4 network, each IPv4 network must have a
            unique name.
          type: string
        use_reversed_relative_position:
          description: A way to determine if the calculation of the parameter <b>relative_position</b>
            should start from the top (<b>0</b>) or the bottom (<b>1</b>) of the VLSM
            organization.
          type: integer
        vlan_id:
          description: The database identifier (ID) of the VLAN you want to associate
            with the network.
          type: integer
        vlsm_space_id:
          description: The database identifier (ID) of a VLSM child space of the space
            specified in <b>site_id</b>. If you specify an ID, the subnet-type network
            you are editing is duplicated as a VLSM block-type network in the child
            space, with the same name but a different ID. This parameter serves the
            same purpose as <b>vlsm_site_name</b>.
          type: integer
        vlsm_space_name:
          description: The name of a VLSM child space of the space specified in <b>site_id</b>.
            If you specify a name, the subnet-type network you are editing is duplicated
            as a VLSM block-type network in the child space, with the same name but
            a different ID. This parameter serves the same purpose as <b>vlsm_site_id</b>.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        network_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        network_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_network_add_success:
      example:
        data:
        - network_id: network_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_network_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_network_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_network_edit_success:
      example:
        data:
        - network_id: network_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_network_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_network_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_network_delete_success:
      example:
        data:
        - network_id: network_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_network_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_network_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_network6_data:
      example:
        data:
        - range_name: range_name
          vlsm_block6_id: vlsm_block6_id
          end_address6_addr: end_address6_addr
          parent_network6_class_name: parent_network6_class_name
          parent_network6_is_terminal: parent_network6_is_terminal
          vlan_id: vlan_id
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          vlsm_space_id: vlsm_space_id
          vlan_vlan_id: vlan_vlan_id
          vlan_name: vlan_name
          parent_network_level: parent_network_level
          domain_id: domain_id
          vlsm_space_name: vlsm_space_name
          domain_name: domain_name
          percent_allocated: percent_allocated
          parent_network6_prefix: parent_network6_prefix
          space_class_name: space_class_name
          parent_space_id: parent_space_id
          space_description: space_description
          network_level: network_level
          parent_vlsm_network6_id: parent_vlsm_network6_id
          network_size: network_size
          space_parent_space_id: space_parent_space_id
          space_is_template: space_is_template
          network6_ripe_waiting_state: network6_ripe_waiting_state
          network6_is_terminal: network6_is_terminal
          vlsm_network6_id: vlsm_network6_id
          network6_ripe_waiting_status: network6_ripe_waiting_status
          percent_used: percent_used
          space_name: space_name
          network6_id: network6_id
          parent_network_path: parent_network_path
          network6_start_hostaddr: network6_start_hostaddr
          parent_network6_percent_allocated: parent_network6_percent_allocated
          network6_is_valid: network6_is_valid
          parent_end_address6_addr: parent_end_address6_addr
          parent_start_address6_addr: parent_start_address6_addr
          network6_end_hostaddr: network6_end_hostaddr
          parent_network6_id: parent_network6_id
          network6_name: network6_name
          range_id: range_id
          start_address6_addr: start_address6_addr
          parent_network_size: parent_network_size
          parent_space_name: parent_space_name
          network6_prefix: network6_prefix
          parent_network6_percent_used: parent_network6_percent_used
          network6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network6_multistatus: network6_multistatus
          parent_network6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          parent_network6_name: parent_network6_name
          network6_lock_network_broadcast: network6_lock_network_broadcast
          network6_class_name: network6_class_name
          network6_is_in_orphan: network6_is_in_orphan
          space_id: space_id
        - range_name: range_name
          vlsm_block6_id: vlsm_block6_id
          end_address6_addr: end_address6_addr
          parent_network6_class_name: parent_network6_class_name
          parent_network6_is_terminal: parent_network6_is_terminal
          vlan_id: vlan_id
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          vlsm_space_id: vlsm_space_id
          vlan_vlan_id: vlan_vlan_id
          vlan_name: vlan_name
          parent_network_level: parent_network_level
          domain_id: domain_id
          vlsm_space_name: vlsm_space_name
          domain_name: domain_name
          percent_allocated: percent_allocated
          parent_network6_prefix: parent_network6_prefix
          space_class_name: space_class_name
          parent_space_id: parent_space_id
          space_description: space_description
          network_level: network_level
          parent_vlsm_network6_id: parent_vlsm_network6_id
          network_size: network_size
          space_parent_space_id: space_parent_space_id
          space_is_template: space_is_template
          network6_ripe_waiting_state: network6_ripe_waiting_state
          network6_is_terminal: network6_is_terminal
          vlsm_network6_id: vlsm_network6_id
          network6_ripe_waiting_status: network6_ripe_waiting_status
          percent_used: percent_used
          space_name: space_name
          network6_id: network6_id
          parent_network_path: parent_network_path
          network6_start_hostaddr: network6_start_hostaddr
          parent_network6_percent_allocated: parent_network6_percent_allocated
          network6_is_valid: network6_is_valid
          parent_end_address6_addr: parent_end_address6_addr
          parent_start_address6_addr: parent_start_address6_addr
          network6_end_hostaddr: network6_end_hostaddr
          parent_network6_id: parent_network6_id
          network6_name: network6_name
          range_id: range_id
          start_address6_addr: start_address6_addr
          parent_network_size: parent_network_size
          parent_space_name: parent_space_name
          network6_prefix: network6_prefix
          parent_network6_percent_used: parent_network6_percent_used
          network6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network6_multistatus: network6_multistatus
          parent_network6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          parent_network6_name: parent_network6_name
          network6_lock_network_broadcast: network6_lock_network_broadcast
          network6_class_name: network6_class_name
          network6_is_in_orphan: network6_is_in_orphan
          space_id: space_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_network6_data_data'
          type: array
      type: object
    ipam_network6_add_input:
      properties:
        parent_network6_id:
          description: The database identifier (ID) of an existing IPv6 network you
            want to set as the parent of the IPv6 network you are adding. You can
            specify a subnet-type network to set up a network-based VLSM organization.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        network6_addr:
          description: The start IP address of the IPv6 network, its first IP address.
          type: string
        network6_end_addr:
          description: The end IP address of the IPv6 network, its last IP address.
          type: string
        network6_prefix:
          description: The prefix of the IPv6 network, an integer that defines the
            number of address the network contains.
          type: string
        allow_tree_reparenting:
          description: A way to allow (1) or prevent (0) changing the parent of the
            network you are adding. Upon edition of the network, this parameter decides
            if you can associate it with a different parent network.
          type: integer
        network6_is_terminal:
          description: A way to determine if a network can contain other networks.
            If set to 1, the network is terminal and cannot contain other subnet-type
            networks. By essence, block-type networks are non-terminal and are always
            set to 0.
          type: integer
        network6_lock_network_broadcast:
          description: A way to prevent <b>(1)</b> users from assigning the broadcast
            IP address and network IP address of the network.
          type: integer
        permit_invalid:
          description: A way to authorize (<b>1</b>) IPv6 networks overlapping within
            a space.
          type: integer
        permit_no_block6:
          description: A way to force the creation of an IPv6 subnet-type network.
            If set to 1, you can create a subnet-type network even if no block-type
            network matching the start address exists.
          type: integer
        relative_position:
          description: The position of the network within the hierarchy of networks
            of a VLSM organization. It calculates between <b>0</b> and <b>n</b> all
            the levels of the organization, its behavior depends on the value of the
            parameter <b>use_reversed_relative_position</b>:<ul class=dashed ><li><b>use_reversed_relative_position=0</b>
            where <b>0</b> indicates a block-type network at the highest level possible,
            in a space-based organization, it belongs to the top space. The levels
            increment from <b>0</b> down to <b>n</b>, the lowest level you set up,
            within networks or spaces.<br/>                                            </li><li><b>use_reversed_relative_position=1</b>
            where <b>1</b> indicates a network located at the lowest level of the
            organization, within networks or spaces. The levels increment from <b>0</b>
            up to <b>n</b>, the network at the highest level of the organization.<br/>                                            </li></ul>
          type: integer
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          enum:
          - "1"
          - "2"
          type: string
        network6_name:
          description: The name of the IPv6 network, each IPv6 network must have a
            unique name.
          type: string
        network_level:
          description: The level of the network within the space:<ul class=dashed
            ><li>                                                Set it to <b>0</b>
            for a block-type network.<br/>                                            </li><li>                                                Set
            it to a value between <b>1</b> and <b>n</b> for a subnet-type network.<br/>                                            </li></ul>If
            you set a value between <b>2</b> and <b>n</b>, you are setting a network-based
            VLSM organization where non terminal subnet-type networks can contain
            other subnet-type networks.
          type: integer
        use_reversed_relative_position:
          description: A way to determine if the calculation of the parameter <b>relative_position</b>
            should start from the top (<b>0</b>) or the bottom (<b>1</b>) of the VLSM
            organization.
          type: integer
        vlan_id:
          description: The database identifier (ID) of the VLAN you want to associate
            with the network.
          type: integer
        vlsm_space_id:
          description: The database identifier (ID) of a VLSM child space of the space
            specified in <b>site_id</b>. If you specify an ID, the subnet-type network
            you are adding is duplicated as a VLSM block-type network in the child
            space, with the same name but a different ID. This parameter serves the
            same purpose as <b>vlsm_site_name</b>.
          type: integer
        vlsm_space_name:
          description: The name of a VLSM child space of the space specified in <b>site_id</b>.
            If you specify a name, the subnet-type network you are adding is duplicated
            as a VLSM block-type network in the child space, with the same name but
            a different ID. This parameter serves the same purpose as <b>vlsm_site_id</b>.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        network6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        network6_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_network6_edit_input:
      properties:
        parent_network6_id:
          description: The database identifier (ID) of an existing IPv6 network you
            want to set as the parent of the IPv6 network you are editing. You can
            specify a subnet-type network to set up a network-based VLSM organization.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        network6_addr:
          description: The start IP address of the IPv6 network, its first IP address.
          type: string
        network6_end_addr:
          description: The end IP address of the IPv6 network, its last IP address.
          type: string
        network6_id:
          description: The database identifier (ID) of the IPv6 network, a unique
            numeric key value automatically incremented when you add an IPv6 network.
            Use the ID to specify which IPv6 network to edit.
          type: integer
        network6_prefix:
          description: The prefix of the IPv6 network, an integer that defines the
            number of address the network contains.
          type: string
        allow_tree_reparenting:
          description: A way to allow (1) or prevent (0) changing the parent of the
            network you are adding. Upon edition of the network, this parameter decides
            if you can associate it with a different parent network.
          type: integer
        network6_is_terminal:
          description: A way to determine if a network can contain other networks.
            If set to 1, the network is terminal and cannot contain other subnet-type
            networks. By essence, block-type networks are non-terminal and are always
            set to 0.
          type: integer
        network6_lock_network_broadcast:
          description: A way to prevent <b>(1)</b> users from assigning the broadcast
            IP address and network IP address of the network.
          type: integer
        permit_invalid:
          description: A way to authorize (<b>1</b>) IPv6 networks overlapping within
            a space.
          type: integer
        permit_no_block6:
          description: A way to force the creation of an IPv6 subnet-type network.
            If set to 1, you can create a subnet-type network even if no block-type
            network matching the start address exists.
          type: integer
        relative_position:
          description: The position of the network within the hierarchy of networks
            of a VLSM organization. It calculates between <b>0</b> and <b>n</b> all
            the levels of the organization, its behavior depends on the value of the
            parameter <b>use_reversed_relative_position</b>:<ul class=dashed ><li><b>use_reversed_relative_position=0</b>
            where <b>0</b> indicates a block-type network at the highest level possible,
            in a space-based organization, it belongs to the top space. The levels
            increment from <b>0</b> down to <b>n</b>, the lowest level you set up,
            within networks or spaces.<br/>                                            </li><li><b>use_reversed_relative_position=1</b>
            where <b>1</b> indicates a network located at the lowest level of the
            organization, within networks or spaces. The levels increment from <b>0</b>
            up to <b>n</b>, the network at the highest level of the organization.<br/>                                            </li></ul>
          type: integer
        row_state:
          description: The object activation status.<ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the object is unmanaged, disabled or both depending on
            the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          enum:
          - "1"
          - "2"
          type: string
        network6_name:
          description: The name of the IPv6 network, each IPv6 network must have a
            unique name.
          type: string
        network_level:
          description: The level of the network within the space:<ul class=dashed
            ><li>                                                Set it to <b>0</b>
            for a block-type network.<br/>                                            </li><li>                                                Set
            it to a value between <b>1</b> and <b>n</b> for a subnet-type network.<br/>                                            </li></ul>If
            you set a value between <b>2</b> and <b>n</b>, you are setting a network-based
            VLSM organization where non terminal subnet-type networks can contain
            other subnet-type networks.
          type: integer
        use_reversed_relative_position:
          description: A way to determine if the calculation of the parameter <b>relative_position</b>
            should start from the top (<b>0</b>) or the bottom (<b>1</b>) of the VLSM
            organization.
          type: integer
        vlan_id:
          description: The database identifier (ID) of the VLAN you want to associate
            with the network.
          type: integer
        vlsm_space_id:
          description: The database identifier (ID) of a VLSM child space of the space
            specified in <b>site_id</b>. If you specify an ID, the subnet-type network
            you are editing is duplicated as a VLSM block-type network in the child
            space, with the same name but a different ID. This parameter serves the
            same purpose as <b>vlsm_site_name</b>.
          type: integer
        vlsm_space_name:
          description: The name of a VLSM child space of the space specified in <b>site_id</b>.
            If you specify a name, the subnet-type network you are editing is duplicated
            as a VLSM block-type network in the child space, with the same name but
            a different ID. This parameter serves the same purpose as <b>vlsm_site_id</b>.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        network6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        network6_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_network6_add_success:
      example:
        data:
        - network6_id: network6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_network6_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_network6_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_network6_edit_success:
      example:
        data:
        - network6_id: network6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_network6_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_network6_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_network6_delete_success:
      example:
        data:
        - network6_id: network6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_network6_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_network6_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_pool_data:
      example:
        data:
        - pool_start_hostaddr: pool_start_hostaddr
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          pool_start_address_addr: pool_start_address_addr
          vlsm_network_id: vlsm_network_id
          parent_network_name: parent_network_name
          pool_class_name: pool_class_name
          space_class_name: space_class_name
          parent_network_size: parent_network_size
          space_description: space_description
          pool_end_address_addr: pool_end_address_addr
          network_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network_size: network_size
          pool_size: pool_size
          space_is_template: space_is_template
          network_name: network_name
          pool_read_only: pool_read_only
          pool_id: pool_id
          pool_multistatus: pool_multistatus
          parent_network_class_name: parent_network_class_name
          pool_name: pool_name
          pool_end_hostaddr: pool_end_hostaddr
          network_id: network_id
          space_name: space_name
          parent_network_id: parent_network_id
          network_start_address_addr: network_start_address_addr
          pool_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          vlsm_block_id: vlsm_block_id
          space_id: space_id
          network_end_address_addr: network_end_address_addr
          network_class_name: network_class_name
        - pool_start_hostaddr: pool_start_hostaddr
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          pool_start_address_addr: pool_start_address_addr
          vlsm_network_id: vlsm_network_id
          parent_network_name: parent_network_name
          pool_class_name: pool_class_name
          space_class_name: space_class_name
          parent_network_size: parent_network_size
          space_description: space_description
          pool_end_address_addr: pool_end_address_addr
          network_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          network_size: network_size
          pool_size: pool_size
          space_is_template: space_is_template
          network_name: network_name
          pool_read_only: pool_read_only
          pool_id: pool_id
          pool_multistatus: pool_multistatus
          parent_network_class_name: parent_network_class_name
          pool_name: pool_name
          pool_end_hostaddr: pool_end_hostaddr
          network_id: network_id
          space_name: space_name
          parent_network_id: parent_network_id
          network_start_address_addr: network_start_address_addr
          pool_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          vlsm_block_id: vlsm_block_id
          space_id: space_id
          network_end_address_addr: network_end_address_addr
          network_class_name: network_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_pool_data_data'
          type: array
      type: object
    ipam_pool_add_input:
      properties:
        pool_end_address_addr:
          description: 'todo(here) :ipam.pool.add.input.pool_end_address_addr. : IPv4
            address'
          type: string
        pool_size:
          description: The size of the pool, the number of IP addresses it contains.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        pool_start_address_addr:
          description: 'todo(here) :ipam.pool.add.input.pool_start_address_addr. :
            IPv4 address'
          type: string
        network_id:
          description: The database identifier (ID) of the IPv4 network, a unique
            numeric key value automatically incremented when you add an IPv4 network.
            Use the ID to specify the IPv4 network of your choice.
          type: integer
        pool_name:
          description: The name of the IPv4 pool, each IPv4 pool must have a unique
            name.
          type: string
        pool_read_only:
          description: The reservation status of the IPv4 pool. If set 1, the IP addresses
            it contains cannot be assigned.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        pool_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        pool_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_pool_edit_input:
      properties:
        pool_end_address_addr:
          description: 'todo(here) :ipam.pool.edit.input.pool_end_address_addr. :
            IPv4 address'
          type: string
        pool_id:
          description: The database identifier (ID) of the IPv4 pool, a unique numeric
            key value automatically incremented when you add an IPv4 pool. Use the
            ID to specify which IPv4 pool to edit.
          type: integer
        pool_size:
          description: The size of the pool, the number of IP addresses it contains.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        pool_start_address_addr:
          description: 'todo(here) :ipam.pool.edit.input.pool_start_address_addr.
            : IPv4 address'
          type: string
        network_id:
          description: The database identifier (ID) of the IPv4 network, a unique
            numeric key value automatically incremented when you add an IPv4 network.
            Use the ID to specify the IPv4 network of your choice.
          type: integer
        pool_name:
          description: The name of the IPv4 pool, each IPv4 pool must have a unique
            name.
          type: string
        pool_read_only:
          description: The reservation status of the IPv4 pool. If set 1, the IP addresses
            it contains cannot be assigned.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        pool_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        pool_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_pool_add_success:
      example:
        data:
        - pool_id: pool_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_pool_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_pool_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_pool_edit_success:
      example:
        data:
        - pool_id: pool_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_pool_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_pool_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_pool_delete_success:
      example:
        data:
        - pool_id: pool_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/app_pool_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_pool_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_pool6_data:
      example:
        data:
        - vlsm_block6_id: vlsm_block6_id
          end_address6_addr: end_address6_addr
          parent_network6_class_name: parent_network6_class_name
          pool6_end_address6_addr: pool6_end_address6_addr
          pool6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          parent_network6_id: parent_network6_id
          network6_name: network6_name
          parent_network6_prefix: parent_network6_prefix
          space_class_name: space_class_name
          start_address6_addr: start_address6_addr
          space_description: space_description
          pool6_read_only: pool6_read_only
          pool6_class_name: pool6_class_name
          network6_end_address6_addr: network6_end_address6_addr
          network6_prefix: network6_prefix
          network6_start_address6_addr: network6_start_address6_addr
          pool6_size: pool6_size
          pool6_start_hostaddr: pool6_start_hostaddr
          network6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_is_template: space_is_template
          vlsm_network6_id: vlsm_network6_id
          parent_network6_name: parent_network6_name
          pool6_id: pool6_id
          pool6_multistatus: pool6_multistatus
          pool6_name: pool6_name
          space_name: space_name
          network6_id: network6_id
          network6_class_name: network6_class_name
          pool6_end_hostaddr: pool6_end_hostaddr
          space_id: space_id
          pool6_start_address6_addr: pool6_start_address6_addr
        - vlsm_block6_id: vlsm_block6_id
          end_address6_addr: end_address6_addr
          parent_network6_class_name: parent_network6_class_name
          pool6_end_address6_addr: pool6_end_address6_addr
          pool6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          parent_network6_id: parent_network6_id
          network6_name: network6_name
          parent_network6_prefix: parent_network6_prefix
          space_class_name: space_class_name
          start_address6_addr: start_address6_addr
          space_description: space_description
          pool6_read_only: pool6_read_only
          pool6_class_name: pool6_class_name
          network6_end_address6_addr: network6_end_address6_addr
          network6_prefix: network6_prefix
          network6_start_address6_addr: network6_start_address6_addr
          pool6_size: pool6_size
          pool6_start_hostaddr: pool6_start_hostaddr
          network6_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_is_template: space_is_template
          vlsm_network6_id: vlsm_network6_id
          parent_network6_name: parent_network6_name
          pool6_id: pool6_id
          pool6_multistatus: pool6_multistatus
          pool6_name: pool6_name
          space_name: space_name
          network6_id: network6_id
          network6_class_name: network6_class_name
          pool6_end_hostaddr: pool6_end_hostaddr
          space_id: space_id
          pool6_start_address6_addr: pool6_start_address6_addr
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_pool6_data_data'
          type: array
      type: object
    ipam_pool6_add_input:
      properties:
        pool6_end_address_addr:
          description: 'todo(here) :ipam.pool6.add.input.pool6_end_address_addr. :
            IPv6 address'
          type: string
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        pool6_start_address_addr:
          description: 'todo(here) :ipam.pool6.add.input.pool6_start_address_addr.
            : IPv6 address'
          type: string
        network6_id:
          description: The database identifier (ID) of the IPv6 network, a unique
            numeric key value automatically incremented when you add an IPv6 network.
            Use the ID to specify the IPv6 network of your choice.
          type: integer
        pool6_name:
          description: The name of the IPv6 pool, each IPv6 pool must have a unique
            name.
          type: string
        pool6_read_only:
          description: The reservation status of the IPv6 pool. If set 1, the IP addresses
            it contains cannot be assigned.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        pool6_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        pool6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_pool6_edit_input:
      properties:
        pool6_end_address_addr:
          description: 'todo(here) :ipam.pool6.edit.input.pool6_end_address_addr.
            : IPv6 address'
          type: string
        pool6_id:
          description: The database identifier (ID) of the IPv6 pool, a unique numeric
            key value automatically incremented when you add an IPv6 pool. Use the
            ID to specify which IPv6 pool to edit.
          type: integer
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify the space of your choice.
          type: integer
        space_name:
          description: The name of the space.
          type: string
        pool6_start_address_addr:
          description: 'todo(here) :ipam.pool6.edit.input.pool6_start_address_addr.
            : IPv6 address'
          type: string
        network6_id:
          description: The database identifier (ID) of the IPv6 network, a unique
            numeric key value automatically incremented when you add an IPv6 network.
            Use the ID to specify the IPv6 network of your choice.
          type: integer
        pool6_name:
          description: The name of the IPv6 pool, each IPv6 pool must have a unique
            name.
          type: string
        pool6_read_only:
          description: The reservation status of the IPv6 pool. If set 1, the IP addresses
            it contains cannot be assigned.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        pool6_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        pool6_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_pool6_add_success:
      example:
        data:
        - pool6_id: pool6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_pool6_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_pool6_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_pool6_edit_success:
      example:
        data:
        - pool6_id: pool6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_pool6_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_pool6_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_pool6_delete_success:
      example:
        data:
        - pool6_id: pool6_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_pool6_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_pool6_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_space_data:
      example:
        data:
        - space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_name: space_name
          parent_space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_class_name: space_class_name
          space_is_template: space_is_template
          space_multistatus: space_multistatus
          parent_space_id: parent_space_id
          space_description: space_description
          parent_space_class_name: parent_space_class_name
          space_id: space_id
          parent_space_name: parent_space_name
        - space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_name: space_name
          parent_space_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          space_class_name: space_class_name
          space_is_template: space_is_template
          space_multistatus: space_multistatus
          parent_space_id: parent_space_id
          space_description: space_description
          parent_space_class_name: parent_space_class_name
          space_id: space_id
          parent_space_name: parent_space_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_space_data_data'
          type: array
      type: object
    ipam_space_add_input:
      properties:
        space_name:
          description: The name of the space, each space must have a unique name.
          type: string
        parent_space_id:
          description: The database identifier (ID) of an existing space you want
            to set as the VLSM parent of the space you are adding. This sets up a
            space-based VLSM organization.
          type: integer
        parent_space_name:
          description: The name of an existing space you want to set as the VLSM parent
            of the space you are adding. This sets up a space-based VLSM organization.
          type: string
        space_description:
          description: The description of the space.
          type: string
        space_is_template:
          description: The template status of the space you are adding. If the space
            is used as template (<b>1</b>), all the IPv4 networks, pools and IP addresses
            it contains are also used as template. You can only set this parameter
            once, you cannot edit its value.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        space_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        space_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_space_edit_input:
      properties:
        space_id:
          description: The database identifier (ID) of the space, a unique numeric
            key value automatically incremented when you add a space. Use the ID to
            specify which space to edit.
          type: integer
        space_name:
          description: The name of the space, each space must have a unique name.
          type: string
        parent_space_id:
          description: The database identifier (ID) of an existing space you want
            to set as the VLSM parent of the space you are editing. This sets up a
            space-based VLSM organization.
          type: integer
        parent_space_name:
          description: The name of an existing space you want to set as the VLSM parent
            of the space you are editing. This sets up a space-based VLSM organization.
          type: string
        space_description:
          description: The description of the space.
          type: string
        space_is_template:
          description: The template status of the space you are editing. If the space
            is used as template (<b>1</b>), all the IPv4 networks, pools and IP addresses
            it contains are also used as template. You can only set this parameter
            once, you cannot edit its value.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        space_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        space_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    ipam_space_add_success:
      example:
        data:
        - space_id: space_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_space_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_space_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_space_edit_success:
      example:
        data:
        - space_id: space_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_space_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_space_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ipam_space_delete_success:
      example:
        data:
        - space_id: space_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/ipam_space_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    ipam_space_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    vlan_domain_data:
      example:
        data:
        - domain_id: domain_id
          domain_name: domain_name
          domain_end_vlan_id: domain_end_vlan_id
          domain_start_vlan_id: domain_start_vlan_id
          domain_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          domain_description: domain_description
          domain_support_vxlan: domain_support_vxlan
          domain_class_name: domain_class_name
        - domain_id: domain_id
          domain_name: domain_name
          domain_end_vlan_id: domain_end_vlan_id
          domain_start_vlan_id: domain_start_vlan_id
          domain_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          domain_description: domain_description
          domain_support_vxlan: domain_support_vxlan
          domain_class_name: domain_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/vlan_domain_data_data'
          type: array
      type: object
    vlan_domain_add_input:
      properties:
        domain_name:
          description: The name of the VLAN domain, each VLAN domain must have a unique
            name.
          type: string
        domain_support_vxlan:
          description: The type of virtual network used by the domain. Set it to <b>1</b>
            to use VXLAN or <b>0</b> to use VLAN.
          type: integer
        domain_description:
          description: The description of the VLAN domain.
          type: string
        domain_end_vlan_id:
          description: The VLAN identifier (ID) of a VLAN, a numeric value between
            1 and 4094. Use the ID to specify the last VLAN in the VLAN domain.
          type: integer
        domain_start_vlan_id:
          description: The VLAN identifier (ID) of a VLAN, a numeric value between
            1 and 4094. Use the ID to specify the first VLAN in the VLAN domain.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        domain_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        domain_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    vlan_domain_edit_input:
      properties:
        domain_id:
          description: The database identifier (ID) of the VLAN domain, a unique numeric
            key value automatically incremented when you add a VLAN domain. Use the
            ID to specify which VLAN domain to edit.
          type: integer
        domain_name:
          description: The name of the VLAN domain, each VLAN domain must have a unique
            name.
          type: string
        domain_support_vxlan:
          description: The type of virtual network used by the domain. Set it to <b>1</b>
            to use VXLAN or <b>0</b> to use VLAN.
          type: integer
        domain_description:
          description: The description of the VLAN domain.
          type: string
        domain_end_vlan_id:
          description: The VLAN identifier (ID) of a VLAN, a numeric value between
            1 and 4094. Use the ID to specify the last VLAN in the VLAN domain.
          type: integer
        domain_start_vlan_id:
          description: The VLAN identifier (ID) of a VLAN, a numeric value between
            1 and 4094. Use the ID to specify the first VLAN in the VLAN domain.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        domain_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        domain_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    vlan_domain_add_success:
      example:
        data:
        - domain_id: domain_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/vlan_domain_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    vlan_domain_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    vlan_domain_edit_success:
      example:
        data:
        - domain_id: domain_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/vlan_domain_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    vlan_domain_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    vlan_domain_delete_success:
      example:
        data:
        - domain_id: domain_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/vlan_domain_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    vlan_domain_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    vlan_range_data:
      example:
        data:
        - range_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_name: range_name
          domain_end_vlan_id: domain_end_vlan_id
          range_end_vlan_id: range_end_vlan_id
          domain_start_vlan_id: domain_start_vlan_id
          domain_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_start_vlan_id: range_start_vlan_id
          domain_description: domain_description
          range_id: range_id
          domain_id: domain_id
          domain_name: domain_name
          range_disable_overlapping: range_disable_overlapping
          range_class_name: range_class_name
          range_description: range_description
          domain_support_vxlan: domain_support_vxlan
          domain_class_name: domain_class_name
        - range_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_name: range_name
          domain_end_vlan_id: domain_end_vlan_id
          range_end_vlan_id: range_end_vlan_id
          domain_start_vlan_id: domain_start_vlan_id
          domain_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_start_vlan_id: range_start_vlan_id
          domain_description: domain_description
          range_id: range_id
          domain_id: domain_id
          domain_name: domain_name
          range_disable_overlapping: range_disable_overlapping
          range_class_name: range_class_name
          range_description: range_description
          domain_support_vxlan: domain_support_vxlan
          domain_class_name: domain_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/vlan_range_data_data'
          type: array
      type: object
    vlan_range_add_input:
      properties:
        domain_id:
          description: The database identifier (ID) of the VLAN domain, a unique numeric
            key value automatically incremented when you add a VLAN domain. Use the
            ID to specify the VLAN domain of your choice.
          type: integer
        domain_name:
          description: The name of the VLAN domain.
          type: string
        range_name:
          description: The name of the VLAN range, each VLAN range must have a unique
            name.
          type: string
        range_description:
          description: The description of the VLAN range.
          type: string
        range_disable_overlapping:
          description: The overlapping restriction status of the VLAN range. Set it
            to <b>1</b> to prevent VLAN ID overlapping in the range.
          type: integer
        range_end_vlan_id:
          description: The VLAN identifier (ID) of an existing VLAN you want to set
            as the last VLAN in the VLAN range.
          type: integer
        range_start_vlan_id:
          description: The VLAN identifier (ID) of an existing VLAN you want to set
            as the first VLAN in the VLAN range.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        range_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        range_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    vlan_range_edit_input:
      properties:
        domain_id:
          description: The database identifier (ID) of the VLAN domain, a unique numeric
            key value automatically incremented when you add a VLAN domain. Use the
            ID to specify the VLAN domain of your choice.
          type: integer
        domain_name:
          description: The name of the VLAN domain.
          type: string
        range_id:
          description: The database identifier (ID) of the VLAN range, a unique numeric
            key value automatically incremented when you add a VLAN range. Use the
            ID to specify which VLAN range to edit.
          type: integer
        range_name:
          description: The name of the VLAN range, each VLAN range must have a unique
            name.
          type: string
        range_description:
          description: The description of the VLAN range.
          type: string
        range_disable_overlapping:
          description: The overlapping restriction status of the VLAN range. Set it
            to <b>1</b> to prevent VLAN ID overlapping in the range.
          type: integer
        range_end_vlan_id:
          description: The VLAN identifier (ID) of an existing VLAN you want to set
            as the last VLAN in the VLAN range.
          type: integer
        range_start_vlan_id:
          description: The VLAN identifier (ID) of an existing VLAN you want to set
            as the first VLAN in the VLAN range.
          type: integer
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        range_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        range_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    vlan_range_add_success:
      example:
        data:
        - range_id: range_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    vlan_range_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    vlan_range_edit_success:
      example:
        data:
        - range_id: range_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    vlan_range_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    vlan_range_delete_success:
      example:
        data:
        - range_id: range_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/dhcp_range_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    vlan_range_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    vlan_vlan_data:
      example:
        data:
        - range_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_name: range_name
          vlan_id: vlan_id
          domain_end_vlan_id: domain_end_vlan_id
          range_end_vlan_id: range_end_vlan_id
          domain_start_vlan_id: domain_start_vlan_id
          vlan_vlan_id: vlan_vlan_id
          domain_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_start_vlan_id: range_start_vlan_id
          free_start_vlan_id: free_start_vlan_id
          domain_description: domain_description
          vlan_name: vlan_name
          range_id: range_id
          domain_id: domain_id
          domain_name: domain_name
          vlan_class_name: vlan_class_name
          range_class_name: range_class_name
          vlan_type: vlan_type
          free_end_vlan_id: free_end_vlan_id
          range_description: range_description
          domain_support_vxlan: domain_support_vxlan
          vlan_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          domain_class_name: domain_class_name
        - range_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_name: range_name
          vlan_id: vlan_id
          domain_end_vlan_id: domain_end_vlan_id
          range_end_vlan_id: range_end_vlan_id
          domain_start_vlan_id: domain_start_vlan_id
          vlan_vlan_id: vlan_vlan_id
          domain_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          range_start_vlan_id: range_start_vlan_id
          free_start_vlan_id: free_start_vlan_id
          domain_description: domain_description
          vlan_name: vlan_name
          range_id: range_id
          domain_id: domain_id
          domain_name: domain_name
          vlan_class_name: vlan_class_name
          range_class_name: range_class_name
          vlan_type: vlan_type
          free_end_vlan_id: free_end_vlan_id
          range_description: range_description
          domain_support_vxlan: domain_support_vxlan
          vlan_class_parameters:
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          - propagation: propagate
            name: name
            inheritance: set
            source: source
            value: value
          domain_class_name: domain_class_name
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/vlan_vlan_data_data'
          type: array
      type: object
    vlan_vlan_add_input:
      properties:
        domain_id:
          description: The database identifier (ID) of the VLAN domain, a unique numeric
            key value automatically incremented when you add a VLAN domain. Use the
            ID to specify the VLAN domain of your choice.
          type: integer
        domain_name:
          description: The name of the VLAN domain.
          type: string
        vlan_vlan_id:
          description: The VLAN identifier (ID) of the VLAN, a unique numeric key
            value within a VLAN domain. Use the ID to specify which VLAN to edit.
          type: integer
        range_id:
          description: The database identifier (ID) of the VLAN range, a unique numeric
            key value automatically incremented when you add a VLAN range. Use the
            ID to specify the VLAN range of your choice.
          type: integer
        range_name:
          description: The name of the VLAN range.
          type: string
        vlan_name:
          description: The name of the VLAN, each VLAN must have a unique name.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        vlan_class_name:
          description: The name of the class to apply to the object you are adding.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        vlan_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    vlan_vlan_edit_input:
      properties:
        domain_id:
          description: The database identifier (ID) of the VLAN domain, a unique numeric
            key value automatically incremented when you add a VLAN domain. Use the
            ID to specify the VLAN domain of your choice.
          type: integer
        domain_name:
          description: The name of the VLAN domain.
          type: string
        vlan_id:
          description: The database identifier (ID) of the VLAN, a unique numeric
            key value automatically incremented when you add a VLAN. Use the ID to
            specify which VLAN to edit.
          type: integer
        vlan_vlan_id:
          description: The VLAN identifier (ID) of the VLAN, a unique numeric key
            value within a VLAN domain. Use the ID to specify which VLAN to edit.
          type: integer
        range_id:
          description: The database identifier (ID) of the VLAN range, a unique numeric
            key value automatically incremented when you add a VLAN range. Use the
            ID to specify the VLAN range of your choice.
          type: integer
        range_name:
          description: The name of the VLAN range.
          type: string
        vlan_name:
          description: The name of the VLAN, each VLAN must have a unique name.
          type: string
        class_parameters_to_delete:
          description: class parameters you want to delete from the object
          items:
            type: string
          type: array
        vlan_class_name:
          description: The name of the class to apply to the object you are editing.
            You must specify the class file directory, e.g. <b>my_directory/my_class.class</b>
            . You cannot use the classes <b>global</b> and <b>default</b>, they are
            reserved by the system.
          type: string
        vlan_class_parameters:
          description: class parameters in json format
          items:
            $ref: '#/components/schemas/ApiClassParameterInputEntry'
          type: array
        warnings:
          description: A way to bypass <b>(accept) </b>any enabled rule that would
            return warning messages. If the service returns an error message, you
            cannot bypass the enabled rules.
          enum:
          - accept
          type: string
      type: object
    vlan_vlan_add_success:
      example:
        data:
        - vlan_id: vlan_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/vlan_vlan_add_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    vlan_vlan_add_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    vlan_vlan_edit_success:
      example:
        data:
        - vlan_id: vlan_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/vlan_vlan_edit_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    vlan_vlan_edit_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    vlan_vlan_delete_success:
      example:
        data:
        - vlan_id: vlan_id
        success: true
        messages:
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
        - msg: msg
          code: 0
          extras:
            key: extras
          type: error
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            $ref: '#/components/schemas/vlan_vlan_delete_success_data'
          maxItems: 1
          minItems: 1
          type: array
      type: object
    vlan_vlan_delete_failed:
      properties:
        success:
          description: state true/false indicate if action succeed
          type: boolean
        messages:
          description: List or notice/warning/error messages
          items:
            $ref: '#/components/schemas/ApiMessageEntry'
          type: array
        data:
          items:
            type: object
          type: array
      type: object
    ApiCountResponseSuccess_data:
      example:
        count: 0
      properties:
        count:
          format: int32
          type: integer
      type: object
    app_application_data_data:
      example:
        application_fqdn: application_fqdn
        application_inactive_nodes: application_inactive_nodes
        application_delayed_time: application_delayed_time
        application_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        application_total_nodes: application_total_nodes
        application_class_name: application_class_name
        parent_application_id: parent_application_id
        application_multistatus: application_multistatus
        gslbserver_id: gslbserver_id
        application_id: application_id
        gslbserver_list: gslbserver_list
        gslbserver_type: gslbserver_type
        gslbserver_name: gslbserver_name
        gslbserver_status: gslbserver_status
        application_name: application_name
        parent_application_name: parent_application_name
      properties:
        application_class_name:
          description: The name of the class applied to the object.
          type: string
        application_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        application_fqdn:
          description: The FQDN of the application.
          type: string
        gslbserver_id:
          description: The database identifier (ID) of the GSLB server associated
            with the application. It is only returned for deployed applications.
          type: string
        gslbserver_list:
          description: The name of all the GSLB servers associated with the application.
            It lists the name of each server separated by a comma.
          type: string
        gslbserver_name:
          description: The name of the GSLB server associated with the application.
            It is only returned for deployed applications.
          type: string
        gslbserver_status:
          description: The status of the GSLB server associated with the application,
            either OK (<b>1</b>), GSLB Stopped (<b>2</b>), GSLB Invalid Credentials
            (<b>4</b>) or GSLB Timeout (<b>5</b>). It is only returned for deployed
            applications.
          type: string
        application_id:
          description: The database identifier (ID) of the application.
          type: string
        application_inactive_nodes:
          description: The number of nodes of the application that are <b>Inactive</b>
            (down).
          type: string
        application_name:
          description: The name of the application.
          type: string
        application_total_nodes:
          description: The number of nodes of the application.
          type: string
        application_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        gslbserver_type:
          description: The type of DNS server associated with the application. It
            is only returned for deployed applications.
          type: string
        application_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        parent_application_id:
          description: The database identifier (ID) of the application. It is only
            returned for deployed applications.
          type: string
        parent_application_name:
          description: The name of the application. It is only returned for deployed
            applications.
          type: string
      type: object
    app_application_add_success_data:
      example:
        application_id: application_id
      properties:
        application_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    app_application_edit_success_data:
      example:
        application_id: application_id
      properties:
        application_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    app_application_delete_success_data:
      example:
        application_id: application_id
      properties:
        application_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    app_node_data_data:
      example:
        healthcheck_timeout: healthcheck_timeout
        healthcheck_id: healthcheck_id
        gslbserver_name: gslbserver_name
        application_name: application_name
        node_delayed_time: node_delayed_time
        admin_status: admin_status
        pool_lb_mode: pool_lb_mode
        healthcheck_name: healthcheck_name
        translated_healthcheck_name: translated_healthcheck_name
        application_fqdn: application_fqdn
        node_status: node_status
        healthcheck_params: healthcheck_params
        node_weight: node_weight
        translated_pool_lb_mode: translated_pool_lb_mode
        node_name: node_name
        parent_application_id: parent_application_id
        healthcheck_failover: healthcheck_failover
        pool_id: pool_id
        gslbserver_id: gslbserver_id
        application_id: application_id
        pool_name: pool_name
        gslbserver_type: gslbserver_type
        node_address_addr: node_address_addr
        healthcheck_freq: healthcheck_freq
        node_address6_addr: node_address6_addr
        healthcheck_failback: healthcheck_failback
        node_id: node_id
      properties:
        admin_status:
          description: The administrative status of the node. <b>1</b> indicates the
            node is managed. <b>0</b> indicates that is unmanaged and ignored.
          type: string
        application_fqdn:
          description: The FQDN of the application the object belongs to.
          type: string
        gslbserver_id:
          description: The database identifier (ID) of the GSLB server associated
            with the application the object belongs to. It is only returned for deployed
            applications.
          type: string
        gslbserver_name:
          description: The name of the GSLB server associated with the application
            the object belongs to. It is only returned for deployed applications.
          type: string
        application_id:
          description: The database identifier (ID) of the application the object
            belongs to.
          type: string
        application_name:
          description: The name of the application the object belongs to.
          type: string
        healthcheck_failback:
          description: The number of times, between <b>1</b> and <b>10</b>, before
            the parameter <b>appnode_status</b> is set to <b>1</b> (active) due to
            the health check result. By default, it is set to <b>3</b>.
          type: string
        healthcheck_failover:
          description: The number of times, between <b>1</b> and <b>10</b>, before
            the parameter <b>appnode_status</b> is set to <b>0</b> (inactive) due
            to the health check result. By default, it is set to <b>3</b>.
          type: string
        healthcheck_freq:
          description: The frequency to which the health check configured for the
            node is performed, in seconds.
          type: string
        healthcheck_id:
          description: The database identifier (ID) of the health check configured
            for the node.
          type: string
        healthcheck_name:
          description: The type of health check configured for the node.
          type: string
        healthcheck_params:
          description: The rest of the health check parameters configured, when relevant.
          type: string
        healthcheck_timeout:
          description: The number of seconds, between <b>1</b> and <b>10</b>, before
            the health check times out if the node is not responding.
          type: string
        node_id:
          description: The database identifier (ID) of the node.
          type: string
        node_address6_addr:
          description: The IPv6 address of the node.
          type: string
        node_address_addr:
          description: The IPv4 address of the node.
          type: string
        node_name:
          description: The name of the node.
          type: string
        node_status:
          description: 'The operational status of the node:'
          type: string
        node_weight:
          description: The weight of the node, an integer between <b>0</b> and <b>255</b>.
            <b>0</b> indicates a backup node.
          type: string
        pool_id:
          description: The database identifier (ID) of the pool.
          type: string
        pool_lb_mode:
          description: The load-balancing mode of the pool the object belongs to,
            either <b>weighted</b>, <b>round-robin</b> or <b>latency</b>.
          type: string
        pool_name:
          description: The name of the pool.
          type: string
        node_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        gslbserver_type:
          description: The type of DNS server associated with the application the
            object belongs to. It is only returned for deployed applications.
          type: string
        parent_application_id:
          description: The database identifier (ID) of the application the object
            belongs to. It is only returned for deployed applications.
          type: string
        translated_healthcheck_name:
          description: The health check type, as displayed in the GUI.
          type: string
        translated_pool_lb_mode:
          description: The load-balancing mode of the pool the object belongs to,
            as displayed in the GUI.
          type: string
      type: object
    app_node_add_success_data:
      example:
        node_id: node_id
      properties:
        node_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    app_node_edit_success_data:
      example:
        node_id: node_id
      properties:
        node_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    app_node_delete_success_data:
      example:
        node_id: node_id
      properties:
        node_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    app_pool_data_data:
      example:
        pool_affinity_state: pool_affinity_state
        application_fqdn: application_fqdn
        pool_inactive_nodes: pool_inactive_nodes
        translated_pool_lb_mode: translated_pool_lb_mode
        pool_weight: pool_weight
        parent_application_id: parent_application_id
        pool_delayed_time: pool_delayed_time
        pool_id: pool_id
        gslbserver_id: gslbserver_id
        application_id: application_id
        pool_total_nodes: pool_total_nodes
        pool_multistatus: pool_multistatus
        pool_name: pool_name
        gslbserver_type: gslbserver_type
        pool_best_active_nodes: pool_best_active_nodes
        gslbserver_name: gslbserver_name
        application_name: application_name
        pool_affinity_session_time: pool_affinity_session_time
        pool_lb_mode: pool_lb_mode
        pool_type: pool_type
      properties:
        application_fqdn:
          description: The FQDN of the application the object belongs to.
          type: string
        gslbserver_id:
          description: The database identifier (ID) of the GSLB server associated
            with the application the object belongs to. It is only returned for deployed
            applications.
          type: string
        gslbserver_name:
          description: The name of the GSLB server associated with the application
            the object belongs to. It is only returned for deployed applications.
          type: string
        application_id:
          description: The database identifier (ID) of the application the object
            belongs to.
          type: string
        application_name:
          description: The name of the application the object belongs to.
          type: string
        pool_affinity_session_time:
          description: The session duration, in seconds. It is only returned if the
            parameter <b>affinity_state</b> is set to <b>1</b>.
          type: string
        pool_affinity_state:
          description: The session affinity activation status.
          type: string
        pool_best_active_nodes:
          description: The maximum number of active nodes with the lowest latency
            that must answer the queries made to the application FQDN. It is only
            returned if the parameter <b>apppool_lb_mode</b> is set to <b>latency</b>.
          type: string
        pool_id:
          description: The database identifier (ID) of the pool.
          type: string
        pool_inactive_nodes:
          description: The number nodes of the pool that are <b>Inactive</b> (down).
          type: string
        pool_lb_mode:
          description: The load-balancing mode of the pool, either <b>weighted</b>,
            <b>round-robin</b> or <b>latency</b>.
          type: string
        pool_name:
          description: The name of the pool.
          type: string
        pool_total_nodes:
          description: The number of nodes of the pool.
          type: string
        pool_type:
          description: The type of the pool, <b>ipv4</b> or <b>ipv6</b>.
          type: string
        pool_weight:
          description: Not documented. Internal use.
          type: string
        pool_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        gslbserver_type:
          description: The type of DNS server associated with the application the
            object belongs to. It is only returned for deployed applications.
          type: string
        pool_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        parent_application_id:
          description: The database identifier (ID) of the application the object
            belongs to. It is only returned for deployed applications.
          type: string
        translated_pool_lb_mode:
          description: The load-balancing mode of the pool, as displayed in the GUI.
          type: string
      type: object
    app_pool_add_success_data:
      example:
        pool_id: pool_id
      properties:
        pool_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    app_pool_edit_success_data:
      example:
        pool_id: pool_id
      properties:
        pool_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    app_pool_delete_success_data:
      example:
        pool_id: pool_id
      properties:
        pool_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    device_device_data_data:
      example:
        dev_name: dev_name
        device_id: device_id
        row_state: row_state
        device_port_used_percent: device_port_used_percent
        device_address_formated: device_address_formated
        interface_free: interface_free
        device_port_free: device_port_free
        device_class_name: device_class_name
        device_address_addr: device_address_addr
        device_space_id: device_space_id
        dev_id: dev_id
        device_name: device_name
        device_port_total: device_port_total
        interface_used_percent: interface_used_percent
        device_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        interface_total: interface_total
        device_port_used: device_port_used
        device_space_name: device_space_name
        interface_used: interface_used
      properties:
        device_class_name:
          description: The name of the class applied to the device, it can be preceded
            by the class directory.
          type: string
        device_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        device_id:
          description: The database identifier (ID) of the Device Manager device.
          type: string
        device_address_addr:
          description: The IP address associated with the Device Manager device.
          type: string
        device_address_formated:
          description: The human readable version of the parameter <b>hostdev_ip_addr</b>.
          type: string
        device_name:
          description: The name of the Device Manager device.
          type: string
        device_space_id:
          description: The database identifier (ID) of the space associated with the
            Device Manager device.
          type: string
        device_space_name:
          description: The name of the space associated with the Device Manager device.
          type: string
        interface_free:
          description: The number of interfaces on the Device Manager device that
            are currently free.
          type: string
        interface_total:
          description: The total number of interfaces on the Device Manager device.
          type: string
        interface_used:
          description: The number of interfaces on the Device Manager device that
            are currently active.
          type: string
        interface_used_percent:
          description: The percentage of interfaces on the Device Manager device that
            are currently active.
          type: string
        dev_id:
          description: The database identifier (ID) of the NetChange network device
            associated with the Device Manager device.
          type: string
        dev_name:
          description: The name of the NetChange network device associated with the
            Device Manager device.
          type: string
        device_port_free:
          description: The number of ports on the Device Manager device that are currently
            free.
          type: string
        device_port_total:
          description: The total number of ports on the Device Manager device.
          type: string
        device_port_used:
          description: The number of ports on the Device Manager device that are currently
            active.
          type: string
        device_port_used_percent:
          description: The percentage of ports on the Device Manager device that are
            currently active.
          type: string
        row_state:
          description: The object activation status:<ul class=dashed ><li><b>0</b>
            indicates the object is present in the database but ignored, i.e. it cannot
            be managed, counted or listed. This status is applied on objects deleted
            from the GUI.<br/>                                            </li><li><b>1</b>
            indicates the object is enabled and managed.<br/>                                            </li><li><b>2</b>
            indicates the object is unmanaged, disabled or both depending on the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          type: string
      type: object
    device_device_add_success_data:
      example:
        device_id: device_id
      properties:
        device_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    device_device_edit_success_data:
      example:
        device_id: device_id
      properties:
        device_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    device_device_delete_success_data:
      example:
        device_id: device_id
      properties:
        device_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    device_interface_data_data:
      example:
        interface_manual_link: interface_manual_link
        pear_iface_id: pear_iface_id
        interface_vendor_key: interface_vendor_key
        interface_nb_ip: interface_nb_ip
        interface_class_name: interface_class_name
        interface_name: interface_name
        interface_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        interface_mac: interface_mac
        device_name: device_name
        data_id: data_id
        interface_add_time: interface_add_time
        interface_type: interface_type
        interface_address_addr: interface_address_addr
        dev_name: dev_name
        interface_address_formated: interface_address_formated
        device_id: device_id
        interface_space_id: interface_space_id
        interface_modify_time: interface_modify_time
        row_state: row_state
        pear_ipl_iface_id: pear_ipl_iface_id
        device_class_name: device_class_name
        dev_enabled: dev_enabled
        dev_id: dev_id
        port_name: port_name
        interface_id: interface_id
        port_enabled: port_enabled
        interface_vendor_mac: interface_vendor_mac
        device_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        port_id: port_id
        interface_auto_link: interface_auto_link
        interface_space_name: interface_space_name
      properties:
        interface_add_time:
          description: The time at which the Device Manager port or interface has
            been added, in decimal UNIX date format.
          type: string
        data_id:
          description: The database identifier (ID) of the Custom Database entry associated
            with the Device Manager port or interface.
          type: string
        device_class_name:
          description: The name of the class applied to the device the object belongs
            to, it can be preceded by the class directory.
          type: string
        device_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        device_id:
          description: The database identifier (ID) of the Device Manager device the
            object belongs to.
          type: string
        device_name:
          description: The name of the Device Manager device.
          type: string
        interface_auto_link:
          description: 'The DM device and port or interface to which the object is
            automatically linked, as follows: <b>&lt;hostdev_name&gt; ( &lt;hostiface_name&gt;
            )</b>'
          type: string
        interface_class_name:
          description: The name of the class applied to the port or interface, it
            can be preceded by the class directory.
          type: string
        interface_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        interface_id:
          description: The database identifier (ID) of the Device Manager port or
            interface.
          type: string
        interface_address_addr:
          description: The IP address associated with the Device Manager port or interface.
          type: string
        interface_address_formated:
          description: The human readable version of the parameter <b>hostiface_ip_addr</b>.
          type: string
        interface_mac:
          description: The MAC address associated with the Device Manager port or
            interface.
          type: string
        interface_manual_link:
          description: 'The DM device and port or interface to which the object is
            manually linked, as follows: <b>&lt;hostdev_name&gt; ( &lt;hostiface_name&gt;
            )</b>'
          type: string
        interface_name:
          description: The name of the Device Manager port or interface.
          type: string
        interface_space_id:
          description: The database identifier (ID) of the space associated with the
            Device Manager port or interface.
          type: string
        interface_space_name:
          description: The name of the space associated with the Device Manager port
            or interface.
          type: string
        interface_type:
          description: A way to indicate if the object is either a <b>port</b> or
            an <b>interface</b>.
          type: string
        dev_enabled:
          description: The activation status of the NetChange network device associated
            with the DM port or interface.<ul class=dashed ><li>                                                If
            set to <b>1</b>, the NetChange network device is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the NetChange network device is unmanaged, disabled or
            both depending on the context.<br/>                                            </li></ul>
          type: string
        dev_id:
          description: The database identifier (ID) of the NetChange network device
            associated with the Device Manager device the object belongs to.
          type: string
        dev_name:
          description: The name of the NetChange network device associated with the
            Device Manager device the object belongs to.
          type: string
        port_enabled:
          description: The activation status of the NetChange port associated with
            the DM port or interface.<ul class=dashed ><li>                                                If
            set to <b>1</b>, the NetChange port is enabled and managed.<br/>                                            </li><li>                                                If
            set to <b>2</b>, the NetChange port is unmanaged, disabled or both depending
            on the context.<br/>                                            </li></ul>
          type: string
        port_id:
          description: The database identifier (ID) of the NetChange port associated
            with the Device Manager port or interface.
          type: string
        port_name:
          description: The name of the NetChange port associated with the DM port
            or interface.
          type: string
        interface_modify_time:
          description: The last time the Device Manager port or interface data was
            reconciled, in decimal UNIX date format.
          type: string
        interface_nb_ip:
          description: The number of IP addresses associated with the Device Manager
            port or interface.
          type: string
        pear_iface_id:
          description: The database identifier (ID) of the Device Manager port or
            interface linked with <b>hostiface_id</b>.
          type: string
        pear_ipl_iface_id:
          description: The database identifier (ID) of the NetChange port linked with
            <b>hostiface_id</b>.
          type: string
        row_state:
          description: The object activation status:<ul class=dashed ><li><b>0</b>
            indicates the object is present in the database but ignored, i.e. it cannot
            be managed, counted or listed. This status is applied on objects deleted
            from the GUI.<br/>                                            </li><li><b>1</b>
            indicates the object is enabled and managed.<br/>                                            </li><li><b>2</b>
            indicates the object is unmanaged, disabled or both depending on the context.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.
          type: string
        interface_vendor_key:
          description: Internal use. Not documented.
          type: string
        interface_vendor_mac:
          description: The vendor details of the client associated with the Device
            Manager port or interface.
          type: string
      type: object
    device_interface_add_success_data:
      example:
        interface_id: interface_id
      properties:
        interface_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    device_interface_edit_success_data:
      example:
        interface_id: interface_id
      properties:
        interface_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    device_interface_delete_success_data:
      example:
        interface_id: interface_id
      properties:
        interface_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    device_link_data_data:
      example:
        device_name: device_name
        interface_id: interface_id
        device_id: device_id
        link_add_time: link_add_time
        link_auto_link: link_auto_link
        interface_name: interface_name
        interface_type: interface_type
        link_id: link_id
        interface_mac: interface_mac
      properties:
        link_add_time:
          description: The time at which the Device Manager port or interface has
            been added, in decimal UNIX date format.
          type: string
        link_auto_link:
          description: A way to determine if the link between two Device Manager devices
            is set automatically (<b>1</b>) or manually (<b>0</b>).
          type: string
        device_id:
          description: The database identifier (ID) of the Device Manager device the
            object belongs to.
          type: string
        device_name:
          description: The name of the Device Manager device.
          type: string
        interface_id:
          description: The database identifier (ID) of the Device Manager port or
            interface.
          type: string
        interface_mac:
          description: The MAC address associated with the Device Manager port or
            interface.
          type: string
        interface_name:
          description: The name of the Device Manager port or interface.
          type: string
        interface_type:
          description: A way to indicate if the object is either a <b>port</b> or
            an <b>interface</b>.
          type: string
        link_id:
          description: The database identifier (ID) of the Device Manager port or
            interface link. Use the ID to specify the port or interface link of your
            choice.
          type: string
      type: object
    device_link_add_success_data:
      example:
        link_id: link_id
      properties:
        link_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    device_link_edit_success_data:
      example:
        link_id: link_id
      properties:
        link_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    device_link_delete_success_data:
      example:
        link_id: link_id
      properties:
        link_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dhcp_acl_data_data:
      example:
        server_name: server_name
        acl_statement: acl_statement
        smart_parent_id: smart_parent_id
        acl_name: acl_name
        acl_spawnwith: acl_spawnwith
        acl_delayed_create_time: acl_delayed_create_time
        acl_delayed_delete_time: acl_delayed_delete_time
        acl_id: acl_id
        server_id: server_id
        acl_match: acl_match
        server_type: server_type
      properties:
        acl_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        acl_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_id:
          description: The database identifier (ID) of the DHCPv4 server the object
            belongs to.
          type: string
        server_name:
          description: The name of the DHCPv4 server the object belongs to.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server the object belongs to: <table><caption>dhcp_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        acl_id:
          description: The database identifier (ID) of the DHCPv4 ACL.
          type: string
        acl_match:
          description: 'The ACL rule associated with the DHCPv4 ACL, as follows: <b>&lt;match
            if (substring(option agent.remote-id,0,6) = dslam1);&gt;</b>'
          type: string
        acl_name:
          description: The name of the DHCPv4 ACL.
          type: string
        acl_spawnwith:
          description: The spawning class associated with the DHCPv4 ACL.
          type: string
        acl_statement:
          description: The statement associated with the DHCPv4 ACL.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
      type: object
    dhcp_acl6_data_data:
      example:
        acl6_statement: acl6_statement
        smart_parent_id: smart_parent_id
        acl6_id: acl6_id
        acl6_match: acl6_match
        acl6_spawnwith: acl6_spawnwith
        acl6_name: acl6_name
        server6_id: server6_id
        acl6_delayed_time: acl6_delayed_time
        server6_type: server6_type
        server6_name: server6_name
      properties:
        acl6_delayed_time:
          description: 'todo(here) :dhcp.acl6.list.output.acl6_delayed_time. : '
          type: string
        server6_id:
          description: 'todo(here) :dhcp.acl6.list.output.server6_id. : '
          type: string
        server6_name:
          description: 'todo(here) :dhcp.acl6.list.output.server6_name. : '
          type: string
        server6_type:
          description: 'todo(here) :dhcp.acl6.list.output.server6_type. : '
          type: string
        acl6_id:
          description: 'todo(here) :dhcp.acl6.list.output.acl6_id. : '
          type: string
        acl6_match:
          description: 'todo(here) :dhcp.acl6.list.output.acl6_match. : '
          type: string
        acl6_name:
          description: 'todo(here) :dhcp.acl6.list.output.acl6_name. : '
          type: string
        acl6_spawnwith:
          description: 'todo(here) :dhcp.acl6.list.output.acl6_spawnwith. : '
          type: string
        acl6_statement:
          description: 'todo(here) :dhcp.acl6.list.output.acl6_statement. : '
          type: string
        smart_parent_id:
          description: 'todo(here) :dhcp.acl6.list.output.smart_parent_id. : '
          type: string
      type: object
    dhcp_aclentry_data_data:
      example:
        aclentry_id: aclentry_id
        server_name: server_name
        smart_parent_id: smart_parent_id
        acl_name: acl_name
        aclentry_delayed_delete_time: aclentry_delayed_delete_time
        aclentry_value: aclentry_value
        aclentry_delayed_create_time: aclentry_delayed_create_time
        acl_id: acl_id
        server_id: server_id
        server_type: server_type
      properties:
        aclentry_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        aclentry_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_id:
          description: The database identifier (ID) of the DHCPv4 server the object
            belongs to.
          type: string
        server_name:
          description: The name of the DHCPv4 server the object belongs to.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server the object belongs to: <table><caption>dhcp_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        acl_id:
          description: The database identifier (ID) of the DHCPv4 ACL.
          type: string
        acl_name:
          description: The name of the DHCPv4 ACL.
          type: string
        aclentry_id:
          description: The database identifier (ID) of the DHCPv4 ACL entry.
          type: string
        aclentry_value:
          description: The value of the DHCPv4 ACL entry.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
      type: object
    dhcp_aclentry6_data_data:
      example:
        aclentry6_value: aclentry6_value
        smart_parent_id: smart_parent_id
        aclentry6_delayed_time: aclentry6_delayed_time
        acl6_id: acl6_id
        aclentry6_id: aclentry6_id
        acl6_name: acl6_name
        server6_id: server6_id
        server6_type: server6_type
        server6_name: server6_name
      properties:
        aclentry6_delayed_time:
          description: 'todo(here) :dhcp.aclentry6.list.output.aclentry6_delayed_time.
            : '
          type: string
        server6_id:
          description: 'todo(here) :dhcp.aclentry6.list.output.server6_id. : '
          type: string
        server6_name:
          description: 'todo(here) :dhcp.aclentry6.list.output.server6_name. : '
          type: string
        server6_type:
          description: 'todo(here) :dhcp.aclentry6.list.output.server6_type. : '
          type: string
        acl6_id:
          description: 'todo(here) :dhcp.aclentry6.list.output.acl6_id. : '
          type: string
        acl6_name:
          description: 'todo(here) :dhcp.aclentry6.list.output.acl6_name. : '
          type: string
        aclentry6_id:
          description: 'todo(here) :dhcp.aclentry6.list.output.aclentry6_id. : '
          type: string
        aclentry6_value:
          description: 'todo(here) :dhcp.aclentry6.list.output.aclentry6_value. : '
          type: string
        smart_parent_id:
          description: 'todo(here) :dhcp.aclentry6.list.output.smart_parent_id. : '
          type: string
      type: object
    dhcp_failover_data_data:
      example:
        server_name: server_name
        failover_peer_port: failover_peer_port
        failover_state: failover_state
        failover_name: failover_name
        failover_delayed_create_time: failover_delayed_create_time
        server_id: server_id
        failover_peer_addr: failover_peer_addr
        failover_split: failover_split
        failover_delayed_delete_time: failover_delayed_delete_time
        server_addr: server_addr
        smart_parent_id: smart_parent_id
        failover_addr: failover_addr
        failover_type: failover_type
        failover_auto_partner_down: failover_auto_partner_down
        peer_server_id: peer_server_id
        failover_multistatus: failover_multistatus
        server_state: server_state
        server_type: server_type
        failover_id: failover_id
        failover_port: failover_port
      properties:
        failover_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        failover_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_id:
          description: The database identifier (ID) of the DHCPv4 server the object
            belongs to.
          type: string
        server_name:
          description: The name of the DHCPv4 server the object belongs to.
          type: string
        server_state:
          description: The status of the DHCPv4 smart architecture.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server the object belongs to: <table><caption>dhcp_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        failover_addr:
          description: The IP address of the primary DHCPv4 server.
          type: string
        failover_auto_partner_down:
          description: The time after which the DHCPv4 failover channel automatically
            switches to <b>partner-down</b> after being in <b>communication-interrupted</b>
            state, in hours.
          type: string
        failover_id:
          description: The database identifier (ID) of the DHCPv4 failover channel,
            a unique numeric key value automatically incremented when you add a failover
            channel.
          type: string
        failover_name:
          description: The name of the DHCPv4 failover channel.
          type: string
        failover_peer_addr:
          description: The IP address of the secondary DHCPv4 server.
          type: string
        failover_peer_port:
          description: The port number of the secondary DHCPv4 server.
          type: string
        failover_port:
          description: The port number of the primary DHCPv4 server.
          type: string
        failover_split:
          description: Internal use. Not documented.
          type: string
        failover_state:
          description: The status of the DHCPv4 failover channel, either <b>startup</b>,
            <b>normal</b>, <b>communications-interrupted</b> or <b>recover-wait</b>.
          type: string
        failover_type:
          description: The type of the DHCPv4 failover channel, either <b>primary</b>
            or <b>secondary</b>.
          type: string
        server_addr:
          description: The IP address of the DHCP server on which the failover channel
            is configured, in hexadecimal format.
          type: string
        failover_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        peer_server_id:
          description: The database identifier (ID) of the secondary DHCPv4 server.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
      type: object
    dhcp_group_data_data:
      example:
        server_name: server_name
        group_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        group_name: group_name
        server_class_name: server_class_name
        group_class_name: group_class_name
        group_multistatus: group_multistatus
        server_id: server_id
        server_version: server_version
        server_addr: server_addr
        smart_parent_id: smart_parent_id
        group_id: group_id
        server_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        smart_parent_name: smart_parent_name
        group_delayed_create_time: group_delayed_create_time
        group_delayed_delete_time: group_delayed_delete_time
        server_type: server_type
      properties:
        group_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        group_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_class_name:
          description: The name of the class applied to the DHCPv4 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server_id:
          description: The database identifier (ID) of the DHCPv4 server the object
            belongs to.
          type: string
        server_name:
          description: The name of the DHCPv4 server the object belongs to.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server the object belongs to: <table><caption>dhcp_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server_version:
          description: The version details of the DHCPv4 server the object belongs
            to.
          type: string
        group_class_name:
          description: The name of the class applied to the DHCPv4 group, it can be
            preceded by the class directory.
          type: string
        group_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        group_id:
          description: The database identifier (ID) of the DHCPv4 group.
          type: string
        group_name:
          description: The name of the DHCPv4 group.
          type: string
        server_addr:
          description: The IP address of the DHCP server the object belongs to, in
            hexadecimal format.
          type: string
        group_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_group_add_success_data:
      example:
        group_id: group_id
      properties:
        group_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dhcp_group_edit_success_data:
      example:
        group_id: group_id
      properties:
        group_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dhcp_group_delete_success_data:
      example:
        group_id: group_id
      properties:
        group_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dhcp_group6_data_data:
      example:
        server6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        server6_version: server6_version
        group6_name: group6_name
        group6_multistatus: group6_multistatus
        group6_delayed_time: group6_delayed_time
        group6_id: group6_id
        server6_name: server6_name
        server6_addr: server6_addr
        smart_parent_id: smart_parent_id
        smart_parent_name: smart_parent_name
        server6_class_name: server6_class_name
        server6_id: server6_id
        server6_type: server6_type
        group6_class_name: group6_class_name
        group6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
      properties:
        group6_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        server6_class_name:
          description: The name of the class applied to the DHCPv6 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 server the object
            belongs to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server the object
            belongs to.
          type: string
        server6_name:
          description: The name of the DHCPv6 server the object belongs to.
          type: string
        server6_type:
          description: 'The type of the DHCPv6 server the object belongs to: <table><caption>dhcp6_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server6_version:
          description: The version details of the DHCPv6 server the object belongs
            to.
          type: string
        group6_class_name:
          description: The name of the class applied to the DHCPv6 group, it can be
            preceded by the class directory.
          type: string
        group6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 group and their
            value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        group6_id:
          description: The database identifier (ID) of the DHCPv6 group.
          type: string
        group6_name:
          description: The name of the DHCPv6 group.
          type: string
        server6_addr:
          description: The IP address of the DHCP server the object belongs to, in
            hexadecimal format.
          type: string
        group6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv6 smart architecture
            managing the DHCPv6 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_lease_data_data:
      example:
        range_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        range_name: range_name
        server_name: server_name
        lease_mac_addr: lease_mac_addr
        sharednetwork_id: sharednetwork_id
        server_class_name: server_class_name
        lease_vendor_id: lease_vendor_id
        server_version: server_version
        lease_addr: lease_addr
        percent: percent
        scope_name: scope_name
        range_id: range_id
        lease_name: lease_name
        smart_parent_id: smart_parent_id
        parameter_request_list: parameter_request_list
        scope_id: scope_id
        lease_circuit_id: lease_circuit_id
        lease_time: lease_time
        smart_parent_name: smart_parent_name
        scope_size: scope_size
        lease_fingerbank_os: lease_fingerbank_os
        lease_address_addr: lease_address_addr
        lease_remote_id: lease_remote_id
        lease_first_time: lease_first_time
        range_end_addr: range_end_addr
        time_to_expire: time_to_expire
        sharednetwork_name: sharednetwork_name
        lease_multistatus: lease_multistatus
        server_id: server_id
        server_addr: server_addr
        lease_clientname: lease_clientname
        lease_period: lease_period
        scope_net_addr: scope_net_addr
        lease_domain: lease_domain
        lease_end_time: lease_end_time
        range_start_addr: range_start_addr
        range_class_name: range_class_name
        range_failover_name: range_failover_name
        lease_mac_vendor: lease_mac_vendor
        lease_client_ident: lease_client_ident
        lease_id: lease_id
        server_type: server_type
        scope_class_name: scope_class_name
      properties:
        server_class_name:
          description: The name of the class applied to the DHCPv4 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server_id:
          description: The database identifier (ID) of the DHCPv4 server the object
            belongs to.
          type: string
        server_name:
          description: The name of the DHCPv4 server the object belongs to.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server the object belongs to: <table><caption>dhcp_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server_version:
          description: The version details of the DHCPv4 server the object belongs
            to.
          type: string
        lease_addr:
          description: The IP address associated with the DHCPv4 lease.
          type: string
        lease_circuit_id:
          description: The circuit identifier (ID) of the relay agent associated with
            the DHCPv4 lease.
          type: string
        lease_client_ident:
          description: The client identifier (ID) of the client associated with the
            DHCPv4 lease.
          type: string
        lease_clientname:
          description: The name of the client associated with the DHCPv4 lease.
          type: string
        lease_domain:
          description: The domain name associated with the DHCPv4 lease.
          type: string
        lease_end_time:
          description: The expiration time of the lease, in decimal UNIX date format.
          type: string
        lease_fingerbank_os:
          description: The operating system details of the client associated with
            the DHCPv4 lease.
          type: string
        lease_first_time:
          description: The first time the DHCPv4 lease has been attributed to the
            client, in decimal UNIX date format.
          type: string
        lease_id:
          description: The database identifier (ID) of the DHCPv4 lease.
          type: string
        lease_address_addr:
          description: The IP address associated with the DHCPv4 lease, in hexadecimal
            format.
          type: string
        lease_mac_addr:
          description: The MAC address associated with the IPv4 lease.
          type: string
        lease_name:
          description: The name of the DHCPv4 lease.
          type: string
        lease_period:
          description: The duration time (time to live) of the DHCPv4 lease, in seconds.
          type: string
        lease_remote_id:
          description: The remote identifier (ID) of the relay agent associated with
            the DHCPv4 lease.
          type: string
        lease_time:
          description: The last time the DHCPv4 lease has been attributed to the client,
            in decimal UNIX date format.
          type: string
        lease_vendor_id:
          description: The vendor class identifier (ID) of the client associated with
            the DHCPv4 lease.
          type: string
        range_class_name:
          description: The name of the class applied to the DHCPv4 range the object
            belongs to, it can be preceded by the class directory.
          type: string
        range_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        range_end_addr:
          description: The last IP address of the DHCPv4 range the lease belongs to.
          type: string
        range_failover_name:
          description: Internal use. Not documented.
          type: string
        range_id:
          description: The database identifier (ID) of the DHCPv4 range the object
            belongs to.
          type: string
        range_name:
          description: The name of the DHCPv4 range the object belongs to.
          type: string
        range_start_addr:
          description: The first IP address of the DHCPv4 range the lease belongs
            to.
          type: string
        scope_class_name:
          description: The name of the class applied to the DHCPv4 scope the object
            belongs to, it can be preceded by the class directory.
          type: string
        scope_id:
          description: The database identifier (ID) of the DHCPv4 scope the object
            belongs to.
          type: string
        scope_name:
          description: The name of the DHCPv4 scope the object belongs to.
          type: string
        scope_net_addr:
          description: The first IP address of the DHCPv4 scope the object belongs
            to.
          type: string
        scope_size:
          description: The number of IP addresses the DHCPv4 scope the object belongs
            to contains.
          type: string
        sharednetwork_id:
          description: The database identifier (ID) of the DHCPv4 shared network the
            object belongs to.
          type: string
        sharednetwork_name:
          description: The name of the DHCPv4 shared network the object belongs to.
          type: string
        server_addr:
          description: The IP address of the DHCP server the object belongs to, in
            hexadecimal format.
          type: string
        lease_mac_vendor:
          description: The vendor details of the client associated with the DHCPv4
            lease.
          type: string
        lease_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        parameter_request_list:
          description: The list of parameters requested with the DHCPv4 lease returned
            by the server, integers separated by a comma.
          type: string
        percent:
          description: The percentage of time the lease has really been in use.
          type: string
        time_to_expire:
          description: The time left to the lease before it expires, in seconds.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_lease6_data_data:
      example:
        scope6_size: scope6_size
        lease6_multistatus: lease6_multistatus
        server6_version: server6_version
        lease6_id: lease6_id
        lease6_address6_addr: lease6_address6_addr
        range6_failover_name: range6_failover_name
        server6_name: server6_name
        lease6_end_time: lease6_end_time
        lease6_first_time: lease6_first_time
        lease6_period: lease6_period
        range6_end_address6_addr: range6_end_address6_addr
        scope6_prefix: scope6_prefix
        smart_parent_id: smart_parent_id
        smart_parent_name: smart_parent_name
        server6_class_name: server6_class_name
        server6_id: server6_id
        lease6_clientname: lease6_clientname
        server6_type: server6_type
        lease6_mac_vendor: lease6_mac_vendor
        lease6_domain: lease6_domain
        range6_id: range6_id
        range6_start_address6_addr: range6_start_address6_addr
        lease6_time: lease6_time
        sharednetwork6_name: sharednetwork6_name
        lease6_client_duid: lease6_client_duid
        lease6_percent: lease6_percent
        server6_addr: server6_addr
        lease6_mac_addr: lease6_mac_addr
        scope6_class_name: scope6_class_name
        scope6_name: scope6_name
        range6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        scope6_id: scope6_id
        sharednetwork6_id: sharednetwork6_id
        lease6_time_to_expire: lease6_time_to_expire
        lease6_name: lease6_name
        scope6_start_address6_addr: scope6_start_address6_addr
        range6_class_name: range6_class_name
      properties:
        server6_class_name:
          description: The name of the class applied to the DHCPv6 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server the object
            belongs to.
          type: string
        server6_name:
          description: The name of the DHCPv6 server the object belongs to.
          type: string
        server6_type:
          description: 'The type of the DHCPv6 server the object belongs to: <table><caption>dhcp6_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server6_version:
          description: The version details of the DHCPv6 server the object belongs
            to.
          type: string
        lease6_client_duid:
          description: The client DHCP Unique Identifier (DUID) associated with the
            DHCPv6 lease.
          type: string
        lease6_clientname:
          description: The name of the client associated with the DHCPv6 lease.
          type: string
        lease6_domain:
          description: The domain name associated with the DHCPv6 lease.
          type: string
        lease6_end_time:
          description: The expiration time of the lease, in decimal UNIX date format.
          type: string
        lease6_first_time:
          description: The first time the DHCPv6 lease has been attributed to the
            client, in decimal UNIX date format.
          type: string
        lease6_id:
          description: The database identifier (ID) of the DHCPv6 lease.
          type: string
        lease6_address6_addr:
          description: The IP address associated with the DHCPv6 lease, in hexadecimal
            format.
          type: string
        lease6_mac_addr:
          description: The MAC address associated with the DHCPv6 lease.
          type: string
        lease6_name:
          description: The name of the DHCPv6 lease.
          type: string
        lease6_period:
          description: The duration time (time to live) of the DHCPv6 lease, in seconds.
          type: string
        lease6_time:
          description: The last time the DHCPv6 lease has been attributed to the client,
            in decimal UNIX date format.
          type: string
        range6_class_name:
          description: The name of the class applied to the DHCPv6 range the object
            belongs to, it can be preceded by the class directory.
          type: string
        range6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 range the object
            belongs to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        range6_end_address6_addr:
          description: The last IP address of the DHCPv6 range the object belongs
            to, in hexadecimal format.
          type: string
        range6_failover_name:
          description: Internal use. Not documented.
          type: string
        range6_id:
          description: The database identifier (ID) of the DHCPv6 range the object
            belongs to.
          type: string
        range6_start_address6_addr:
          description: The first IP address of the DHCPv6 range the object belongs
            to, in hexadecimal format.
          type: string
        scope6_class_name:
          description: The name of the class applied to the DHCPv6 scope the object
            belongs to, it can be preceded by the class directory.
          type: string
        scope6_id:
          description: The database identifier (ID) of the DHCPv6 scope the object
            belongs to.
          type: string
        scope6_name:
          description: The name of the DHCPv6 scope the object belongs to.
          type: string
        scope6_prefix:
          description: The prefix of the DHCPv6 scope the object belongs to.
          type: string
        scope6_size:
          description: The number of IP addresses the DHCPv6 scope the object belongs
            to contains.
          type: string
        scope6_start_address6_addr:
          description: The first IP address of the DHCPv6 scope the object belongs
            to, in hexadecimal format.
          type: string
        sharednetwork6_id:
          description: The database identifier (ID) of the DHCPv6 shared network the
            object belongs to.
          type: string
        sharednetwork6_name:
          description: The name of the DHCPv6 shared network the object belongs to.
          type: string
        server6_addr:
          description: The IP address of the DHCP server the object belongs to, in
            hexadecimal format.
          type: string
        lease6_mac_vendor:
          description: The vendor details of the client associated with the DHCPv6
            lease.
          type: string
        lease6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        lease6_percent:
          description: The percentage of time the lease has really been in use.
          type: string
        lease6_time_to_expire:
          description: The time left to the lease before it expires, in seconds.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv6 smart architecture
            managing the DHCPv6 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_range_data_data:
      example:
        range_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        range_name: range_name
        server_name: server_name
        range_lease_count: range_lease_count
        sharednetwork_id: sharednetwork_id
        server_class_name: server_class_name
        scope_start_address_addr: scope_start_address_addr
        server_version: server_version
        range_size: range_size
        scope_name: scope_name
        range_id: range_id
        range_lease_percent: range_lease_percent
        smart_parent_id: smart_parent_id
        scope_id: scope_id
        server_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        scope_if_name: scope_if_name
        smart_parent_name: smart_parent_name
        scope_size: scope_size
        range_delayed_create_time: range_delayed_create_time
        range_delayed_delete_time: range_delayed_delete_time
        server_comment: server_comment
        range_end_addr: range_end_addr
        scope_if_addr: scope_if_addr
        range_state: range_state
        sharednetwork_name: sharednetwork_name
        server_id: server_id
        server_addr: server_addr
        scope_net_mask: scope_net_mask
        scope_net_addr: scope_net_addr
        range_start_addr: range_start_addr
        range_class_name: range_class_name
        range_failover_name: range_failover_name
        scope_space_id: scope_space_id
        scope_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        range_multistatus: range_multistatus
        range_start_address_addr: range_start_address_addr
        range_end_address_addr: range_end_address_addr
        server_type: server_type
        range_acl: range_acl
        scope_class_name: scope_class_name
      properties:
        range_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        range_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_class_name:
          description: The name of the class applied to the DHCPv4 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server_comment:
          description: The description of the DHCPv4 server the object belongs to.
          type: string
        server_id:
          description: The database identifier (ID) of the DHCPv4 server the object
            belongs to.
          type: string
        server_name:
          description: The name of the DHCPv4 server the object belongs to.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server the object belongs to: <table><caption>dhcp_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server_version:
          description: The version details of the DHCPv4 server the object belongs
            to.
          type: string
        range_acl:
          description: 'The list of ACLs associated with the DHCPv4 range, as follows:
            <b>&lt;ACL_name&gt;;&lt;ACL_name&gt;;</b>... .'
          type: string
        range_class_name:
          description: The name of the class applied to the DHCPv4 range, it can be
            preceded by the class directory.
          type: string
        range_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        range_end_addr:
          description: The last IP address of the DHCPv4 range.
          type: string
        range_end_address_addr:
          description: The last IP address of the DHCPv4 range, in hexadecimal format.
          type: string
        range_failover_name:
          description: Internal use. Not documented.
          type: string
        range_id:
          description: The database identifier (ID) of the DHCPv4 range.
          type: string
        range_lease_count:
          description: The total number of leases currently delivered by the DHCPv4
            range.
          type: string
        range_lease_percent:
          description: The percentage of leases currently delivered by the DHCPv4
            range.
          type: string
        range_name:
          description: The name of the DHCPv4 range.
          type: string
        range_size:
          description: The number of IP addresses the DHCPv4 range contains.
          type: string
        range_start_addr:
          description: The first IP address of the DHCPv4 range.
          type: string
        range_start_address_addr:
          description: The first IP address of the DHCPv4 range, in hexadecimal format.
          type: string
        range_state:
          description: Internal use. Not documented.
          type: string
        scope_class_name:
          description: The name of the class applied to the DHCPv4 scope the object
            belongs to, it can be preceded by the class directory.
          type: string
        scope_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        scope_id:
          description: The database identifier (ID) of the DHCPv4 scope the object
            belongs to.
          type: string
        scope_if_addr:
          description: Internal use. Not documented.
          type: string
        scope_if_name:
          description: Internal use. Not documented.
          type: string
        scope_name:
          description: The name of the DHCPv4 scope the object belongs to.
          type: string
        scope_net_addr:
          description: The first IP address of the DHCPv4 scope the object belongs.
          type: string
        scope_net_mask:
          description: The netmask of the DHCPv4 scope the object belongs to. It is
            expressed in dot-decimal notation and defines the number of addresses
            the scope contains.
          type: string
        scope_space_id:
          description: The database identifier (ID) of the space associated with the
            DHCPv4 scope the object belongs to.
          type: string
        scope_size:
          description: The number of IP addresses the DHCPv4 scope the object belongs
            to contains.
          type: string
        scope_start_address_addr:
          description: The first IP address of the DHCPv4 scope the object belongs
            to, in hexadecimal format.
          type: string
        sharednetwork_id:
          description: The database identifier (ID) of the DHCPv4 shared network the
            object belongs to.
          type: string
        sharednetwork_name:
          description: The name of the DHCPv4 shared network the object belongs to.
          type: string
        server_addr:
          description: The IP address of the DHCP server the object belongs to, in
            hexadecimal format.
          type: string
        range_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_range_add_success_data:
      example:
        range_id: range_id
      properties:
        range_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dhcp_range_edit_success_data:
      example:
        range_id: range_id
      properties:
        range_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dhcp_range_delete_success_data:
      example:
        range_id: range_id
      properties:
        range_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dhcp_range6_data_data:
      example:
        server6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        scope6_size: scope6_size
        server6_version: server6_version
        range6_state: range6_state
        scope6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        range6_failover_name: range6_failover_name
        server6_name: server6_name
        range6_end_address6_addr: range6_end_address6_addr
        scope6_prefix: scope6_prefix
        server6_comment: server6_comment
        smart_parent_id: smart_parent_id
        smart_parent_name: smart_parent_name
        server6_class_name: server6_class_name
        server6_id: server6_id
        server6_type: server6_type
        range6_delayed_time: range6_delayed_time
        range6_acl: range6_acl
        range6_id: range6_id
        range6_start_address6_addr: range6_start_address6_addr
        sharednetwork6_name: sharednetwork6_name
        range6_multistatus: range6_multistatus
        scope6_end_address6_addr: scope6_end_address6_addr
        server6_addr: server6_addr
        range6_size: range6_size
        scope6_class_name: scope6_class_name
        scope6_name: scope6_name
        scope6_space_id: scope6_space_id
        range6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        scope6_id: scope6_id
        range6_lease_count: range6_lease_count
        sharednetwork6_id: sharednetwork6_id
        scope6_start_address6_addr: scope6_start_address6_addr
        range6_class_name: range6_class_name
      properties:
        range6_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        server6_class_name:
          description: The name of the class applied to the DHCPv6 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 server the object
            belongs to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server6_comment:
          description: The description of the DHCPv6 server the object belongs to.
          type: string
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server the object
            belongs to.
          type: string
        server6_name:
          description: The name of the DHCPv6 server the object belongs to.
          type: string
        server6_type:
          description: 'The type of the DHCPv6 server the object belongs to: <table><caption>dhcp6_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server6_version:
          description: The version details of the DHCPv6 server the object belongs
            to.
          type: string
        range6_acl:
          description: 'The list of ACLs associated with the DHCPv6 range, as follows:
            <b>&lt;ACL_name&gt;;&lt;ACL_name&gt;;...</b> .'
          type: string
        range6_class_name:
          description: The name of the class applied to the DHCPv6 range, it can be
            preceded by the class directory.
          type: string
        range6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 range and their
            value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        range6_end_address6_addr:
          description: The last IP address of the DHCPv6 range, in hexadecimal format.
          type: string
        range6_failover_name:
          description: Internal use. Not documented.
          type: string
        range6_id:
          description: The database identifier (ID) of the DHCPv6 range.
          type: string
        range6_lease_count:
          description: The total number of leases currently delivered by the DHCPv6
            range.
          type: string
        range6_size:
          description: The number of IP addresses the DHCPv6 range contains.
          type: string
        range6_start_address6_addr:
          description: The first IP address of the DHCPv6 range, in hexadecimal format.
          type: string
        range6_state:
          description: Internal use. Not documented.
          type: string
        scope6_class_name:
          description: The name of the class applied to the DHCPv6 scope the object
            belongs to, it can be preceded by the class directory.
          type: string
        scope6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 scope the object
            belongs to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        scope6_end_address6_addr:
          description: The last IP address of the DHCPv6 scope the object belongs
            to, in hexadecimal format.
          type: string
        scope6_id:
          description: The database identifier (ID) of the DHCPv6 scope the object
            belongs to.
          type: string
        scope6_name:
          description: The name of the DHCPv6 scope the object belongs to.
          type: string
        scope6_prefix:
          description: The prefix of the DHCPv6 scope the object belongs to.
          type: string
        scope6_space_id:
          description: The database identifier (ID) of the space associated with the
            DHCPv6 scope the object belongs to.
          type: string
        scope6_size:
          description: The number of IP addresses the DHCPv6 scope the object belongs
            to contains.
          type: string
        scope6_start_address6_addr:
          description: The first IP address of the DHCPv6 scope the object belongs
            to, in hexadecimal format.
          type: string
        sharednetwork6_id:
          description: The database identifier (ID) of the DHCPv6 shared network the
            object belongs to.
          type: string
        sharednetwork6_name:
          description: The name of the DHCPv6 shared network the object belongs to.
          type: string
        server6_addr:
          description: The IP address of the DHCP server the object belongs to, in
            hexadecimal format.
          type: string
        range6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv6 smart architecture
            managing the DHCPv6 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_range6_add_success_data:
      example:
        range6_id: range6_id
      properties:
        range6_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dhcp_range6_edit_success_data:
      example:
        range6_id: range6_id
      properties:
        range6_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dhcp_range6_delete_success_data:
      example:
        range6_id: range6_id
      properties:
        range6_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dhcp_scope_data_data:
      example:
        server_name: server_name
        sharednetwork_id: sharednetwork_id
        server_class_name: server_class_name
        scope_start_address_addr: scope_start_address_addr
        server_version: server_version
        scope_space_name: scope_space_name
        scope_name: scope_name
        scope_delayed_create_time: scope_delayed_create_time
        smart_parent_id: smart_parent_id
        scope_multistatus: scope_multistatus
        scope_id: scope_id
        server_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        smart_parent_name: smart_parent_name
        scope_size: scope_size
        smart_arch: smart_arch
        scope_end_address_addr: scope_end_address_addr
        failover_name: failover_name
        sharednetwork_name: sharednetwork_name
        scope_delayed_delete_time: scope_delayed_delete_time
        server_id: server_id
        server_addr: server_addr
        scope_net_mask: scope_net_mask
        scope_net_addr: scope_net_addr
        scope_space_id: scope_space_id
        scope_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        server_type: server_type
        failover_id: failover_id
        scope_class_name: scope_class_name
      properties:
        scope_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        scope_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_class_name:
          description: The name of the class applied to the DHCPv4 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server_id:
          description: The database identifier (ID) of the DHCPv4 server the object
            belongs to.
          type: string
        server_name:
          description: The name of the DHCPv4 server the object belongs to.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server the object belongs to: <table><caption>dhcp_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server_version:
          description: The version details of the DHCPv4 server the object belongs
            to.
          type: string
        failover_id:
          description: The database identifier (ID) of the DHCPv4 failover channel
            associated with the object.
          type: string
        failover_name:
          description: The name of the DHCPv4 failover channel associated with the
            object.
          type: string
        scope_class_name:
          description: The name of the class applied to the DHCPv4 scope, it can be
            preceded by the class directory.
          type: string
        scope_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        scope_end_address_addr:
          description: The last IP address of the DHCPv4 scope, in hexadecimal format.
          type: string
        scope_id:
          description: The database identifier (ID) of the DHCPv4 scope.
          type: string
        scope_name:
          description: The name of the DHCPv4 scope.
          type: string
        scope_net_addr:
          description: The first IP address of the DHCPv4 scope.
          type: string
        scope_net_mask:
          description: The netmask of the DHCPv4 scope. It is expressed in dot-decimal
            notation and defines the number of addresses the scope contains.
          type: string
        scope_space_id:
          description: The database identifier (ID) of the space associated with the
            DHCPv4 scope.
          type: string
        scope_space_name:
          description: The name of the space associated with the DHCPv4 scope.
          type: string
        scope_size:
          description: The number of IP addresses the DHCPv4 scope contains.
          type: string
        scope_start_address_addr:
          description: The first IP address of the DHCPv4 scope, in hexadecimal format.
          type: string
        sharednetwork_id:
          description: The database identifier (ID) of the DHCPv4 shared network the
            object belongs to.
          type: string
        sharednetwork_name:
          description: The name of the DHCPv4 shared network the object belongs to.
          type: string
        server_addr:
          description: The IP address of the DHCP server the object belongs to, in
            hexadecimal format.
          type: string
        scope_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_arch:
          description: The type of the DHCPv4 smart architecture the object belongs
            to.<table><caption>vdhcp_arch possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >masterslave</td><td >The One-to-One smart architecture sets a pair of
            DHCP servers in a Master/Backup configuration.</td></tr><tr><td >star</td><td
            >The One-to-Many smart architecture sets a multi-site failover configuration
            at the cost of n-servers+1.</td></tr><tr><td >splitscope</td><td >The
            Split-Scope smart architecture sets a pair of DHCP servers in a configuration
            where the two scopes listen to the same subnet, but the range of addresses
            is divided.</td></tr><tr><td >single</td><td >The Single-Server smart
            architecture manages a single DHCP server.</td></tr></tbody></table></p><br/>
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_scope_add_success_data:
      example:
        scope_id: scope_id
      properties:
        scope_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dhcp_scope_edit_success_data:
      example:
        scope_id: scope_id
      properties:
        scope_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dhcp_scope_delete_success_data:
      example:
        scope_id: scope_id
      properties:
        scope_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dhcp_scope6_data_data:
      example:
        scope6_delayed_time: scope6_delayed_time
        server6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        scope6_size: scope6_size
        server6_version: server6_version
        scope6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        server6_name: server6_name
        scope6_prefix: scope6_prefix
        smart_parent_id: smart_parent_id
        scope6_sort_name: scope6_sort_name
        scope6_multistatus: scope6_multistatus
        failover6_name: failover6_name
        smart_parent_name: smart_parent_name
        smart_arch: smart_arch
        server6_class_name: server6_class_name
        server6_id: server6_id
        server6_type: server6_type
        scope6_space_name: scope6_space_name
        failover6_id: failover6_id
        sharednetwork6_name: sharednetwork6_name
        scope6_end_address6_addr: scope6_end_address6_addr
        server6_addr: server6_addr
        scope6_class_name: scope6_class_name
        scope6_name: scope6_name
        scope6_space_id: scope6_space_id
        scope6_id: scope6_id
        sharednetwork6_id: sharednetwork6_id
        scope6_start_address6_addr: scope6_start_address6_addr
      properties:
        scope6_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        server6_class_name:
          description: The name of the class applied to the DHCPv6 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 server the object
            belongs to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server the object
            belongs to.
          type: string
        server6_name:
          description: The name of the DHCPv6 server the object belongs to.
          type: string
        server6_type:
          description: 'The type of the DHCPv6 server the object belongs to: <table><caption>dhcp6_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server6_version:
          description: The version details of the DHCPv6 server the object belongs
            to.
          type: string
        failover6_id:
          description: The database identifier (ID) of the DHCPv6 failover channel
            associated with the object.
          type: string
        failover6_name:
          description: The name of the DHCPv6 failover channel associated with the
            object.
          type: string
        scope6_class_name:
          description: The name of the class applied to the DHCPv6 scope, it can be
            preceded by the class directory.
          type: string
        scope6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 scope and their
            value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        scope6_end_address6_addr:
          description: The last IP address of the DHCPv6 scope, in hexadecimal format.
          type: string
        scope6_id:
          description: The database identifier (ID) of the DHCPv6 scope.
          type: string
        scope6_name:
          description: The name of the DHCPv6 scope.
          type: string
        scope6_prefix:
          description: The prefix of the DHCPv6 scope.
          type: string
        scope6_space_id:
          description: The database identifier (ID) of the space associated with the
            DHCPv6 scope.
          type: string
        scope6_space_name:
          description: The name of the space associated with the DHCPv6 scope.
          type: string
        scope6_size:
          description: The number of IP addresses the DHCPv6 scope contains.
          type: string
        scope6_sort_name:
          description: Internal use. Not documented.
          type: string
        scope6_start_address6_addr:
          description: The first IP address of the DHCPv6 scope, in hexadecimal format.
          type: string
        sharednetwork6_id:
          description: The database identifier (ID) of the DHCPv6 shared network the
            object belongs to.
          type: string
        sharednetwork6_name:
          description: The name of the DHCPv6 shared network the object belongs to.
          type: string
        server6_addr:
          description: The IP address of the DHCP server the object belongs to, in
            hexadecimal format.
          type: string
        scope6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_arch:
          description: The type of the DHCPv6 smart architecture the object belongs
            to.<table><caption>vdhcp6_arch possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >single</td><td >The Single-Server smart architecture manages a single
            DHCPv6 server.</td></tr><tr><td >splitscope</td><td >The Split-Scope smart
            architecture sets a pair of DHCP servers in a configuration where the
            two scopes listen to the same subnet, but the range of addresses is divided.</td></tr><tr><td
            >stateless</td><td >The Stateless smart architecture offers a limited
            number of options to the DHCP clients. The IP address is delivered thanks
            to the subnet gateway and it is impossible to create any ranges or statics
            or to retrieve any leases.</td></tr></tbody></table></p><br/>
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv6 smart architecture
            managing the DHCPv6 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_scope6_add_success_data:
      example:
        scope6_id: scope6_id
      properties:
        scope6_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dhcp_scope6_edit_success_data:
      example:
        scope6_id: scope6_id
      properties:
        scope6_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dhcp_scope6_delete_success_data:
      example:
        scope6_id: scope6_id
      properties:
        scope6_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dhcp_server_data_data:
      example:
        server_stat_enabled: server_stat_enabled
        server_cisco_password: server_cisco_password
        server_uboottime: server_uboottime
        server_stat_time: server_stat_time
        server_ms_use_ssl: server_ms_use_ssl
        server_version: server_version
        server_stat_niceness: server_stat_niceness
        server_multistatus: server_multistatus
        smart_parent_name: smart_parent_name
        server_localtime: server_localtime
        smart_ref2_server_id: smart_ref2_server_id
        server_tcp_port: server_tcp_port
        server_snmp_timeout: server_snmp_timeout
        server_stat_period: server_stat_period
        server_windhcp_protocol: server_windhcp_protocol
        server_ipmdhcp_protocol: server_ipmdhcp_protocol
        server_id: server_id
        server_addr: server_addr
        reverse_proxy_conf: reverse_proxy_conf
        server_msrpc_login: server_msrpc_login
        server_name: server_name
        smart_parent_arch: smart_parent_arch
        server_cisco_login: server_cisco_login
        server_class_name: server_class_name
        server_snmp_profile_id: server_snmp_profile_id
        smart_ref2_server_name: smart_ref2_server_name
        smart_ref1_server_name: smart_ref1_server_name
        server_snmp_use_tcp: server_snmp_use_tcp
        smart_parent_id: smart_parent_id
        server_cisco_use_ssh: server_cisco_use_ssh
        server_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        server_https_login: server_https_login
        smart_arch: smart_arch
        server_cisco_root_password: server_cisco_root_password
        server_state: server_state
        server_comment: server_comment
        server_synching: server_synching
        smart_ref1_server_id: smart_ref1_server_id
        server_is_package: server_is_package
        server_snmp_port: server_snmp_port
        total_smart_members: total_smart_members
        server_isolated: server_isolated
        smart_param1: smart_param1
        server_msrpc_domain: server_msrpc_domain
        ref2_server_name: ref2_server_name
        server_snmp_retry: server_snmp_retry
        ref1_server_name: ref1_server_name
        server_snmp_id: server_snmp_id
        smart_members_name: smart_members_name
        server_type: server_type
      properties:
        server_cisco_login:
          description: Internal use. Not documented.
          type: string
        server_cisco_password:
          description: Internal use. Not documented.
          type: string
        server_cisco_root_password:
          description: Internal use. Not documented.
          type: string
        server_cisco_use_ssh:
          description: Internal use. Not documented.
          type: string
        server_class_name:
          description: The name of the class applied to the DHCPv4 server, it can
            be preceded by the class directory.
          type: string
        server_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server_comment:
          description: The description of the DHCPv4 server.
          type: string
        server_id:
          description: The database identifier (ID) of the DHCPv4 server, a unique
            numeric key value automatically incremented when you add a DHCPv4 server.
          type: string
        server_localtime:
          description: The local time on the DHCPv4 server, in decimal UNIX date format.
          type: string
        server_name:
          description: The name of the DHCPv4 server.
          type: string
        server_state:
          description: 'The status of the DHCPv4 server: <table><caption>dhcp_state
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ER</td><td >The license used in SOLIDserver is not compliant with the
            added server: the license is invalid.</td></tr><tr><td >ES</td><td >The
            server configuration could not be parsed properly.</td></tr><tr><td >ET</td><td
            >The server does not answer anymore due to a scheduled configuration of
            the server.</td></tr><tr><td >IS</td><td >There was a setting error during
            the server declaration. For instance, some settings were added to a server
            that does not support them or a smart architecture is not managing any
            physical server.</td></tr><tr><td >IC</td><td >The SSL credentials are
            invalid</td></tr><tr><td >IP</td><td >The account used to add the Agentless
            DHCP server does not have sufficient privileges to manage it.</td></tr><tr><td
            >LS</td><td >The server configuration is not viable.</td></tr><tr><td
            >N</td><td >The server does not have a status as it has not synchronized
            yet.</td></tr><tr><td >Y</td><td >The server is operational.</td></tr></tbody></table></p><br/>'
          type: string
        server_synching:
          description: The synchronization status of the DHCPv4 server. <b>1</b> indicates
            that the server is currently being synchronized.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server: <table><caption>dhcp_type possible
            values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server_uboottime:
          description: Internal use. Not documented.
          type: string
        server_version:
          description: The version details of the DHCPv4 server.
          type: string
        server_addr:
          description: The IP address of the DHCP server, in hexadecimal format.
          type: string
        server_https_login:
          description: Internal use. Not documented.
          type: string
        server_is_package:
          description: The DHCP server package information. <b>Y</b> for an EfficientIP
            DHCP Package server, <b>N</b> for an appliance or virtual machine, <b>U</b>
            the package information is irrelevant. For servers with a <b>dhcp_type</b>
            set to <b>ipm</b>, <b>U</b> indicates either EfficientIP DHCP Packages
            or appliances/virtual machines.
          type: string
        server_ipmdhcp_protocol:
          description: Internal use. Not documented.
          type: string
        server_isolated:
          description: A way to determine if the server can update any other module
            <b>(1)</b>.
          type: string
        server_ms_use_ssl:
          description: Internal use. Not documented.
          type: string
        server_msrpc_domain:
          description: The domain name of the Microsoft DHCP server.
          type: string
        server_msrpc_login:
          description: The login of the Microsoft DHCP server.
          type: string
        server_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        ref1_server_name:
          description: The name of the Master or Single DHCPv4 server within the smart
            architecture.
          type: string
        ref2_server_name:
          description: Internal use. Not documented.
          type: string
        reverse_proxy_conf:
          description: TODO:dhcp_server_list.output.reverse_proxy_conf
          type: string
        server_snmp_id:
          description: Internal use. Not documented.
          type: string
        server_snmp_port:
          description: Internal use. Not documented.
          type: string
        server_snmp_profile_id:
          description: Internal use. Not documented.
          type: string
        server_snmp_retry:
          description: Internal use. Not documented.
          type: string
        server_snmp_timeout:
          description: Internal use. Not documented.
          type: string
        server_snmp_use_tcp:
          description: Internal use. Not documented.
          type: string
        server_stat_enabled:
          description: Internal use. Not documented.
          type: string
        server_stat_niceness:
          description: Internal use. Not documented.
          type: string
        server_stat_period:
          description: Internal use. Not documented.
          type: string
        server_stat_time:
          description: Internal use. Not documented.
          type: string
        server_tcp_port:
          description: Internal use. Not documented.
          type: string
        total_smart_members:
          description: The total number of servers managed by the DHCPv4 smart architecture.
          type: string
        smart_arch:
          description: 'The type of the DHCPv4 smart architecture: <table><caption>vdhcp_arch
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >masterslave</td><td >The One-to-One smart architecture sets a pair of
            DHCP servers in a Master/Backup configuration.</td></tr><tr><td >star</td><td
            >The One-to-Many smart architecture sets a multi-site failover configuration
            at the cost of n-servers+1.</td></tr><tr><td >splitscope</td><td >The
            Split-Scope smart architecture sets a pair of DHCP servers in a configuration
            where the two scopes listen to the same subnet, but the range of addresses
            is divided.</td></tr><tr><td >single</td><td >The Single-Server smart
            architecture manages a single DHCP server.</td></tr></tbody></table></p><br/>'
          type: string
        smart_members_name:
          description: 'The list of the servers managed by the DHCPv4 smart architecture,
            as follows: <b>&lt;dhcp_name&gt;,&lt;dhcp_name&gt;,...</b> .'
          type: string
        smart_param1:
          description: Internal use. Not documented.
          type: string
        smart_parent_arch:
          description: The type of the DHCPv4 smart architecture managing the DHCPv4
            server. No value indicates that the server is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server. <b>0</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server. <b>#</b> indicates that the server is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_ref1_server_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            the server belongs to.
          type: string
        smart_ref1_server_name:
          description: Internal use. Not documented.
          type: string
        smart_ref2_server_id:
          description: Internal use. Not documented.
          type: string
        smart_ref2_server_name:
          description: Internal use. Not documented.
          type: string
        server_windhcp_protocol:
          description: Internal use. Not documented.
          type: string
      type: object
    dhcp_server6_data_data:
      example:
        server6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        server6_snmp_id: server6_snmp_id
        server6_version: server6_version
        smart_parent_arch: smart_parent_arch
        server6_state: server6_state
        smart_ref2_server6_id: smart_ref2_server6_id
        server6_stat_period: server6_stat_period
        server6_snmp_profile_id: server6_snmp_profile_id
        server6_multistatus: server6_multistatus
        server6_name: server6_name
        server6_stat_time: server6_stat_time
        server6_comment: server6_comment
        server6_snmp_timeout: server6_snmp_timeout
        smart_parent_id: smart_parent_id
        smart_parent_name: smart_parent_name
        smart_ref1_server6_id: smart_ref1_server6_id
        smart_arch: smart_arch
        server6_class_name: server6_class_name
        server6_id: server6_id
        server6_type: server6_type
        server6_snmp_port: server6_snmp_port
        server6_stat_niceness: server6_stat_niceness
        smart_ref1_server6_name: smart_ref1_server6_name
        smart_ref2_server6_name: smart_ref2_server6_name
        server6_synching: server6_synching
        total_smart_members: total_smart_members
        server6_isolated: server6_isolated
        smart_param1: smart_param1
        server6_last_refresh_time: server6_last_refresh_time
        server6_https_login: server6_https_login
        server6_uboottime: server6_uboottime
        server6_addr: server6_addr
        server6_snmp_retry: server6_snmp_retry
        smart_members_name: smart_members_name
        server6_snmp_use_tcp: server6_snmp_use_tcp
        server6_is_package: server6_is_package
        reverse_proxy_conf: reverse_proxy_conf
        server6_stat_enabled: server6_stat_enabled
      properties:
        server6_class_name:
          description: The name of the class applied to the DHCPv6 server, it can
            be preceded by the class directory.
          type: string
        server6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 server and their
            value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server6_comment:
          description: The description of the DHCPv6 server.
          type: string
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server, a unique
            numeric key value automatically incremented when you add a DHCPv6 server.
          type: string
        server6_last_refresh_time:
          description: Internal use. Not documented.
          type: string
        server6_name:
          description: The name of the DHCPv6 server.
          type: string
        server6_state:
          description: 'The status of the DHCPv6 server: <table><caption>dhcp6_state
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ER</td><td >The license used in SOLIDserver is not compliant with the
            added server: the license is invalid.</td></tr><tr><td >ES</td><td >The
            server configuration could not be parsed properly.</td></tr><tr><td >ET</td><td
            >The server does not answer anymore due to a scheduled configuration of
            the server.</td></tr><tr><td >IS</td><td >There was a setting error during
            the server declaration. For instance, some settings were added to a server
            that does not support them or a smart architecture is not managing any
            physical server.</td></tr><tr><td >IC</td><td >The SSL credentials are
            invalid</td></tr><tr><td >IP</td><td >The account used to add the Agentless
            DHCP server does not have sufficient privileges to manage it.</td></tr><tr><td
            >LS</td><td >The server configuration is not viable.</td></tr><tr><td
            >N</td><td >The server does not have a status as it has not synchronized
            yet.</td></tr><tr><td >Y</td><td >The server is operational.</td></tr></tbody></table></p><br/>'
          type: string
        server6_synching:
          description: The synchronization status of the DHCPv6 server. <b>1</b> indicates
            that the server is currently being synchronized.
          type: string
        server6_type:
          description: 'The type of the DHCPv6 server: <table><caption>dhcp6_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server6_uboottime:
          description: Internal use. Not documented.
          type: string
        server6_version:
          description: The version details of the DHCPv6 server.
          type: string
        server6_addr:
          description: The IP address of the DHCP server, in hexadecimal format.
          type: string
        server6_https_login:
          description: Internal use. Not documented.
          type: string
        server6_is_package:
          description: The DHCPv6 server package information. <b>Y</b> for an EfficientIP
            DHCP Package server, <b>N</b> for an appliance or virtual machine, <b>U</b>
            the package information is irrelevant. For servers with a <b>dhcp6_type</b>
            set to <b>ipm</b>, <b>U</b> indicates either EfficientIP DHCP Packages
            or appliances/virtual machines.
          type: string
        server6_isolated:
          description: A way to determine if the server can update any other module
            <b>(1)</b>.
          type: string
        server6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        reverse_proxy_conf:
          description: TODO:dhcp6_server6_list.output.reverse_proxy_conf
          type: string
        server6_snmp_id:
          description: Internal use. Not documented.
          type: string
        server6_snmp_port:
          description: Internal use. Not documented.
          type: string
        server6_snmp_profile_id:
          description: Internal use. Not documented.
          type: string
        server6_snmp_retry:
          description: Internal use. Not documented.
          type: string
        server6_snmp_timeout:
          description: Internal use. Not documented.
          type: string
        server6_snmp_use_tcp:
          description: Internal use. Not documented.
          type: string
        server6_stat_enabled:
          description: Internal use. Not documented.
          type: string
        server6_stat_niceness:
          description: Internal use. Not documented.
          type: string
        server6_stat_period:
          description: Internal use. Not documented.
          type: string
        server6_stat_time:
          description: Internal use. Not documented.
          type: string
        total_smart_members:
          description: The total number of servers managed by the DHCPv6 smart architecture.
          type: string
        smart_arch:
          description: The type of the DHCPv6 smart architecture.<table><caption>vdhcp6_arch
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >single</td><td >The Single-Server smart architecture manages a single
            DHCPv6 server.</td></tr><tr><td >splitscope</td><td >The Split-Scope smart
            architecture sets a pair of DHCP servers in a configuration where the
            two scopes listen to the same subnet, but the range of addresses is divided.</td></tr><tr><td
            >stateless</td><td >The Stateless smart architecture offers a limited
            number of options to the DHCP clients. The IP address is delivered thanks
            to the subnet gateway and it is impossible to create any ranges or statics
            or to retrieve any leases.</td></tr></tbody></table></p><br/>
          type: string
        smart_members_name:
          description: 'The list of the servers managed by the DHCPv6 smart architecture,
            as follows: <b>&lt;dhcp6_name&gt;,&lt;dhcp6_name&gt;,...</b> .'
          type: string
        smart_param1:
          description: Internal use. Not documented.
          type: string
        smart_parent_arch:
          description: The type of the DHCPv6 smart architecture managing the DHCPv6
            server. No value indicates that the server is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv6 smart architecture
            managing the DHCPv6 server. <b>0</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv6 smart architecture managing the DHCPv6
            server. <b>#</b> indicates that the server is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_ref1_server6_id:
          description: The database identifier (ID) of the DHCPv6 smart architecture
            the server belongs to.
          type: string
        smart_ref1_server6_name:
          description: Internal use. Not documented.
          type: string
        smart_ref2_server6_id:
          description: Internal use. Not documented.
          type: string
        smart_ref2_server6_name:
          description: Internal use. Not documented.
          type: string
      type: object
    dhcp_sharednetwork_data_data:
      example:
        server_name: server_name
        smart_parent_id: smart_parent_id
        sharednetwork_multistatus: sharednetwork_multistatus
        sharednetwork_id: sharednetwork_id
        sharednetwork_name: sharednetwork_name
        server_id: server_id
        sharednetwork_delayed_create_time: sharednetwork_delayed_create_time
        sharednetwork_delayed_delete_time: sharednetwork_delayed_delete_time
        server_type: server_type
      properties:
        sharednetwork_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        sharednetwork_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_id:
          description: The database identifier (ID) of the DHCPv4 server the object
            belongs to.
          type: string
        server_name:
          description: The name of the DHCPv4 server the object belongs to.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server the object belongs to: <table><caption>dhcp_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        sharednetwork_id:
          description: The database identifier (ID) of the DHCPv4 shared network.
          type: string
        sharednetwork_name:
          description: The name of the DHCPv4 shared network.
          type: string
        sharednetwork_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
      type: object
    dhcp_sharednetwork_add_success_data:
      example:
        sharednetwork_id: sharednetwork_id
      properties:
        sharednetwork_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dhcp_sharednetwork_edit_success_data:
      example:
        sharednetwork_id: sharednetwork_id
      properties:
        sharednetwork_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dhcp_sharednetwork_delete_success_data:
      example:
        sharednetwork_id: sharednetwork_id
      properties:
        sharednetwork_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dhcp_sharednetwork6_data_data:
      example:
        smart_parent_id: smart_parent_id
        row_state: row_state
        sharednetwork6_name: sharednetwork6_name
        sharednetwork6_multistatus: sharednetwork6_multistatus
        sharednetwork6_delayed_time: sharednetwork6_delayed_time
        smart_arch: smart_arch
        sharednetwork6_id: sharednetwork6_id
        server6_id: server6_id
        server6_type: server6_type
        server6_name: server6_name
      properties:
        sharednetwork6_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server the object
            belongs to.
          type: string
        server6_name:
          description: The name of the DHCPv6 server the object belongs to.
          type: string
        server6_type:
          description: 'The type of the DHCPv6 server the object belongs to: <table><caption>dhcp6_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        sharednetwork6_id:
          description: The database identifier (ID) of the DHCPv6 shared network.
          type: string
        sharednetwork6_name:
          description: The name of the DHCPv6 shared network.
          type: string
        sharednetwork6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        row_state:
          description: 'The object activation status: <ul class=dashed ><li>                                                If
            set to <b>0</b>, the object is present in the database but ignored, i.e.
            it cannot be managed, counted or listed. This status is applied on objects
            deleted from the GUI.<br/>                                            </li><li>                                                If
            set to <b>1</b>, the object is enabled and managed.<br/>                                            </li></ul>By
            default, <b>row_enabled</b> is set to <b>1</b> when an object is created.'
          type: string
        smart_arch:
          description: The type of the DHCPv6 smart architecture the object belongs
            to.<table><caption>vdhcp6_arch possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >single</td><td >The Single-Server smart architecture manages a single
            DHCPv6 server.</td></tr><tr><td >splitscope</td><td >The Split-Scope smart
            architecture sets a pair of DHCP servers in a configuration where the
            two scopes listen to the same subnet, but the range of addresses is divided.</td></tr><tr><td
            >stateless</td><td >The Stateless smart architecture offers a limited
            number of options to the DHCP clients. The IP address is delivered thanks
            to the subnet gateway and it is impossible to create any ranges or statics
            or to retrieve any leases.</td></tr></tbody></table></p><br/>
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv6 smart architecture
            managing the DHCPv6 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
      type: object
    dhcp_sharednetwork6_add_success_data:
      example:
        sharednetwork6_id: sharednetwork6_id
      properties:
        sharednetwork6_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dhcp_sharednetwork6_edit_success_data:
      example:
        sharednetwork6_id: sharednetwork6_id
      properties:
        sharednetwork6_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dhcp_sharednetwork6_delete_success_data:
      example:
        sharednetwork6_id: sharednetwork6_id
      properties:
        sharednetwork6_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dhcp_static_data_data:
      example:
        server_name: server_name
        static_mac_vendor: static_mac_vendor
        sharednetwork_id: sharednetwork_id
        server_class_name: server_class_name
        group_class_name: group_class_name
        scope_start_address_addr: scope_start_address_addr
        server_version: server_version
        static_multistatus: static_multistatus
        scope_name: scope_name
        static_mac_addr: static_mac_addr
        static_delayed_create_time: static_delayed_create_time
        static_delayed_delete_time: static_delayed_delete_time
        smart_parent_id: smart_parent_id
        scope_id: scope_id
        server_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        smart_parent_name: smart_parent_name
        scope_size: scope_size
        static_last_seen: static_last_seen
        static_expire_time: static_expire_time
        scope_end_address_addr: scope_end_address_addr
        static_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        static_addr: static_addr
        group_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        group_name: group_name
        static_domain: static_domain
        static_id: static_id
        sharednetwork_name: sharednetwork_name
        static_class_name: static_class_name
        server_id: server_id
        scope_net_mask: scope_net_mask
        scope_net_addr: scope_net_addr
        group_id: group_id
        scope_space_id: scope_space_id
        scope_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        static_address_addr: static_address_addr
        static_name: static_name
        static_identifier: static_identifier
        server_type: server_type
        scope_class_name: scope_class_name
      properties:
        static_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        static_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_class_name:
          description: The name of the class applied to the DHCPv4 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server_id:
          description: The database identifier (ID) of the DHCPv4 server the object
            belongs to.
          type: string
        server_name:
          description: The name of the DHCPv4 server the object belongs to.
          type: string
        server_type:
          description: 'The type of the DHCPv4 server the object belongs to: <table><caption>dhcp_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >msrpc</td><td >Microsoft DHCP server</td></tr><tr><td >dcs</td><td >Nominum
            DCS server</td></tr><tr><td >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server_version:
          description: The version details of the DHCPv4 server the object belongs
            to.
          type: string
        group_class_name:
          description: The name of the class applied to the DHCPv4 group the static
            belongs to, it can be preceded by the class directory.
          type: string
        group_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        group_id:
          description: The database identifier (ID) of the DHCPv4 group.
          type: string
        group_name:
          description: The name of the DHCPv4 group associated with the object.
          type: string
        static_addr:
          description: The IP address associated with the DHCPv4 static.
          type: string
        static_class_name:
          description: The name of the class applied to the DHCPv4 static, it can
            be preceded by the class directory.
          type: string
        static_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        static_domain:
          description: The domain name associated with the DHCPv4 static.
          type: string
        static_expire_time:
          description: The expiration time of the lease associated with the DHCPv4
            static, in decimal UNIX date format.
          type: string
        static_id:
          description: The database identifier (ID) of the DHCPv4 static.
          type: string
        static_identifier:
          description: TODO:dhcp_static_list.output.static_identifier
          type: string
        static_address_addr:
          description: The IP address associated with the DHCPv4 static, in hexadecimal
            format.
          type: string
        static_last_seen:
          description: The last time the MAC address associated with the DHCPv4 static
            was seen on the network, in decimal UNIX date format.
          type: string
        static_mac_addr:
          description: The MAC address associated with the DHCPv4 static. It is composed
            of 7 sections, <b>00:11:22:33:44:55:66</b>, where <b>00</b> is the MAC
            address type. The type <b>01</b> indicates Ethernet.
          type: string
        static_name:
          description: The name of the DHCPv4 static.
          type: string
        scope_class_name:
          description: The name of the class applied to the DHCPv4 scope the object
            belongs to, it can be preceded by the class directory.
          type: string
        scope_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        scope_end_address_addr:
          description: The last IP address of the DHCPv4 scope the object belongs
            to, in hexadecimal format.
          type: string
        scope_id:
          description: The database identifier (ID) of the DHCPv4 scope the object
            belongs to.
          type: string
        scope_name:
          description: The name of the DHCPv4 scope the object belongs to.
          type: string
        scope_net_addr:
          description: The first IP address of the DHCPv4 scope the object belongs.
          type: string
        scope_net_mask:
          description: The netmask of the DHCPv4 scope the object belongs to. It is
            expressed in dot-decimal notation and defines the number of addresses
            the scope contains.
          type: string
        scope_space_id:
          description: The database identifier (ID) of the space associated with the
            DHCPv4 scope the object belongs to.
          type: string
        scope_size:
          description: The number of IP addresses the DHCPv4 scope the object belongs
            to contains.
          type: string
        scope_start_address_addr:
          description: The first IP address of the DHCPv4 scope the object belongs
            to, in hexadecimal format.
          type: string
        sharednetwork_id:
          description: The database identifier (ID) of the DHCPv4 shared network the
            object belongs to.
          type: string
        sharednetwork_name:
          description: The name of the DHCPv4 shared network the object belongs to.
          type: string
        static_mac_vendor:
          description: The vendor details of the client associated with the DHCPv4
            static.
          type: string
        static_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv4 smart architecture
            managing the DHCPv4 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_static_add_success_data:
      example:
        static_id: static_id
      properties:
        static_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dhcp_static_edit_success_data:
      example:
        static_id: static_id
      properties:
        static_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dhcp_static_delete_success_data:
      example:
        static_id: static_id
      properties:
        static_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dhcp_static6_data_data:
      example:
        server6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        scope6_size: scope6_size
        static6_address6_addr: static6_address6_addr
        server6_version: server6_version
        scope6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        static6_mac_vendor: static6_mac_vendor
        group6_id: group6_id
        server6_name: server6_name
        smart_parent_id: smart_parent_id
        static6_class_name: static6_class_name
        smart_parent_name: smart_parent_name
        server6_class_name: server6_class_name
        server6_id: server6_id
        static6_time: static6_time
        server6_type: server6_type
        static6_name: static6_name
        static6_client_duid: static6_client_duid
        sharednetwork6_name: sharednetwork6_name
        group6_name: group6_name
        static6_id: static6_id
        static6_end_time: static6_end_time
        scope6_end_address6_addr: scope6_end_address6_addr
        static6_domain: static6_domain
        scope6_class_name: scope6_class_name
        scope6_name: scope6_name
        scope6_space_id: scope6_space_id
        static6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        scope6_id: scope6_id
        static6_multistatus: static6_multistatus
        static6_mac_addr: static6_mac_addr
        sharednetwork6_id: sharednetwork6_id
        group6_class_name: group6_class_name
        static6_state: static6_state
        scope6_start_address6_addr: scope6_start_address6_addr
        static6_delayed_time: static6_delayed_time
        group6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
      properties:
        static6_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        server6_class_name:
          description: The name of the class applied to the DHCPv6 server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 server the object
            belongs to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server6_id:
          description: The database identifier (ID) of the DHCPv6 server the object
            belongs to.
          type: string
        server6_name:
          description: The name of the DHCPv6 server the object belongs to.
          type: string
        server6_type:
          description: 'The type of the DHCPv6 server the object belongs to: <table><caption>dhcp6_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DHCP server or EfficientIP DHCP Package</td></tr><tr><td
            >vdhcp</td><td >EfficientIP DHCP smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server6_version:
          description: The version details of the DHCPv6 server the object belongs
            to.
          type: string
        group6_class_name:
          description: The name of the class applied to the DHCPv6 group the static
            belongs to, it can be preceded by the class directory.
          type: string
        group6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 group the static
            belongs to, and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        group6_id:
          description: The database identifier (ID) of the DHCPv6 group.
          type: string
        group6_name:
          description: The name of the DHCPv6 group associated with the object.
          type: string
        static6_class_name:
          description: The name of the class applied to the DHCPv6static, it can be
            preceded by the class directory.
          type: string
        static6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 static and their
            value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        static6_client_duid:
          description: The client DHCP Unique Identifier (DUID) associated with the
            DHCPv6 static.
          type: string
        static6_domain:
          description: The domain name associated with the DHCPv6 static.
          type: string
        static6_end_time:
          description: Internal use. Not documented.
          type: string
        static6_id:
          description: The database identifier (ID) of the DHCPv6 static.
          type: string
        static6_address6_addr:
          description: The IP address associated with the DHCPv6 static, in hexadecimal
            format.
          type: string
        static6_mac_addr:
          description: The MAC address associated with the DHCPv6 static.
          type: string
        static6_name:
          description: The name of the DHCPv6 static.
          type: string
        static6_state:
          description: Internal use. Not documented.
          type: string
        static6_time:
          description: Internal use. Not documented.
          type: string
        scope6_class_name:
          description: The name of the class applied to the DHCPv6 scope the object
            belongs to, it can be preceded by the class directory.
          type: string
        scope6_class_parameters:
          description: 'The class parameters applied to the DHCPv6 scope the object
            belongs to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        scope6_end_address6_addr:
          description: The last IP address of the DHCPv6 scope the object belongs
            to, in hexadecimal format.
          type: string
        scope6_id:
          description: The database identifier (ID) of the DHCPv6 scope the object
            belongs to.
          type: string
        scope6_name:
          description: The name of the DHCPv6 scope the object belongs to.
          type: string
        scope6_space_id:
          description: The database identifier (ID) of the space associated with the
            DHCPv6 scope the object belongs to.
          type: string
        scope6_size:
          description: The number of IP addresses the DHCPv6 scope the object belongs
            to contains.
          type: string
        scope6_start_address6_addr:
          description: The first IP address of the DHCPv6 scope the object belongs
            to, in hexadecimal format.
          type: string
        sharednetwork6_id:
          description: The database identifier (ID) of the DHCPv6 shared network the
            object belongs to.
          type: string
        sharednetwork6_name:
          description: The name of the DHCPv6 shared network the object belongs to.
          type: string
        static6_mac_vendor:
          description: The vendor details of the client associated with the DHCPv6
            static.
          type: string
        static6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DHCPv6 smart architecture
            managing the DHCPv6 server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DHCPv4 smart architecture managing the DHCPv4
            server the object belongs to. <b>#</b> indicates that the server is not
            managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dhcp_static6_add_success_data:
      example:
        static6_id: static6_id
      properties:
        static6_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dhcp_static6_edit_success_data:
      example:
        static6_id: static6_id
      properties:
        static6_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dhcp_static6_delete_success_data:
      example:
        static6_id: static6_id
      properties:
        static6_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dns_acl_data_data:
      example:
        acl_value: acl_value
        acl_name: acl_name
        acl_id: acl_id
        server_id: server_id
      properties:
        server_id:
          description: The database identifier (ID) of the DNS server the object belongs
            to.
          type: string
        acl_id:
          description: The database identifier (ID) of the DNS ACL.
          type: string
        acl_name:
          description: The name of the DNS ACL.
          type: string
        acl_value:
          description: 'The values of the DNS ACL in order of priority, as follows:
            &lt;value_1&gt;;&lt;value_2&gt;... .'
          type: string
      type: object
    dns_acl_add_success_data:
      example:
        acl_id: acl_id
      properties:
        acl_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dns_acl_edit_success_data:
      example:
        acl_id: acl_id
      properties:
        acl_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dns_acl_delete_success_data:
      example:
        acl_id: acl_id
      properties:
        acl_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dns_rr_data_data:
      example:
        rr_id: rr_id
        rr_value_ip4_addr: rr_value_ip4_addr
        server_version: server_version
        rr_ttl: rr_ttl
        smart_parent_name: smart_parent_name
        rr_full_name_utf: rr_full_name_utf
        zone_space_name: zone_space_name
        zone_type: zone_type
        rr_all_value: rr_all_value
        view_class_name: view_class_name
        rr_value1: rr_value1
        server_id: server_id
        rr_glue: rr_glue
        rr_delayed_delete_time: rr_delayed_delete_time
        rr_last_update_time: rr_last_update_time
        zone_masters: zone_masters
        zone_is_reverse: zone_is_reverse
        view_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        rr_full_name: rr_full_name
        server_name: server_name
        zone_sort_zone: zone_sort_zone
        zone_forwarders: zone_forwarders
        server_class_name: server_class_name
        zone_class_name: zone_class_name
        zone_name: zone_name
        rr_name_address_addr: rr_name_address_addr
        rr_value7: rr_value7
        zone_id: zone_id
        rr_value6: rr_value6
        rr_delayed_time: rr_delayed_time
        rr_class_name: rr_class_name
        smart_parent_id: smart_parent_id
        rr_value3: rr_value3
        rr_value2: rr_value2
        rr_value5: rr_value5
        rr_value4: rr_value4
        rr_type: rr_type
        server_cloud: server_cloud
        view_name: view_name
        server_comment: server_comment
        rr_value_address_addr: rr_value_address_addr
        view_id: view_id
        zone_is_rpz: zone_is_rpz
        rr_multistatus: rr_multistatus
        rr_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        zone_name_utf: zone_name_utf
        rr_name_ip4_addr: rr_name_ip4_addr
        rr_delayed_create_time: rr_delayed_create_time
        server_type: server_type
      properties:
        rr_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        rr_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        rr_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        server_class_name:
          description: The name of the class applied to the DNS server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server_cloud:
          description: Internal use. Not documented.
          type: string
        server_comment:
          description: The description of the DNS server the object belongs to.
          type: string
        server_id:
          description: The database identifier (ID) of the DNS server the object belongs
            to.
          type: string
        server_name:
          description: The name of the DNS server the object belongs to.
          type: string
        server_type:
          description: The type of the DNS server the object belongs to.<table><caption>dns_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DNS server or EfficientIP DNS Package</td></tr><tr><td
            >msdaemon</td><td >Agentless Microsoft DNS server</td></tr><tr><td >ans</td><td
            >Nominum DNS server (ANS)</td></tr><tr><td >aws</td><td >Amazon Route
            53 server</td></tr><tr><td >other</td><td >Generic DNS server</td></tr><tr><td
            >vdns</td><td >EfficientIP DNS smart architecture</td></tr></tbody></table></p><br/>
          type: string
        server_version:
          description: The version details of the DNS server the object belongs to.
          type: string
        view_class_name:
          description: The name of the class applied to the DNS view the object belongs
            to, it can be preceded by the class directory.
          type: string
        view_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        view_id:
          description: The database identifier (ID) of the DNS view the object belongs
            to.
          type: string
        view_name:
          description: The name of the DNS view the object belongs to.
          type: string
        zone_class_name:
          description: The name of the class applied to the DNS zone the object belongs
            to, it can be preceded by the class directory.
          type: string
        zone_forwarders:
          description: 'The IP address(es) of the forwarder(s) associated with the
            DNS zone the resource record belongs to. It lists the DNS servers to which
            any unknown query on this zone should be sent, as follows: <b>&lt;ip_address1&gt;;&lt;ip_address2&gt;;...</b>
            .'
          type: string
        zone_id:
          description: The database identifier (ID) of the DNS zone the object belongs
            to.
          type: string
        zone_is_reverse:
          description: A way to determine if the DNS zone the resource record belongs
            to provides reverse resolution (<b>1</b>) or direct/name resolution (<b>0</b>),
          type: string
        zone_is_rpz:
          description: The RPZ status of the DNS zone the resource record belongs
            to. <b>1</b> indicates that the DNS zone the record belongs to is a Response
            Policy Zone.
          type: string
        zone_masters:
          description: 'For resource records in slave DNS zones, the IP address of
            the DNS server and, if relevant, the name of the DNS view that contain
            the master DNS zone, as follows: <b>&lt;ip_addr&gt;;</b> or <b>&lt;ip_addr&gt;
            key &lt;dnsview_name&gt;;</b> .'
          type: string
        zone_name:
          description: The name of the DNS zone the object belongs to.
          type: string
        zone_name_utf:
          description: The name of the DNS zone the resource record belongs to, in
            UTF-8 format.
          type: string
        zone_space_name:
          description: The name of the space associated with the DNS zone the RR belongs
            to.
          type: string
        zone_sort_zone:
          description: Internal use. Not documented.
          type: string
        zone_type:
          description: The type of the DNS zone the object belongs to, either <b>master</b>,
            <b>slave</b>, <b>forward</b>, <b>stub</b>, <b>hint</b> or <b>delegation-only</b>.
          type: string
        rr_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        rr_all_value:
          description: 'The concatenated values of the DNS resource record, as follows:
            <b>&lt;value1&gt;, &lt;value2&gt;, &lt;value3&gt;, &lt;value4&gt;, &lt;value5&gt;,
            &lt;value6&gt;, &lt;value7&gt;</b>.'
          type: string
        rr_class_name:
          description: TODO:dns_rr_list.output.rr_class_name
          type: string
        rr_class_parameters:
          description: The class parameters applied to the DNS rr the object belongs
            to and their value
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        rr_full_name:
          description: The full name of the DNS resource record.
          type: string
        rr_full_name_utf:
          description: The name of the DNS resource record in UTF-8 format.
          type: string
        rr_glue:
          description: The shortname of the DNS resource record.
          type: string
        rr_id:
          description: The database identifier (ID) of the DNS resource record.
          type: string
        rr_last_update_time:
          description: TODO:dns_rr_list.output.rr_last_update_time
          type: string
        rr_name_ip4_addr:
          description: Internal use. Not documented.
          type: string
        rr_name_address_addr:
          description: Internal use. Not documented.
          type: string
        rr_type:
          description: 'The type of the DNS resource record.<table><caption>rr_type
            possible values</caption><br/><thead><tr><th>Value</th><th>Record type
            description</th></tr><br/></thead><br/><tbody><tr><td >SOA</td><td >Start
            of Authority. Defines the zone name, an email contact and various time
            and refresh values applicable to the zone. It is automatically generated
            upon creation of a zone and cannot be added manually.</td></tr><tr><td
            >NS</td><td >Name Server. Defines the authoritative name server(s) for
            the domain (defined by the SOA record) or the subdomain. The NS record
            that indicates which server has authority over a zone is automatically
            generated upon the creation of a zone, once the server has been synchronized.</td></tr><tr><td
            >A</td><td >IPv4 Address. An IPv4 address for a host.</td></tr><tr><td
            >PTR</td><td >Pointer Record. Address Resolution, from an IP address (IPv4
            or IPv6) to a host. Used in reverse mapping.</td></tr><tr><td >AAAA</td><td
            >IPv6 Address. An IPv6 address for a host.</td></tr><tr><td >CNAME</td><td
            >Canonical Name. An alias name for a host.</td></tr><tr><td >MX</td><td
            >Mail Exchange. The mail server/exchanger that services this zone.</td></tr><tr><td
            >SRV</td><td >Services record. Defines services available in the zone,
            for example, LDAP, HTTP, etc...</td></tr><tr><td >DNAME</td><td >Delegation
            of Reverse Names. Delegation of reverse addresses primarily in IPv6. (Deprecated,
            use the CNAME RR instead)</td></tr><tr><td >TXT</td><td >Text. Information
            associated with a name.</td></tr><tr><td >DS</td><td >Delegation Signer,
            a DNSSEC related RR used to verify the validity of the ZSK of a subdomain.</td></tr><tr><td
            >DNSKEY</td><td >DNS Key. It contains the public cryptographic key used
            to sign the zone with DNSSEC.</td></tr><tr><td >65534</td><td >A private
            type record automatically added to the zone once it is signed with DNSSEC.</td></tr><tr><td
            >HINFO</td><td >System Information. Information about a host: hardware
            type and operating system description.</td></tr><tr><td >MINFO</td><td
            >Mailbox mail list Information. Defines the mail administrator for a mail
            list and optionally a mailbox to receive error messages relating to the
            mail list.</td></tr><tr><td >AFSDB</td><td >AFS Database. Location of
            the AFS servers.</td></tr><tr><td >WKS</td><td >Well-Known Service. Defines
            the services and protocols supported by a host. (Deprecated, use the SRV
            RR instead)</td></tr><tr><td >NAPTR</td><td >Naming Authority Pointer
            Record. General purpose definition of rule set to be used by applications
            e.g. VoIP.</td></tr><tr><td >NSAP</td><td >Network Service Access Point.
            Defines record (equivalent of an A record) maps a host name to an endpoint
            address.</td></tr></tbody></table></p><br/>'
          type: string
        rr_value_ip4_addr:
          description: Internal use. Not documented.
          type: string
        rr_value_address_addr:
          description: Internal use. Not documented.
          type: string
        rr_ttl:
          description: The time to live of the DNS resource record, in seconds.
          type: string
        rr_value1:
          description: The first or only value required for the DNS resource record,
            as detailed in the service description.
          type: string
        rr_value2:
          description: The second value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value3:
          description: The third value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value4:
          description: The fourth value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value5:
          description: The fifth value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value6:
          description: The sixth value of the DNS resource record, depending on its
            type, as detailed in the service description.
          type: string
        rr_value7:
          description: The seventh value of the DNS resource record, depending on
            its type, as detailed in the service description.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DNS smart architecture
            managing the DNS server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DNS smart architecture managing the DNS server
            the object belongs to. <b>#</b> indicates that the server the object belongs
            to is not managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dns_rr_add_success_data:
      example:
        rr_id: rr_id
      properties:
        rr_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dns_rr_edit_success_data:
      example:
        rr_id: rr_id
      properties:
        rr_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dns_rr_delete_success_data:
      example:
        rr_id: rr_id
      properties:
        rr_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dns_server_data_data:
      example:
        server_ipm_protocol: server_ipm_protocol
        server_stat_enabled: server_stat_enabled
        server_dnssec_validation: server_dnssec_validation
        server_ipm_type: server_ipm_type
        server_stat_time: server_stat_time
        server_gss_keytab_id: server_gss_keytab_id
        server_ldap_domain: server_ldap_domain
        server_version: server_version
        server_stat_niceness: server_stat_niceness
        server_recursion: server_recursion
        az_keyid: az_keyid
        server_multistatus: server_multistatus
        smart_parent_name: smart_parent_name
        server_allow_recursion: server_allow_recursion
        server_key_name: server_key_name
        az_tenantid: az_tenantid
        server_key_proto: server_key_proto
        server_also_notify: server_also_notify
        server_allow_query_cache: server_allow_query_cache
        server_notify: server_notify
        ad_domain: ad_domain
        server_stat_period: server_stat_period
        server_id: server_id
        server_addr: server_addr
        server_gslb_supported: server_gslb_supported
        ad_user: ad_user
        server_forward: server_forward
        server_role: server_role
        reverse_proxy_conf: reverse_proxy_conf
        server_querylog_state: server_querylog_state
        server_name: server_name
        smart_parent_arch: smart_parent_arch
        server_gss_enabled: server_gss_enabled
        server_class_name: server_class_name
        az_subscriptionid: az_subscriptionid
        server_forwarders: server_forwarders
        server_hybrid: server_hybrid
        server_guardian_gui_management_supported: server_guardian_gui_management_supported
        smart_parent_id: smart_parent_id
        server_blast_status: server_blast_status
        server_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        server_ipm_is_package: server_ipm_is_package
        smart_arch: smart_arch
        server_aws_keyid: server_aws_keyid
        server_ipm_login: server_ipm_login
        server_state: server_state
        server_cloud: server_cloud
        server_blast_enabled: server_blast_enabled
        server_comment: server_comment
        server_synching: server_synching
        total_smart_members: total_smart_members
        server_isolated: server_isolated
        server_windns_port: server_windns_port
        server_force_hybrid: server_force_hybrid
        smart_public_ns_list: smart_public_ns_list
        server_key_value: server_key_value
        server_snmp_id: server_snmp_id
        smart_members_name: smart_members_name
        server_allow_query: server_allow_query
        server_ldap_user: server_ldap_user
        server_guardian_supported: server_guardian_supported
        server_windns_protocol: server_windns_protocol
        server_allow_transfer: server_allow_transfer
        server_windns_use_ssl: server_windns_use_ssl
        az_group: az_group
        server_type: server_type
      properties:
        ad_domain:
          description: TODO:dns_server_list.output.ad_domain
          type: string
        ad_user:
          description: TODO:dns_server_list.output.ad_user
          type: string
        server_aws_keyid:
          description: The AWS access key identifier (ID) of the DNS server.
          type: string
        az_group:
          description: TODO:dns_server_list.output.az_group
          type: string
        az_keyid:
          description: TODO:dns_server_list.output.az_keyid
          type: string
        az_subscriptionid:
          description: TODO:dns_server_list.output.az_subscriptionid
          type: string
        az_tenantid:
          description: TODO:dns_server_list.output.az_tenantid
          type: string
        server_allow_query:
          description: 'The ACL values associated with the allow-query configuration
            of the DNS server, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        server_allow_query_cache:
          description: 'The ACL values associated with the allow-query-cache configuration
            of the DNS server, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        server_allow_recursion:
          description: 'The ACL values associated with the allow-recursion configuration
            of the DNS server, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        server_allow_transfer:
          description: 'The ACL values associated with the allow-transfer configuration
            of the DNS server, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        server_also_notify:
          description: The IP address and port of the DNS server managing the smart
            architecture. If the parameter <b>dns_notify</b> is set to <b>yes</b>
            or <b>explicit</b>, the server specified is instantly notified of any
            slave zones updates.
          type: string
        server_class_name:
          description: The name of the class applied to the DNS server, it can be
            preceded by the class directory.
          type: string
        server_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server_cloud:
          description: Internal use. Not documented.
          type: string
        server_comment:
          description: The description of the DNS server.
          type: string
        server_force_hybrid:
          description: Internal use. Not documented.
          type: string
        server_forward:
          description: 'The forwarding mode of the DNS server. No value indicates
            that the forwarding is disabled: <table><caption>dns_forward possible
            values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >first</td><td >The server sends the queries to the forwarder(s). If no
            answer is returned, it attempts to answer the queries on its own.</td></tr><tr><td
            >only</td><td >The server only forwards the queries to the forwarder(s).
            Required by some reverse forward zones (e.g., in the case of private addresses).</td></tr></tbody></table></p><br/>'
          type: string
        server_forwarders:
          description: 'The IP address(es) of the forwarder(s) associated with the
            DNS server. It lists the DNS servers to which any unknown zone should
            be sent, as follows: <b>&lt;ip_address1&gt;;&lt;ip_address2&gt;;...</b>
            .'
          type: string
        server_hybrid:
          description: Internal use. Not documented.
          type: string
        server_id:
          description: The database identifier (ID) of the DNS server, a unique numeric
            key value automatically incremented when you add a DNS server.
          type: string
        server_key_name:
          description: The name of the DNS TSIG key associated with the DNS server.
          type: string
        server_key_proto:
          description: The encryption protocol of the TSIG key associated with the
            DNS server.
          type: string
        server_key_value:
          description: The value of the TSIG key associated with the DNS server.
          type: string
        server_name:
          description: The name of the DNS server.
          type: string
        server_notify:
          description: The notify status of the DNS server:<table><caption>dns_notify
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >no</td><td >No notify message is sent when changes are performed in the
            master zones.</td></tr><tr><td >yes</td><td >The notify messages are sent
            to the target of the NS records of the master zone. They are also sent
            to the IP address(es) specified in the parameter .</td></tr><tr><td >explicit</td><td
            >The notify messages are only sent to the IP address(es) specified in
            the parameter .</td></tr></tbody></table></p><br/>
          type: string
        server_recursion:
          description: The recursion status of the DNS server:<table><caption>dns_recursion
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >no</td><td >The server only provides iterative query behavior - normally
            resulting in a referral. If the answer to the query already exists in
            the cache it will be returned whatever the value of this statement.</td></tr><tr><td
            >yes</td><td >The server always provides recursive query behavior if requested
            by the client.</td></tr></tbody></table></p><br/>
          type: string
        server_role:
          description: The role of the DNS server in the smart architecture, either
            <b>master</b>, <b>hidden-master</b>, <b>pseudo-master</b> or <b>slave</b>.
          type: string
        server_state:
          description: 'The status of the DNS server:<table><caption>dns_state possible
            values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ER</td><td >The license used in SOLIDserver is not compliant with the
            added server: the license is invalid.</td></tr><tr><td >ES</td><td >The
            server configuration could not be parsed properly.</td></tr><tr><td >ET</td><td
            >The server does not answer anymore due to a scheduled configuration of
            the server.</td></tr><tr><td >IC</td><td >The SSL credentials are invalid</td></tr><tr><td
            >IP</td><td >The provided account does not have sufficient privileges
            to remotely manage the MS server.</td></tr><tr><td >IR</td><td >SOLIDserver
            cannot resolve the AWS DNS service. The Amazon services are unreachable
            and the Amazon Route 53 server cannot be managed. Make sure that the DNS
            resolvers declared on the page  are valid.</td></tr><tr><td >IS</td><td
            >There was a setting error during the server declaration. For instance,
            some settings were added to a server that does not support them or a smart
            architecture is not managing any physical server.</td></tr><tr><td >IT</td><td
            >The server editions performed from the GUI are not pushed to the server
            because SOLIDserver time and date are incorrect. You must use the UTC
            system on the appliance, especially when managing Amazon Route 53 servers.</td></tr><tr><td
            >LS</td><td >The server configuration is not viable.</td></tr><tr><td
            >N</td><td >The server does not have a status as it has not synchronized
            yet.</td></tr><tr><td >UE</td><td >An error occurred that SOLIDserver
            could not identify.</td></tr><tr><td >Y</td><td >The server is operational.</td></tr></tbody></table></p><br/>'
          type: string
        server_synching:
          description: The synchronization status of the DNS server. <b>1</b> indicates
            that the server is currently being synchronized.
          type: string
        server_type:
          description: 'The type of the DNS server: <table><caption>dns_type possible
            values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DNS server or EfficientIP DNS Package</td></tr><tr><td
            >msdaemon</td><td >Agentless Microsoft DNS server</td></tr><tr><td >ans</td><td
            >Nominum DNS server (ANS)</td></tr><tr><td >aws</td><td >Amazon Route
            53 server</td></tr><tr><td >other</td><td >Generic DNS server</td></tr><tr><td
            >vdns</td><td >EfficientIP DNS smart architecture</td></tr></tbody></table></p><br/>'
          type: string
        server_version:
          description: The version details of the DNS server.
          type: string
        server_blast_enabled:
          description: The status of the service DNS Guardian, either enabled (<b>1</b>)
            or disabled (<b>0</b>).
          type: string
        server_blast_status:
          description: The status of the Guardian server, either OK (<b>1</b>), Stopped
            (<b>2</b>), Invalid Credentials (<b>4</b>) or Timeout (<b>5</b>).
          type: string
        server_gslb_supported:
          description: The license GSLB activation status. <b>1</b> indicates your
            license includes GSLB and your appliance supports it.
          type: string
        server_guardian_gui_management_supported:
          description: Internal use. Not documented.
          type: string
        server_guardian_supported:
          description: The license Guardian activation status. <b>1</b> indicates
            your license includes Guardian and your appliance supports its latest
            features.
          type: string
        server_dnssec_validation:
          description: The DNSSEC resolution status of the DNS server. <b>yes</b>
            indicates it is enabled.
          type: string
        server_gss_enabled:
          description: The GSS-TSIG status of the DNS server. <b>1</b> indicates that
            GSS-TSIG is enabled on the server.
          type: string
        server_gss_keytab_id:
          description: The database identifier (ID) of the DNS GSS-TSIG keytab.
          type: string
        server_addr:
          description: The IP address of the DNS server, in hexadecimal format.
          type: string
        server_ipm_login:
          description: Internal use. Not documented.
          type: string
        server_ipm_is_package:
          description: The DNS server package information. <b>Y</b> for an EfficientIP
            DNS Package server, <b>N</b> for an appliance or virtual machine, <b>U</b>
            the package information is irrelevant. For servers with a <b>dns_type</b>
            set to <b>ipm</b>, <b>U</b> indicates either EfficientIP DNS Packages
            or appliances/virtual machines.
          type: string
        server_ipm_protocol:
          description: Internal use. Not documented.
          type: string
        server_ipm_type:
          description: 'The engine type of the DNS server: <b>named</b> (BIND engine),
            <b>nsd</b> (NSD engine) or <b>unbound</b> (Unbound engine).'
          type: string
        server_isolated:
          description: A way to determine if the server can update any other module
            <b>(1)</b>.
          type: string
        server_ldap_domain:
          description: The LDAP domain associated with the DNS server.
          type: string
        server_ldap_user:
          description: The LDAP login associated with the DNS server.
          type: string
        server_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        server_querylog_state:
          description: The DNS querylog status. <b>1</b> indicates that the DNS server
            querylog is enabled.
          type: string
        reverse_proxy_conf:
          description: TODO:dns_server_list.output.reverse_proxy_conf
          type: string
        server_snmp_id:
          description: Internal use. Not documented.
          type: string
        server_stat_enabled:
          description: Internal use. Not documented.
          type: string
        server_stat_niceness:
          description: Internal use. Not documented.
          type: string
        server_stat_period:
          description: Internal use. Not documented.
          type: string
        server_stat_time:
          description: Internal use. Not documented.
          type: string
        total_smart_members:
          description: The total number of servers managed by the DNS smart architecture.
          type: string
        smart_arch:
          description: The type of the DNS smart architecture:<table><caption>vdns_arch
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >masterslave</td><td >Master/Slave</td></tr><tr><td >stealth</td><td >Stealth</td></tr><tr><td
            >multimaster</td><td >Multi-Master</td></tr><tr><td >single</td><td >Single-Server</td></tr><tr><td
            >farm</td><td >Farm</td></tr></tbody></table></p><br/>
          type: string
        smart_members_name:
          description: 'The list of the servers managed by the DNS smart architecture,
            as follows: <b>&lt;dns_name&gt;,&lt;dns_name&gt;,...</b> .'
          type: string
        smart_parent_arch:
          description: The type of the DNS smart architecture managing the DNS server.
            No value indicates that the server is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DNS smart architecture
            managing the DNS server. <b>0</b> indicates that the server is not managed
            by a smart architecture or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DNS smart architecture managing the DNS server.
            <b>#</b> indicates that the server is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_public_ns_list:
          description: 'The list of the published name servers associated with the
            DNS smart architecture, as follows: <b>&lt;ns1&gt;;&lt;ns2&gt;;...</b>
            .'
          type: string
        server_windns_port:
          description: Internal use. Not documented.
          type: string
        server_windns_protocol:
          description: Internal use. Not documented.
          type: string
        server_windns_use_ssl:
          description: Internal use. Not documented.
          type: string
      type: object
    dns_view_data_data:
      example:
        server_name: server_name
        view_order: view_order
        server_class_name: server_class_name
        server_gss_keytab_id: server_gss_keytab_id
        server_version: server_version
        view_allow_transfer: view_allow_transfer
        view_delayed_create_time: view_delayed_create_time
        view_delayed_delete_time: view_delayed_delete_time
        view_recursion: view_recursion
        smart_parent_id: smart_parent_id
        server_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        smart_parent_name: smart_parent_name
        server_cloud: server_cloud
        view_name: view_name
        server_comment: server_comment
        view_class_name: view_class_name
        view_key_name: view_key_name
        view_id: view_id
        server_id: server_id
        server_addr: server_addr
        view_multistatus: view_multistatus
        view_match_clients: view_match_clients
        view_match_to: view_match_to
        view_allow_recursion: view_allow_recursion
        view_allow_query: view_allow_query
        view_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        server_type: server_type
      properties:
        view_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        view_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_class_name:
          description: The name of the class applied to the DNS server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server_cloud:
          description: Internal use. Not documented.
          type: string
        server_comment:
          description: The description of the DNS server the object belongs to.
          type: string
        server_id:
          description: The database identifier (ID) of the DNS server the object belongs
            to.
          type: string
        server_name:
          description: The name of the DNS server the object belongs to.
          type: string
        server_type:
          description: The type of the DNS server the object belongs to.<table><caption>dns_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DNS server or EfficientIP DNS Package</td></tr><tr><td
            >msdaemon</td><td >Agentless Microsoft DNS server</td></tr><tr><td >ans</td><td
            >Nominum DNS server (ANS)</td></tr><tr><td >aws</td><td >Amazon Route
            53 server</td></tr><tr><td >other</td><td >Generic DNS server</td></tr><tr><td
            >vdns</td><td >EfficientIP DNS smart architecture</td></tr></tbody></table></p><br/>
          type: string
        server_version:
          description: The version details of the DNS server the object belongs to.
          type: string
        view_allow_query:
          description: 'The ACL values associated with the allow-query configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_allow_recursion:
          description: 'The ACL values associated with the allow-recursion configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_allow_transfer:
          description: 'The ACL values associated with the allow-transfer configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_class_name:
          description: The name of the class applied to the DNS view, it can be preceded
            by the class directory.
          type: string
        view_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        view_id:
          description: The database identifier (ID) of the DNS view.
          type: string
        view_key_name:
          description: The name of the DNS TSIG key associated with the DNS view.
          type: string
        view_match_clients:
          description: 'The ACL values associated with the match clients configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;...</b>
            . Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_match_to:
          description: 'The ACL values associated with the match destination configuration
            of the DNS view, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;...</b>
            . Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        view_name:
          description: The name of the DNS view.
          type: string
        view_order:
          description: The level of the DNS view, where <b>0</b> represents the highest
            level in the views hierarchy. The parameters <b>dnsview_match_client</b>
            and <b>dnsview_match_to</b> of each view in a server are reviewed following
            this order.
          type: string
        view_recursion:
          description: The recursion status of the DNS view:<table><caption>dnsview_recursion
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >no</td><td >The view only provides iterative query behavior - normally
            resulting in a referral. If the answer to the query already exists in
            the cache it will be returned whatever the value of this statement.</td></tr><tr><td
            >yes</td><td >The view always provides recursive query behavior if requested
            by the client.</td></tr></tbody></table></p><br/>
          type: string
        server_gss_keytab_id:
          description: The database identifier (ID) of the DNS GSS-TSIG keytab.
          type: string
        server_addr:
          description: The IP address of the DNS server the object belongs to, in
            hexadecimal format.
          type: string
        view_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DNS smart architecture
            managing the DNS server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DNS smart architecture managing the DNS server
            the object belongs to. <b>#</b> indicates that the server the object belongs
            to is not managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dns_view_add_success_data:
      example:
        view_id: view_id
      properties:
        view_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dns_view_edit_success_data:
      example:
        view_id: view_id
      properties:
        view_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dns_view_delete_success_data:
      example:
        view_id: view_id
      properties:
        view_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dns_viewparam_data_data:
      example:
        server_name: server_name
        viewparam_key: viewparam_key
        viewparam_delayed_delete_time: viewparam_delayed_delete_time
        viewparam_delayed_create_time: viewparam_delayed_create_time
        viewparam_value: viewparam_value
        view_id: view_id
        oid: oid
        server_id: server_id
        view_name: view_name
      properties:
        viewparam_value:
          description: The value of the DNS option set on the view.
          type: string
        viewparam_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        viewparam_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_id:
          description: The database identifier (ID) of the DNS server the object belongs
            to.
          type: string
        server_name:
          description: The name of the DNS server the object belongs to.
          type: string
        view_id:
          description: The database identifier (ID) of the DNS view.
          type: string
        view_name:
          description: The name of the DNS view.
          type: string
        oid:
          description: The database identifier (ID) of the DNS view param the object
            belongs to.
          type: string
        viewparam_key:
          description: The name of the DNS option set on the view.
          type: string
      type: object
    dns_viewparam_add_success_data:
      example:
        viewparam_id: viewparam_id
      properties:
        viewparam_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dns_viewparam_edit_success_data:
      example:
        viewparam_id: viewparam_id
      properties:
        viewparam_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dns_viewparam_delete_success_data:
      example:
        viewparam_id: viewparam_id
      properties:
        viewparam_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dns_zone_data_data:
      example:
        server_ipm_protocol: server_ipm_protocol
        zone_space_id: zone_space_id
        zone_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        server_ipm_type: server_ipm_type
        server_gss_keytab_id: server_gss_keytab_id
        server_version: server_version
        smart_parent_name: smart_parent_name
        zone_rpz_log: zone_rpz_log
        zone_space_name: zone_space_name
        zone_type: zone_type
        zone_allow_update: zone_allow_update
        zone_rev_sort_zone: zone_rev_sort_zone
        zone_ad_integrated: zone_ad_integrated
        zone_forward: zone_forward
        view_class_name: view_class_name
        zone_allow_transfer: zone_allow_transfer
        zone_also_notify: zone_also_notify
        zone_synching: zone_synching
        zone_use_update_policy: zone_use_update_policy
        server_id: server_id
        server_addr: server_addr
        zone_xfer_done: zone_xfer_done
        zone_masters: zone_masters
        zone_is_reverse: zone_is_reverse
        zone_notify: zone_notify
        view_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        server_name: server_name
        zone_num_keys: zone_num_keys
        zone_sort_zone: zone_sort_zone
        zone_forwarders: zone_forwarders
        server_class_name: server_class_name
        zone_class_name: zone_class_name
        zone_name: zone_name
        zone_order: zone_order
        zone_delayed_create_time: zone_delayed_create_time
        zone_id: zone_id
        smart_parent_id: smart_parent_id
        server_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        zone_ds: zone_ds
        server_state: server_state
        server_cloud: server_cloud
        view_name: view_name
        server_comment: server_comment
        view_id: view_id
        zone_delayed_delete_time: zone_delayed_delete_time
        zone_is_rpz: zone_is_rpz
        server_force_hybrid: server_force_hybrid
        zone_name_utf: zone_name_utf
        zone_allow_query: zone_allow_query
        zone_multistatus: zone_multistatus
        server_type: server_type
      properties:
        zone_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        zone_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_class_name:
          description: The name of the class applied to the DNS server the object
            belongs to, it can be preceded by the class directory.
          type: string
        server_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        server_cloud:
          description: Internal use. Not documented.
          type: string
        server_comment:
          description: The description of the DNS server the object belongs to.
          type: string
        server_force_hybrid:
          description: Internal use. Not documented.
          type: string
        server_id:
          description: The database identifier (ID) of the DNS server the object belongs
            to.
          type: string
        server_name:
          description: The name of the DNS server the object belongs to.
          type: string
        server_state:
          description: 'The status of the DNS server the object belongs to. <table><caption>dns_state
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ER</td><td >The license used in SOLIDserver is not compliant with the
            added server: the license is invalid.</td></tr><tr><td >ES</td><td >The
            server configuration could not be parsed properly.</td></tr><tr><td >ET</td><td
            >The server does not answer anymore due to a scheduled configuration of
            the server.</td></tr><tr><td >IC</td><td >The SSL credentials are invalid</td></tr><tr><td
            >IP</td><td >The provided account does not have sufficient privileges
            to remotely manage the MS server.</td></tr><tr><td >IR</td><td >SOLIDserver
            cannot resolve the AWS DNS service. The Amazon services are unreachable
            and the Amazon Route 53 server cannot be managed. Make sure that the DNS
            resolvers declared on the page  are valid.</td></tr><tr><td >IS</td><td
            >There was a setting error during the server declaration. For instance,
            some settings were added to a server that does not support them or a smart
            architecture is not managing any physical server.</td></tr><tr><td >IT</td><td
            >The server editions performed from the GUI are not pushed to the server
            because SOLIDserver time and date are incorrect. You must use the UTC
            system on the appliance, especially when managing Amazon Route 53 servers.</td></tr><tr><td
            >LS</td><td >The server configuration is not viable.</td></tr><tr><td
            >N</td><td >The server does not have a status as it has not synchronized
            yet.</td></tr><tr><td >UE</td><td >An error occurred that SOLIDserver
            could not identify.</td></tr><tr><td >Y</td><td >The server is operational.</td></tr></tbody></table></p><br/>'
          type: string
        server_type:
          description: The type of the DNS server the object belongs to.<table><caption>dns_type
            possible values</caption><br/><thead><tr><th>Type</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >ipm</td><td >EfficientIP DNS server or EfficientIP DNS Package</td></tr><tr><td
            >msdaemon</td><td >Agentless Microsoft DNS server</td></tr><tr><td >ans</td><td
            >Nominum DNS server (ANS)</td></tr><tr><td >aws</td><td >Amazon Route
            53 server</td></tr><tr><td >other</td><td >Generic DNS server</td></tr><tr><td
            >vdns</td><td >EfficientIP DNS smart architecture</td></tr></tbody></table></p><br/>
          type: string
        server_version:
          description: The version details of the DNS server the object belongs to.
          type: string
        view_class_name:
          description: The name of the class applied to the DNS view the object belongs
            to, it can be preceded by the class directory.
          type: string
        view_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        view_id:
          description: The database identifier (ID) of the DNS view the object belongs
            to.
          type: string
        view_name:
          description: The name of the DNS view the object belongs to.
          type: string
        zone_ad_integrated:
          description: The AD integrated status of the DNS zone. <b>1</b> indicates
            that the DNS zone belongs to an Active Directory integrated Microsoft
            DNS server.
          type: string
        zone_allow_query:
          description: 'The ACL values associated with the allow-query configuration
            of the DNS zone, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        zone_allow_transfer:
          description: 'The ACL values associated with the allow-transfer configuration
            of the DNS zone, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        zone_allow_update:
          description: 'The ACL values associated with the allow-update configuration
            of the DNS zone, as follows: <b>&lt;value1&gt;;&lt;value2&gt;;... </b>.
            Values may include IP and network addresses, the name of TSIG keys and
            ACLs, preceded by <b>!</b> if the access is denied.'
          type: string
        zone_also_notify:
          description: The IP address and port of the DNS server managing the smart
            architecture the DNS zone belongs to. If the parameter <b>dnszone_notify</b>
            is set to <b>yes</b> or <b>explicit</b>, the server specified is instantly
            notified of any slave zones updates.
          type: string
        zone_class_name:
          description: The name of the class applied to the DNS zone, it can be preceded
            by the class directory.
          type: string
        zone_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        zone_forward:
          description: The forwarding mode of the DNS zone.<table><caption>dnszone_forward
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >first</td><td >The zone sends the queries to the forwarder(s). If no
            answer is returned, it attempts to answer the queries on its own.</td></tr><tr><td
            >only</td><td >The zone only forwards the queries to the forwarder(s).
            Required by some reverse forward zones (e.g., in the case of private addresses).</td></tr></tbody></table></p><br/>
            If the parameter has no value, it indicates that the forwarding is disabled.
          type: string
        zone_forwarders:
          description: 'The IP address(es) of the forwarder(s) associated with the
            DNS zone. It lists the DNS servers to which any unknown query on this
            zone should be sent, as follows: <b>&lt;ip_address1&gt;;&lt;ip_address2&gt;;...</b>
            .'
          type: string
        zone_id:
          description: The database identifier (ID) of the DNS zone.
          type: string
        zone_is_reverse:
          description: A way to determine if the DNS zone provides reverse resolution
            (1) or direct/name resolution (0),
          type: string
        zone_is_rpz:
          description: The RPZ status of the DNS zone. <b>1</b> indicates that the
            DNS zone is a Response Policy Zone.
          type: string
        zone_masters:
          description: 'For slave DNS zones, the IP address of the DNS server and,
            if relevant, the name of the DNS view that contain the master DNS zone,
            as follows: <b>&lt;ip_addr&gt;;</b> or <b>&lt;ip_addr&gt; key &lt;dnsview_name&gt;;</b>
            .'
          type: string
        zone_name:
          description: The name of the DNS zone.
          type: string
        zone_name_utf:
          description: The name of the DNS zone in UTF-8 format.
          type: string
        zone_notify:
          description: The notify status of the DNS zone.<table><caption>dnszone_notify
            possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >no</td><td >No notify message is sent.</td></tr><tr><td >yes</td><td
            >A notify message is sent to the name servers defined in the NS records
            of the zone and to the IP address(es) specified in the parameter .</td></tr><tr><td
            >explicit</td><td >A notify message is sent only to the IP address(es)
            specified in the parameter .</td></tr></tbody></table></p><br/>The notify
            message is not sent to the server itself or to the primary server defined
            in the SOA record of the zone.
          type: string
        zone_order:
          description: The level of the DNS zone, where 0 represents the highest level
            in the zones hierarchy. The RPZ rules parameters of each zone are reviewed
            following this order. The zones with the parameter zone_is_rpz set to
            0 will always return 0 for the parameter zone_order.
          type: string
        zone_rev_sort_zone:
          description: Internal use. Not documented.
          type: string
        zone_rpz_log:
          description: TODO:dns_zone_list.output.zone_rpz_log
          type: string
        zone_space_id:
          description: The database identifier (ID) of the space associated with the
            DNS zone.
          type: string
        zone_space_name:
          description: The name of the space associated with the DNS zone.
          type: string
        zone_sort_zone:
          description: Internal use. Not documented.
          type: string
        zone_synching:
          description: The synchronization status of the DNS zone. <b>1</b> indicates
            that the zone is currently being synchronized.
          type: string
        zone_type:
          description: The type of the DNS zone, either <b>master</b>, <b>slave</b>,
            <b>forward</b>, <b>stub</b>, <b>hint</b> or <b>delegation-only</b>.
          type: string
        zone_xfer_done:
          description: Internal use. Not documented.
          type: string
        zone_ds:
          description: The DNSSEC delegation signer (DS) fingerprint key associated
            with the DNS zone, if it is signed.
          type: string
        server_gss_keytab_id:
          description: The database identifier (ID) of the DNS GSS-TSIG keytab.
          type: string
        server_addr:
          description: The IP address of the DNS server the object belongs to, in
            hexadecimal format.
          type: string
        server_ipm_protocol:
          description: Internal use. Not documented.
          type: string
        server_ipm_type:
          description: 'The engine type of the DNS server the DNS zone belongs to:
            <b>named</b> (BIND engine), <b>nsd</b> (NSD engine) or <b>unbound</b>
            (Unbound engine).'
          type: string
        zone_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        zone_num_keys:
          description: The number of keys associated with the zone. This number of
            keys includes all ZSK and KSK.
          type: string
        zone_use_update_policy:
          description: The update policy status of the DNS zone. <b>1</b> indicates
            that the DNS zone uses a specific GSS-TSIG/update-policy. The parameter
            <b>gss_enabled</b> of the server the zone belongs to must be set to <b>1</b>.
          type: string
        smart_parent_id:
          description: The database identifier (ID) of the DNS smart architecture
            managing the DNS server the object belongs to. <b>0</b> indicates that
            the server the object belongs to is not managed by a smart architecture
            or is a smart architecture itself.
          type: string
        smart_parent_name:
          description: The name of the DNS smart architecture managing the DNS server
            the object belongs to. <b>#</b> indicates that the server the object belongs
            to is not managed by a smart architecture or is a smart architecture itself.
          type: string
      type: object
    dns_zone_add_success_data:
      example:
        zone_id: zone_id
      properties:
        zone_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dns_zone_edit_success_data:
      example:
        zone_id: zone_id
      properties:
        zone_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dns_zone_delete_success_data:
      example:
        zone_id: zone_id
      properties:
        zone_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    dns_zoneparam_data_data:
      example:
        server_name: server_name
        zone_id: zone_id
        zoneparam_key: zoneparam_key
        zoneparam_delayed_delete_time: zoneparam_delayed_delete_time
        zoneparam_delayed_create_time: zoneparam_delayed_create_time
        zoneparam_value: zoneparam_value
        zone_name: zone_name
        oid: oid
        server_id: server_id
      properties:
        zoneparam_value:
          description: The value of the DNS option set on the zone.
          type: string
        zoneparam_delayed_create_time:
          description: The delay of creation status. <b>1</b> indicates that the object
            is not created yet.
          type: string
        zoneparam_delayed_delete_time:
          description: The delay of deletion status. <b>1</b> indicates that the object
            is not deleted yet.
          type: string
        server_id:
          description: The database identifier (ID) of the DNS server the object belongs
            to.
          type: string
        server_name:
          description: The name of the DNS server the object belongs to.
          type: string
        zone_id:
          description: The database identifier (ID) of the DNS zone.
          type: string
        zone_name:
          description: The name of the DNS zone.
          type: string
        oid:
          description: The database identifier (ID) of the DNS zone param the object
            belongs to.
          type: string
        zoneparam_key:
          description: The name of the DNS option set on the zone.
          type: string
      type: object
    dns_zoneparam_add_success_data:
      example:
        zoneparam_id: zoneparam_id
      properties:
        zoneparam_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    dns_zoneparam_edit_success_data:
      example:
        zoneparam_id: zoneparam_id
      properties:
        zoneparam_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    dns_zoneparam_delete_success_data:
      example:
        zoneparam_id: zoneparam_id
      properties:
        zoneparam_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    guardian_policy_data_data:
      example:
        server_name: server_name
        policy_id: policy_id
        policy_description: policy_description
        policy_name: policy_name
        server_id: server_id
        policy_delayed_time: policy_delayed_time
        parent_policy_id: parent_policy_id
        policy_readonly: policy_readonly
      properties:
        policy_delayed_time:
          description: The delay of creation/deletion status. <b>1</b> indicates that
            the object is not created/deleted yet.
          type: string
        server_id:
          description: The database identifier (ID) of the Guardian server associated
            with the policy. It is only returned for deployed policies.
          type: string
        server_name:
          description: The name of the Guardian server associated with the policy.
            It is only returned for deployed policies.
          type: string
        policy_description:
          description: The description of the policy.
          type: string
        policy_id:
          description: The database identifier (ID) of the policy.
          type: string
        policy_name:
          description: The name of the policy.
          type: string
        policy_readonly:
          description: The read only status of the policy. If set to <b>1</b>, the
            policy cannot be edited.
          type: string
        parent_policy_id:
          description: The database identifier (ID) of the policy. It is only returned
            for deployed policies.
          type: string
      type: object
    guardian_policy_add_success_data:
      example:
        policy_id: policy_id
      properties:
        policy_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    guardian_policy_edit_success_data:
      example:
        policy_id: policy_id
      properties:
        policy_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    guardian_policy_delete_success_data:
      example:
        policy_id: policy_id
      properties:
        policy_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    ipam_address_data_data:
      example:
        space_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        address_alias: address_alias
        network_is_terminal: network_is_terminal
        pool_class_name: pool_class_name
        free_end_address_addr: free_end_address_addr
        address_class_name: address_class_name
        space_class_name: space_class_name
        port_portnumber: port_portnumber
        space_description: space_description
        parent_network_end_hostaddr: parent_network_end_hostaddr
        device_id: device_id
        network_size: network_size
        tag_container_dhcpstatic: tag_container_dhcpstatic
        port_ifvlan: port_ifvlan
        space_is_template: space_is_template
        address_id: address_id
        network_name: network_name
        parent_network_class_name: parent_network_class_name
        network_id: network_id
        space_name: space_name
        network_start_address_addr: network_start_address_addr
        pool_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        network_class_name: network_class_name
        free_start_address_addr: free_start_address_addr
        address_hostaddr: address_hostaddr
        interface_name: interface_name
        pool_start_address_addr: pool_start_address_addr
        address_multistatus: address_multistatus
        address_name: address_name
        port_slotnumber: port_slotnumber
        parent_network_name: parent_network_name
        free_scope_size: free_scope_size
        tag_pool_dhcprange: tag_pool_dhcprange
        device_name: device_name
        parent_network_start_hostaddr: parent_network_start_hostaddr
        address_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        parent_network_size: parent_network_size
        pool_end_address_addr: pool_end_address_addr
        parent_vlsm_network_id: parent_vlsm_network_id
        dev_name: dev_name
        network_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        address_addr: address_addr
        network_lock_network_broadcast: network_lock_network_broadcast
        address_type: address_type
        static_id: static_id
        pool_size: pool_size
        pool_read_only: pool_read_only
        address_mac_addr: address_mac_addr
        pool_id: pool_id
        address_mac_last_seen: address_mac_last_seen
        parent_network_start_address_addr: parent_network_start_address_addr
        pool_name: pool_name
        dev_id: dev_id
        port_name: port_name
        lease_end_time: lease_end_time
        interface_id: interface_id
        parent_network_id: parent_network_id
        space_id: space_id
        network_end_address_addr: network_end_address_addr
        lease_id: lease_id
        parent_network_end_address_addr: parent_network_end_address_addr
      properties:
        static_id:
          description: The database identifier (ID) of the DHCP static associated
            with the IP address.
          type: string
        lease_end_time:
          description: The expiration time of the lease, if the IP address was imported
            from the DHCP, in decimal UNIX date format.
          type: string
        lease_id:
          description: The database identifier (ID) of the DHCP lease associated the
            IP address.
          type: string
        free_end_address_addr:
          description: An IP address in hexadecimal format. For addresses <b>In use</b>
            (<b>type</b><b>ip</b>), it returns the IP address itself.For free addresses
            (<b>type</b><b>free</b>), it returns the last IP address of a range of
            IPv4 addresses that are not assigned yet. The first address in that range
            is returned in <b>free_start_ip_addr</b>.
          type: string
        free_scope_size:
          description: The number of IP addresses that are not assigned yet (<b>type</b><b>free</b>)
            between <b>free_start_ip_addr</b> and <b>free_end_ip_addr</b>.
          type: string
        free_start_address_addr:
          description: An IP address in hexadecimal format. For addresses <b>In use</b>
            (<b>type</b><b>ip</b>), it returns the IP address itself.For free addresses
            (<b>type</b><b>free</b>), it returns the first IP address of a range of
            IPv4 addresses that are not assigned yet. The last address in that range
            is returned in <b>free_end_ip_addr</b>.
          type: string
        address_hostaddr:
          description: The human readable version of the parameter <b>ip_addr</b>.
          type: string
        device_id:
          description: The database identifier (ID) of the Device Manager device associated
            with the IP address.
          type: string
        device_name:
          description: The name of the Device Manager device associated with the IP
            address.
          type: string
        interface_id:
          description: The database identifier (ID) of the Device Manager interface
            associated with the IP address.
          type: string
        interface_name:
          description: The name of the Device Manager interface associated with the
            IP address.
          type: string
        address_addr:
          description: The IPv4 address itself, in hexadecimal format.
          type: string
        address_alias:
          description: The name of the IPv4 alias(es) associated with the IPv4 address.
          type: string
        address_class_name:
          description: The name of the class applied to the IPv4 address, it can be
            preceded by the class directory.
          type: string
        address_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        address_id:
          description: The database identifier (ID) of the IPv4 address.
          type: string
        dev_id:
          description: The database identifier (ID) of the NetChange network device
            associated with the IP address.
          type: string
        dev_name:
          description: The name of the NetChange network device associated with the
            IP address.
          type: string
        port_ifvlan:
          description: The VLAN identifier (ID) of the NetChange port, for IP addresses
            which MAC addresses is imported from NetChange.
          type: string
        port_name:
          description: The name of the NetChange port associated with the IP address.
          type: string
        port_portnumber:
          description: The number of the port, for IP addresses which MAC addresses
            is imported from NetChange.
          type: string
        port_slotnumber:
          description: The slot number of the port, for IP addresses which MAC addresses
            is imported from NetChange.
          type: string
        address_mac_last_seen:
          description: The last time the MAC address associated with the IP address
            was seen on the network, in decimal UNIX date format.
          type: string
        network_lock_network_broadcast:
          description: A way to prevent <b>(1)</b> users from assigning the broadcast
            IP address and network IP address of the network the IP address belongs
            to.
          type: string
        address_mac_addr:
          description: The MAC address associated with the IPv4 address.
          type: string
        address_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        address_name:
          description: The name of the IPv4 address.
          type: string
        parent_network_class_name:
          description: The name of the class applied to the parent of the IPv4 network
            the object belongs to, it can be preceded by the class directory.
          type: string
        parent_network_end_hostaddr:
          description: The human readable version of the parameter <b>parent_subnet_end_ip_addr</b>.
          type: string
        parent_network_end_address_addr:
          description: The last IP address of the parent of the IPv4 network the IP
            address belongs to.
          type: string
        parent_network_id:
          description: The database identifier (ID) of the parent IPv4 network. It
            identifies the parent of the IPv4 network the object belongs to. <b>0</b>
            indicates that the network the object belongs to has no parent network.
          type: string
        parent_network_name:
          description: The name of the parent IPv4 network:<ul class=dashed ><li><b>#</b>
            indicates that the network the object belongs to has no parent network.<br/>                                            </li><li><b>Default</b>
            indicates that the network the object belongs to is in an orphan network.<br/>                                            </li></ul>
          type: string
        parent_network_size:
          description: The number of IP addresses of the parent of the network the
            object belongs to.
          type: string
        parent_network_start_hostaddr:
          description: The human readable version of the parameter <b>parent_subnet_start_ip_addr</b>.
          type: string
        parent_network_start_address_addr:
          description: The first IP address of the parent of the IPv4 network the
            IP address belongs to.
          type: string
        parent_vlsm_network_id:
          description: The database identifier (ID) of the IPv4 subnet-type network,
            located in the VLSM parent space, from which the parent of the network
            the IP address belongs to was duplicated. <b>0</b> indicates that the
            parent of the network the IP address belongs to is not a VLSM block-type
            network duplicated from a parent space.
          type: string
        pool_class_name:
          description: The name of the class applied to the IPv4 pool the object belongs
            to, it can be preceded by the class directory.
          type: string
        pool_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        pool_end_address_addr:
          description: The last IP address of the IPv4 pool the IP address belongs
            to.
          type: string
        pool_id:
          description: The database identifier (ID) of the IPv4 pool the object belongs
            to.
          type: string
        pool_name:
          description: The name of the IPv4 pool the object belongs to.
          type: string
        pool_read_only:
          description: The reservation status of the pool the IPv4 address belongs
            to. If set <b>1</b>, the pool is reserved and you cannot assign the IP
            address.
          type: string
        pool_size:
          description: The number of IP addresses that contains the pool the IPv4
            address belongs to.
          type: string
        pool_start_address_addr:
          description: The first IP address of the IPv4 pool the IP address belongs
            to.
          type: string
        space_class_name:
          description: The name of the class applied to the space the object belongs
            to, it can be preceded by the class directory.
          type: string
        space_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        space_description:
          description: The description of the space the object belongs to.
          type: string
        space_id:
          description: The database identifier (ID) of the space the object belongs
            to, a unique numeric key value automatically incremented when you add
            a space.
          type: string
        space_is_template:
          description: The template status of the space the object belongs to. If
            the space is used as template (1), all the IPv4 networks, pools and IP
            addresses it contains are also used as template.
          type: string
        space_name:
          description: The name of the space the object belongs to.
          type: string
        network_class_name:
          description: The name of the class applied to the IPv4 network the object
            belongs to, it can be preceded by the class directory.
          type: string
        network_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        network_end_address_addr:
          description: The last IP address of the IPv4 network the object belongs
            to.
          type: string
        network_id:
          description: The database identifier (ID) of the IPv4 network the object
            belongs to.
          type: string
        network_is_terminal:
          description: A way to determine if the network the IP address belongs to
            is terminal (<b>1</b>) or non-terminal (<b>0</b>).
          type: string
        network_name:
          description: The name of the IPv4 network the object belongs to. <b>Default</b>
            indicates that the network the object belongs to is an orphan network.
          type: string
        network_size:
          description: The number of IP addresses the network the object belongs to
            contains.
          type: string
        network_start_address_addr:
          description: The first IP address of the IPv4 network the object belongs
            to.
          type: string
        tag_container_dhcpstatic:
          description: A way to determine if the terminal network or pool the IP address
            belongs to is configured to Create DHCP static (<b>1</b>) or not (<b>0</b>).
          type: string
        tag_pool_dhcprange:
          description: A way to determine if the pool the IP address belongs to is
            configured to Create DHCP range (<b>1</b>) or not (<b>0</b>).
          type: string
        address_type:
          description: A way to determine if you can assign the IP address (<b>free</b>)
            or if it is In use (<b>ip</b>).
          type: string
      type: object
    ipam_address_add_success_data:
      example:
        address_id: address_id
      properties:
        address_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    ipam_address_edit_success_data:
      example:
        address_id: address_id
      properties:
        address_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    ipam_address_delete_success_data:
      example:
        address_id: address_id
      properties:
        address_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    ipam_address6_data_data:
      example:
        parent_network6_class_name: parent_network6_class_name
        pool6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        space_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        address6_multistatus: address6_multistatus
        parent_network6_prefix: parent_network6_prefix
        space_class_name: space_class_name
        space_description: space_description
        address6_alias: address6_alias
        free_start_address6_addr: free_start_address6_addr
        parent_network6_end_hostaddr: parent_network6_end_hostaddr
        network6_start_address6_addr: network6_start_address6_addr
        pool6_size: pool6_size
        parent_vlsm_network6_id: parent_vlsm_network6_id
        device_id: device_id
        network_size: network_size
        network6_is_terminal: network6_is_terminal
        vlsm_network6_id: vlsm_network6_id
        address6_type: address6_type
        pool6_id: pool6_id
        pool6_name: pool6_name
        space_name: space_name
        network6_id: network6_id
        pool6_end_address6_addr: pool6_end_address6_addr
        interface_name: interface_name
        parent_network6_id: parent_network6_id
        network6_name: network6_name
        free_scope_size: free_scope_size
        free_end_address6_addr: free_end_address6_addr
        device_name: device_name
        address6_name: address6_name
        parent_network6_start_address6_addr: parent_network6_start_address6_addr
        parent_network6_start_hostaddr: parent_network6_start_hostaddr
        pool6_read_only: pool6_read_only
        pool6_class_name: pool6_class_name
        network6_end_address6_addr: network6_end_address6_addr
        address6_mac_addr: address6_mac_addr
        parent_space_name: parent_space_name
        network6_prefix: network6_prefix
        address6_id: address6_id
        network6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        parent_network6_size: parent_network6_size
        parent_network6_name: parent_network6_name
        parent_network6_end_address6_addr: parent_network6_end_address6_addr
        network6_lock_network_broadcast: network6_lock_network_broadcast
        network6_size: network6_size
        interface_id: interface_id
        address6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        network6_class_name: network6_class_name
        address6_addr: address6_addr
        address6_hostaddr: address6_hostaddr
        space_id: space_id
        address6_class_name: address6_class_name
        pool6_start_address6_addr: pool6_start_address6_addr
      properties:
        free_end_address6_addr:
          description: An IP address in hexadecimal format. For addresses <b>In use</b>
            (<b>type</b><b>ip6</b>), it returns the IP address itself.For free addresses
            (<b>type</b><b>free</b>), it returns the last IP address of a range of
            IPv6 addresses that are not assigned yet. The first address in that range
            is returned in <b>free_start_ip6_addr</b>.
          type: string
        free_scope_size:
          description: The number of IP addresses that are not assigned yet (<b>type</b><b>free</b>)
            between <b>free_start_ip6_addr</b> and <b>free_end_ip6_addr</b>.
          type: string
        free_start_address6_addr:
          description: An IP address in hexadecimal format. For addresses <b>In use</b>
            (<b>type</b><b>ip6</b>), it returns the IP address itself.For free addresses
            (<b>type</b><b>free</b>), it returns the first IP address of a range of
            IPv6 addresses that are not assigned yet. The last address in that range
            is returned in <b>free_end_ip6_addr</b>.
          type: string
        address6_hostaddr:
          description: The human readable version of the parameter <b>ip6_addr</b>.
          type: string
        device_id:
          description: The database identifier (ID) of the Device Manager device associated
            with the IP address.
          type: string
        device_name:
          description: The name of the Device Manager device associated with the IP
            address.
          type: string
        interface_id:
          description: The database identifier (ID) of the Device Manager interface
            associated with the IP address.
          type: string
        interface_name:
          description: The name of the Device Manager interface associated with the
            IP address.
          type: string
        address6_addr:
          description: The IPv6 address itself.
          type: string
        address6_alias:
          description: The name of the IPv6 alias(es) associated with the IPv6 address.
          type: string
        address6_class_name:
          description: The name of the class applied to the IPv6 address, it can be
            preceded by the class directory.
          type: string
        address6_class_parameters:
          description: 'The class parameters applied to the IPv6 address and their
            value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        address6_id:
          description: The database identifier (ID) of the IPv6 address.
          type: string
        address6_mac_addr:
          description: The MAC address associated with the IPv6 address.
          type: string
        address6_name:
          description: The name of the IPv6 address.
          type: string
        network6_lock_network_broadcast:
          description: A way to prevent <b>(1)</b> users from assigning the broadcast
            IP address and network IP address of the network the IP address belongs
            to.
          type: string
        address6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        parent_space_name:
          description: The name of the space where is located the parent of the network
            the IPv6 address belongs to. <b>#</b> indicates that the network the IPv6
            address belongs to has no parent network.
          type: string
        parent_network6_class_name:
          description: The name of the class applied to the parent of the IPv6 network
            the object belongs to, it can be preceded by the class directory.
          type: string
        parent_network6_end_hostaddr:
          description: The human readable version of the parameter <b>parent_subnet6_end_ip6_addr</b>.
          type: string
        parent_network6_end_address6_addr:
          description: The last IP address of the parent of the IPv6 network the IP
            address belongs to.
          type: string
        parent_network6_id:
          description: The database identifier (ID) of the parent IPv6 network. It
            identifies the parent of the IPv6 network the object belongs to. <b>0</b>
            indicates that the network the object belongs to has no parent network.
          type: string
        parent_network6_name:
          description: The name of the parent IPv6 network. <b>#</b> indicates that
            the network the object belongs to has no parent network.
          type: string
        parent_network6_prefix:
          description: The prefix of the parent of the IPv6 network the object belongs
            to.
          type: string
        parent_network6_size:
          description: The number of IP addresses of the network parent, in hexadecimal
            format.
          type: string
        parent_network6_start_hostaddr:
          description: The human readable version of the parameter <b>parent_subnet6_start_ip6_addr</b>.
          type: string
        parent_network6_start_address6_addr:
          description: The first IP address of the parent of the IPv6 network the
            IP address belongs to.
          type: string
        parent_vlsm_network6_id:
          description: The database identifier (ID) of the IPv6 subnet-type network,
            located in the VLSM parent space, from which the parent of the network
            the IP address belongs to was duplicated. <b>0</b> indicates that the
            parent of the network the IP address belongs to is not a VLSM block-type
            network duplicated from a parent space.
          type: string
        pool6_class_name:
          description: The name of the class applied to the IPv6 pool the object belongs
            to, it can be preceded by the class directory.
          type: string
        pool6_class_parameters:
          description: 'The class parameters applied to the IPv6 pool the object belongs
            to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        pool6_end_address6_addr:
          description: The last IP address of the IPv6 pool the IP address belongs
            to.
          type: string
        pool6_id:
          description: The database identifier (ID) of the IPv6 pool the object belongs
            to.
          type: string
        pool6_name:
          description: The name of the IPv6 pool the object belongs to.
          type: string
        pool6_read_only:
          description: The reservation status of the pool the IPv6 address belongs
            to. If set <b>1</b>, the pool is reserved and you cannot assign the IP
            address.
          type: string
        pool6_size:
          description: The number of IP addresses that contains the pool the IPv6
            address belongs to.
          type: string
        pool6_start_address6_addr:
          description: The first IP address of the IPv6 pool the IP address belongs
            to.
          type: string
        space_class_name:
          description: The name of the class applied to the space the object belongs
            to, it can be preceded by the class directory.
          type: string
        space_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        space_description:
          description: The description of the space the object belongs to.
          type: string
        space_id:
          description: The database identifier (ID) of the space the object belongs
            to, a unique numeric key value automatically incremented when you add
            a space.
          type: string
        space_name:
          description: The name of the space the object belongs to.
          type: string
        network6_class_name:
          description: The name of the class applied to the IPv6 network the object
            belongs to, it can be preceded by the class directory.
          type: string
        network6_class_parameters:
          description: 'The class parameters applied to the IPv6 network the object
            belongs to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        network6_end_address6_addr:
          description: The last IP address of the IPv6 network the object belongs
            to.
          type: string
        network6_id:
          description: The database identifier (ID) of the IPv6 network the object
            belongs to.
          type: string
        network6_is_terminal:
          description: A way to determine if the network the IP address belongs to
            is terminal (<b>1</b>) or non-terminal (<b>0</b>).
          type: string
        network6_name:
          description: The name of the IPv6 network the object belongs to.
          type: string
        network6_prefix:
          description: The prefix of the IPv6 network the object belongs to.
          type: string
        network6_size:
          description: The number of IP addresses the network the object belongs to
            contains.
          type: string
        network6_start_address6_addr:
          description: The first IP address of the IPv6 network the object belongs
            to.
          type: string
        network_size:
          description: The number of IP addresses the network the object belongs to
            contains.
          type: string
        address6_type:
          description: A way to determine if you can assign the IP address (<b>free</b>)
            or if it is In use (<b>ip</b>6).
          type: string
        vlsm_network6_id:
          description: The database identifier (ID) of the IPv6 subnet-type network,
            located in the VLSM parent space, from which the network the IP address
            belongs to was duplicated. <b>0</b> indicates the network the IP address
            belongs to is not a VLSM block-type network duplicated from a parent space.
          type: string
      type: object
    ipam_address6_add_success_data:
      example:
        address6_id: address6_id
      properties:
        address6_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    ipam_address6_edit_success_data:
      example:
        address6_id: address6_id
      properties:
        address6_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    ipam_address6_delete_success_data:
      example:
        address6_id: address6_id
      properties:
        address6_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    ipam_alias_data_data:
      example:
        device_id: device_id
        address_addr: address_addr
        address_type: address_type
        static_id: static_id
        alias_id: alias_id
        address_id: address_id
        address_name: address_name
        alias_name: alias_name
        address_mac_addr: address_mac_addr
        pool_id: pool_id
        alias_type: alias_type
        network_id: network_id
        interface_id: interface_id
        address_class_name: address_class_name
        address_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        space_name: space_name
        space_class_name: space_class_name
        port_id: port_id
        space_id: space_id
        lease_id: lease_id
      properties:
        alias_name:
          description: The name of the IPv4 alias.
          type: string
        static_id:
          description: The database identifier (ID) of the DHCP static associated
            with the IP address.
          type: string
        lease_id:
          description: The database identifier (ID) of the DHCP lease associated the
            IP address.
          type: string
        device_id:
          description: The database identifier (ID) of the Device Manager device associated
            with the IP address.
          type: string
        interface_id:
          description: The database identifier (ID) of the Device Manager interface
            associated with the IP address.
          type: string
        address_addr:
          description: The IPv4 alias itself, in hexadecimal format.
          type: string
        address_class_name:
          description: The name of the class applied to the object.
          type: string
        address_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        address_id:
          description: The database identifier (ID) of the IPv4 address associated
            with the alias.
          type: string
        alias_id:
          description: The database identifier (ID) of the IPv4 alias.
          type: string
        alias_type:
          description: The type of the alias, either <b>CNAME</b> or <b>A</b>.
          type: string
        address_type:
          description: A way to determine if you can assign the IP alias (<b>free</b>)
            or if it is In use (<b>ip</b>).
          type: string
        port_id:
          description: The database identifier (ID) of the NetChange port associated
            with the IP address.
          type: string
        address_mac_addr:
          description: The MAC address associated with the IPv4 alias.
          type: string
        address_name:
          description: 'todo(here) :ipam.alias.list.output.address_name. : '
          type: string
        pool_id:
          description: The database identifier (ID) of the IPv4 pool the object belongs
            to.
          type: string
        space_class_name:
          description: The name of the class applied to the space the object belongs
            to, it can be preceded by the class directory.
          type: string
        space_id:
          description: The database identifier (ID) of the space the object belongs
            to, a unique numeric key value automatically incremented when you add
            a space.
          type: string
        space_name:
          description: The name of the space the object belongs to.
          type: string
        network_id:
          description: The database identifier (ID) of the IPv4 network the object
            belongs to.
          type: string
      type: object
    ipam_alias_add_success_data:
      example:
        alias_id: alias_id
      properties:
        alias_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    ipam_alias_edit_success_data:
      example:
        alias_id: alias_id
      properties:
        alias_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    ipam_alias_delete_success_data:
      example:
        alias_id: alias_id
      properties:
        alias_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    ipam_alias6_data_data:
      example:
        address6_id: address6_id
        device_id: device_id
        alias_name: alias_name
        pool6_id: pool6_id
        interface_id: interface_id
        address6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        address6_name: address6_name
        space_name: space_name
        network6_id: network6_id
        space_class_name: space_class_name
        alias6_type: alias6_type
        port_id: port_id
        address6_addr: address6_addr
        alias6_id: alias6_id
        address6_mac_addr: address6_mac_addr
        space_id: space_id
        address6_class_name: address6_class_name
      properties:
        alias_name:
          description: The name of the alias.
          type: string
        device_id:
          description: The database identifier (ID) of the Device Manager device associated
            with the IP address.
          type: string
        interface_id:
          description: The database identifier (ID) of the Device Manager interface
            associated with the IP address.
          type: string
        address6_addr:
          description: The IPv6 alias itself.
          type: string
        address6_class_name:
          description: The name of the class applied to the object.
          type: string
        address6_class_parameters:
          description: 'The class parameters applied to the IPv6 alias and their value:
            <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        address6_id:
          description: The database identifier (ID) of the IPv6 address associated
            with the alias.
          type: string
        address6_mac_addr:
          description: The MAC address associated with the IPv6 alias.
          type: string
        address6_name:
          description: The name of the IPv6 address associated with the alias.
          type: string
        alias6_id:
          description: The database identifier (ID) of the IPv6 alias.
          type: string
        alias6_type:
          description: The type of the alias, either <b>CNAME</b> or <b>AAAA</b>.
          type: string
        port_id:
          description: The database identifier (ID) of the NetChange port associated
            with the IP address.
          type: string
        pool6_id:
          description: The database identifier (ID) of the IPv6 pool the object belongs
            to.
          type: string
        space_class_name:
          description: The name of the class applied to the space the object belongs
            to, it can be preceded by the class directory.
          type: string
        space_id:
          description: The database identifier (ID) of the space the object belongs
            to, a unique numeric key value automatically incremented when you add
            a space.
          type: string
        space_name:
          description: The name of the space the object belongs to.
          type: string
        network6_id:
          description: The database identifier (ID) of the IPv6 network the object
            belongs to.
          type: string
      type: object
    ipam_alias6_add_success_data:
      example:
        alias6_id: alias6_id
      properties:
        alias6_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    ipam_alias6_edit_success_data:
      example:
        alias6_id: alias6_id
      properties:
        alias6_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    ipam_alias6_delete_success_data:
      example:
        alias6_id: alias6_id
      properties:
        alias6_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    ipam_network_data_data:
      example:
        range_name: range_name
        network_address_used_size: network_address_used_size
        vlan_id: vlan_id
        space_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        vlsm_space_id: vlsm_space_id
        vlan_vlan_id: vlan_vlan_id
        network_is_terminal: network_is_terminal
        vlan_name: vlan_name
        parent_network_level: parent_network_level
        domain_id: domain_id
        vlsm_space_name: vlsm_space_name
        domain_name: domain_name
        network_ripe_waiting_status: network_ripe_waiting_status
        space_class_name: space_class_name
        network_start_hostaddr: network_start_hostaddr
        parent_space_id: parent_space_id
        space_description: space_description
        parent_network_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        network_address_used_percent: network_address_used_percent
        network_level: network_level
        network_size: network_size
        space_parent_space_id: space_parent_space_id
        space_is_template: space_is_template
        network_name: network_name
        parent_network_is_terminal: parent_network_is_terminal
        parent_network_class_name: parent_network_class_name
        network_address_free_size: network_address_free_size
        network_id: network_id
        space_name: space_name
        network_is_valid: network_is_valid
        parent_network_path: parent_network_path
        network_allocated_percent: network_allocated_percent
        network_start_address_addr: network_start_address_addr
        network_class_name: network_class_name
        network_end_hostaddr: network_end_hostaddr
        vlsm_network_id: vlsm_network_id
        parent_network_name: parent_network_name
        range_id: range_id
        parent_network_size: parent_network_size
        network_used_size: network_used_size
        network_ripe_waiting_state: network_ripe_waiting_state
        parent_vlsm_network_id: parent_vlsm_network_id
        parent_space_name: parent_space_name
        network_used_percent: network_used_percent
        network_is_in_orphan: network_is_in_orphan
        network_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        network_lock_network_broadcast: network_lock_network_broadcast
        parent_network_start_address_addr: parent_network_start_address_addr
        network_multistatus: network_multistatus
        parent_network_id: parent_network_id
        vlsm_block_id: vlsm_block_id
        network_end_address_addr: network_end_address_addr
        space_id: space_id
        parent_network_end_address_addr: parent_network_end_address_addr
        network_allocated_size: network_allocated_size
      properties:
        network_end_hostaddr:
          description: The human readable version of the parameter <b>end_ip_addr</b>.
          type: string
        network_end_address_addr:
          description: 'todo(here) :ipam.network.list.output.network_end_address_addr.
            : '
          type: string
        network_is_in_orphan:
          description: A way to determine if the network has a parent (<b>0</b>) or
            if it belongs to a container <b>Orphan networks</b> (<b>1</b>).
          type: string
        network_is_terminal:
          description: A way to determine if a network can contain other networks.
            If set to <b>1</b>, the network is terminal and cannot contain other subnet-type
            networks. Block-type networks are always set to <b>0</b>.
          type: string
        network_lock_network_broadcast:
          description: A way to prevent <b>(1)</b> users from assigning the broadcast
            IP address and network IP address of the network.
          type: string
        network_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        parent_network_end_address_addr:
          description: 'todo(here) :ipam.network.list.output.parent_network_end_address_addr.
            : '
          type: string
        parent_network_is_terminal:
          description: A way to determine if the parent network is terminal (<b>1</b>)
            or non-terminal (<b>0</b>).
          type: string
        parent_space_id:
          description: The database identifier (ID) of the space where is located
            the parent network. <b>0</b> indicates that the network has no parent
            network.
          type: string
        parent_space_name:
          description: The name of the space where is located the parent network.
            <b>#</b> indicates that the network has no parent network.
          type: string
        parent_network_start_address_addr:
          description: 'todo(here) :ipam.network.list.output.parent_network_start_address_addr.
            : '
          type: string
        parent_network_class_name:
          description: The name of the class applied to the parent IPv4 network, it
            can be preceded by the class directory.
          type: string
        parent_network_class_parameters:
          description: 'The class parameters applied to the parent IPv4 network and
            their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;...</b>
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        parent_network_id:
          description: The database identifier (ID) of the parent IPv4 network. <b>0</b>
            indicates that the network has no parent network.
          type: string
        parent_network_level:
          description: The level of the parent network within the space. It returns
            values between <b>0</b> (block-type network) and <b>n</b> (subnet-type
            network). A value higher than <b>1</b> indicates a VLSM organization where
            a block-type network can belong to another subnet-type network.
          type: string
        parent_network_name:
          description: The name of the parent IPv4 network:<ul class=dashed ><li><b>#</b>
            indicates that the network has no parent network.<br/>                                            </li><li><b>Default</b>
            indicates that the network belongs to an orphan network.<br/>                                            </li></ul>
          type: string
        parent_network_path:
          description: The path toward the parent network in the database. <b>#</b>
            indicates the network has no parent network.
          type: string
        parent_network_size:
          description: The number of IP addresses of the network parent.
          type: string
        parent_vlsm_network_id:
          description: The database identifier (ID) of the IPv4 subnet-type network,
            located in the VLSM parent space, from which the parent network was duplicated.
            <b>0</b> indicates that the parent network is not a VLSM block-type network
            duplicated from a parent space.
          type: string
        space_class_name:
          description: The name of the class applied to the space the object belongs
            to, it can be preceded by the class directory.
          type: string
        space_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        space_description:
          description: The description of the space the object belongs to.
          type: string
        space_id:
          description: The database identifier (ID) of the space the object belongs
            to, a unique numeric key value automatically incremented when you add
            a space.
          type: string
        space_is_template:
          description: The template status of the space the object belongs to. If
            the space is used as template (1), all the IPv4 networks, pools and IP
            addresses it contains are also used as template.
          type: string
        space_name:
          description: The name of the space the object belongs to.
          type: string
        space_parent_space_id:
          description: The database identifier (ID) of the VLSM parent of the space
            where is located the network. <b>0</b> indicates that the space where
            is located the network has no parent space.
          type: string
        network_start_hostaddr:
          description: The human readable version of the parameter <b>start_ip_addr</b>.
          type: string
        network_start_address_addr:
          description: 'todo(here) :ipam.network.list.output.network_start_address_addr.
            : '
          type: string
        network_allocated_percent:
          description: The percentage of subnet-type networks the non-terminal network
            contains.
          type: string
        network_allocated_size:
          description: The sum of the size of all the subnet-type networks that belong
            to the block-type network.
          type: string
        network_class_name:
          description: The name of the class applied to the IPv4 network, it can be
            preceded by the class directory.
          type: string
        network_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        network_id:
          description: The database identifier (ID) of the IPv4 network.
          type: string
        network_address_free_size:
          description: The total number of free addresses, for terminal networks only.
            It excludes the network and broadcast IP address.
          type: string
        network_address_used_percent:
          description: The percentage of IP addresses <b>In use</b> in terminal networks.
          type: string
        network_address_used_size:
          description: The number of IP addresses <b>In use</b> in terminal networks.
          type: string
        network_is_valid:
          description: The network validity. A valid network (<b>1</b>) has a size,
            prefix and/or netmask that match.
          type: string
        network_level:
          description: The level of the network within the space. It returns values
            between <b>0</b> (block-type network) and <b>n</b> (subnet-type network).
            A value higher than <b>1</b> indicates a VLSM organization where a block-type
            network can belong to another subnet-type network.
          type: string
        network_name:
          description: The name of the IPv4 network. <b>Default</b> indicates that
            the network is an orphan network.
          type: string
        network_size:
          description: The number of IP addresses the IPv4 network contains.
          type: string
        network_used_percent:
          description: The percentage of terminal networks the non-terminal network
            contains.
          type: string
        network_used_size:
          description: The sum of the size of all the terminal networks within the
            block-type network. This sum includes the terminal networks that might
            belong to non-terminal subnet-type networks.
          type: string
        domain_id:
          description: The database identifier (ID) of the VLAN domain associated
            with the network.
          type: string
        domain_name:
          description: The name of the VLAN domain associated with the network.
          type: string
        range_id:
          description: The database identifier (ID) of the VLAN range associated with
            the network.
          type: string
        range_name:
          description: The name of the VLAN range associated with the network.
          type: string
        vlan_id:
          description: The database identifier (ID) of the VLAN associated with the
            network.
          type: string
        vlan_name:
          description: The name of the VLAN associated with the network.
          type: string
        vlan_vlan_id:
          description: The VLAN identifier (ID) of the VLAN associated with the network.
          type: string
        vlsm_block_id:
          description: The database identifier (ID) of the IPv4 VLSM block-type network
            duplicated, in a VLSM child space, from the network. <b>0</b> indicates
            that the network is not duplicated as a VLSM block-type network in a child
            space.
          type: string
        vlsm_space_id:
          description: The database identifier (ID) of the VLSM child space where
            the network is duplicated as a VLSM block-type network. <b>0</b> indicates
            that the network is not duplicated as a VLSM block-type network in a child
            space.
          type: string
        vlsm_space_name:
          description: The name of the VLSM child space where the network is duplicated
            as a VLSM block-type network. <b>0</b> indicates that the network is not
            duplicated as a VLSM block-type network in a child space.
          type: string
        vlsm_network_id:
          description: The database identifier (ID) of the IPv4 subnet-type network,
            located in the VLSM parent space, from which the network was duplicated.
            <b>0</b> indicates that the network is not a VLSM block-type network duplicated
            from a parent space.
          type: string
        network_ripe_waiting_state:
          description: 'The state of the exchange between SOLIDserver and the RIPE
            for the assigned network: <table><caption>waiting_state possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >must_send_mail_add</td><td >An email must be sent to the RIPE to notify
            them of a subnet-type network creation.</td></tr><tr><td >wait_mail_add</td><td
            >A network creation email was sent to the RIPE, no reply has been received
            yet.</td></tr><tr><td >must_send_mail_del</td><td >An email must be sent
            to the RIPE to notify them of a subnet-type network deletion.</td></tr><tr><td
            >wait_mail_del</td><td >A network deletion email was sent to the RIPE,
            no reply has been received yet.</td></tr><tr><td >wait_aw_confirm</td><td
            >The number of IP addresses of the assigned network exceeds the Assignment
            Window declared during your RIPE configuration.</td></tr></tbody></table></p><br/>'
          type: string
        network_ripe_waiting_status:
          description: The status of a RIPE assigned network within SOLIDserver until
            it is confirmed that you can create or delete it. If set to <b>1</b>,
            it is about to be created. If set to <b>2</b>, it is about to be deleted.
          type: string
      type: object
    ipam_network_add_success_data:
      example:
        network_id: network_id
      properties:
        network_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    ipam_network_edit_success_data:
      example:
        network_id: network_id
      properties:
        network_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    ipam_network_delete_success_data:
      example:
        network_id: network_id
      properties:
        network_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    ipam_network6_data_data:
      example:
        range_name: range_name
        vlsm_block6_id: vlsm_block6_id
        end_address6_addr: end_address6_addr
        parent_network6_class_name: parent_network6_class_name
        parent_network6_is_terminal: parent_network6_is_terminal
        vlan_id: vlan_id
        space_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        vlsm_space_id: vlsm_space_id
        vlan_vlan_id: vlan_vlan_id
        vlan_name: vlan_name
        parent_network_level: parent_network_level
        domain_id: domain_id
        vlsm_space_name: vlsm_space_name
        domain_name: domain_name
        percent_allocated: percent_allocated
        parent_network6_prefix: parent_network6_prefix
        space_class_name: space_class_name
        parent_space_id: parent_space_id
        space_description: space_description
        network_level: network_level
        parent_vlsm_network6_id: parent_vlsm_network6_id
        network_size: network_size
        space_parent_space_id: space_parent_space_id
        space_is_template: space_is_template
        network6_ripe_waiting_state: network6_ripe_waiting_state
        network6_is_terminal: network6_is_terminal
        vlsm_network6_id: vlsm_network6_id
        network6_ripe_waiting_status: network6_ripe_waiting_status
        percent_used: percent_used
        space_name: space_name
        network6_id: network6_id
        parent_network_path: parent_network_path
        network6_start_hostaddr: network6_start_hostaddr
        parent_network6_percent_allocated: parent_network6_percent_allocated
        network6_is_valid: network6_is_valid
        parent_end_address6_addr: parent_end_address6_addr
        parent_start_address6_addr: parent_start_address6_addr
        network6_end_hostaddr: network6_end_hostaddr
        parent_network6_id: parent_network6_id
        network6_name: network6_name
        range_id: range_id
        start_address6_addr: start_address6_addr
        parent_network_size: parent_network_size
        parent_space_name: parent_space_name
        network6_prefix: network6_prefix
        parent_network6_percent_used: parent_network6_percent_used
        network6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        network6_multistatus: network6_multistatus
        parent_network6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        parent_network6_name: parent_network6_name
        network6_lock_network_broadcast: network6_lock_network_broadcast
        network6_class_name: network6_class_name
        network6_is_in_orphan: network6_is_in_orphan
        space_id: space_id
      properties:
        network6_end_hostaddr:
          description: The human readable version of the parameter <b>end_ip6_addr</b>.
          type: string
        end_address6_addr:
          description: The last IP address of the IPv6 network, in hexadecimal format.
          type: string
        network6_is_in_orphan:
          description: A way to determine if the network has a parent (<b>0</b>) or
            if it belongs to a container <b>Orphan networks</b> (<b>1</b>).
          type: string
        network6_is_terminal:
          description: A way to determine if a network can contain other networks.
            If set to <b>1</b>, the network is terminal and cannot contain other subnet-type
            networks. Block-type networks are always set to <b>0</b>.
          type: string
        network6_lock_network_broadcast:
          description: A way to prevent <b>(1)</b> users from assigning the broadcast
            IP address and network IP address of the network.
          type: string
        network6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        parent_end_address6_addr:
          description: The last IP address of the parent IPv6 network, in hexadecimal
            format.
          type: string
        parent_network6_is_terminal:
          description: A way to determine if the parent network is terminal (<b>1</b>)
            or non-terminal (<b>0</b>).
          type: string
        parent_network6_percent_allocated:
          description: The percentage of subnet-type networks the parent network contains.
          type: string
        parent_network6_percent_used:
          description: The percentage of terminal networks the parent network contains.
          type: string
        parent_space_id:
          description: The database identifier (ID) of the space where is located
            the parent network. <b>0</b> indicates that the network has no parent
            network.
          type: string
        parent_space_name:
          description: The name of the space where is located the parent network.
            <b>#</b> indicates that the network has no parent network.
          type: string
        parent_start_address6_addr:
          description: The first IP address of the parent IPv6 network, in hexadecimal
            format.
          type: string
        parent_network6_class_name:
          description: The name of the class applied to the parent IPv6 network, it
            can be preceded by the class directory.
          type: string
        parent_network6_class_parameters:
          description: 'The class parameters applied to the parent IPv6 network and
            their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;...</b>
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        parent_network6_id:
          description: The database identifier (ID) of the parent IPv6 network. <b>0</b>
            indicates that the network has no parent network.
          type: string
        parent_network6_name:
          description: The name of the parent IPv6 network. <b>#</b> indicates that
            the network has no parent network.
          type: string
        parent_network6_prefix:
          description: The prefix of the parent of the IPv6 network the object belongs
            to.
          type: string
        parent_network_level:
          description: The level of the parent network within the space. It returns
            values between <b>0</b> (block-type network) and <b>n</b> (subnet-type
            network). A value higher than <b>1</b> indicates a VLSM organization where
            a block-type network can belong to another subnet-type network.
          type: string
        parent_network_path:
          description: The path toward the parent network in the database. <b>#</b>
            indicates the network has no parent network.
          type: string
        parent_network_size:
          description: The number of IP addresses of the network parent, in hexadecimal
            format.
          type: string
        parent_vlsm_network6_id:
          description: The database identifier (ID) of the IPv6 subnet-type network,
            located in the VLSM parent space, from which the parent network was duplicated.
            <b>0</b> indicates that the parent network is not a VLSM block-type network
            duplicated from a parent space.
          type: string
        percent_allocated:
          description: The percentage of subnet-type networks the non-terminal network
            contains.
          type: string
        percent_used:
          description: The percentage of terminal networks the non-terminal network
            contains.
          type: string
        space_class_name:
          description: The name of the class applied to the space the object belongs
            to, it can be preceded by the class directory.
          type: string
        space_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        space_description:
          description: The description of the space the object belongs to.
          type: string
        space_id:
          description: The database identifier (ID) of the space the object belongs
            to, a unique numeric key value automatically incremented when you add
            a space.
          type: string
        space_is_template:
          description: The template status of the space the object belongs to. If
            the space is used as template (1), all the IPv4 networks, pools and IP
            addresses it contains are also used as template.
          type: string
        space_name:
          description: The name of the space the object belongs to.
          type: string
        space_parent_space_id:
          description: The database identifier (ID) of the VLSM parent of the space
            where is located the network. <b>0</b> indicates that the space where
            is located the network has no parent space.
          type: string
        network6_start_hostaddr:
          description: The human readable version of the parameter <b>start_ip6_addr</b>.
          type: string
        start_address6_addr:
          description: The first IP address of the IPv6 network, in hexadecimal format.
          type: string
        network6_class_name:
          description: The name of the class applied to the IPv6 network, it can be
            preceded by the class directory.
          type: string
        network6_class_parameters:
          description: 'The class parameters applied to the IPv6 network and their
            value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        network6_id:
          description: The database identifier (ID) of the IPv6 network.
          type: string
        network6_is_valid:
          description: The network validity. A valid network (<b>1</b>) has a prefix
            and last IP address that match.
          type: string
        network6_name:
          description: The name of the IPv6 network.
          type: string
        network6_prefix:
          description: The prefix of the IPv6 network.
          type: string
        network_level:
          description: The level of the network within the space. It returns values
            between <b>0</b> (block-type network) and <b>n</b> (subnet-type network).
            A value higher than <b>1</b> indicates a VLSM organization where a block-type
            network can belong to another subnet-type network.
          type: string
        network_size:
          description: The number of IP addresses the IPv6 network contains.
          type: string
        domain_id:
          description: The database identifier (ID) of the VLAN domain associated
            with the network.
          type: string
        domain_name:
          description: The name of the VLAN domain associated with the network.
          type: string
        range_id:
          description: The database identifier (ID) of the VLAN range associated with
            the network.
          type: string
        range_name:
          description: The name of the VLAN range associated with the network.
          type: string
        vlan_id:
          description: The database identifier (ID) of the VLAN associated with the
            network.
          type: string
        vlan_name:
          description: The name of the VLAN associated with the network.
          type: string
        vlan_vlan_id:
          description: The VLAN identifier (ID) of the VLAN associated with the network.
          type: string
        vlsm_block6_id:
          description: The database identifier (ID) of the IPv6 VLSM block-type network
            duplicated, in a VLSM child space, from the network. <b>0</b> indicates
            that the network is not duplicated as a VLSM block-type network in a child
            space.
          type: string
        vlsm_space_id:
          description: The database identifier (ID) of the VLSM child space where
            the network is duplicated as a VLSM block-type network. <b>0</b> indicates
            that the network is not duplicated as a VLSM block-type network in a child
            space.
          type: string
        vlsm_space_name:
          description: The name of the VLSM child space where the network is duplicated
            as a VLSM block-type network. <b>0</b> indicates that the network is not
            duplicated as a VLSM block-type network in a child space.
          type: string
        vlsm_network6_id:
          description: The database identifier (ID) of the IPv6 subnet-type network,
            located in the VLSM parent space, from which the network was duplicated.
            <b>0</b> indicates that the network is not a VLSM block-type network duplicated
            from a parent space.
          type: string
        network6_ripe_waiting_state:
          description: 'The state of the exchange between SOLIDserver and the RIPE
            for the assigned network: <table><caption>waiting_state possible values</caption><br/><thead><tr><th>Status</th><th>Description</th></tr><br/></thead><br/><tbody><tr><td
            >must_send_mail_add</td><td >An email must be sent to the RIPE to notify
            them of a subnet-type network creation.</td></tr><tr><td >wait_mail_add</td><td
            >A network creation email was sent to the RIPE, no reply has been received
            yet.</td></tr><tr><td >must_send_mail_del</td><td >An email must be sent
            to the RIPE to notify them of a subnet-type network deletion.</td></tr><tr><td
            >wait_mail_del</td><td >A network deletion email was sent to the RIPE,
            no reply has been received yet.</td></tr><tr><td >wait_aw_confirm</td><td
            >The number of IP addresses of the assigned network exceeds the Assignment
            Window declared during your RIPE configuration.</td></tr></tbody></table></p><br/>'
          type: string
        network6_ripe_waiting_status:
          description: The status of a RIPE assigned network within SOLIDserver until
            it is confirmed that you can create or delete it. If set to <b>1</b>,
            it is about to be created. If set to <b>2</b>, it is about to be deleted.
          type: string
      type: object
    ipam_network6_add_success_data:
      example:
        network6_id: network6_id
      properties:
        network6_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    ipam_network6_edit_success_data:
      example:
        network6_id: network6_id
      properties:
        network6_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    ipam_network6_delete_success_data:
      example:
        network6_id: network6_id
      properties:
        network6_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    ipam_pool_data_data:
      example:
        pool_start_hostaddr: pool_start_hostaddr
        space_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        pool_start_address_addr: pool_start_address_addr
        vlsm_network_id: vlsm_network_id
        parent_network_name: parent_network_name
        pool_class_name: pool_class_name
        space_class_name: space_class_name
        parent_network_size: parent_network_size
        space_description: space_description
        pool_end_address_addr: pool_end_address_addr
        network_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        network_size: network_size
        pool_size: pool_size
        space_is_template: space_is_template
        network_name: network_name
        pool_read_only: pool_read_only
        pool_id: pool_id
        pool_multistatus: pool_multistatus
        parent_network_class_name: parent_network_class_name
        pool_name: pool_name
        pool_end_hostaddr: pool_end_hostaddr
        network_id: network_id
        space_name: space_name
        parent_network_id: parent_network_id
        network_start_address_addr: network_start_address_addr
        pool_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        vlsm_block_id: vlsm_block_id
        space_id: space_id
        network_end_address_addr: network_end_address_addr
        network_class_name: network_class_name
      properties:
        pool_end_hostaddr:
          description: The human readable version of the parameter <b>end_ip_addr</b>.
          type: string
        pool_end_address_addr:
          description: The last IP address of the IPv4 pool, in hexadecimal format.
          type: string
        pool_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        parent_network_class_name:
          description: The name of the class applied to the parent of the IPv4 network
            the object belongs to, it can be preceded by the class directory.
          type: string
        parent_network_id:
          description: The database identifier (ID) of the parent IPv4 network. It
            identifies the parent of the IPv4 network the object belongs to. <b>0</b>
            indicates that the network the object belongs to has no parent network.
          type: string
        parent_network_name:
          description: The name of the parent IPv4 network:<ul class=dashed ><li><b>#</b>
            indicates that the network the object belongs to has no parent network.<br/>                                            </li><li><b>Default</b>
            indicates that the network the object belongs to is in an orphan network.<br/>                                            </li></ul>
          type: string
        parent_network_size:
          description: The number of IP addresses of the parent of the network the
            object belongs to.
          type: string
        pool_class_name:
          description: The name of the class applied to the IPv4 pool, it can be preceded
            by the class directory.
          type: string
        pool_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        pool_id:
          description: The database identifier (ID) of the IPv4 pool.
          type: string
        pool_name:
          description: The name of the IPv4 pool.
          type: string
        pool_read_only:
          description: The reservation status of the IPv4 pool. If set <b>1</b>, the
            IP addresses it contains cannot be assigned.
          type: string
        pool_size:
          description: The number of IP addresses the IPv4 pool contains.
          type: string
        pool_start_address_addr:
          description: The first IP address of the IPv4 pool, in hexadecimal format.
          type: string
        space_class_name:
          description: The name of the class applied to the space the object belongs
            to, it can be preceded by the class directory.
          type: string
        space_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        space_description:
          description: The description of the space the object belongs to.
          type: string
        space_id:
          description: The database identifier (ID) of the space the object belongs
            to, a unique numeric key value automatically incremented when you add
            a space.
          type: string
        space_is_template:
          description: The template status of the space the object belongs to. If
            the space is used as template (1), all the IPv4 networks, pools and IP
            addresses it contains are also used as template.
          type: string
        space_name:
          description: The name of the space the object belongs to.
          type: string
        pool_start_hostaddr:
          description: The human readable version of the parameter <b>start_ip_addr</b>.
          type: string
        network_class_name:
          description: The name of the class applied to the IPv4 network the object
            belongs to, it can be preceded by the class directory.
          type: string
        network_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        network_end_address_addr:
          description: The last IP address of the IPv4 network the object belongs
            to.
          type: string
        network_id:
          description: The database identifier (ID) of the IPv4 network the object
            belongs to.
          type: string
        network_name:
          description: The name of the IPv4 network the object belongs to. <b>Default</b>
            indicates that the network the object belongs to is an orphan network.
          type: string
        network_size:
          description: The number of IP addresses the network the object belongs to
            contains.
          type: string
        network_start_address_addr:
          description: The first IP address of the IPv4 network the object belongs
            to.
          type: string
        vlsm_block_id:
          description: The database identifier (ID) of the IPv4 VLSM block-type network
            duplicated, in a VLSM child space, from the network the pool belongs to.
            <b>0</b> indicates that the parent of the network the pool belongs to
            is not duplicated as a VLSM block-type network in a child space.
          type: string
        vlsm_network_id:
          description: The database identifier (ID) of the IPv4 subnet-type network,
            located in the VLSM parent space, from which the parent of the network
            the pool belongs to was duplicated. <b>0</b> indicates that the parent
            of the network the pool belongs to is not a VLSM block-type network duplicated
            from a parent space.
          type: string
      type: object
    ipam_pool6_data_data:
      example:
        vlsm_block6_id: vlsm_block6_id
        end_address6_addr: end_address6_addr
        parent_network6_class_name: parent_network6_class_name
        pool6_end_address6_addr: pool6_end_address6_addr
        pool6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        space_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        parent_network6_id: parent_network6_id
        network6_name: network6_name
        parent_network6_prefix: parent_network6_prefix
        space_class_name: space_class_name
        start_address6_addr: start_address6_addr
        space_description: space_description
        pool6_read_only: pool6_read_only
        pool6_class_name: pool6_class_name
        network6_end_address6_addr: network6_end_address6_addr
        network6_prefix: network6_prefix
        network6_start_address6_addr: network6_start_address6_addr
        pool6_size: pool6_size
        pool6_start_hostaddr: pool6_start_hostaddr
        network6_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        space_is_template: space_is_template
        vlsm_network6_id: vlsm_network6_id
        parent_network6_name: parent_network6_name
        pool6_id: pool6_id
        pool6_multistatus: pool6_multistatus
        pool6_name: pool6_name
        space_name: space_name
        network6_id: network6_id
        network6_class_name: network6_class_name
        pool6_end_hostaddr: pool6_end_hostaddr
        space_id: space_id
        pool6_start_address6_addr: pool6_start_address6_addr
      properties:
        pool6_end_hostaddr:
          description: The human readable version of the parameter <b>end_ip6_addr</b>.
          type: string
        end_address6_addr:
          description: The last IP address of the IPv6 pool, in hexadecimal format.
          type: string
        pool6_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        parent_network6_class_name:
          description: The name of the class applied to the parent of the IPv6 network
            the object belongs to, it can be preceded by the class directory.
          type: string
        parent_network6_id:
          description: The database identifier (ID) of the parent IPv6 network. It
            identifies the parent of the IPv6 network the object belongs to. <b>0</b>
            indicates that the network the object belongs to has no parent network.
          type: string
        parent_network6_name:
          description: The name of the parent IPv6 network. <b>#</b> indicates that
            the network the object belongs to has no parent network.
          type: string
        parent_network6_prefix:
          description: The prefix of the parent of the IPv6 network the object belongs
            to.
          type: string
        pool6_class_name:
          description: The name of the class applied to the IPv6 pool, it can be preceded
            by the class directory.
          type: string
        pool6_class_parameters:
          description: 'The class parameters applied to the IPv6 pool and their value:
            <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        pool6_end_address6_addr:
          description: The last IP address of the IPv6 pool, in hexadecimal format.
          type: string
        pool6_id:
          description: The database identifier (ID) of the IPv6 pool.
          type: string
        pool6_name:
          description: The name of the IPv6 pool.
          type: string
        pool6_read_only:
          description: The reservation status of the IPv6 pool. If set <b>1</b>, the
            IP addresses it contains cannot be assigned.
          type: string
        pool6_size:
          description: The number of IP addresses the IPv6 pool contains.
          type: string
        pool6_start_address6_addr:
          description: The first IP address of the IPv6 pool, in hexadecimal format.
          type: string
        space_class_name:
          description: The name of the class applied to the space the object belongs
            to, it can be preceded by the class directory.
          type: string
        space_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        space_description:
          description: The description of the space the object belongs to.
          type: string
        space_id:
          description: The database identifier (ID) of the space the object belongs
            to, a unique numeric key value automatically incremented when you add
            a space.
          type: string
        space_is_template:
          description: The template status of the space the object belongs to. If
            the space is used as template (1), all the IPv4 networks, pools and IP
            addresses it contains are also used as template.
          type: string
        space_name:
          description: The name of the space the object belongs to.
          type: string
        pool6_start_hostaddr:
          description: The human readable version of the parameter <b>start_ip6_addr</b>.
          type: string
        start_address6_addr:
          description: The first IP address of the IPv6 pool, in hexadecimal format.
          type: string
        network6_class_name:
          description: The name of the class applied to the IPv6 network the object
            belongs to, it can be preceded by the class directory.
          type: string
        network6_class_parameters:
          description: 'The class parameters applied to the IPv6 network the object
            belongs to and their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;</b>...
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        network6_end_address6_addr:
          description: The last IP address of the IPv6 network the object belongs
            to.
          type: string
        network6_id:
          description: The database identifier (ID) of the IPv6 network the object
            belongs to.
          type: string
        network6_name:
          description: The name of the IPv6 network the object belongs to.
          type: string
        network6_prefix:
          description: The prefix of the IPv6 network the object belongs to.
          type: string
        network6_start_address6_addr:
          description: The first IP address of the IPv6 network the object belongs
            to.
          type: string
        vlsm_block6_id:
          description: The database identifier (ID) of the IPv6 VLSM block-type network
            duplicated, in a VLSM child space, from the network the pool belongs to.
            <b>0</b> indicates that the parent of the network the pool belongs to
            is not duplicated as a VLSM block-type network in a child space.
          type: string
        vlsm_network6_id:
          description: The database identifier (ID) of the IPv6 subnet-type network,
            located in the VLSM parent space, from which the parent of the network
            the pool belongs to was duplicated. <b>0</b> indicates that the parent
            of the network the pool belongs to is not a VLSM block-type network duplicated
            from a parent space.
          type: string
      type: object
    ipam_pool6_add_success_data:
      example:
        pool6_id: pool6_id
      properties:
        pool6_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    ipam_pool6_edit_success_data:
      example:
        pool6_id: pool6_id
      properties:
        pool6_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    ipam_pool6_delete_success_data:
      example:
        pool6_id: pool6_id
      properties:
        pool6_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    ipam_space_data_data:
      example:
        space_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        space_name: space_name
        parent_space_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        space_class_name: space_class_name
        space_is_template: space_is_template
        space_multistatus: space_multistatus
        parent_space_id: parent_space_id
        space_description: space_description
        parent_space_class_name: parent_space_class_name
        space_id: space_id
        parent_space_name: parent_space_name
      properties:
        space_multistatus:
          description: 'The Multi-status information is displayed as follows: <i>&lt;number-of-instances&gt;@&lt;message-number&gt;@&lt;multi-status-severity&gt;@&lt;module&gt;</i>.
            The different severity levels are:<br><b>Multi-status severity levels</b>    <table
            border=1>        <thead>        <tr >            <td><b>Message number</b></td>            <td><b>Severity</b></td>            <td><b>Description</b></td>        </tr>        </thead>        <tbody>        <tr  valign=middle>            <td>0
            - 16</td>            <td>Emergency</td>            <td>The object configuration
            prevents the system from running properly. Action is required.</td>        </tr>        <tr  valign=middle>            <td>17
            - 33</td>            <td>Critical</td>            <td>The object configuration
            is in critical conditions. Immediate action is recommended.</td>        </tr>        <tr  valign=middle>            <td>34
            - 50</td>            <td>Error</td>            <td>The object configuration
            failed at some level. Action is recommended.</td>        </tr>        <tr  valign=middle>            <td>51
            - 66</td>            <td>Warning</td>            <td>The object configuration
            triggers error messages if no action is taken. Action to be taken at your
            discretion.</td>        </tr>        <tr  valign=middle>            <td>67
            - 83</td>            <td>Notice</td>            <td>The object configuration
            is normal but undergoing events that might trigger errors. No immediate
            action required.</td>        </tr>        <tr  valign=middle>            <td>84
            - 100</td>            <td>Informational</td>            <td>The object
            configuration is normal, operational messages (might inform you about
            potential incompatibilities with other modules, etc). No action required.</td>        </tr>        </tbody></table>'
          type: string
        parent_space_class_name:
          description: The name of the class applied to the VLSM parent space, it
            can be preceded by the class directory.
          type: string
        parent_space_class_parameters:
          description: 'The class parameters applied to the VLSM parent space and
            their value: <b>&lt;class-parameter1&gt;=&lt;value1&gt;&amp;&lt;class-parameter2&gt;=&lt;value2&gt;&amp;...</b>
            .'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        parent_space_id:
          description: The database identifier (ID) of the VLSM parent space. <b>0</b>
            indicates that space has no parent space.
          type: string
        parent_space_name:
          description: The name of the VLSM parent space. <b>#</b> indicates that
            space has no parent space.
          type: string
        space_class_name:
          description: The name of the class applied to the space, it can be preceded
            by the class directory.
          type: string
        space_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        space_description:
          description: The description of the space.
          type: string
        space_id:
          description: The database identifier (ID) of the space.
          type: string
        space_is_template:
          description: The template status of the space. If the space is used as template
            (<b>1</b>), all the IPv4 networks, pools and IP addresses it contains
            are also used as template.
          type: string
        space_name:
          description: The name of the space.
          type: string
      type: object
    ipam_space_add_success_data:
      example:
        space_id: space_id
      properties:
        space_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    ipam_space_edit_success_data:
      example:
        space_id: space_id
      properties:
        space_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    ipam_space_delete_success_data:
      example:
        space_id: space_id
      properties:
        space_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    vlan_domain_data_data:
      example:
        domain_id: domain_id
        domain_name: domain_name
        domain_end_vlan_id: domain_end_vlan_id
        domain_start_vlan_id: domain_start_vlan_id
        domain_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        domain_description: domain_description
        domain_support_vxlan: domain_support_vxlan
        domain_class_name: domain_class_name
      properties:
        domain_support_vxlan:
          description: The type of virtual network used by the domain, VXLAN (<b>1</b>)
            or VLAN (<b>0</b>).
          type: string
        domain_class_name:
          description: The name of the class applied to the VLAN domain, it can be
            preceded by the class directory.
          type: string
        domain_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        domain_description:
          description: The description of the VLAN domain.
          type: string
        domain_end_vlan_id:
          description: The VLAN identifier (ID) of the last VLAN in the VLAN domain.
          type: string
        domain_id:
          description: The database identifier (ID) of the VLAN domain.
          type: string
        domain_name:
          description: The name of the VLAN domain.
          type: string
        domain_start_vlan_id:
          description: The VLAN identifier (ID) of the first VLAN in the VLAN domain.
          type: string
      type: object
    vlan_domain_add_success_data:
      example:
        domain_id: domain_id
      properties:
        domain_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    vlan_domain_edit_success_data:
      example:
        domain_id: domain_id
      properties:
        domain_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    vlan_domain_delete_success_data:
      example:
        domain_id: domain_id
      properties:
        domain_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
    vlan_range_data_data:
      example:
        range_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        range_name: range_name
        domain_end_vlan_id: domain_end_vlan_id
        range_end_vlan_id: range_end_vlan_id
        domain_start_vlan_id: domain_start_vlan_id
        domain_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        range_start_vlan_id: range_start_vlan_id
        domain_description: domain_description
        range_id: range_id
        domain_id: domain_id
        domain_name: domain_name
        range_disable_overlapping: range_disable_overlapping
        range_class_name: range_class_name
        range_description: range_description
        domain_support_vxlan: domain_support_vxlan
        domain_class_name: domain_class_name
      properties:
        domain_support_vxlan:
          description: The type of virtual network used by the domain the range belongs
            to, VXLAN (<b>1</b>) or VLAN (<b>0</b>).
          type: string
        domain_class_name:
          description: The name of the class applied to the VLAN domain the object
            belongs to, it can be preceded by the class directory.
          type: string
        domain_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        domain_description:
          description: The description of the VLAN domain the object belongs to.
          type: string
        domain_end_vlan_id:
          description: The VLAN identifier (ID) of the last VLAN in the VLAN domain
            the object belongs to.
          type: string
        domain_id:
          description: The database identifier (ID) of the VLAN domain the object
            belongs to.
          type: string
        domain_name:
          description: The name of the VLAN domain the object belongs to.
          type: string
        domain_start_vlan_id:
          description: The VLAN identifier (ID) of the first VLAN in the VLAN domain
            the object belongs to.
          type: string
        range_class_name:
          description: The name of the class applied to the VLAN range, it can be
            preceded by the class directory.
          type: string
        range_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        range_description:
          description: The description of the VLAN range.
          type: string
        range_disable_overlapping:
          description: The overlapping restriction status of the VLAN range. <b>1</b>
            indicates that when creating VLANs in the range, their IDs should not
            overlap.
          type: string
        range_end_vlan_id:
          description: The VLAN identifier (ID) of the last VLAN in the VLAN range.
          type: string
        range_id:
          description: The database identifier (ID) of the VLAN range.
          type: string
        range_name:
          description: The name of the VLAN range.
          type: string
        range_start_vlan_id:
          description: The VLAN identifier (ID) of the first VLAN in the VLAN range.
          type: string
      type: object
    vlan_vlan_data_data:
      example:
        range_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        range_name: range_name
        vlan_id: vlan_id
        domain_end_vlan_id: domain_end_vlan_id
        range_end_vlan_id: range_end_vlan_id
        domain_start_vlan_id: domain_start_vlan_id
        vlan_vlan_id: vlan_vlan_id
        domain_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        range_start_vlan_id: range_start_vlan_id
        free_start_vlan_id: free_start_vlan_id
        domain_description: domain_description
        vlan_name: vlan_name
        range_id: range_id
        domain_id: domain_id
        domain_name: domain_name
        vlan_class_name: vlan_class_name
        range_class_name: range_class_name
        vlan_type: vlan_type
        free_end_vlan_id: free_end_vlan_id
        range_description: range_description
        domain_support_vxlan: domain_support_vxlan
        vlan_class_parameters:
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        - propagation: propagate
          name: name
          inheritance: set
          source: source
          value: value
        domain_class_name: domain_class_name
      properties:
        free_end_vlan_id:
          description: For free VLAN IDs (<b>type</b><b>free</b>), it returns the
            last VLAN of a range of VLANs that are not assigned yet. The first VLAN
            in that range is returned in the parameter <b>free_start_vlan_id</b>.
          type: string
        free_start_vlan_id:
          description: For free VLAN IDs (<b>type</b><b>free</b>), it returns the
            first VLAN of the range of VLANs that are not assigned yet. The last VLAN
            in that range is returned in the parameter <b>free_end_vlan_id</b>.
          type: string
        domain_support_vxlan:
          description: The type of virtual network used by the domain the VLAN belongs
            to, VXLAN (<b>1</b>) or VLAN (<b>0</b>).
          type: string
        vlan_type:
          description: The type of the VLAN (<b>free</b> or <b>used</b>).
          type: string
        domain_class_name:
          description: The name of the class applied to the VLAN domain the object
            belongs to, it can be preceded by the class directory.
          type: string
        domain_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        domain_description:
          description: The description of the VLAN domain the object belongs to.
          type: string
        domain_end_vlan_id:
          description: The VLAN identifier (ID) of the last VLAN in the VLAN domain
            the object belongs to.
          type: string
        domain_id:
          description: The database identifier (ID) of the VLAN domain the object
            belongs to.
          type: string
        domain_name:
          description: The name of the VLAN domain the object belongs to.
          type: string
        domain_start_vlan_id:
          description: The VLAN identifier (ID) of the first VLAN in the VLAN domain
            the object belongs to.
          type: string
        range_class_name:
          description: The name of the class applied to the VLAN range the object
            belongs to, it can be preceded by the class directory.
          type: string
        range_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        range_description:
          description: The description of the VLAN range the object belongs to.
          type: string
        range_end_vlan_id:
          description: The VLAN identifier (ID) of the last VLAN in the VLAN range
            the object belongs to.
          type: string
        range_id:
          description: The database identifier (ID) of the VLAN range the object belongs
            to.
          type: string
        range_name:
          description: The name of the VLAN range the object belongs to.
          type: string
        range_start_vlan_id:
          description: The VLAN identifier (ID) of the first VLAN in the VLAN range
            the object belongs to.
          type: string
        vlan_class_name:
          description: The name of the class applied to the object.
          type: string
        vlan_class_parameters:
          description: '#general.output.class_parameters#'
          items:
            $ref: '#/components/schemas/ApiClassParameterOutputEntry'
          type: array
        vlan_id:
          description: The database identifier (ID) of the VLAN.
          type: string
        vlan_name:
          description: The name of the VLAN.
          type: string
        vlan_vlan_id:
          description: The VLAN identifier (ID) of the VLAN.
          type: string
      type: object
    vlan_vlan_add_success_data:
      example:
        vlan_id: vlan_id
      properties:
        vlan_id:
          description: The database identifier (ID) of the object you added.
          type: string
      type: object
    vlan_vlan_edit_success_data:
      example:
        vlan_id: vlan_id
      properties:
        vlan_id:
          description: The database identifier (ID) of the object you edited.
          type: string
      type: object
    vlan_vlan_delete_success_data:
      example:
        vlan_id: vlan_id
      properties:
        vlan_id:
          description: The database identifier (ID) of the object you deleted.
          type: string
      type: object
  securitySchemes:
    BasicAuth:
      scheme: basic
      type: http
    HttpHeaderLoginKey:
      in: header
      name: X-IPM-Username
      type: apiKey
    HttpHeaderPasswordKey:
      in: header
      name: X-IPM-Password
      type: apiKey
